---
title: "Construct, Merge, Solve & Adapt with Reinforcement Learning for the min-max Multiple Traveling Salesman Problem"
title_zh: 结合强化学习的“构建、合并、求解与自适应”算法求解极小极大多旅行商问题
authors: "Guillem Rodríguez-Corominas, Maria J. Blesa, Christian Blum"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23579v1"
tags: ["query:nco-lm"]
score: 9.0
evidence: 强化学习用于多旅行商问题
tldr: 本文针对最小化最大路径长度的多旅行商问题（min-max mTSP），提出了一种结合强化学习与精确优化的混合算法RL-CMSA。该方法利用强化学习学习城市对的关联性以引导解的构建，通过合并路径并求解受限的混合整数线性规划（MILP）模型来寻找最优组合，并辅以局部搜索进行精细化调整。实验证明，该方法在处理大规模及多旅行商实例时，性能显著优于现有的混合遗传算法，为解决复杂组合优化问题提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决min-max mTSP中工作量平衡的挑战，通过混合算法克服单一启发式或精确算法在求解效率与质量上的瓶颈。
method: 提出RL-CMSA框架，利用强化学习更新q值引导概率聚类，并结合MILP求解路径池与局部搜索算子进行迭代优化。
result: 在随机及TSPLIB基准测试中，RL-CMSA在相同时间限制下一致获得（近）最优解，且在复杂实例上的表现优于最先进的混合遗传算法。
conclusion: 证明了将强化学习的探索能力与数学规划的开发能力相结合，是解决大规模多旅行商调度问题的有效途径。
---

## 摘要
多旅行商问题 (mTSP) 将旅行商问题扩展为 m 条路径，这些路径从同一个仓库出发并返回，且共同访问所有客户恰好一次。在极小极大 (min-max) 变体中，目标是最小化最长路径，以反映工作量平衡。我们针对对称单仓库极小极大 mTSP 提出了一种混合方法：结合强化学习的构建、合并、求解与自适应算法 (RL-CMSA)。该方法利用学习到的成对 q 值引导概率聚类，迭代地构建多样化解，将路径合并到一个紧凑的池中，求解受限的集合覆盖混合整数线性规划 (MILP)，并通过路径间的移除、平移和交换操作来优化解。q 值通过强化高质量解中城市对的共现来更新，而解池则通过老化和剪枝机制进行自适应调整。这种精确优化与强化引导构建的结合平衡了探索与利用。在随机实例和 TSPLIB 实例上的计算结果表明，RL-CMSA 能够稳定地找到（近）最优解，并且在相当的时间限制下优于最先进的混合遗传算法，尤其是在实例规模和旅行商数量增加时。

## Abstract
The Multiple Traveling Salesman Problem (mTSP) extends the Traveling Salesman Problem to m tours that start and end at a common depot and jointly visit all customers exactly once. In the min-max variant, the objective is to minimize the longest tour, reflecting workload balance. We propose a hybrid approach, Construct, Merge, Solve & Adapt with Reinforcement Learning (RL-CMSA), for the symmetric single-depot min-max mTSP. The method iteratively constructs diverse solutions using probabilistic clustering guided by learned pairwise q-values, merges routes into a compact pool, solves a restricted set-covering MILP, and refines solutions via inter-route remove, shift, and swap moves. The q-values are updated by reinforcing city-pair co-occurrences in high-quality solutions, while the pool is adapted through ageing and pruning. This combination of exact optimization and reinforcement-guided construction balances exploration and exploitation. Computational results on random and TSPLIB instances show that RL-CMSA consistently finds (near-)best solutions and outperforms a state-of-the-art hybrid genetic algorithm under comparable time limits, especially as instance size and the number of salesmen increase.

---

## 论文详细总结（自动生成）

这是一份关于论文《Construct, Merge, Solve & Adapt with Reinforcement Learning for the min-max Multiple Traveling Salesman Problem》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：多旅行商问题（mTSP）是物流、巡逻和无人机规划中的核心优化问题。其中“极小极大”（min-max）变体旨在最小化所有旅行商中最长的那条路径，这直接关系到任务的**工作量平衡**和**总完成时间**。
*   **核心问题**：min-max mTSP 是 NP-hard 问题，且由于目标函数是非线性的（涉及 $max$ 操作），比传统的最小化总成本（min-sum）更具挑战性。
*   **研究动机**：现有的启发式算法（如遗传算法）在处理大规模实例或旅行商数量较多时，解的质量和鲁棒性仍有提升空间。本文试图通过结合**强化学习（RL）**、**混合整数线性规划（MILP）**和**元启发式框架（CMSA）**来提供一种更高效的混合求解方案。

### 2. 方法论
论文提出了 **RL-CMSA** 框架，其核心思想是利用强化学习来引导解的构建，并利用精确算法在受限空间内寻找最优组合。
*   **构建阶段 (Construct)**：
    *   **聚类**：使用类似 k-means++ 的播种机制选择 $m$ 个中心，并根据学习到的 **q 值**（代表城市对在高质量解中共同出现的概率）将城市分配给各个旅行商。
    *   **路由**：对每个聚类使用贪婪插入和局部搜索（2-opt, Or-opt）生成初始路径。
*   **合并阶段 (Merge)**：将生成的路径存入一个候选池 $R_{cand}$，通过哈希去重并剔除长度超过当前最优解的路径。
*   **求解阶段 (Solve)**：建立一个**集合覆盖 MILP 模型**。从路径池中选择 $m$ 条路径覆盖所有城市，目标是最小化选定路径中的最大长度。由于路径池规模受限，该模型可由 CPLEX 快速求解。
*   **改进阶段 (Improve)**：对 MILP 的输出进行修复（去除重复访问的城市）和局部搜索优化（跨路径的 Shift 和 Swap 操作）。
*   **学习阶段 (Learn)**：采用 **Q-learning** 机制。如果城市对 $(i, j)$ 出现在当前最佳解中，则减小其 $q$ 值（强化关联）；否则增大 $q$ 值。这会直接影响下一轮的聚类分配。
*   **自适应阶段 (Adapt)**：引入“年龄”机制，长期未出现在最佳解中的路径会被移出池子，保持搜索的动态性。

### 3. 实验设计
*   **数据集**：
    1.  **随机实例**：生成了 $n \in \{50, 100, 200\}$ 的城市实例，城市分布在单位圆内以增加平衡难度。
    2.  **TSPLIB 实例**：选取了 eil51, berlin52, eil76, rat99 等标准测试集。
*   **实验场景**：针对每个实例，测试了旅行商比例 $m$ 分别为城市总数的 1%, 5%, 10%, 15% 的情况。
*   **Benchmark（基准）**：对比了目前该领域最先进的 **HGA（混合遗传算法，2024）**。
*   **评价维度**：平均目标值、找到最佳解的频率（#b）、收敛到最佳解所需的时间。

### 4. 资源与算力
*   **硬件环境**：Ars Magna 计算集群，配置为 Intel Xeon 2.2 GHz CPU（10 核），92 GB RAM。
*   **软件环境**：使用 CPLEX 作为 MILP 求解器。
*   **算力消耗**：论文未提及 GPU，主要依赖 CPU 计算。每个实例的计算时间限制严格设定为 $n$ 秒（即 50 到 200 秒不等）。

### 5. 实验数量与充分性
*   **实验规模**：对 60 个随机实例和 4 个 TSPLIB 实例进行了详尽测试，每个配置独立运行 40 次以保证统计显著性。
*   **充分性分析**：
    *   **统计检验**：使用了 Wilcoxon 符号秩检验和 Holm 校正，证明了 RL-CMSA 在大多数配置下具有显著优势。
    *   **可视化分析**：引入了**搜索轨迹网络（STN）**和结构距离分析，直观展示了 RL-CMSA 相比 HGA 具有更强的收敛性和更集中的搜索能力。
    *   **参数调优**：使用 i-Race 工具进行了科学的参数自动调优，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：RL-CMSA 在绝大多数随机实例和 TSPLIB 实例上的平均表现优于 HGA。
*   **规模效应**：随着旅行商数量 $m$ 的增加，RL-CMSA 的优势愈发明显。这是因为 $m$ 越大，单条路径越短，MILP 阶段重组路径的组合灵活性越高。
*   **鲁棒性**：RL-CMSA 在多次运行中找到最佳解的频率显著高于 HGA，表现出极高的稳定性。
*   **局限发现**：在 $n=200, m=1\%$（即旅行商极少、路径极长）的情况下，HGA 表现略优，说明 CMSA 框架在处理超长路径重组时存在挑战。

### 7. 优点
*   **混合创新**：成功将强化学习的“探索”能力与数学规划的“开发”能力结合，解决了 min-max 目标的非线性难题。
*   **动态自适应**：通过 Q 值学习城市间的空间关联，使算法能够根据不同实例特征自动调整聚类策略。
*   **高效性**：在相同的时间预算下，不仅解质量更高，且通常能更快找到当前最优解。

### 8. 不足与局限
*   **对 $m$ 的依赖**：当 $m$ 非常小时，路径池中的长路径难以通过 MILP 有效组合，导致性能受限。
*   **特征维度单一**：目前的 RL 仅学习城市对的共现概率，尚未利用更高阶的特征（如路径形状、城市密度分布等）。
*   **依赖商业求解器**：算法性能部分依赖于 CPLEX 的效率，对于没有商业求解器授权的环境可能存在应用障碍。

（完）
