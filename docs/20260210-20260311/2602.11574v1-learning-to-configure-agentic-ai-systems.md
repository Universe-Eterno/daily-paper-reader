---
title: Learning to Configure Agentic AI Systems
title_zh: 学习配置代理式 AI 系统
authors: "Aditya Taparia, Som Sagar, Ransalu Senanayake"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.11574v1"
tags: ["query:nco-lm"]
score: 8.0
evidence: 强化学习用于智能体配置的组合设计空间
tldr: "针对当前大模型智能体系统配置多采用固定模板或人工启发式规则，导致处理不同难度任务时效率低下且表现脆弱的问题，本文提出了ARC（Agentic Resource & Configuration learner）。ARC利用强化学习训练轻量级分层策略，为每个查询动态定制最优的工作流、工具和资源预算。实验表明，该方法在推理和工具增强问答等多个基准测试中，不仅将任务准确率提升了高达25%，还显著降低了Token消耗和运行成本，证明了按需配置智能体是提升系统性能与效率的强大方案。"
selection_source: fresh_fetch
motivation: 现有的智能体系统通常采用“一刀切”的固定配置，无法针对不同难度的查询灵活调整资源，导致性能受限且计算资源浪费。
method: 提出ARC框架，通过强化学习训练一种轻量级分层策略，实现对智能体工作流、工具和Token预算的动态查询级配置。
result: "在多个推理和问答基准测试中，ARC相比强基准方法提升了高达25%的准确率，并同时显著减少了Token使用量和运行时间。"
conclusion: 学习针对每个查询的个性化智能体配置是替代传统固定设计、实现高效且鲁棒的智能体系统的有效途径。
---

## 摘要
配置基于大语言模型（LLM）的智能体系统涉及从庞大的组合设计空间中选择工作流、工具、Token 预算和提示词，目前通常由固定的庞大模板或人工调优的启发式方法处理。这导致了脆弱的行为和不必要的计算开销，因为无论输入查询是简单还是困难，通常都会应用相同的繁琐配置。我们将智能体配置建模为一个基于查询的决策问题，并引入了 ARC（代理式资源与配置学习器），它利用强化学习来学习轻量级的层次化策略，从而动态地定制这些配置。在涵盖推理和工具增强问答的多个基准测试中，所学习的策略始终优于强大的手工设计和其他基准模型，在将任务准确率提高多达 25% 的同时，还降低了 Token 和运行时间成本。这些结果表明，学习针对每个查询的智能体配置是替代“一刀切”设计的有力方案。

## 速览
**TLDR**：针对当前大模型智能体系统配置多采用固定模板或人工启发式规则，导致处理不同难度任务时效率低下且表现脆弱的问题，本文提出了ARC（Agentic Resource & Configuration learner）。ARC利用强化学习训练轻量级分层策略，为每个查询动态定制最优的工作流、工具和资源预算。实验表明，该方法在推理和工具增强问答等多个基准测试中，不仅将任务准确率提升了高达25%，还显著降低了Token消耗和运行成本，证明了按需配置智能体是提升系统性能与效率的强大方案。 \
**Motivation**：现有的智能体系统通常采用“一刀切”的固定配置，无法针对不同难度的查询灵活调整资源，导致性能受限且计算资源浪费。 \
**Method**：提出ARC框架，通过强化学习训练一种轻量级分层策略，实现对智能体工作流、工具和Token预算的动态查询级决策。 \
**Result**：在多个推理和问答基准测试中，ARC相比强基准方法提升了高达25%的准确率，并同时显著减少了Token消耗和运行时间。 \
**Conclusion**：学习针对每个查询的个性化智能体配置是替代传统固定设计、实现高效且鲁棒的智能体系统的关键途径。

---

## Abstract
Configuring LLM-based agent systems involves choosing workflows, tools, token budgets, and prompts from a large combinatorial design space, and is typically handled today by fixed large templates or hand-tuned heuristics. This leads to brittle behavior and unnecessary compute, since the same cumbersome configuration is often applied to both easy and hard input queries. We formulate agent configuration as a query-wise decision problem and introduce ARC (Agentic Resource & Configuration learner), which learns a light-weight hierarchical policy using reinforcement learning to dynamically tailor these configurations. Across multiple benchmarks spanning reasoning and tool-augmented question answering, the learned policy consistently outperforms strong hand-designed and other baselines, achieving up to 25% higher task accuracy while also reducing token and runtime costs. These results demonstrate that learning per-query agent configurations is a powerful alternative to "one size fits all" designs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Configure Agentic AI Systems》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前基于大语言模型（LLM）的智能体系统通常采用“一刀切”的固定配置（如固定的工作流、工具集和提示词模板）。这种做法存在两大弊端：
    1.  **脆弱性与低效**：简单查询被分配了过度复杂的资源（浪费 Token 和时间），而复杂查询可能因资源不足或上下文过长（“迷失在中间”现象）而失败。
    2.  **巨大的搜索空间**：智能体配置涉及工作流选择、工具分配、Token 预算和提示词组合，其组合空间可达 $10^5$ 以上，人工调优或暴力搜索均不可行。
*   **核心目标**：将智能体配置建模为一个“按查询决策”的问题，通过学习一个轻量级的策略，为每个输入实时定制最优的架构和资源分配。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **ARC (Agentic Resource & Configuration learner)** 框架，利用**分层强化学习 (HRL)** 在冻结的骨干 LLM 之上训练轻量级控制器。
*   **关键技术细节**：
    *   **分层策略架构**：
        *   **结构策略 ($\pi_{struct}$)**：高层决策，负责选择工作流模式（共 9 种，如顺序、并行、投票、评估-优化等）、启用的工具集和各智能体的 Token 预算等级。
        *   **提示词策略 ($\pi_{prompt}$)**：低层决策，根据结构策略的输出，为每个智能体角色序列化地组合系统提示词片段。
    *   **动作掩码 (Action Masking)**：为了处理无效的组合（如在单智能体流中分配多智能体工具），ARC 使用掩码技术将搜索空间有效缩减了约 32.6%。
    *   **奖励函数设计**：采用多目标奖励函数，平衡**任务准确率**、**步骤惩罚**、**Token 消耗惩罚**以及**工具使用塑造奖励**（鼓励仅在必要时调用工具）。
    *   **训练流程**：首先使用 PPO 算法进行端到端强化学习，随后通过**监督微调 (SFT) 精炼阶段**，利用 RL 过程中的“精英轨迹”进行蒸馏，以稳定策略并提升性能下限。

### 3. 实验设计
*   **数据集与场景**：
    *   **推理能力**：GSM8k（数学）、DROP（阅读理解）、MedQA（医学问答）。
    *   **工具使用能力**：HotpotQA（多跳问答）、GAIA（通用 AI 助手基准）。
*   **对比基准 (Benchmarks)**：
    *   **基础模型**：Qwen 2.5 7B Instruct, Gemini 2.5 Flash Lite。
    *   **搜索/优化框架**：Grid/Greedy Search, AutoGen, DSPy, GEPA。
    *   **RL 基准**：RL Bandits（多臂老虎机）, RL Episodes（平铺式 PPO）。
*   **实验充分性**：涵盖了 5 个主流基准测试，进行了跨任务迁移实验、跨模型规模（7B 到 72B）的泛化实验，以及针对嵌入模型和提示词生成器的详尽消融实验。

### 4. 资源与算力
*   **算力设备**：论文在附录 B 中提到，对于 7B 规模的模型，使用**单张现代 GPU（如 A100、V100 或 RTX 4090）**即可完成训练和推理。
*   **内存要求**：至少 64GB CPU RAM。
*   **训练规模**：每个数据集至少进行 4,000 次训练回合（Episodes）。
*   **API 模式**：支持通过 OpenRouter 等 API 调用外部模型，此时无需本地加速器。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了数十组对比实验。除了主性能表外，还包括：
    *   **19 种嵌入模型**的对比，以确定最佳的状态表示。
    *   **12 种 LLM** 作为提示词生成器的效果对比。
    *   **帕累托前沿分析**：展示了准确率与成本之间的最优权衡。
*   **客观性**：实验设计考虑了随机性（报告了均值和标准差），并使用了多种不同架构的骨干模型（开源 Qwen 与闭源 Gemini），证明了方法的通用性。

### 6. 主要结论与发现
*   **性能提升**：ARC 在多个任务上显著优于固定架构和现有的优化框架，准确率提升最高达 25%-31%。
*   **成本效益**：ARC 能够根据查询难度动态调整，在保持高准确率的同时显著降低了 Token 消耗，定义了新的帕累托前沿。
*   **跨规模泛化**：在 7B 模型上训练的配置策略可以直接应用于 32B 和 72B 模型，表现出极强的结构先验不变性。
*   **SFT 的必要性**：后训练的 SFT 阶段能将平均奖励提升 5%-35%，有效减少了 RL 策略的随机性。

### 7. 优点
*   **动态自适应**：打破了“一刀切”的智能体设计范式，实现了真正的按需分配资源。
*   **分层设计**：巧妙地解决了组合爆炸问题，使复杂配置的搜索变得可处理。
*   **轻量级**：无需微调庞大的骨干 LLM，仅训练极小的策略网络，部署成本低。
*   **理论支撑**：为 SFT 精炼阶段提供了形式化的性能保证证明。

### 8. 不足与局限
*   **提示词库依赖**：ARC 的性能受限于预定义的提示词原子库。在 MedQA 等高度专业化的领域，如果库中缺乏领域知识，其表现可能逊于专门针对该领域优化的静态提示词（如 GEPA）。
*   **冷启动成本**：虽然推理高效，但针对新任务仍需要数千次 RL 回合的训练积累。
*   **工具迁移限制**：策略的迁移高度依赖于工具集的重合度，如果新任务的工具完全不同，策略需要重新训练。

（完）
