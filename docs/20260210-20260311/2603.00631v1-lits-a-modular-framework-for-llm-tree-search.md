---
title: "LiTS: A Modular Framework for LLM Tree Search"
title_zh: LiTS：一种用于大语言模型（LLM）树搜索的模块化框架
authors: "Xinzhe Li, Yaguang Tao"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00631v1"
tags: ["query:nco-lm"]
score: 7.0
evidence: 用于推理和规划的大模型树搜索框架
tldr: LiTS是一个专为大语言模型（LLM）推理设计的模块化Python框架，它将树搜索过程解耦为策略、转换和奖励模型三个核心组件，支持MCTS和BFS等多种算法。该框架通过装饰器注册机制实现了高度的可扩展性，允许研究人员在数学推理、环境规划和工具调用等不同领域灵活复用组件。研究还发现，在无限动作空间中，LLM策略的多样性而非奖励质量是制约树搜索效果的主要瓶颈。
selection_source: fresh_fetch
motivation: 针对当前LLM树搜索实现中组件耦合度高、难以在不同算法和领域间灵活重用的问题，开发一个通用的模块化框架。
method: 提出LiTS框架，将树搜索分解为策略、转换和奖励模型三个可插拔组件，并提供统一的接口支持多种搜索算法。
result: 在MATH500、Crosswords和MapEval等任务上验证了组件的跨算法复用性，并揭示了策略多样性对搜索性能的关键影响。
conclusion: LiTS为LLM推理研究提供了高效的工具支持，并指出在无限动作空间下提升策略多样性是优化树搜索的重要方向。
---

## 摘要
LiTS 是一个用于通过树搜索进行大语言模型（LLM）推理的模块化 Python 框架。它将树搜索分解为三个可重用的组件（策略 Policy、转换 Transition 和奖励模型 RewardModel），这些组件可插入 MCTS 和 BFS 等算法中。基于装饰器的注册机制使领域专家能够通过注册组件扩展到新领域，并使算法研究人员能够实现自定义搜索算法。我们在 MATH500（语言推理）、Crosswords（环境规划）和 MapEval（工具使用）上展示了其组合性，表明组件和算法是正交的：在每种任务类型中，组件可在不同算法间重用，而算法可适用于所有组件和领域。我们还报告了一个模式崩溃（mode-collapse）的发现：在无限动作空间中，LLM 策略的多样性（而非奖励质量）是有效树搜索的瓶颈。演示视频见 https://youtu.be/nRGX43YrR3I。该软件包以 Apache 2.0 许可证发布于 https://github.com/xinzhel/lits-llm，包含安装说明和可运行示例，方便用户重现演示的工作流。

## 速览
**TLDR**：LiTS是一个专为大语言模型（LLM）推理设计的模块化Python框架，它将树搜索过程解耦为策略、转换和奖励模型三个核心组件，支持MCTS和BFS等多种算法。该框架通过装饰器注册机制实现了高度的可扩展性，允许研究人员在数学推理、环境规划和工具调用等不同领域灵活复用组件。研究还发现，在无限动作空间中，LLM策略的多样性而非奖励质量是制约树搜索效果的主要瓶颈。 \
**Motivation**：针对当前LLM树搜索实现中组件耦合度高、难以在不同算法和领域间灵活重用的问题，开发一个通用的模块化框架。 \
**Method**：提出LiTS框架，将树搜索分解为策略、转换和奖励模型三个可插拔组件，并提供统一的接口支持多种搜索算法。 \
**Result**：在MATH500、Crosswords和MapEval等任务上验证了组件的跨算法复用性，并揭示了策略多样性对搜索性能的关键影响。 \
**Conclusion**：LiTS为LLM推理研究提供了高效的工具支持，并指出在无限动作空间下提升策略多样性是优化树搜索的重要方向。

---

## Abstract
LiTS is a modular Python framework for LLM reasoning via tree search. It decomposes tree search into three reusable components (Policy, Transition, and RewardModel) that plug into algorithms like MCTS and BFS. A decorator-based registry enables domain experts to extend to new domains by registering components, and algorithmic researchers to implement custom search algorithms. We demonstrate composability on MATH500 (language reasoning), Crosswords (environment planning), and MapEval (tool use), showing that components and algorithms are orthogonal: components are reusable across algorithms within each task type, and algorithms work across all components and domains. We also report a mode-collapse finding: in infinite action spaces, LLM policy diversity (not reward quality) is the bottleneck for effective tree search. A demonstration video is available at https://youtu.be/nRGX43YrR3I. The package is released under the Apache 2.0 license at https://github.com/xinzhel/lits-llm, including installation instructions and runnable examples that enable users to reproduce the demonstrated workflows.

---

## 论文详细总结（自动生成）

以下是对论文《LiTS: A Modular Framework for LLM Tree Search》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前大语言模型（LLM）的树搜索推理方法（如 Tree-of-Thoughts, RAP, ReST-MCTS）通常是针对特定任务定制的，缺乏通用性。当研究人员尝试将现有算法适配到新领域或对比不同搜索方法时，往往需要大量的重复开发工作。
*   **研究动机**：开发一个模块化的 Python 框架，实现搜索算法与领域逻辑的解耦。该框架旨在服务两类用户：
    *   **AI/NLP 研究员**：专注于开发新的搜索算法或推理结构，无需关心底层领域实现。
    *   **领域专家**：专注于注入特定领域的逻辑（提示词、工具、环境动力学），无需修改搜索算法内核。

### 2. 方法论：LiTS 框架设计
*   **核心思想**：将树搜索分解为三个可重用的核心组件：**策略（Policy）**、**转换（Transition）**和**奖励模型（RewardModel）**。
*   **关键技术细节**：
    *   **统一语法结构**：定义了 `Action` → `Step` → `State` → `Node` 的分层数据类型系统。这种抽象使得搜索算法（如 MCTS）可以在不知道具体任务内容的情况下运行。
    *   **模块化组件**：
        *   **Policy**：根据当前状态生成候选动作。
        *   **Transition**：执行动作并产生新状态（支持调用外部工具或环境）。
        *   **RewardModel**：评估动作质量，为搜索提供引导。
    *   **装饰器注册机制**：通过 `@register_transition`、`@register_policy` 等装饰器，用户可以像“插拔配件”一样扩展新领域或新算法，无需修改核心代码。
    *   **任务类型覆盖**：支持环境感知（Environment Grounded）、语言推理（Language Grounded）和工具调用（Tool Use）三大类任务。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **语言推理**：使用 **MATH500** 数据集，评估数学解题能力。
    *   **环境规划**：使用 **BlocksWorld**（积木世界）和 **Crosswords**（填字游戏）。
    *   **工具调用**：使用 **MapEval-SQL**，测试模型调用 SQL 工具解决地理问题的能力。
*   **对比方法**：
    *   **基准方法**：Chain-of-Thought (CoT)、ReAct。
    *   **搜索算法**：蒙特卡洛树搜索 (MCTS)、广度优先搜索 (BFS)。
    *   **具体实现对比**：RAP、ReST-MCTS*、Tree-of-Thoughts (ToT)。

### 4. 资源与算力
*   **算力使用**：
    *   **API 调用**：环境规划和工具调用实验使用了 **Claude 3.5 Sonnet**（通过 AWS Bedrock API）。
    *   **本地部署**：语言推理实验使用了本地部署的 **Llama3-8B**（包括基础版和指令微调版）。
*   **时长与成本**：
    *   论文报告了详细的时间和成本：例如，在 MATH500 上运行 ReST (MCTS) 耗时 26 小时；在 BlocksWorld 上运行 MCTS 的 API 成本约为 21.99 美元。
    *   **未明确说明**：文中未具体列出本地部署 Llama3-8B 所使用的 GPU 型号和数量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   MATH500：100 个样本。
    *   BlocksWorld & Crosswords：各 30 个样本。
    *   MapEval-SQL：10 个样本。
*   **充分性评价**：
    *   作为一篇**框架演示类论文**，其实验设计足以验证其“模块化”和“可组合性”的核心主张。
    *   实验覆盖了三种截然不同的任务类型，展示了组件在不同算法间的复用，具有较好的客观性和公平性。
    *   但由于树搜索计算成本极高，样本量相对较小（如 MapEval 仅 10 例），在统计显著性上可能存在局限。

### 6. 主要结论与发现
*   **双向复用性**：证明了组件与算法是正交的。同一套领域组件可以无缝切换 MCTS 或 BFS 算法；同一套算法也可以通过更换组件适配不同领域。
*   **模式崩溃（Mode-Collapse）发现**：在无限动作空间（如填字游戏）中，**LLM 策略的多样性**是树搜索的瓶颈，而非奖励模型的质量。即使提供完美奖励，如果模型生成的候选动作高度重复，树搜索也会失效。
*   **奖励模型瓶颈**：在工具调用任务中，LLM 作为裁判的自我偏好偏差（Self-preference bias）会导致搜索偏向冗长但错误的路径。

### 7. 优点
*   **高度解耦**：通过装饰器和统一接口，极大地降低了开发和对比不同推理算法的门槛。
*   **可观察性强**：内置 `InferenceLogger`，可细粒度追踪 Token 使用、延迟和搜索路径，方便成本分析。
*   **兼容性好**：工具调用协议兼容 LangChain，易于集成现有生态。
*   **开源贡献**：提供了完整的代码库、安装说明和演示视频，具有很高的实用价值。

### 8. 不足与局限
*   **样本覆盖有限**：受限于 API 成本和计算时间，部分实验的样本量较小，可能无法完全代表大规模应用下的性能。
*   **搜索成本高昂**：实验数据表明，树搜索的 Token 消耗和时间成本远高于 CoT（如成本增加 10-40 倍），这限制了其在实时场景中的应用。
*   **策略多样性难题**：论文虽然指出了动作空间中的模式崩溃问题，但尚未在框架层面提供彻底解决多样性不足的通用方案。

（完）
