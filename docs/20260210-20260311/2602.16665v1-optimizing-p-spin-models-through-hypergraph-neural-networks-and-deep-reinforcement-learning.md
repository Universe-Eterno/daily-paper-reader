---
title: Optimizing p-spin models through hypergraph neural networks and deep reinforcement learning
title_zh: 通过超图神经网络与深度强化学习优化 p-自旋模型
authors: "Li Zeng, Mutian Shen, Tianle Pu, Zohar Nussinov, Qing Feng, Chao Chen, Zhong Liu, Changjun Fan"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16665v1"
tags: ["query:nco-lm"]
score: 10.0
evidence: 超图神经网络与深度强化学习用于组合优化
tldr: 针对具有多体相互作用的p-spin模型求解NP-hard基态难题，本文提出PLANCK框架。该框架结合超图神经网络与深度强化学习，利用规范对称性优化高阶相互作用。PLANCK在小规模实例上训练后，展现出强大的零样本泛化能力，在处理大规模系统时性能优于传统热退火方法，并能有效解决k-XORSAT和超图最大割等多种组合优化问题。
selection_source: fresh_fetch
motivation: 传统的p-spin模型求解方法在处理大规模、高阶相互作用的复杂系统时效率低下且难以扩展。
method: 提出基于超图神经网络和深度强化学习的PLANCK框架，通过物理启发的对称性感知设计直接优化高阶相互作用。
result: PLANCK在多种拓扑结构下均优于最先进的热退火方法，并能零样本泛化至比训练集大几个数量级的系统。
conclusion: 该研究为解决高阶无序系统和复杂组合优化问题提供了一种结合统计力学与机器学习的高效算法范式。
---

## 摘要
p-自旋玻璃以超越传统成对情况（p>2）的受挫多体相互作用为特征，是典型的无序系统，其基态搜索属于 NP-hard 问题，且对于大规模实例在计算上难以实现。解决这一问题不仅是理解高阶无序、结构玻璃和拓扑相的基础，也是广泛硬组合优化任务的核心。尽管经过数十年的进展，仍然缺乏针对通用大规模 p-自旋模型的高效且可扩展的求解器。在此，我们介绍了 PLANCK，这是一个基于超图神经网络构建的受物理启发的深度强化学习框架。PLANCK 直接优化任意高阶相互作用，并在训练和推理过程中系统地利用规范对称性。PLANCK 仅在小型合成实例上进行训练，却对规模大几个数量级的系统表现出强大的零样本泛化能力，并在所有测试的结构拓扑和耦合分布中始终优于最先进的热退火方法。此外，在不进行任何修改的情况下，PLANCK 为一大类 NP-hard 组合问题实现了近乎最优的解，包括随机 k-XORSAT、超图最大割和传统最大割。该框架提供了一种受物理启发的算法范式，桥接了统计力学与强化学习。这种对称性感知的设计不仅推进了高阶无序系统的可处理前沿，还为基于机器学习的求解器解决此前难以处理的组合优化挑战开辟了具有前景的途径。

## Abstract
p-spin glasses, characterized by frustrated many-body interactions beyond the conventional pairwise case (p>2), are prototypical disordered systems whose ground-state search is NP-hard and computationally prohibitive for large instances. Solving this problem is not only fundamental for understanding high-order disorder, structural glasses, and topological phases, but also central to a wide spectrum of hard combinatorial optimization tasks. Despite decades of progress, there still lacks an efficient and scalable solver for generic large-scale p-spin models. Here we introduce PLANCK, a physics-inspired deep reinforcement learning framework built on hypergraph neural networks. PLANCK directly optimizes arbitrary high-order interactions, and systematically exploits gauge symmetry throughout both training and inference. Trained exclusively on small synthetic instances, PLANCK exhibits strong zero-shot generalization to systems orders of magnitude larger, and consistently outperforms state-of-the-art thermal annealing methods across all tested structural topologies and coupling distributions. Moreover, without any modification, PLANCK achieves near-optimal solutions for a broad class of NP-hard combinatorial problems, including random k-XORSAT, hypergraph max-cut, and conventional max-cut. The presented framework provides a physics-inspired algorithmic paradigm that bridges statistical mechanics and reinforcement learning. The symmetry-aware design not only advances the tractable frontiers of high-order disordered systems, but also opens a promising avenue for machine-learning-based solvers to tackle previously intractable combinatorial optimization challenges.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing p-spin models through hypergraph neural networks and deep reinforcement learning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：p-自旋玻璃模型（$p>2$）是统计物理中的核心模型，用于描述结构玻璃、超冷液体及拓扑量子纠错等复杂系统。其基态搜索属于典型的 NP-hard 问题，具有极其崎岖且分形化的能量景观（Gardner Landscape）。
*   **核心挑战**：传统精确算法（如分支定界）仅能处理极小规模系统；启发式算法（如模拟退火 SA、并行回火 PT）在处理高阶相互作用时容易陷入局部最优，且计算开销随规模指数增长。
*   **整体含义**：本文旨在开发一种通用的、受物理启发的深度学习框架，能够直接处理高阶相互作用，并在不同规模和拓扑结构之间实现高效泛化。

### 2. 方法论：PLANCK 框架
*   **核心思想**：将基态搜索建模为马尔可夫决策过程（MDP），利用**超图神经网络（PHGNN）**捕捉多体相互作用，并通过**深度强化学习（DRL）**学习翻转自旋的策略。
*   **关键技术细节**：
    *   **超图表示**：节点代表自旋，超边代表 $p$ 体相互作用，直接在原始高阶 Hamiltonian 上操作，避免了引入辅助变量的“二次化”降阶过程。
    *   **规范对称性感知（Gauge Symmetry）**：利用自旋玻璃的规范不变性进行特征增强。通过变换将任意配置映射到“全向上”或“全向下”状态，使模型学习到能量景观的本质特征，显著缩小搜索空间。
    *   **PHGNN 编码器**：采用消息传递机制，在节点（自旋）和超边（耦合）之间交替聚合信息，生成具有对称性保护的嵌入表示。
    *   **混合推理策略**：在测试阶段，将训练好的 Q 网络与模拟退火结合。根据温度动态调整“神经引导翻转”与“热随机翻转”的比例，利用规范变换实现路径重置，增强全局探索能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **二维晶格**：三角形（$p=3$）、正方形（$p=4$）、六边形（$p=6$）。
    *   **耦合分布**：双峰分布（Bimodal）和高斯分布（Gaussian）。
    *   **通用 NP-hard 问题**：随机 k-XORSAT、超图最大割（Hypergraph Max-Cut）、传统最大割（Max-Cut, 使用 Gset 基准）。
*   **Benchmark（对比方法）**：
    *   传统启发式：模拟退火（SA）、并行回火（PT）、贪婪搜索。
    *   精确求解器：Gurobi（用于小规模验证）。
    *   机器学习基准：PI-GNN、HypOp、RUN-CSP、DSDP 等。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **64 核 CPU、512GB RAM** 以及 **NVIDIA A100 GPU (32GB)** 的计算机上运行。
*   **训练与推理成本**：
    *   **离线训练**：在小型合成实例（如 $L=4$ 或 $L=5$）上进行。
    *   **在线推理**：对于稀疏系统，时间复杂度随规模 $N$ 呈线性增长 $O(N)$；对于稠密系统为 $O(N^2)$。
    *   **优势**：训练成本是一次性的，训练好的模型可直接应用于不同规模的系统，无需重新训练。

### 5. 实验数量与充分性
*   **实验规模**：针对每种拓扑和分布，均在 50 个独立实例上进行了测试，并报告了平均值和标准误差（SEM）。
*   **泛化性验证**：模型仅在极小规模（如 25 个自旋）上训练，但在比训练集大 4-6 倍甚至更广规模的系统上进行了零样本（Zero-shot）测试。
*   **消融实验**：对消息传递层数 $K$、强化学习折扣因子 $\gamma$、奖励步数 $n$ 等超参数进行了消融研究，验证了深度消息传递对捕捉复杂相关性的必要性。
*   **客观性**：通过固定计算预算（$N_{initial}=5000$）确保了 PLANCK 与 SA、PT 之间的公平对比。

### 6. 主要结论与发现
*   **性能卓越**：在所有测试的晶格拓扑和耦合分布中，PLANCK 找到的能量密度均显著低于 SA 和 PT。
*   **强大的泛化力**：展现了跨规模的零样本泛化能力，且在处理未见过的 NP-hard 问题（如 Max-Cut）时达到了 SOTA 水平。
*   **物理可解释性**：在 Baxter-Wu 模型分析中，PLANCK 自动发现了一种“人类专家式”的策略——通过翻转六边形簇的核心自旋来同时消除多个受挫项，而非简单的随机尝试。

### 7. 优点
*   **物理启发设计**：系统性地整合了规范对称性，这在自旋玻璃优化中是极具创新性的。
*   **高阶直接处理**：无需降阶（Quadratization），保留了问题的原始几何结构，计算效率更高。
*   **统一框架**：一个模型即可处理从纯 $p$ 到混合 $p$ 的多种组合优化任务，通用性极强。

### 8. 不足与局限
*   **超参数敏感性**：消息传递层数 $K$ 对系统尺寸和 $p$ 值有较强依赖，可能需要针对特定拓扑微调。
*   **探索受限**：虽然引入了混合策略，但在极大规模的决策空间中，单次轨迹的探索深度仍可能受限。
*   **训练依赖**：虽然泛化性强，但仍依赖于高质量的小规模合成数据进行初始训练。

（完）
