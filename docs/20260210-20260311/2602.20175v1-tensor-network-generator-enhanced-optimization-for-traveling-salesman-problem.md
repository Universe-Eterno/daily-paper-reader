---
title: Tensor Network Generator-Enhanced Optimization for Traveling Salesman Problem
title_zh: 针对旅行商问题的张量网络生成器增强优化
authors: "Ryo Sakai, Chen-Yu Liu"
date: 2026-02-12
pdf: "https://arxiv.org/pdf/2602.20175v1"
tags: ["query:nco-lm"]
score: 10.0
evidence: 旅行商问题的神经求解器
tldr: 本研究提出了一种基于张量网络生成增强优化（TN-GEO）框架解决旅行商问题（TSP）的新方法。该方法利用基于矩阵乘积态（MPS）的张量网络Born机作为生成模型，通过排列编码和带掩码的自回归采样确保生成路径的合法性，避免了传统二进制编码的冗余。此外，引入了关注局部相关性的k-site MPS变体。实验表明，该方法在TSPLIB基准测试中优于传统的启发式算法，为组合优化提供了高效的量子启发式方案。
selection_source: fresh_fetch
motivation: 旨在解决传统二进制编码在处理TSP问题时存在的变量冗余及约束处理困难，提升组合优化的求解效率。
method: 采用基于MPS的张量网络Born机，结合排列编码、掩码自回归采样以及关注局部k-gram相关性的k-site变体进行建模。
result: 在包含多达52个城市的TSPLIB实例上，该方法性能优于交换和2-opt爬山算法等经典启发式方法。
conclusion: TN-GEO框架结合k-site MPS能有效捕捉路径局部特征，是解决大规模组合优化问题的一种极具潜力的生成式优化工具。
---

## 摘要
我们展示了张量网络生成器增强优化（TN-GEO）框架在解决旅行商问题（TSP）中的应用，TSP 是一项基础的组合优化挑战。我们的方法采用基于自动微分矩阵乘积态（MPS）的张量网络波恩机作为生成模型，利用波恩定则定义候选解上的概率分布。与基于二进制编码的方法（需要 $N^2$ 个变量和惩罚项来强制执行有效路径约束）不同，我们采用了基于整数变量的排列公式，并使用带掩码的自回归采样，从而在构造上保证每个生成的样本都是有效的路径。我们还引入了一种 $k$-位点 MPS 变体，该变体使用滑动窗口方法学习 $k$-gram（连续城市子序列）上的分布，从而实现对更大规模实例的参数高效建模。在包含多达 52 个城市的 TSPLIB 基准实例上的实验验证表明，TN-GEO 的性能可以优于包括交换（swap）和 2-opt 爬山算法在内的经典启发式算法。与全 MPS 情况相比，更注重局部相关性的 $k$-位点变体表现出更好的结果。

## Abstract
We present an application of the tensor network generator-enhanced optimization (TN-GEO) framework to address the traveling salesman problem (TSP), a fundamental combinatorial optimization challenge. Our approach employs a tensor network Born machine based on automatically differentiable matrix product states (MPS) as the generative model, using the Born rule to define probability distributions over candidate solutions. Unlike approaches based on binary encoding, which require $N^2$ variables and penalty terms to enforce valid tour constraints, we adopt a permutation-based formulation with integer variables and use autoregressive sampling with masking to guarantee that every generated sample is a valid tour by construction. We also introduce a $k$-site MPS variant that learns distributions over $k$-grams (consecutive city subsequences) using a sliding window approach, enabling parameter-efficient modeling for larger instances. Experimental validation on TSPLIB benchmark instances with up to 52 cities demonstrates that TN-GEO can outperform classical heuristics including swap and 2-opt hill-climbing. The $k$-site variants, which put more focus on local correlations, show better results compared to the full-MPS case.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种利用张量网络（Tensor Network）增强生成式优化框架来解决旅行商问题（TSP）的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：TSP 是组合优化中的 NP-hard 难题。传统的量子或量子启发式算法通常采用二进制编码（需要 $N^2$ 个变量），这不仅导致搜索空间巨大，且绝大多数状态是非法路径，需要复杂的惩罚项来约束。
*   **核心目标**：开发一种基于整数编码的生成式模型，通过张量网络（MPS）直接在有效排列空间内进行采样，提高求解效率并降低约束处理的复杂度。

### 2. 方法论
*   **核心思想**：采用 **TN-GEO（张量网络生成器增强优化）** 框架。这是一个迭代优化过程：生成候选解 -> 计算路径长度 -> 根据成本进行 Softmax 加权 -> 训练生成模型以拟合该分布 -> 循环往复。
*   **关键技术细节**：
    *   **张量网络波恩机（TNBM）**：使用矩阵乘积态（MPS）来表示波恩定则下的概率分布 $P(x) = |\Psi(x)|^2/Z$。
    *   **带掩码的自回归采样**：在采样每个城市时，通过“掩码”掉已访问的城市，确保生成的序列在构造上就是一个合法的排列（Permutation），无需拒绝采样。
    *   **$k$-位点 MPS 变体**：为了处理更大规模的问题，模型不再学习整个路径，而是学习长度为 $k$ 的子序列（$k$-grams）。通过滑动窗口方式生成路径，利用了 TSP 问题的局部几何相关性。
    *   **自动微分（AD）训练**：不同于传统的 DMRG 算法，该研究利用 JAX 框架通过自动微分进行全局梯度下降优化。

### 3. 实验设计
*   **数据集**：使用了 **TSPLIB** 标准基准测试集中的 6 个实例：burma14, ulysses16, ulysses22, att48, eil51, berlin52（城市数量从 14 到 52 不等）。
*   **Benchmark（基准）**：对比了两种经典的局部搜索启发式算法：
    *   **Swap（交换）**：随机交换两个城市位置的爬山算法。
    *   **2-opt**：反转路径片段以消除交叉的局部优化算法。
*   **对比维度**：评估了不同键维度（Bond Dimension $\chi$）对分布质量的影响，以及不同 $k$ 值（$k=2, 4, 8$ 及全量 $N$）对优化结果的影响。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到使用了 JAX 和 Optax（AdamW 优化器）。文中指出自动微分的内存开销与 $B \times \chi^2 \times N$ 成正比，这限制了全量 MPS 处理超过 50 个城市规模的能力。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 个不同规模的 TSPLIB 实例进行了测试，并进行了消融实验（如改变 $k$ 值和键维度）。
*   **充分性评价**：实验设计较为客观，展示了模型从随机分布到收敛至最优解的过程。但对比对象（Swap 和 2-opt）属于较弱的基准，未与 SOTA（如 LKH 算法或最先进的神经组合优化模型）进行对比，因此在证明“优越性”方面仍处于初步验证阶段。

### 6. 主要结论与发现
*   **性能表现**：TN-GEO 在所有测试实例上均优于简单的 Swap 和 2-opt 算法。
*   **$k$-位点优势**：中间大小的 $k$ 值（如 $k=4, 8$）在较大规模实例上表现最好，甚至优于全量 MPS。这表明 TSP 的近优解主要由局部几何模式决定。
*   **稳定性**：随着城市规模增加，TN-GEO 的解质量（Gap）相对稳定，表现出较好的鲁棒性。

### 7. 优点
*   **约束处理优雅**：通过带掩码的自回归采样彻底解决了排列约束问题，避免了 $N^2$ 变量的冗余。
*   **参数高效**：$k$-位点变体显著降低了模型复杂度，使其能够扩展到更大规模的问题。
*   **量子启发式创新**：将张量网络的物理特性（如纠缠熵、波恩定则）成功转化为解决经典组合优化问题的生成式工具。

### 8. 不足与局限
*   **规模限制**：受限于自动微分的内存开销，目前仅测试到 52 个城市，尚未验证其在数千个城市规模下的表现。
*   **基准较弱**：对比的启发式算法过于基础，缺乏与现代高性能 TSP 求解器的深度对比。
*   **超参数敏感**：温度调度（Temperature Scheduling）和键维度的选择对结果影响较大，可能需要针对不同问题进行精细调优。

（完）
