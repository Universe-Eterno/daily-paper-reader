---
title: Enhancing CVRP Solver through LLM-driven Automatic Heuristic Design
title_zh: 通过大语言模型驱动的自动启发式设计增强 CVRP 求解器
authors: "Zhuoliang Xie, Fei Liu, Zhenkun Wang, Qingfu Zhang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23092v1"
tags: ["query:nco-lm"]
score: 10.0
evidence: 大模型驱动的CVRP自动启发式设计
tldr: 本研究针对带容量限制的车辆路径问题（CVRP）这一NP-hard难题，提出了AILS-AHD框架。该方法利用大语言模型（LLM）的生成能力，结合进化搜索框架，自动设计并优化迭代局部搜索中的破坏算子（ruin heuristics）。此外，还引入了LLM驱动的加速机制。实验表明，该方法在处理大规模实例时表现卓越，在CVRPLib基准测试中刷新了8项已知最佳解，展示了LLM在启发式算法设计中的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的CVRP求解方法在面对大规模复杂实例时，面临着巨大的计算挑战和启发式算子设计困难。
method: 提出AILS-AHD框架，通过LLM与进化搜索相结合，动态生成并优化AILS算法中的破坏启发式算子。
result: 在与AILS-II和HGS等顶尖求解器的对比中，该方法在CVRPLib大规模基准测试的10个实例中刷新了8个最佳已知解。
conclusion: LLM驱动的自动启发式设计为解决复杂的组合优化问题提供了一种高效且极具潜力的新范式。
---

## 摘要
带容量限制的车辆路径问题 (CVRP) 是一项基础的组合优化挑战，专注于在车辆容量限制下优化车队运营。尽管在运筹学领域已得到广泛研究，但 CVRP 的 NP 困难特性仍带来显著的计算挑战，尤其是在处理大规模实例时。本研究提出了 AILS-AHD（结合自动启发式设计的自适应迭代局部搜索），这是一种利用大语言模型 (LLMs) 革新 CVRP 求解的新颖方法。我们的方法将进化搜索框架与 LLMs 相结合，在 AILS 方法中动态生成并优化破坏启发式算法 (ruin heuristics)。此外，我们还引入了一种基于 LLM 的加速机制以提升计算效率。与包括 AILS-II 和 HGS 在内的最先进求解器进行的全面实验评估表明，AILS-AHD 在中等规模和大规模实例上均表现出卓越的性能。值得注意的是，我们的方法在 CVRPLib 大规模基准测试的 10 个实例中为 8 个找到了新的已知最佳解 (best-known solutions)，凸显了 LLM 驱动的启发式设计在推动车辆路径优化领域发展方面的潜力。

## 速览
**TLDR**：本研究针对带容量限制的车辆路径问题（CVRP）这一NP-hard难题，提出了AILS-AHD框架。该方法利用大语言模型（LLM）的生成能力，结合进化搜索框架，自动设计并优化迭代局部搜索中的破坏算子（ruin heuristics）。此外，还引入了LLM驱动的加速机制。实验表明，该方法在处理大规模实例时表现卓越，在CVRPLib基准测试中刷新了8项已知最佳解，展示了LLM在启发式算法设计中的巨大潜力。 \
**Motivation**：传统的CVRP求解方法在面对大规模复杂实例时，面临着巨大的计算挑战和启发式算子设计困难。 \
**Method**：提出AILS-AHD框架，通过LLM与进化搜索相结合，动态生成并优化AILS算法中的破坏启发式算子。 \
**Result**：在与AILS-II和HGS等顶尖求解器的对比中，该方法在CVRPLib大规模基准测试的10个实例中刷新了8个最佳已知解。 \
**Conclusion**：LLM驱动的自动启发式设计为解决复杂的组合优化问题提供了一种高效且极具潜力的新范式。

---

## Abstract
The Capacitated Vehicle Routing Problem (CVRP), a fundamental combinatorial optimization challenge, focuses on optimizing fleet operations under vehicle capacity constraints. While extensively studied in operational research, the NP-hard nature of CVRP continues to pose significant computational challenges, particularly for large-scale instances. This study presents AILS-AHD (Adaptive Iterated Local Search with Automatic Heuristic Design), a novel approach that leverages Large Language Models (LLMs) to revolutionize CVRP solving. Our methodology integrates an evolutionary search framework with LLMs to dynamically generate and optimize ruin heuristics within the AILS method. Additionally, we introduce an LLM-based acceleration mechanism to enhance computational efficiency. Comprehensive experimental evaluations against state-of-the-art solvers, including AILS-II and HGS, demonstrate the superior performance of AILS-AHD across both moderate and large-scale instances. Notably, our approach establishes new best-known solutions for 8 out of 10 instances in the CVRPLib large-scale benchmark, underscoring the potential of LLM-driven heuristic design in advancing the field of vehicle routing optimization.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enhancing CVRP Solver through LLM-driven Automatic Heuristic Design》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对**带容量限制的车辆路径问题（CVRP）**展开研究。CVRP 是物流和交通领域的经典 NP-hard 组合优化问题。
*   **研究动机**：传统的求解方法中，精确算法难以应对大规模实例；深度学习求解器泛化性较弱；而性能最强的**元启发式算法**（如 HGS, AILS）高度依赖专家经验来手动设计复杂的算子（如破坏与重建算子）。
*   **核心目标**：利用大语言模型（LLM）的编程与逻辑推理能力，结合进化计算（EC）框架，实现**启发式算子的自动化设计（AHD）**，以减少人工干预并突破现有求解器的性能瓶颈。

### 2. 方法论：AILS-AHD 框架
论文提出了 **AILS-AHD**（Adaptive Iterated Local Search with Automatic Heuristic Design），其核心流程如下：
*   **基础框架**：以自适应迭代局部搜索（AILS）为骨架，包含初始化、迭代局部搜索（扰动+局部搜索）和动态参数更新（如扰动程度 $\omega_k$ 和参考距离 $d_r$ 的自适应调整）。
*   **LLM 驱动的自动设计**：
    *   **进化搜索**：维护一个启发式算子种群。通过 Prompt 将任务描述、代码模板和特征输入提供给 LLM（GPT-4o）。
    *   **算子生成**：LLM 充当“变异”和“交叉”算子，生成新的“破坏（Ruin）”启发式代码。
    *   **特征工程**：为 LLM 提供距离矩阵、K-近邻列表、节点需求、路由平均节点数等特征，使其能编写基于空间和需求逻辑的复杂算子。
*   **加速机制**：
    *   **CoT 预评估**：利用思维链（Chain-of-Thought）让 LLM 先判断生成的代码是否有潜力，过滤低质量代码。
    *   **早停机制**：在评估阶段，若算子在初始测试实例上表现极差，则提前终止评估以节省计算资源。

### 3. 实验设计
*   **数据集（Benchmarks）**：
    *   **X Set**：100 个中等规模实例（100-1000 节点），CVRP 领域最常用的基准。
    *   **AGS Set**：10 个大规模实例（3000-30000 节点），基于真实城市几何数据。
*   **对比方法**：
    *   **HGS**（混合遗传搜索）：目前公认的最强 CVRP 求解器之一。
    *   **AILS-II**：最先进的单解迭代局部搜索方法。
    *   **手动变体**：AILS-C（基于 K-近邻）和 AILS-S（基于序列）。
*   **评估指标**：平均误差（Avg. Gap）、找到已知最佳解（BKS）的数量、收敛速度。

### 4. 资源与算力
*   **硬件资源**：实验在 2 颗 **Intel Xeon Gold 6254 CPU** 上运行。
*   **LLM 调用**：使用了 **GPT-4o** 模型。在自动设计阶段，种群大小为 25，进化 10 代，总计约 **1000 次 LLM 调用**。
*   **运行时间**：
    *   常规评估：每个实例运行 $3 \times n$ 秒（$n$ 为节点数）。
    *   寻找新 BKS：每个大规模实例运行 $10 \times n$ 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 100 到 30,000 节点的广泛区间，总计测试了 110 个标准实例。
*   **重复性与统计**：中等规模实例独立运行 30 次，大规模实例独立运行 10 次，并提供了标准差和显著性检验（+/-/=），实验设计符合学术规范。
*   **消融实验**：针对 LLM 设计的算子中的关键参数（如邻域扩展因子 $\lambda$）进行了专门的消融分析，验证了算子的稳定性。
*   **客观性**：通过使用相同的 AILS 框架仅替换算子，公平地证明了 LLM 生成算子的优越性。

### 6. 主要结论与发现
*   **刷新纪录**：AILS-AHD 在 10 个大规模实例中刷新了 **8 个已知最佳解（BKS）**，并在中等规模实例中刷新了 1 个 BKS。
*   **性能超越**：在平均 Gap 指标上，LLM 设计的算子（如 Ours-EN）显著优于 HGS 和 AILS-II，尤其是在处理超大规模城市级路径规划时。
*   **收敛性**：LLM 生成的算子（如“扩展邻域选择”）展现出比人工设计算子更强的跳出局部最优的能力和更快的收敛速度。

### 7. 优点（亮点）
*   **自动化程度高**：成功将 LLM 从简单的代码辅助工具提升为能够理解复杂空间几何逻辑的算法设计者。
*   **加速方案实用**：引入 CoT 预判和早停机制，有效解决了 LLM 驱动进化算法中“评估成本高昂”的痛点。
*   **实战性能强**：不仅在学术指标上领先，更在极具挑战性的大规模真实地理数据集上取得了突破。

### 8. 不足与局限
*   **框架依赖**：该方法目前高度依赖 AILS 这一特定的元启发式框架，尚未验证在其他框架（如禁忌搜索或大邻域搜索）中的通用性。
*   **算子覆盖有限**：目前主要集中在“破坏（Ruin）”算子的自动化设计，尚未对“重建（Recreate）”或局部搜索算子进行全面的自动化探索。
*   **成本风险**：虽然引入了加速机制，但 1000 次 GPT-4o 调用仍存在一定的 API 成本和网络延迟依赖。

（完）
