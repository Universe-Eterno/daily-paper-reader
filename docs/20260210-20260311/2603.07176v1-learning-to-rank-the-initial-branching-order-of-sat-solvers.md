---
title: Learning to Rank the Initial Branching Order of SAT Solvers
title_zh: 学习对 SAT 求解器的初始分支顺序进行排序
authors: "Arvid Eriksson, Gabriel Poesia, Roman Bresson, Karl Henrik Johansson, David Broman"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07176v1"
tags: ["query:nco-lm"]
score: 9.0
evidence: GNN 用于预测 SAT 求解器的分支顺序
tldr: 本研究探讨了利用图神经网络（GNN）预测CDCL SAT求解器的初始分支顺序，以提高求解效率。通过提出三种可扩展的标签生成方法，训练GNN在求解前对变量进行排序。实验表明，该方法在随机3-CNF和伪工业基准测试中显著提升了速度，并具备良好的泛化能力，但在处理复杂工业实例时受限于求解器的动态启发式机制。
selection_source: fresh_fetch
motivation: 寻找高效的分支顺序对提升SAT求解性能至关重要，但预先确定最优顺序具有很大挑战性。
method: 提出利用图神经网络作为预处理步骤，结合三种高效的标签生成方法来预测CDCL求解器的初始变量分支顺序。
result: 该方法在随机3-CNF和伪工业数据集上实现了显著加速，并能泛化至比训练集规模大得多的实例。
conclusion: 虽然初始排序对特定类型问题有效，但对于复杂工业实例，求解器的动态启发式机制会迅速覆盖初始设置，限制了预测效果。
---

## 摘要
寻找良好的分支顺序是高效解决 SAT 问题的关键，但寻找此类分支顺序本身就是一个难题。因此，在求解之前使用基于学习的方法来预测良好的分支顺序具有潜力。在本文中，我们研究了使用图神经网络（GNN）作为冲突驱动子句学习（CDCL）SAT 求解器的预处理步骤来预测分支顺序。我们表明，通过提供良好的初始分支，现有的 CDCL SAT 求解器可以获得显著的性能提升。此外，我们提供了三种标注方法，以易于处理的方式寻找此类初始分支顺序。最后，我们训练了一个图神经网络来预测这些分支顺序，并通过评估表明，GNN 初始化的排序在随机 3-CNF 和伪工业基准测试上产生了显著的加速，并且具有泛化到比训练集大得多的实例的能力。然而，我们也发现预测在加速更困难和工业实例方面效果不佳。我们将此归因于求解器的动态启发式算法（这些算法会迅速覆盖提供的初始化）以及这些实例的复杂性，这使得 GNN 预测变得困难。

## Abstract
Finding good branching orders is key to solving SAT problems efficiently, but finding such branching orders is a difficult problem. Using a learning based approach to predict a good branching order before solving, therefore, has potential. In this paper, we investigate predicting branching orders using graph neural networks as a preprocessing step to conflict-driven clause learning (CDCL) SAT solvers. We show that there are significant gains to be made in existing CDCL SAT solvers by providing a good initial branching. Further, we provide three labeling methods to find such initial branching orders in a tractable way. Finally, we train a graph neural network to predict these branching orders and show through our evaluations that a GNN-initialized ordering yields significant speedups on random 3-CNF and pseudo-industrial benchmarks, with generalization capabilities to instances much larger than the training set. However, we also find that the predictions fail at speeding up more difficult and industrial instances. We attribute this to the solver's dynamic heuristics, which rapidly overwrite the provided initialization, and to the complexity of these instances, making GNN prediction hard.

---

## 论文详细总结（自动生成）

这篇论文《Learning to Rank the Initial Branching Order of SAT Solvers》探讨了如何利用机器学习优化布尔可满足性（SAT）求解器的性能。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在冲突驱动子句学习（CDCL）SAT 求解器中，变量的分支顺序（Branching Order）对求解效率至关重要。虽然现代求解器拥有高效的动态启发式算法（如 VSIDS），但初始的分支顺序往往被忽视。
*   **研究动机**：现有的“神经-符号”混合求解器通常需要在求解过程中频繁调用神经网络，这带来了巨大的计算开销。本文旨在探索一种**非侵入式的预处理方法**：在求解开始前，利用图神经网络（GNN）预测一个优化的初始分支顺序，从而在不改变求解器核心逻辑的前提下提升速度。

### 2. 方法论
*   **核心思想**：将 SAT 实例表示为图，通过 GNN 预测变量的重要性得分，并据此初始化求解器的内部数据结构（如 VSIDS 的活动值和 VMTF 队列）。
*   **关键技术细节**：
    *   **图表示**：采用三部图（Tripartite Graph）结构，包含变量节点、子句节点和一个用于加速信息传递的“元节点（Meta-node）”。边权重表示变量在子句中的正负极性。
    *   **标签生成（三种方法）**：
        1.  **冲突标注（Conflict Labeling）**：统计变量在单次求解中参与冲突的次数。
        2.  **首变量标注（First Variable Labeling）**：通过多次随机实验，测量将特定变量置于首位时的平均传播数（Propagations）。
        3.  **遗传标注（Genetic Labeling）**：利用爬山算法优化变量排列，寻找使传播数最小化的顺序。
    *   **损失函数**：使用信息检索领域的 **LambdaRank** 排序损失函数，重点优化排在最前面的变量的准确性。
    *   **求解器集成**：修改了 MiniSat 和 CaDiCaL 求解器，通过初始化变量的活动水平（Activity Levels）来注入预测的顺序。

### 3. 实验设计
*   **数据集/场景**：
    *   **随机 3-CNF**：来自 SATLIB 库，变量规模从 20 到 250 不等。
    *   **G4SATBench**：包含组合问题和伪工业（Pseudo-industrial）问题的基准测试。
    *   **工业实例**：2015-2024 年 SAT 竞赛的主赛道题目。
*   **Benchmark 与对比方法**：
    *   **基准（Baselines）**：求解器默认顺序（Default）、随机顺序（Random）。
    *   **对比指标**：传播数（Propagations，系统无关指标）和墙钟时间（Wall-clock time）。

### 4. 资源与算力
*   **硬件使用**：
    *   **训练**：单张 **NVIDIA A100 GPU**。
    *   **标注与测试**：AMD EPYC 7742 64核 CPU。
*   **耗时**：
    *   训练 10 个 Epoch 耗时不到 1 分钟。
    *   使用 64 核 CPU 对 10,000 个实例进行标注耗时不到 1 分钟。
    *   推理时间：对于超过 150 个变量的实例，GNN 在 CPU 上的推理开销相对于求解时间是可以忽略不计的。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在随机 3-CNF 上使用了 9,000 个训练实例和 1,000 个验证实例。
    *   在 G4SATBench 上使用了 14,000 个训练实例。
    *   针对工业实例，在 DataBack 数据集上预训练并在 2015-2023 竞赛数据上微调。
*   **充分性评价**：实验设计较为全面，涵盖了从简单随机问题到复杂工业问题的梯度。通过消融实验对比了三种不同的标注方法，并验证了模型在变量规模扩大 5-10 倍时的泛化能力。实验过程考虑了系统随机性，使用了 95% 置信区间，表现出较高的客观性。

### 6. 主要结论与发现
*   **显著加速**：在随机 3-CNF 和伪工业实例上，GNN 预测的顺序使求解速度提升了 **10%-50%**。
*   **泛化能力**：模型在小规模实例（20-40 变量）上训练，能有效泛化到规模大得多的实例（250+ 变量）。
*   **工业实例的挑战**：在复杂的工业实例上，该方法未能表现出明显优势。
*   **失效原因分析**：
    1.  **启发式覆盖**：长时求解过程中，求解器自身的动态启发式算法会迅速覆盖初始设置。
    2.  **预测难度**：工业问题的结构极其复杂，GNN 难以捕捉到有效的排序特征。

### 7. 优点
*   **低开销集成**：作为预处理步骤，仅需在求解前调用一次 GNN，避免了在线神经启发式算法的高昂推理成本。
*   **标注创新**：提出了三种不同维度的标注策略（白盒、黑盒、优化），为后续研究提供了参考。
*   **排序视角**：将分支顺序问题转化为“学习排序（Learning to Rank）”问题，比传统的分类或回归更符合 SAT 求解的逻辑。

### 8. 不足与局限
*   **工业级应用受限**：目前无法在大型工业实例上取得突破，这限制了该方法的实际商用价值。
*   **动态性不足**：初始排序的效用随求解时间的增加而递减，缺乏在求解过程中持续引导的能力。
*   **小规模开销**：对于极简单的实例，GNN 的推理时间可能超过求解器本身的求解时间。

（完）
