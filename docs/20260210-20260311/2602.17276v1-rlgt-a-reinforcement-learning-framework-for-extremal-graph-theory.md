---
title: "RLGT: A reinforcement learning framework for extremal graph theory"
title_zh: RLGT：一种用于极值图论的强化学习框架
authors: "Ivan Damnjanović, Uroš Milivojević, Irena Đorđević, Dragan Stevanović"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17276v1"
tags: ["query:nco"]
score: 9.0
evidence: 强化学习用于极值图论中的组合优化
tldr: 本研究提出了RLGT，一个专门针对极值图论的强化学习框架。该框架系统化了以往利用强化学习解决图论问题的研究，支持无向图、有向图、自环及多色边等多种复杂图结构。通过优化计算性能和模块化设计，RLGT旨在为极值图论中的组合优化问题提供高效的自动化搜索工具，助力研究者发现数学猜想的反例和新的数值界限。
selection_source: fresh_fetch
motivation: 旨在解决现有强化学习在图论应用中缺乏系统性框架、支持图类型有限且计算效率有待提高的问题。
method: 开发了名为RLGT的模块化强化学习框架，支持多种图属性并优化了图的表示与计算性能。
result: 该框架成功系统化了前人关于拉普拉斯谱半径、拉姆齐数及图兰型问题的研究，并提供了更广泛的图结构支持。
conclusion: RLGT为极值图论研究提供了一个高效、通用且易于扩展的工具，将有力推动该领域自动化研究的发展。
---

## 摘要
强化学习（RL）是机器学习的一个子领域，专注于开发能够随时间自主学习最优决策策略的模型。在最近的一篇开创性论文中，Wagner 展示了如何通过将极值图论中的各种问题重新表述为组合优化问题，从而应用深度交叉熵强化学习方法来解决这些问题。随后，许多研究人员开始致力于改进和扩展 Wagner 引入的框架，从而创建了各种专门用于图论的强化学习环境。此外，通过使用强化学习，极值图论中的许多问题得到了解决。特别是，关于图的拉普拉斯谱半径（Laplacian spectral radius）的几个不等式被证伪，某些拉姆齐数（Ramsey numbers）获得了新的下界，并且在禁止结构为长度为 3 和 4 的圈的图兰型（Turán-type）极值问题上做出了贡献。在本文中，我们提出了图论强化学习（RLGT），这是一个新颖的强化学习框架，它系统化了之前的工作，并支持无向图和有向图（无论是否有自环），以及任意数量的边颜色。该框架能够高效地表示图，并旨在通过优化的计算性能以及简洁且模块化的设计，促进未来极值图论中基于强化学习的研究。

## Abstract
Reinforcement learning (RL) is a subfield of machine learning that focuses on developing models that can autonomously learn optimal decision-making strategies over time. In a recent pioneering paper, Wagner demonstrated how the Deep Cross-Entropy RL method can be applied to tackle various problems from extremal graph theory by reformulating them as combinatorial optimization problems. Subsequently, many researchers became interested in refining and extending the framework introduced by Wagner, thereby creating various RL environments specialized for graph theory. Moreover, a number of problems from extremal graph theory were solved through the use of RL. In particular, several inequalities concerning the Laplacian spectral radius of graphs were refuted, new lower bounds were obtained for certain Ramsey numbers, and contributions were made to the Turán-type extremal problem in which the forbidden structures are cycles of length three and four. Here, we present Reinforcement Learning for Graph Theory (RLGT), a novel RL framework that systematizes the previous work and provides support for both undirected and directed graphs, with or without loops, and with an arbitrary number of edge colors. The framework efficiently represents graphs and aims to facilitate future RL-based research in extremal graph theory through optimized computational performance and a clean and modular design.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RLGT**（Reinforcement Learning for Graph Theory），这是一个专门为极值图论研究设计的强化学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：极值图论关注在特定约束下使某个图不变量（如谱半径、能量等）达到最大或最小的图结构。近年来，Wagner 等人证明了强化学习（RL）在寻找数学猜想反例和发现新的数值界限方面具有巨大潜力。
*   **核心问题**：现有的图论 RL 工具存在碎片化、计算效率低、支持的图类型有限（通常仅限无向简单图）以及代码模块化程度不足等问题。
*   **研究动机**：开发一个统一、高效且灵活的框架，系统化以往的研究成果，并扩展对有向图、自环图及多色边图的支持，以降低数学家使用 RL 解决组合优化问题的门槛。

### 2. 方法论：核心思想与技术细节
RLGT 采用 Python 编写，基于模块化设计，主要分为三个核心包：
*   **`graphs`（图表示层）**：
    *   **多格式支持**：支持 8 种图表示格式（如位掩码、邻接矩阵、展平的行优先/顺时针格式等），并能自动进行格式转换。
    *   **扩展性**：原生支持有向图、无向图、自环以及任意 $k$ 种边颜色（以往框架多仅支持 2 色）。
    *   **向量化计算**：利用 NumPy 的向量化特性，支持以“批处理（Batch）”形式同时操作多个图，显著提升计算效率。
*   **`environments`（环境层）**：
    *   提供了 9 种 RL 环境，分为三类：**线性环境**（逐边构建）、**全局环境**（随机翻转/重着色）和**局部环境**（基于顶点的游走与修改）。
    *   支持“稀疏”和“密集”两种奖励反馈模式。
*   **`agents`（智能体层）**：
    *   集成了三种主流 RL 算法：**深度交叉熵方法 (DCE)**、**REINFORCE** 和 **近端策略优化 (PPO)**。
    *   使用 PyTorch 实现神经网络逻辑，并支持自定义随机动作机制以增强探索能力。

### 3. 实验设计
论文通过三个具体的极值图论应用展示了框架的能力：
*   **应用 1：拉普拉斯谱半径**：尝试证伪 Brankov 等人提出的 68 个关于拉普拉斯谱半径上界的不等式。
*   **应用 2：图能量与匹配数**：针对 Akbari 等人关于图能量 $E(G)$ 与匹配数 $\nu(G)$ 关系的猜想寻找反例。
*   **应用 3：Mostar 指数**：尝试寻找 Mostar 指数最大值的反例（用于验证框架在无法证伪猜想时的启发式搜索能力）。
*   **Benchmark**：主要对比和复现了 Wagner (2021) 和 Ghebleh (2024) 的研究结果。

### 4. 资源与算力
*   **硬件说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **软件栈**：明确提到了使用 Python、NumPy（用于向量化）、PyTorch（用于深度学习模型）、Poetry（依赖管理）以及 SageMath（用于计算复杂的图不变量，如匹配数）。
*   **效率提示**：作者强调了 NumPy 向量化在现代高端计算机上能提供满意的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   成功复现并证伪了 25 个拉普拉斯谱半径相关的猜想。
    *   在图能量问题中，成功发现了 14 阶图的反例，并识别出了反例的结构模式。
    *   对 Mostar 指数进行了多轮搜索（如 $n=21$ 的案例）。
*   **充分性评价**：实验覆盖了从谱图论到结构图论的不同领域，涵盖了证伪已知猜想和验证开放猜想两种场景。实验设计较为客观，通过复现前人工作证明了框架的可靠性。

### 6. 主要结论与发现
*   **框架有效性**：RLGT 能够高效地复现并超越以往专门化工具的研究成果。
*   **算法偏好**：实验发现 **深度交叉熵 (DCE) 智能体结合线性构建环境** 在解决此类极值图问题时表现最稳定、最高效。
*   **局限性发现**：REINFORCE 和 PPO 在非线性环境（如局部/全局环境）中容易受到奖励函数波动（如处理连通性约束）的影响，导致训练不稳定。

### 7. 优点
*   **高度模块化**：图表示、环境逻辑和学习算法完全解耦，易于扩展。
*   **通用性强**：打破了以往工具对图类型的限制，支持多色边和有向图，适用范围极广。
*   **易用性**：提供了自动格式转换和批处理支持，降低了从数学定义到 RL 状态空间的转换难度。
*   **工程质量**：采用了现代软件工程实践（单元测试、依赖管理、代码规范），保证了研究的可复现性。

### 8. 不足与局限
*   **算力细节缺失**：未提供具体的训练时间对比，难以量化评估其相对于前代工具的加速比。
*   **算法稳定性**：在处理具有严格结构约束（如必须连通）的图不变量时，非线性环境下的 RL 算法（PPO/REINFORCE）表现欠佳，容易陷入局部最优。
*   **超参数敏感性**：虽然框架易用，但针对特定数学问题，仍需人工精细调整神经网络架构和奖励函数（如对不连通图施加惩罚）。

（完）
