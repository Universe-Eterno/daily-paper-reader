---
title: A Canceling Heuristic for the Directed Traveling Salesman Problem
title_zh: 一种用于有向旅行商问题的消去启发式算法
authors: "Steffen Borgwardt, Zachary Sorenson"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26564v1"
tags: ["query:nco"]
score: 9.0
evidence: 针对旅行商问题（TSP）的启发式算法
tldr: 针对经典的定向旅行商问题（TSP），本文提出了一种基于最小费用流问题的启发式改进算法。该方法借鉴了流网络中的循环取消（cycle canceling）思想，通过在给定路径与外部弧之间交替搜索循环来优化次优解。研究表明，该方法在搜索过程中产生的子回路较少，配合简单的修补步骤即可有效缩小与最优解的差距，显著提升求解质量。
selection_source: fresh_fetch
motivation: 旨在利用最小费用流问题中成熟的循环取消算法，探索其在改进TSP局部最优解方面的潜力。
method: 提出一种限制搜索空间的启发式方法，通过寻找在路径内外弧交替的循环或环流来优化路径，并辅以轻量级修补步骤。
result: 实验证明该方法生成的子回路数量较少，且在闭合最优解差距方面表现出极高的成功率。
conclusion: 将流取消机制应用于TSP优化是可行且高效的，为解决定向旅行商问题提供了一种结合流理论的新型启发式策略。
---

## 摘要
旅行商问题（TSP）是组合优化中经典且困难的问题之一。我们开发了一种新的启发式算法，利用最小费用流问题与 TSP 之间的联系来改进给定的次优路径，例如使用经典启发式算法找到的局部最优解。最小费用流问题可以通过线性规划或基于圈消去（cycle canceling）的组合算法高效求解。我们研究了在 TSP 背景下流消去（flow-canceling）的潜力。通过将搜索空间限制在路径内外弧交替的圈和环流中，实验结果表明，仅产生了少量的子路径，且轻量级的修补步骤足以实现较高的成功率并缩小与最优解之间的差距。

## Abstract
The Traveling Salesman Problem (TSP) is one of the classic and hard problems in combinatorial optimization. We develop a new heuristic that uses a connection between Minimum Cost Flow Problems and the TSP to improve on a given suboptimal tour, such as a local optimum found using a classic heuristic.   Minimum Cost Flow Problems can be solved efficiently through linear programming or combinatorial algorithms based on cycle canceling. We investigate the potential of flow-canceling in the context of the TSP. Through a restriction of the search space to cycles and circulations that alternate between arcs in- and outside of the tour, practical results exhibit that only a low number of subtours is created, and a lightweight patching step suffices for a high success rate and gap closure towards an optimum.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Cycap**（Cycle Cancel and Patch）的新型启发式算法，旨在解决有向旅行商问题（DTSP）。以下是对该论文的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何跳出经典启发式算法（如 $k$-opt）在解决旅行商问题（TSP）时容易陷入的局部最优解。
*   **研究动机**：TSP 是组合优化中的 NP-hard 问题。虽然 $k$-opt 及其变体（如 Lin-Kernighan）在对称 TSP 上表现优异，但在有向 TSP（DTSP）中效果受限。作者注意到 TSP 可以看作是最小费用流问题（Minimum Cost Flow）中 0,1-环流问题的特殊形式。因此，他们试图引入网络流理论中成熟的“循环消去”（Cycle Canceling）技术来改进 TSP 的路径质量。

### 2. 论文提出的方法论：Cycap 启发式算法
核心思想是将当前路径视为一个流，通过在残余网络中寻找并消去“负费用循环”来降低总成本。
*   **路径图（Tour Graph）与路径交替循环**：定义了一种特殊的残余网络，其中的弧分为“插入弧”（非路径弧）和“删除弧”（路径弧的反向）。算法搜索的是**路径交替循环（Tour-alternating cycles）**，即在路径内外弧之间交替切换的循环。
*   **分离图（Separated Graph）构造**：这是关键技术细节。为了高效找到路径交替循环，作者构造了一个双分图（Bipartite Graph）。该构造保证了双分图中的任何有向循环都能一一对应回原图中的路径交替循环，且费用一致。
*   **算法流程**：
    1.  **初始化**：运行 $k$-opt 获得一个局部最优路径。
    2.  **分离图转换**：基于当前路径构造分离图。
    3.  **负循环/环流检测**：在分离图中寻找负费用结构。
    4.  **消去步骤（Cancel Step）**：更新流，这会导致路径成本降低，但可能会产生多个不连通的子回路（Subtours）。
    5.  **修补步骤（Patching）**：使用改进的修补算法将子回路和孤立顶点重新连接成一条完整的哈密顿回路。
*   **三种变体**：
    *   **Cycap-F**：基于 Floyd-Warshall 算法检测负循环。
    *   **Cycap-M**：寻找最小平均费用循环（Minimum-mean cycle）。
    *   **Cycap-C**：通过线性规划（LP）求解最小费用环流（Minimum-cost circulation）。

### 3. 实验设计
*   **数据集**：使用了经典的 **TSPLIB** 基准测试库，涵盖了对称 TSP（如 22cities, pr76, 128cities）和有向 TSP（如 ftv 系列, kro124p）。
*   **Benchmark（对比方法）**：
    *   基础算法：2-opt, 3-opt, 以及 2+3-opt 组合。
    *   针对有向图，使用了作者改进的 $k^*$-opt（考虑了路径段反转的成本差异）。
*   **评估指标**：成功率（改进局部最优的比例）、缺口闭合率（Gap Closure，即缩小与最优解差距的百分比）和运行时间。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 CPU/GPU 型号）。
*   **计算复杂度**：作者在理论上分析了算法复杂度。分离图构造为 $\Theta(n^2)$，Floyd-Warshall 变体为 $O(n^3)$，环流 LP 求解则取决于具体的 LP 求解器效率。实验中提到对于 124 个节点的规模，Cycap-C 的运行时间在几秒到几十秒之间。

### 5. 实验数量与充分性
*   **实验规模**：每个配置均进行了 **100 次独立运行**，以消除随机性影响。
*   **充分性**：实验覆盖了从 20 到 128 个节点的不同规模，对比了对称与有向两种场景。
*   **客观性**：作者不仅报告了 Cycap 自身的改进，还测试了“Cycap + 第二次 $k$-opt”的组合效果，展示了该算法作为其他启发式算法插件的潜力。实验设计较为公平，对 $k$-opt 运行时间设置了合理的上限。

### 6. 主要结论与发现
*   **Cycap-C 表现最优**：在有向 TSP 上，基于环流（Circulation）的变体在成功率和缺口闭合率上显著优于基于单循环的变体，且运行速度更快。
*   **有向图效果更佳**：该算法在有向 TSP 上的表现远超对称 TSP，通常能闭合 70% 以上的优化缺口。
*   **子回路数量极少**：实验发现消去步骤后产生的子回路数量通常非常少（中位数为 2-4 个），这使得简单的修补步骤就能保持高质量的解。
*   **跳出局部最优**：证明了 Cycap 可以找到 $k$-opt（即使是 $k=3$）无法发现的改进路径。

### 7. 优点
*   **理论结合紧密**：将成熟的网络流理论巧妙地应用于 TSP 领域，提供了坚实的数学基础。
*   **分离图创新**：通过分离图构造简化了复杂的路径交替搜索问题。
*   **灵活性**：可以作为现有 TSP 求解器的增强模块，易于集成。

### 8. 不足与局限
*   **规模限制**：虽然在 100+ 节点表现良好，但对于数千或数万节点的超大规模 TSP，Floyd-Warshall 或 LP 求解的开销可能会变得巨大。
*   **对称 TSP 优势不明显**：在对称 TSP 上，虽然有改进，但相对于已非常成熟的 Lin-Kernighan 等算法，其竞争力稍弱。
*   **修补步骤的贪婪性**：修补步骤采用贪婪策略，虽然简单高效，但可能会损失一部分由消去步骤带来的成本收益。

（完）
