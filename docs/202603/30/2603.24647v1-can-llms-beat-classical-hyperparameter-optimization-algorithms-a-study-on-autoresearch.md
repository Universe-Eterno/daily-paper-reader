---
title: Can LLMs Beat Classical Hyperparameter Optimization Algorithms? A Study on autoresearch
title_zh: LLM 能击败经典的超参数优化算法吗？关于 autoresearch 的研究
authors: "Fabio Ferreira, Lucca Wobbe, Arjun Krishnakumar, Frank Hutter, Arber Zela"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24647v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 用于超参数优化和代码编辑的LLM智能体
tldr: 本研究探讨了LLM在超参数优化（HPO）中与传统算法的竞争能力。通过autoresearch框架，研究发现虽然传统算法在固定空间内表现更佳，但LLM通过直接编辑代码在非约束空间展现了潜力。为结合两者优势，作者提出了Centaur混合方法，将CMA-ES的内部状态共享给LLM。实验证明，Centaur显著提升了优化效果，且0.8B的小型模型在混合模式下即可超越大型模型，证明了领域知识与数值优化结合的有效性。
selection_source: fresh_fetch
motivation: 旨在探究LLM在超参数优化任务中能否超越传统算法，并解决LLM难以追踪优化状态及传统算法缺乏领域知识的问题。
method: 对比了LLM代码编辑与传统HPO算法，并提出了一种将CMA-ES内部状态（如均值向量、步长）与LLM结合的混合优化方法Centaur。
result: Centaur混合方法在实验中表现最佳，且其0.8B版本优于27B版本，证明了小型LLM配合强力传统优化器的有效性。
conclusion: 将LLM的领域知识与传统优化器的数值状态相结合是实现高效HPO的关键，且在混合模式下小型模型比大型模型更具性价比。
---

## 摘要
autoresearch 仓库使 LLM 智能体能够通过直接编辑训练代码，在无约束的搜索空间中搜索最优超参数配置。在给定的固定计算预算和约束条件下，我们使用 autoresearch 作为测试平台，在微调小型语言模型的超参数任务上，对比了经典超参数优化（HPO）算法与基于 LLM 的方法。在固定的超参数搜索空间内，CMA-ES 和 TPE 等经典 HPO 方法的表现始终优于基于 LLM 的智能体。然而，尽管仅使用了自托管的开源 27B 模型，但在无约束搜索空间中直接编辑训练源码的 LLM 智能体大幅缩小了与经典方法之间的差距。避免显存溢出（OOM）失败的方法优于搜索多样性更高的方法，这表明可靠性比探索广度更重要。虽然中小型 LLM 难以在多次试验中追踪优化状态，但经典方法缺乏领域知识。为了弥补这一差距，我们引入了 Centaur，这是一种将 CMA-ES 的内部状态（包括均值向量、步长和协方差矩阵）与 LLM 共享的混合方法。Centaur 在我们的实验中取得了最佳结果，其 0.8B 变体的表现优于 27B 变体，这表明当与强大的经典优化器配对时，廉价的 LLM 就足够了。0.8B 模型不足以进行无约束的代码编辑，但足以进行混合优化；而在所测试的开源模型中，将模型规模扩大到 27B 对于固定搜索空间方法并没有带来优势。代码可在 https://github.com/ferreirafabio/autoresearch-automl 获取。

## Abstract
The autoresearch repository enables an LLM agent to search for optimal hyperparameter configurations on an unconstrained search space by editing the training code directly. Given a fixed compute budget and constraints, we use \emph{autoresearch} as a testbed to compare classical hyperparameter optimization (HPO) algorithms against LLM-based methods on tuning the hyperparameters of a small language model. Within a fixed hyperparameter search space, classical HPO methods such as CMA-ES and TPE consistently outperform LLM-based agents. However, an LLM agent that directly edits training source code in an unconstrained search space narrows the gap to classical methods substantially despite using only a self-hosted open-weight 27B model. Methods that avoid out-of-memory failures outperform those with higher search diversity, suggesting that reliability matters more than exploration breadth. While small and mid-sized LLMs struggle to track optimization state across trials, classical methods lack domain knowledge. To bridge this gap, we introduce Centaur, a hybrid that shares CMA-ES's internal state, including mean vector, step-size, and covariance matrix, with an LLM. Centaur achieves the best result in our experiments, with its 0.8B variant outperforming the 27B variant, suggesting that a cheap LLM suffices when paired with a strong classical optimizer. The 0.8B model is insufficient for unconstrained code editing but sufficient for hybrid optimization, while scaling to 27B provides no advantage for fixed search space methods with the open-weight models tested. Code is available at https://github.com/ferreirafabio/autoresearch-automl.

---

## 论文详细总结（自动生成）

### 论文总结：LLM 能击败经典的超参数优化算法吗？关于 autoresearch 的研究

#### 1. 论文的核心问题与整体含义
本文的研究动机源于 Andrej Karpathy 开发的 `autoresearch` 项目，该项目展示了 LLM 智能体可以通过直接修改训练代码来优化模型性能。然而，后续研究指出经典的超参数优化（HPO）算法（如 TPE）在相同预算下可能表现更好。
**核心问题**：在超参数优化任务中，基于 LLM 的方法（包括直接编辑代码和在固定空间搜索）能否超越 CMA-ES、TPE 等经典算法？
**研究背景**：HPO 是 AutoML 的核心，LLM 的引入带来了领域知识，但其在数值优化和状态追踪方面的能力尚存疑问。

#### 2. 论文提出的方法论
论文评估了三种路径：经典 HPO、纯 LLM 优化、以及一种新提出的混合方法。
*   **自动化搜索空间构建**：利用抽象语法树（AST）解析 `train.py`，自动提取所有大写命名的超参数（共 14 个），减少人工干预。
*   **Centaur（半人马）混合优化器**：这是本文的核心创新。
    *   **核心思想**：将经典优化器 CMA-ES 的内部数学状态（均值向量 $\mu$、步长 $\sigma$、协方差矩阵 $C$）与 LLM 共享。
    *   **流程**：在 70% 的试验中由 CMA-ES 提出建议；在 30% 的试验中，将 CMA-ES 的内部状态、历史 Top-5 配置及最近 20 次试验记录输入 LLM，由 LLM 覆盖（Override）并提出新建议。
    *   **优势**：结合了 CMA-ES 的数值景观学习能力和 LLM 的领域直觉（如对 Transformer 架构的理解）。

#### 3. 实验设计
*   **场景与任务**：优化 `nanochat`（约 50M 参数的 Decoder-only Transformer），在 FineWeb 数据集上训练，目标是最小化验证集比特每字节（val_bpb）。
*   **Benchmark 与对比方法**：
    *   **经典方法**：TPE、CMA-ES、SMAC、随机搜索（Random Search）。
    *   **LLM 方法**：LLAMBO（两种变体）、Karpathy Agent（固定空间版）、Karpathy Agent（代码编辑版）。
    *   **混合方法**：Centaur（0.8B 和 27B 变体）。

#### 4. 资源与算力
*   **硬件**：每组实验使用单台 **NVIDIA H200 GPU**（141 GB HBM3e 显存）。
*   **预算**：每种方法固定运行 **24 小时**。
*   **LLM 配置**：使用 vLLM 自托管开源模型 **Qwen3.5-27B** 和 **Qwen3.5-0.8B**。为了公平，训练显存限制在 80GB，剩余显存留给 LLM 推理。

#### 5. 实验数量与充分性
*   **实验规模**：共测试了 9 种方法，每种方法运行 3 个随机种子。
*   **消融实验**：
    *   对比了 LLM 模型规模（0.8B vs 27B）的影响。
    *   对 Centaur 中 LLM 介入的比例（$r$）进行了消融研究（0.1 到 0.8）。
    *   初步对比了闭源模型 Gemini 2.5 Flash。
*   **客观性**：实验在相同的计算预算和硬件环境下进行，通过 AST 自动提取参数减少了人类先验知识的偏差，实验设计较为严谨、公平。

#### 6. 论文的主要结论与发现
*   **经典算法仍具优势**：在固定的 14 个超参数搜索空间内，TPE 和 CMA-ES 的表现始终优于纯 LLM 方法。
*   **代码编辑的潜力**：直接编辑源码的 LLM 智能体（Karpathy Agent Code）表现优于固定空间的 LLM，且大幅缩小了与经典算法的差距。
*   **可靠性胜过多样性**：LLM 方法的高失败率（OOM，显存溢出）是其瓶颈。能够学习避开 OOM 区域的算法（如经典算法）比搜索多样性高的算法表现更好。
*   **Centaur 夺冠**：混合方法 Centaur 取得了最佳结果。有趣的是，**0.8B 的小模型在混合模式下优于 27B 模型**，说明在有强大优化器辅助时，LLM 仅需提供微调建议，无需极强的推理能力。
*   **规模效应的差异**：对于代码编辑，模型规模至关重要（27B 远强于 0.8B）；但对于固定空间的优化，增加模型参数并无显著收益。

#### 7. 优点
*   **混合策略新颖**：Centaur 通过共享优化器内部状态（而非仅历史记录），为 LLM 提供了更深层的优化上下文。
*   **工程实践性强**：自动化提取超参数和严格的算力预算控制，使其结果对实际生产环境具有参考价值。
*   **揭示了 LLM 的局限**：明确指出中小型 LLM 在追踪优化状态和处理数值约束（如 OOM）方面的短板。

#### 8. 不足与局限
*   **任务单一**：仅在 `nanochat` 一个任务上进行了测试，尚未验证在其他领域（如 CV 或强化学习）的泛化性。
*   **搜索范围仍需人工**：虽然超参数是自动提取的，但每个参数的取值范围（Range）仍需人工指定，这依然引入了人类先验。
*   **模型覆盖有限**：主要测试了 Qwen 系列开源模型，虽然对比了 Gemini，但未深入探讨更多前沿大模型（如 GPT-4o 或 Claude 3.5）在代码编辑上的极限。

（完）
