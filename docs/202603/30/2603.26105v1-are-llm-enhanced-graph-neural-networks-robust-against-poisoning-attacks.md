---
title: Are LLM-Enhanced Graph Neural Networks Robust against Poisoning Attacks?
title_zh: LLM增强的图神经网络对中毒攻击具有鲁棒性吗？
authors: "Yuhang Ma, Jie Wang, Zheng Yan"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26105v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大模型增强的图神经网络
tldr: 本研究针对大语言模型（LLM）增强的图神经网络（GNN）在投毒攻击下的鲁棒性进行了系统评估。通过构建包含24种模型组合、多种结构与文本攻击手段的评估框架，研究发现在面对图结构和文本属性的恶意篡改时，LLM增强的GNN相比传统浅层嵌入模型表现出显著更高的准确率和更强的抗干扰能力。该研究不仅揭示了LLM特征增强带来的鲁棒性优势及其成因，还为未来的图学习攻防研究提供了新的基准和方向。
selection_source: fresh_fetch
motivation: 旨在填补LLM增强的GNN在面对同时操纵图结构和文本属性的投毒攻击时鲁棒性研究的空白。
method: 提出了一个综合评估框架，对8种LLM/LM增强器与3种GNN骨干组成的24个模型在多种结构和文本攻击下进行测试。
result: 实验证明LLM增强的GNN在各种攻击场景下均优于浅层嵌入基准，具有更高的准确性和更低的相对性能下降。
conclusion: LLM增强的GNN因其对结构和标签信息的有效编码而更具鲁棒性，为图学习的攻防研究提供了新见解。
---

## 摘要
大语言模型（LLMs）通过利用语义特征丰富节点表示，推动了图神经网络（GNNs）的发展，催生了在性能上取得显著提升的LLM增强型GNN。然而，这些模型在面对中毒攻击（在训练期间操纵图结构和文本属性）时的鲁棒性仍未得到探索。为了填补这一空白，我们提出了一个鲁棒性评估框架，系统地评估了中毒攻击下的LLM增强型GNN。我们的框架支持多维度的全面评估。具体而言，我们通过将八种基于LLM或语言模型（LM）的特征增强器与三种代表性的GNN骨干网络相结合，评估了24个受害者模型。为了确保攻击覆盖的多样性，我们纳入了六种结构中毒攻击（包括针对性和非针对性）以及三种在字符、单词和句子层面运行的文本中毒攻击。此外，我们采用了四个真实世界数据集，其中包括一个在LLM出现后发布的数据集，以避免LLM预训练期间潜在的基准真相（ground truth）泄露，从而确保评估的公平性。大量实验表明，在各种攻击设置下，LLM增强型GNN比基于浅层嵌入的基准模型表现出显著更高的准确率和更低的相对准确率下降（RDA）。我们的深入分析确定了促成这种鲁棒性的关键因素，例如节点表示中结构和标签信息的有效编码。基于这些见解，我们从攻击和防御两个角度概述了未来的研究方向，并提出了一种新的组合攻击以及一种图净化防御方法。为了支持未来的研究，我们在 https://github.com/CyberAlSec/LLMEGNNRP 发布了框架的源代码。

## Abstract
Large Language Models (LLMs) have advanced Graph Neural Networks (GNNs) by enriching node representations with semantic features, giving rise to LLM-enhanced GNNs that achieve notable performance gains. However, the robustness of these models against poisoning attacks, which manipulate both graph structures and textual attributes during training, remains unexplored. To bridge this gap, we propose a robustness assessment framework that systematically evaluates LLM-enhanced GNNs under poisoning attacks. Our framework enables comprehensive evaluation across multiple dimensions. Specifically, we assess 24 victim models by combining eight LLM- or Language Model (LM)-based feature enhancers with three representative GNN backbones. To ensure diversity in attack coverage, we incorporate six structural poisoning attacks (both targeted and non-targeted) and three textual poisoning attacks operating at the character, word, and sentence levels. Furthermore, we employ four real-world datasets, including one released after the emergence of LLMs, to avoid potential ground truth leakage during LLM pretraining, thereby ensuring fair evaluation. Extensive experiments show that LLM-enhanced GNNs exhibit significantly higher accuracy and lower Relative Drop in Accuracy (RDA) than a shallow embedding-based baseline across various attack settings. Our in-depth analysis identifies key factors that contribute to this robustness, such as the effective encoding of structural and label information in node representations. Based on these insights, we outline future research directions from both offensive and defensive perspectives, and propose a new combined attack along with a graph purification defense. To support future research, we release the source code of our framework at~\url{https://github.com/CyberAlSec/LLMEGNNRP}.

---

## 论文详细总结（自动生成）

以下是对论文《Are LLM-Enhanced Graph Neural Networks Robust against Poisoning Attacks?》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）的发展，研究者开始利用 LLM 提取文本属性的深层语义来增强图神经网络（GNN），形成了 **LLM增强型GNN**。虽然这类模型在性能上取得了显著提升，但其在**中毒攻击（Poisoning Attacks）**下的鲁棒性尚不明确。中毒攻击通过在训练阶段恶意篡改图结构或节点文本属性来破坏模型。
本文的研究动机在于填补这一空白，解决现有研究中模型覆盖不全、攻击类型单一以及由于 LLM 预训练数据包含基准数据集而导致的“标签泄露”评估不公等问题。

### 2. 方法论：核心思想与技术细节
论文提出了一个系统的鲁棒性评估框架，涵盖了从数据准备到攻击实施、模型训练及多维度评估的全流程：
*   **三种集成范式**：
    1.  **LLM-Explanation（解释型）**：利用 LLM 生成文本解释，再由小规模语言模型（LM）编码（如 TAPE, KEA）。
    2.  **LLM-Embedding（嵌入型）**：直接利用 LLM 的隐藏层或 API 获取嵌入（如 LLaMA-2, OpenAI TE3L）。
    3.  **LM-Embedding（微调型）**：对中等规模 LM 进行任务相关的微调（如 SimTeG, E5-Large）。
*   **双模态攻击体系**：
    *   **结构攻击**：包括针对性攻击（Nettack, SGA, NAG-R）和非针对性攻击（Mettack, DICE, PGA），通过增删边破坏拓扑。
    *   **文本攻击**：涵盖字符级（DeepWordBug）、单词级（Bert-Attack）和句子级（MAYA）的对抗性文本篡改。
*   **评估指标**：采用准确率（ACC）和相对准确率下降（RDA）来衡量绝对和相对鲁棒性。

### 3. 实验设计
*   **数据集**：使用了四个真实世界文本属性图（TAG）数据集：Cora、Pubmed、Ogbn-products，以及**Tape-arxiv23**（关键点：该数据集发布于 LLM 爆发之后，有效避免了预训练阶段的基准真相泄露）。
*   **基准模型（Benchmark）**：以浅层嵌入方法（BoW, TF-IDF, Word2Vec）作为对比基准。
*   **受害者模型**：组合了 8 种 LLM/LM 增强器与 3 种经典 GNN 骨干（GCN, GAT, GraphSAGE），共计 24 种模型组合。
*   **实验设置**：采用灰盒设置（攻击者已知数据但不知模型参数），模拟实际应用场景。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4070 Ti Super** 的机器上运行。
*   **算力消耗**：文中提到，使用 LLaMA-2-7b 模型为 Pubmed 数据集生成嵌入大约需要 **1 小时**。
*   **软件框架**：基于 PyTorch、PyG、DeepRobust 和 OpenAttack 构建。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了 24 种模型在 9 种攻击算法、不同扰动率（低/高）下的表现。
*   **分析维度**：除了性能测试，还进行了 t-SNE 可视化、Davies-Bouldin 指数（DBI）分析、互信息（MI）计算等，探讨了标签信息和结构信息的保留情况。
*   **充分性评价**：实验设计非常充分且客观。通过引入 post-LLM 数据集和控制变量分析（如数据重叠率实验），有力地证明了鲁棒性提升源于架构集成而非简单的知识泄露。

### 6. 主要结论与发现
*   **LLM 显著增强鲁棒性**：在所有攻击场景下，LLM 增强型 GNN 的鲁棒性均远优于浅层嵌入基准，尤其在高扰动率下优势更明显。
*   **结构攻击更具威胁**：相比文本属性攻击，图结构中毒对模型性能的破坏力更大。
*   **骨干网络差异**：在 GNN 骨干中，**GraphSAGE** 表现出最强的鲁棒性，归功于其特征连接和均值聚合机制。
*   **鲁棒性来源**：LLM 生成的高质量嵌入能更有效地编码标签语义和局部结构属性，即使结构被破坏，语义特征仍能提供判别力。
*   **防御潜力**：利用 LLM 嵌入的余弦相似度进行图净化（过滤异常边）是一种简单且极其有效的防御手段。

### 7. 优点
*   **评估全面**：是目前针对 LLM-GNN 鲁棒性覆盖模型最广、攻击类型最全的研究。
*   **公平性考量**：通过使用 2023 年后的数据集，解决了 LLM 研究中普遍存在的“测试集在训练集中”的偏见问题。
*   **深度洞察**：不仅给出了实验结果，还通过多种量化指标解释了 LLM 增强鲁棒性的内在机理。
*   **前瞻性**：提出了组合攻击和基于净化的防御方案，为后续研究指明了方向。

### 8. 不足与局限
*   **攻击设置限制**：主要集中在灰盒攻击，未深入探讨白盒攻击（已知模型梯度）下的极端情况。
*   **计算成本**：虽然 LLM 提升了鲁棒性，但生成大规模图节点的嵌入（尤其是使用 7B 以上模型时）计算开销巨大，限制了实时应用。
*   **防御参数敏感性**：提出的图净化防御依赖于阈值选择，而该阈值在不同同质性（Homophily）的图上表现不一，缺乏自适应调整机制。

（完）
