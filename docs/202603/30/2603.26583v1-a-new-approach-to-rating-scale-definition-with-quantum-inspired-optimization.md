---
title: A new approach to rating scale definition with quantum-inspired optimization
title_zh: 一种基于量子启发式优化的评级量表定义新方法
authors: "Patrizio Spada, Laura Cappelli, Francesca Cibrario, Christian Mattia, Daniele Magnaldi, Matteo Argenton, Enrico Calore, Sebastiano Fabio Schifano, Concezio Bozzi, Davide Corbelletto"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26583v1"
tags: ["query:nco"]
score: 8.0
evidence: 使用QUBO和启发式算法解决复杂的组合约束优化问题
tldr: 在金融领域，为贷款申请人定义信用评级量表是一个受严格制度约束的复杂组合优化问题。本文提出了一种基于二次无约束二进制优化（QUBO）模型的新方法，该模型特别适用于量子硬件。通过使用经典启发式算法验证并与暴力破解法进行基准测试，研究证明了该框架在处理复杂金融约束下的评级量表定义问题时具有一致的解质量和良好的适用性，为未来量子计算在金融领域的应用奠定了基础。
selection_source: fresh_fetch
motivation: 金融机构在定义信用评级量表时面临复杂的组合约束优化挑战，传统方法难以高效处理。
method: 提出一种适用于量子硬件的二次无约束二进制优化（QUBO）模型来重新表述评级量表定义问题。
result: 通过经典启发式算法验证了模型有效性，并与暴力破解法对比显示出一致的高质量解。
conclusion: 该量子启发式优化框架为解决复杂金融约束下的信用评级问题提供了一种高效且具扩展性的新途径。
---

## 摘要
在金融领域，评估贷款申请人的信誉要求贷款机构使用评级量表对借款人进行聚类。金融机构必须在遵守严格制度约束的情况下定义这些量表，这导致需要解决一个复杂的组合约束优化问题。本研究探讨了如何使用二次无约束二值优化（QUBO）模型来解决这一问题，该模型是一种适用于量子硬件的公式化表达。我们通过使用经典启发式算法测试所提出的公式化方法来验证该方法。随后，我们将结果与暴力破解法进行基准测试，以证明解的一致性质量，并突出该框架在更复杂场景下的适用性。

## Abstract
In finance, assessing the creditworthiness of loan applicants requires lenders to cluster borrowers using rating scales. Financial institutions must define the scales in compliance with strict institutional constraints, resulting in solving a complex combinatorial constrained optimization problem. This contribution studies how to solve this problem using a Quadratic Unconstrained Binary Optimization (QUBO) model, a formulation suitable for quantum hardware. We validate this approach by testing the proposed formulation with classical heuristics. We then benchmark the results against a brute-force method to demonstrate consistent solution quality and highlight the framework's suitability for more complex scenarios.

---

## 论文详细总结（自动生成）

这篇论文提出了一种利用量子启发式优化（特别是 QUBO 模型）来定义金融信用评级量表的新方法。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在银行信用风险管理中，评级量表定义（Rating Scale Definition）是信用评分的第三阶段。它要求将成千上万名借款人根据信用分数划分为有限的、非空的等级（Grades）。
*   **核心挑战**：这一过程受到欧盟法律（如 ECB 和 EBA 指南）的严格约束，包括单调性、集中度、等级规模等。这本质上是一个复杂的**约束组合优化问题**。随着借款人数量 $n$ 和等级数量 $m$ 的增加，可能的划分方案呈指数级增长，传统暴力搜索在计算上是不可行的。
*   **研究动机**：探索量子计算（或量子启发式算法）是否能通过二次无约束二进制优化（QUBO）模型，比传统方法更高效地处理这些复杂的金融约束。

### 2. 论文提出的方法论
*   **核心思想**：将带约束的组合优化问题转化为无约束的最小化问题。通过引入惩罚项（Penalties），将金融约束嵌入到目标函数中，使其适用于量子退火机或 gate-based 量子硬件。
*   **关键技术细节**：
    *   **变量定义**：使用二进制决策变量 $x_{ij}$，表示第 $i$ 个借款人是否属于第 $j$ 个等级。
    *   **逻辑约束**：构建“二进制阶梯矩阵”（BSM），确保每个借款人仅属于一个等级，且等级顺序与分数顺序一致。
    *   **金融约束参数化**：
        *   **单调性**：确保违约率随等级索引增加而上升。论文采用了一种线性化近似方法来避免引入过多的辅助变量。
        *   **集中度**：利用调整后的赫芬达尔指数（Herfindahl Index）来防止借款人过度集中在某些等级。
        *   **等级规模阈值**：通过**松弛变量（Slack Variables）**将不等式约束（如等级人数占比在 1% 到 15% 之间）转化为 QUBO 形式。
    *   **目标函数**：$\Xi(\vec{x}, \vec{s}) = \Xi_{逻辑} + \Xi_{单调} + \Xi_{集中} + \Xi_{规模}$，每一项都配有权重系数 $\mu$。

### 3. 实验设计
*   **数据集**：使用符合现实分布的**合成数据集**，模拟信用分数和违约事件。
*   **基准测试（Benchmark）**：
    *   **暴力破解法（Brute-force）**：开发了一个约束组合算法，遍历所有可能的划分方案，用于评估小规模问题的计算耗时。
    *   **Gurobi 优化器**：作为主要的经典对比方法，利用其 `solve_qubo` 功能处理中大规模实例。
    *   **D-Wave 工具**：使用 D-Wave 的 `dimod` 库进行小规模验证。
*   **对比维度**：计算时间、解的质量（是否满足所有约束）、模型的可扩展性。

### 4. 资源与算力
*   **硬件平台**：实验主要在 **LEONARDO 超级计算机**（位于 Cineca）上运行。
*   **软件工具**：使用了 Gurobi 商业优化器（学术许可）和 Python 库（如 `dimod`）。
*   **算力细节**：文中未详细列出具体的 GPU 数量或 CPU 核心数，但提到暴力破解法在处理 $n=70, m=6$ 的小规模问题时已需耗时约 28 小时，而 QUBO 模型配合 Gurobi 在 3 分钟内即可找到 $n=175, m=9$ 的可行解。

### 5. 实验数量与充分性
*   **实验规模**：测试了从极小规模（$n=8$）到中等规模（$n=175$）的多种组合。
*   **验证充分性**：
    *   针对“单调性近似”专门做了**混淆矩阵分析**，证明近似模型找到的解在绝大多数情况下确实满足原始的严格单调性约束。
    *   通过两组不同的惩罚参数（$\mu$）设置，验证了模型对不同业务偏好（如更看重单调性还是更看重集中度）的适应性。
*   **客观性**：实验设计较为客观，明确指出了暴力破解法的指数级增长趋势（幂律分布），并承认了参数 $\mu$ 调节的经验性。

### 6. 论文的主要结论与发现
*   **可行性确认**：证明了评级量表定义问题可以被有效地表述为 QUBO 模型。
*   **性能优势**：相比于直接处理约束的组合算法，QUBO 框架配合现代启发式求解器（如 Gurobi）在处理大规模金融数据时具有显著的效率优势。
*   **量子准备就绪**：该模型与量子退火（Quantum Annealing）和 QAOA 算法具有直接的对应关系，为未来在真实量子硬件上运行奠定了数学基础。

### 7. 优点
*   **创新性**：这是首个将 QUBO 应用于信用评级“聚类/分组”阶段的研究，填补了量子金融在该细分领域的空白。
*   **数学严谨性**：详细推导了如何将复杂的金融不等式约束转化为二进制二次形式，特别是对松弛变量的处理非常标准。
*   **实用性**：不仅考虑了理论模型，还考虑了监管机构（如 EBA）的实际要求。

### 8. 不足与局限
*   **参数敏感性**：惩罚系数 $\mu$ 的选择目前依赖经验（Empirical method），缺乏自动化的最优参数确定机制。如果 $\mu$ 设置不当，可能导致解不可行或质量低下。
*   **约束覆盖不全**：由于计算复杂性，论文暂时忽略了“同质性”和“异质性”统计检验约束（t-检验和 z-检验），这些约束在实际业务中也非常重要。
*   **硬件实测缺失**：虽然声称适用于量子硬件，但所有实验均在经典计算机上模拟完成，未在真实的量子退火机（如 D-Wave）上进行大规模实测。

（完）
