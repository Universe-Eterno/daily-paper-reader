---
title: Hierarchical Control Framework Integrating LLMs with RL for Decarbonized HVAC Operation
title_zh: 集成大语言模型与强化学习的脱碳暖通空调运行分层控制框架
authors: "Dianyu Zhong, Tian Xing, Kailai Sun, Xu Yang, Heye Huang, Irfan Qaisar, Tinggang Jia, Shaobo Wang, Qianchuan Zhao"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26050v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 将大模型与强化学习集成用于数值优化
tldr: 针对多区域建筑HVAC系统在脱碳运行中面临的动作空间庞大和探索效率低的问题，本文提出一种集成大语言模型（LLM）与强化学习（RL）的分层控制框架。该框架利用微调后的LLM根据历史数据生成动作掩码以缩小搜索空间，再由RL智能体进行约束优化。实验表明，该方法在提升热舒适度的同时显著降低了能耗，为复杂建筑环境的智能控制提供了新思路。
selection_source: fresh_fetch
motivation: 传统强化学习在多区域HVAC控制中面临动作空间爆炸和探索效率低的问题，而LLM虽具语义知识但缺乏闭环数值优化能力。
method: 提出一种分层框架，利用微调后的LLM生成状态相关的可行动作掩码来修剪动作空间，并结合掩码强化学习进行高效的约束优化。
result: "在7区域办公楼模拟中，该方法将平均不满意百分比（PPD）降至7.30%，且日均能耗降至140.90 kWh，均优于纯RL和纯LLM基准。"
conclusion: LLM引导的动作掩码技术能有效提升多区域HVAC控制的效率与稳定性，是实现建筑节能减排的有效途径。
---

## 摘要
供暖、通风与空调（HVAC）系统占建筑能耗的很大一部分。环境不确定性和动态的人员占用行为给脱碳 HVAC 控制带来了挑战。强化学习（RL）可以优化长周期的舒适度与能耗权衡，但在多区域建筑中面临动作空间指数级增长和探索效率低下的问题。大语言模型（LLMs）能够编码语义上下文和运行知识，但单独使用时缺乏可靠的闭环数值优化，可能导致舒适度与能耗权衡的可靠性降低。为了解决这些局限性，我们提出了一种分层控制框架，其中利用历史建筑运行数据训练的微调 LLM 生成与状态相关的可行动作掩码，将组合联合动作空间剪枝为操作上合理的子集。随后，一个带掩码的基于价值的 RL 智能体在缩减后的空间内执行约束优化，从而提高探索效率和训练稳定性。在使用 7 区域办公楼真实传感器和人员占用数据校准的高保真模拟器中进行评估，该方法实现了 7.30% 的平均 PPD（预期不满意百分比），相比舒适度表现最好的原始 RL 基准（DQN）降低了 39.1%，相比表现最好的原始 LLM 基准降低了 53.1%，同时将每日 HVAC 能耗降至 140.90 kWh，低于所有原始 RL 基准。结果表明，LLM 引导的动作掩码是实现高效多区域 HVAC 控制的一种极具前景的途径。

## Abstract
Heating, ventilation, and air conditioning (HVAC) systems account for a substantial share of building energy consumption. Environmental uncertainty and dynamic occupancy behavior bring challenges in decarbonized HVAC control. Reinforcement learning (RL) can optimize long-horizon comfort-energy trade-offs but suffers from exponential action-space growth and inefficient exploration in multi-zone buildings. Large language models (LLMs) can encode semantic context and operational knowledge, yet when used alone they lack reliable closed-loop numerical optimization and may result in less reliable comfort-energy trade-offs. To address these limitations, we propose a hierarchical control framework in which a fine-tuned LLM, trained on historical building operation data, generates state-dependent feasible action masks that prune the combinatorial joint action space into operationally plausible subsets. A masked value-based RL agent then performs constrained optimization within this reduced space, improving exploration efficiency and training stability. Evaluated in a high-fidelity simulator calibrated with real-world sensor and occupancy data from a 7-zone office building, the proposed method achieves a mean PPD of 7.30%, corresponding to reductions of 39.1% relative to DQN, the best vanilla RL baseline in comfort, and 53.1% relative to the best vanilla LLM baseline, while reducing daily HVAC energy use to 140.90~kWh, lower than all vanilla RL baselines. The results suggest that LLM-guided action masking is a promising pathway toward efficient multi-zone HVAC control.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hierarchical Control Framework Integrating LLMs with RL for Decarbonized HVAC Operation》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：建筑行业贡献了全球约34%的二氧化碳排放，其中暖通空调（HVAC）系统是能耗大户。实现建筑脱碳的关键在于“以人为本的控制”（OCC），即根据动态的人员占用和环境变化优化控制。
*   **核心挑战**：
    1.  **强化学习（RL）的局限**：在多区域建筑中，动作空间随区域数量呈指数级增长（动作空间爆炸），导致探索效率低下且训练不稳定。
    2.  **大语言模型（LLM）的局限**：虽然LLM具备丰富的语义上下文和运行知识，但作为独立控制器时，缺乏可靠的闭环数值优化能力，难以在复杂的物理动力学环境下实现精确的舒适度与能耗权衡。
*   **研究动机**：结合LLM的语义推理能力（定性引导）与RL的数值优化能力（定量决策），解决多区域HVAC控制中的维度灾难问题。

### 2. 方法论
论文提出了一种**分层控制框架（Hierarchical LLM-RL）**：
*   **核心思想**：将控制过程分为两层。上层LLM负责“剪枝”，将庞大的动作空间缩小为合理的子集；下层RL负责“精选”，在缩小后的空间内寻找最优解。
*   **关键技术细节**：
    1.  **LLM微调（SFT）**：使用基于kNN（k-最近邻）算法从历史运行数据中提取的“可行控制模式”作为标签，通过LoRA技术对Llama-3-8B进行微调，使其能根据当前建筑状态生成“动作掩码”（Action Mask）。
    2.  **动作掩码生成**：LLM输出每个区域可行的风速范围（如：[0, 1]），从而将原始的组合动作空间（如16,384个组合）大幅削减。
    3.  **掩码强化学习（Masked DQN）**：在DQN算法中引入掩码机制。在计算Q值和选择动作时，将LLM判定为不可行的动作Q值设为极小值，强制智能体仅在安全、合理的范围内探索。
    4.  **缓存机制（Caching）**：为提高推理效率，引入了LLM掩码缓存，对于相似状态直接调用已生成的掩码，减少实时推理开销。

### 3. 实验设计
*   **实验场景**：一个位于中国河北省的7区域办公建筑。
*   **模拟器**：基于真实传感器和人员占用数据校准的高保真HVAC模拟器（包含室内热模型、FCU设备模型和水泵管网模型）。
*   **对比基准（Benchmarks）**：
    *   **纯LLM控制**：Meta-Llama-3-8B, Qwen2.5-14B, Qwen2.5-72B。
    *   **纯RL控制**：A2C, PPO, DQN（均为Vanilla版本）。
*   **评估指标**：平均不满意百分比（PPD）、平均绝对PMV（热舒适指标）、日均能耗（kWh）、训练收敛速度。

### 4. 资源与算力
*   **模型训练**：使用了LoRA（低秩自适应）进行参数高效微调。
*   **训练配置**：采用FP16精度，Batch Size为16，训练5个Epoch，学习率为2e-5。
*   **算力说明**：论文**未明确列出具体的GPU型号和数量**，但提到通过缓存机制将单步推理延迟从2.27秒降低到了0.087秒，表明该框架在普通硬件上具备实时部署的可能性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了1000个训练轮次（天）的RL训练对比。
    *   所有RL实验均使用了**5个不同的随机种子**以确保结果的可重复性和统计显著性。
    *   包含了针对LLM规模（8B vs 14B vs 72B）的对比实验。
    *   提供了帕累托前沿（Pareto Front）分析，展示舒适度与能耗的权衡。
*   **充分性评价**：实验设计较为充分，涵盖了从冷启动表现、训练曲线到最终稳态运行的全面对比，并加入了消融性质的分析（如LLM+Cache的效率分析）。

### 6 主要结论与发现
*   **性能提升**：该框架实现的平均PPD为7.30%，相比表现最好的纯RL（DQN）舒适度提升了39.1%，相比纯LLM提升了53.1%。
*   **节能效果**：日均能耗为140.90 kWh，低于所有纯RL基准。
*   **训练效率**：LLM生成的掩码平均削减了约**81.56%**的动作空间，显著加快了RL的收敛速度并增强了训练稳定性（标准差显著降低）。
*   **发现**：单纯增加LLM的规模（如从8B到72B）虽然能提升舒适度，但往往以增加能耗为代价；而分层框架能打破这种“规模瓶颈”，实现更优的帕累托解。

### 7. 优点与亮点
*   **知识融合**：成功将人类的历史操作经验（通过LLM捕获）与机器的自主优化（通过RL实现）结合，解决了RL在复杂系统中“盲目探索”的问题。
*   **可解释性**：LLM生成的分析文本为控制决策提供了语义解释，增强了系统的透明度。
*   **实用性强**：通过缓存机制解决了LLM推理慢的痛点，且模拟器经过真实数据校准，具有较高的工程参考价值。

### 8. 不足与局限
*   **数据依赖**：LLM的微调高度依赖高质量的历史运行数据，对于缺乏历史记录的新建筑可能难以直接应用。
*   **动作离散性**：目前仅针对离散的风速控制（0-3级），尚未扩展到连续变量（如冷冻水流量、设定点温度）的精细化控制。
*   **季节局限**：实验主要集中在夏季制冷模式，未验证其在冬季供暖或过渡季的泛化能力。
*   **偏差风险**：如果历史数据中存在低效的操作模式，LLM可能会学习并固化这些偏差，限制RL发现更优策略的潜力。

（完）
