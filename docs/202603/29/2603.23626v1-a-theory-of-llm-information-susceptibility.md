---
title: A Theory of LLM Information Susceptibility
title_zh: 大语言模型信息敏感度理论
authors: "Zhuo-Yang Song, Hua Xing Zhu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23626v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大语言模型作为智能体系统中的优化模块
tldr: 本研究提出了大语言模型（LLM）信息敏感度理论，旨在探讨LLM作为智能体优化模块时的性能提升极限。研究假设在资源充足时，固定LLM无法增加策略集对预算的性能敏感度。通过构建多变量效用函数框架并进行跨领域实验验证，研究发现嵌套且共同扩展的架构能开启固定配置无法实现的响应通道。该理论为AI系统设计提供了预测性约束，并指出嵌套架构是实现开放式自我改进的关键。
selection_source: fresh_fetch
motivation: 旨在理解大语言模型在智能体系统中作为优化模块时，其干预效果的根本限制和资源利用效率。
method: 提出了信息敏感度假设，并开发了一个多变量效用函数框架，用于分析多预算通道下的性能表现。
result: 实验证明固定LLM的干预存在敏感度上限，而嵌套且协同扩展的架构能够突破该界限并提升性能。
conclusion: 研究表明嵌套架构可能是实现人工智能系统持续、开放式自我改进的必要结构性前提。
---

## 摘要
大语言模型（LLMs）正越来越多地作为优化模块部署在智能体系统中，然而这种由 LLM 介导的改进的根本限制仍不为人所知。在此，我们提出了一种 LLM 信息敏感度理论，其核心假设是：当计算资源足够大时，固定 LLM 的干预不会增加策略集相对于预算的性能敏感度。我们开发了一个多变量效用函数框架，将该假设推广到具有多个协变预算通道的架构中，并讨论了共缩放（co-scaling）能够超过敏感度边界的条件。我们在结构多样的领域和跨越一个数量级的模型规模上对该理论进行了实证验证，并表明嵌套的共缩放架构开启了固定配置无法提供的响应通道。这些结果阐明了 LLM 干预在何时有效、何时无效，证明了统计物理学工具可以为 AI 系统设计提供预测性约束。如果敏感度假设普遍成立，该理论表明嵌套架构可能是实现开放式智能体自我改进的必要结构条件。

## Abstract
Large language models (LLMs) are increasingly deployed as optimization modules in agentic systems, yet the fundamental limits of such LLM-mediated improvement remain poorly understood. Here we propose a theory of LLM information susceptibility, centred on the hypothesis that when computational resources are sufficiently large, the intervention of a fixed LLM does not increase the performance susceptibility of a strategy set with respect to budget. We develop a multi-variable utility-function framework that generalizes this hypothesis to architectures with multiple co-varying budget channels, and discuss the conditions under which co-scaling can exceed the susceptibility bound. We validate the theory empirically across structurally diverse domains and model scales spanning an order of magnitude, and show that nested, co-scaling architectures open response channels unavailable to fixed configurations. These results clarify when LLM intervention helps and when it does not, demonstrating that tools from statistical physics can provide predictive constraints for the design of AI systems. If the susceptibility hypothesis holds generally, the theory suggests that nested architectures may be a necessary structural condition for open-ended agentic self-improvement.

---

## 论文详细总结（自动生成）

这篇论文由北京大学的研究人员撰写，提出了一套关于大语言模型（LLM）在智能体系统中作为优化模块时的性能极限理论。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：目前 LLM 常被集成到搜索、规划和验证等智能体流程中以提升性能，但这种“LLM 介导的改进”是否存在根本性的理论极限？
*   **核心问题**：增加计算预算（如搜索宽度、样本数量）时，引入一个**固定的 LLM 层**是否能持续提高性能的增长速率（即缩放效率）？
*   **整体含义**：论文试图通过统计物理学中的“线性响应理论”视角，为 AI 智能体设计提供一套预测性框架，界定 LLM 何时能提供增益，以及何时会成为瓶颈。

### 2. 论文提出的方法论
*   **核心思想：信息敏感度假设**。论文定义了“性能敏感度”（Performance Susceptibility），即效用函数 $J$ 相对于计算预算 $B$ 的偏导数 $\partial J / \partial B$。
*   **关键假设**：在计算资源足够大的极限情况下，由固定 LLM 处理后的策略集，其性能敏感度不会超过原始基础策略集的敏感度。
*   **数学表达**：引入相对敏感度 $\alpha$。当 $\alpha \leq 1$ 时，意味着 LLM 无法改善性能随预算增加的渐近缩放轨迹。
*   **多变量扩展**：将效用函数推广为 $J(B_1, B_2, \dots, B_n)$，分析多个预算通道（如生成模型规模与选择模型规模）的协变关系。
*   **嵌套架构（Nested Architectures）**：提出只有当架构中的各个组件（如生成器和选择器）随预算共同缩放（Co-scaling）时，才能突破敏感度边界（即 $\alpha_{total} > 1$），实现开放式的自我改进。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **俄罗斯方块（Tetris）**：组合游戏，测试 LLM 在搜索树中的选择能力。
    2.  **0/1 背包问题（Knapsack）**：组合优化，测试 LLM 对算法候选方案的重排。
    3.  **常识排序（Ranking）**：事实检索，测试 LLM 在噪声环境下的知识提取。
    4.  **AIME 数学竞赛**：多步推理，测试 LLM 作为“选择器”在多个解法中的判断力。
*   **Benchmark 与对比方法**：
    *   **基础策略（Base）**：深度优先搜索（DFS）、多数投票（Majority Vote）、带噪声的算法基准。
    *   **LLM 介入策略（Derived）**：使用不同规模的 Qwen 系列模型（7B 到 200B+）作为固定层或协同缩放层。
*   **消融实验**：针对 Prompt 工程（思维链、专家提示等）和奖励函数设计进行了鲁棒性测试。

### 4. 资源与算力
*   **模型使用**：实验使用了 Qwen-2.5 系列（7B, 14B, 32B, 72B）以及 Qwen3-Max（约 200B 参数）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、集群规模或总训练/推理时长。实验主要通过 API 调用完成，侧重于推理侧的预算缩放分析。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了 4 个结构迥异的领域。
    *   模型规模跨越了一个数量级（7B 到 200B）。
    *   俄罗斯方块实验使用了 40 个独立随机种子；AIME 数学使用了 60 道难题并进行了多次采样（$k$ 值从 1 到 21）。
*   **充分性评价**：实验设计较为充分且客观。通过跨领域验证证明了理论的普适性，并通过不同 Prompt 的对比排除了“提示词工程不足”导致的性能瓶颈解释。

### 6. 论文的主要结论与发现
*   **敏感度上限**：在固定架构下，LLM 虽然在低预算时能提供常数级的性能提升，但在高预算极限下，其性能增长率（敏感度）始终受限于基础算法。
*   **临界点存在**：在 AIME 实验中发现，当样本数 $k \approx 12$ 时，LLM 选择器的优势开始低于统计上的多数投票法（$\alpha$ 跌破 1）。
*   **共缩放的必要性**：实验证明，只有让“选择器”和“生成器”的规模同步增长（嵌套架构），才能开启新的响应通道，实现超越单一组件限制的性能提升。
*   **自我进化的条件**：理论推导表明，嵌套共缩放是 LLM 实现开放式自我进化的必要结构条件。

### 7. 优点
*   **理论深度**：将统计物理学的线性响应理论引入 LLM 智能体研究，提供了比单纯实验观察更深刻的理论洞察。
*   **预测性**：该理论不仅解释了现有现象，还能指导开发者决定何时该增加搜索深度，何时该升级 LLM 模块。
*   **跨领域一致性**：在游戏、优化、知识和推理四个完全不同的领域观察到了高度一致的敏感度行为。

### 8. 不足与局限
*   **形式化证明缺失**：目前该理论主要基于“经验假设”和实验验证，尚缺乏严格的数学形式化证明（如基于信息论的严谨推导）。
*   **场景覆盖**：实验尚未覆盖长程对话、多智能体协作或连续动作空间等更复杂的动态环境。
*   **计算成本忽略**：理论主要关注性能随预算的缩放，但在实际工程中，嵌套架构带来的计算成本激增可能在达到“敏感度优势”前就已耗尽资源。

（完）
