---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 用于大语言模型推理优化的多智能体强化学习
tldr: 针对多智能体协作推理中存在的交互模糊性、信用分配困难以及奖励信号噪声导致的训练不稳定问题，本文提出了一种鲁棒的多智能体强化学习框架。该框架包含DACR结构化推理流程（回答-评论-重写）以明确各智能体的边际贡献，以及ARE自适应鲁棒估计器来处理重尾和噪声奖励。实验证明，该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和对噪声奖励的鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体协作推理面临交互模糊导致的信用分配难题，且易受重尾和噪声奖励干扰，导致训练不稳定。
method: 提出了由DACR（三阶段推理管道）和ARE（自适应鲁棒估计器）组成的强化学习框架，实现明确的贡献归因和稳健的优势估计。
result: 在数学推理和具身智能基准测试中，该方法在同构和异构设置下均优于基准，且在噪声奖励环境下表现出极强的鲁棒性。
conclusion: 该研究通过结构化交互和鲁棒估计技术，有效解决了多智能体强化学习中的信用分配与噪声干扰问题，提升了协作推理的性能。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性模糊了生成、批判和修订之间的界限，使得跨智能体的信用分配变得困难。此外，在这种环境下的策略优化容易受到重尾和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地将每个智能体的边际贡献归因于其合作伙伴的性能。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在噪声奖励的情况下，我们的方法在同构和异构设置下均始终优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：大语言模型（LLM）的多智能体协作虽能提升推理能力，但面临两大挑战：
    1.  **交互模糊性**：生成、批判和修订过程交织，导致难以准确评估每个智能体对最终结果的贡献（信用分配难题）。
    2.  **奖励信号不稳定性**：推理任务的奖励通常由验证器或奖励模型生成，往往具有**重尾分布**和**高噪声**特征。传统的策略优化算法（如 GRPO）依赖批次经验均值进行归一化，极易受异常值干扰，导致训练震荡甚至不收敛。
*   **核心目标**：构建一个鲁棒的多智能体强化学习框架，既能明确信用分配，又能抵御噪声奖励的干扰。

### 2. 方法论
该框架由两个核心组件组成：
*   **DACR（双智能体回答-批判-重写协议）**：
    *   **三阶段流程**：独立回答（生成初始链条） $\rightarrow$ 相互批判（交换答案并指出错误） $\rightarrow$ 修订（根据对方反馈优化自身答案）。
    *   **交叉评价奖励机制**：智能体 $i$ 的总奖励不仅包含其最终答案的得分，还包含一个“交叉改进项” $\Delta$。如果智能体 A 的批判促使智能体 B 的修订版得分提高，则 A 获得额外奖励。这实现了轻量化且明确的信用归因。
*   **ARE（自适应鲁棒估计器）**：
    *   **核心思想**：改进经典的“均值中位数”（Median-of-Means, MoM）估计器。将样本分块，在块内使用**自适应鲁棒损失函数**（Barron Loss）代替简单均值，再取各块结果的中位数。
    *   **技术细节**：通过交替优化估计鲁棒参数（形状参数 $\alpha$ 和尺度参数 $c$），并利用**渐进非凸性（GNC）**范式解决非凸优化问题，确保在重尾分布下获得比样本均值更稳健的中心位置估计。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：在 MATH 7.5k 上训练，在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外/泛化性测试）上评估。
    *   **具身智能**：无人机视觉语言导航（Aerial VLN）任务，使用 FlightGPT 环境。
*   **基准方法（Benchmarks）**：
    *   基础模型（Qwen2.5-1.5B/7B, Llama3.1-8B）。
    *   基于标准 GRPO 的多智能体协作基准。
*   **对比设置**：涵盖同构智能体（相同模型）和异构智能体（不同系列/规模模型）的配对。

### 4. 资源与算力
*   **硬件环境**：使用了 **1 台拥有 8 张 NVIDIA A800 GPU 的节点**。
*   **软件框架**：使用 Ray 进行分布式训练编排，vLLM 作为推理引擎（4 个引擎，张量并行度为 2）。
*   **训练细节**：启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度实验，包括：
    1.  **性能对比**：在 6 个数学基准上对比了不同模型规模下的 ARE 与基准方法。
    2.  **消融实验**：验证了三阶段交互（DACR）相对于独立回答的增益。
    3.  **噪声压力测试**：通过注入柯西噪声（Cauchy noise），测试了在不同污染比例（20%~80%）下的鲁棒性。
    4.  **跨领域验证**：从纯文本推理扩展到视觉导航任务。
*   **充分性评价**：实验设计较为全面，涵盖了分布内/外测试、消融分析及极端噪声模拟，客观地证明了方法在稳定性和准确性上的双重优势。

### 6. 主要结论与发现
*   **鲁棒性显著**：在注入重尾噪声的环境下，ARE 显著优于标准 GRPO，有效防止了训练发散。
*   **性能提升**：在数学任务中，ARE 配合 DACR 在多个 OOD 基准上取得了大幅提升（如 AMC23 上 Qwen2.5-1.5B 提升了 10%）。
*   **泛化能力**：在具身智能任务中，ARE 提升了无人机在未见环境（Unseen environments）下的导航成功率和路径效率。
*   **理论支撑**：论文证明了 ARE 在有限方差和重尾状态下的渐近正态性和偏差界限。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还提供了严谨的统计学证明（如中心极限定理的扩展）。
*   **即插即用**：ARE 作为一种估计器，可以轻易集成到现有的类 GRPO 算法中。
*   **结构化交互**：DACR 协议通过强制性的“批判”环节，解决了多智能体强化学习中常见的“搭便车”或信用分配模糊问题。

### 8. 不足与局限
*   **计算开销**：ARE 涉及内部的迭代优化（GNC-IRLS），相比简单的批次均值计算，会增加一定的训练耗时。
*   **智能体规模限制**：实验主要集中在双智能体协作，尚未探讨在更多智能体（如 5 个以上）复杂交互场景下的扩展性。
*   **视觉任务简化**：在视觉导航实验中，为了适配算力降低了图像分辨率，可能未完全发挥视觉语言模型（VLM）的潜力。

（完）
