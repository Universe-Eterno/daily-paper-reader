---
title: Multi-LLM Query Optimization
title_zh: 多大语言模型查询优化
authors: "Arlen Dean, Zijin Zhang, Stefanus Jasin, Yuqing Liu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.24617v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 多大模型部署的查询优化被建模为NP难问题
tldr: 针对多异构大语言模型（LLM）并行分类任务中的查询分配优化问题，本文提出了一种鲁棒的离线查询规划框架。研究证明了该问题为NP-hard，并开发了一种基于Chernoff界和联合界分解的代理模型，旨在满足可靠性约束的同时最小化总查询成本。通过设计的AFPTAS算法，在保证渐近最优性的同时，有效降低了多模型部署的计算开销。
selection_source: fresh_fetch
motivation: 旨在解决在保证分类可靠性的前提下，如何跨多个异构大语言模型最优地分配查询以最小化总成本的问题。
method: 将多类误差分解为成对比较，并结合Chernoff型集中不等式构建了一个闭式、可分离且保可行性的代理优化目标。
result: 证明了原问题为NP-hard，并提出了一种AFPTAS算法，该算法在误差容限趋于零时具有渐近紧致性。
conclusion: 该研究为多模型并行部署提供了理论支撑和高效的近似算法，实现了成本与可靠性之间的最优权衡。
---

## 摘要
并行部署多个大语言模型（LLM）来对未知的真实标签进行分类是一种常见做法，但在异构模型之间如何最优地分配查询的问题仍未得到充分理解。在本文中，我们制定了一个鲁棒的离线查询规划问题，旨在最小化总查询成本，同时满足状态误差约束，以保证每个可能的真实标签的可靠性。我们首先通过最小权重集合覆盖问题的归约，证明了该问题是 NP 难的。为了克服这一难解性，我们通过将多类误差的联合边界分解为两两比较，并结合 Chernoff 型集中不等式，开发了一个代理模型。所得代理模型在查询计数方面具有闭式、乘法可分的表达式，并保证能够保持可行性。我们进一步证明了该代理模型在优化层面是渐近紧致的：随着误差容限的缩小，代理最优成本与真实最优成本的比值收敛于 1，且具有显式的速率 $O\left(\log\log(1/α_{\min}) / \log(1/α_{\min})\right)$。最后，我们设计了一个渐近全多项式时间近似方案（AFPTAS），该方案返回一个在代理最优值 $(1+\varepsilon)$ 因子范围内的代理可行查询计划。

## Abstract
Deploying multiple large language models (LLMs) in parallel to classify an unknown ground-truth label is a common practice, yet the problem of optimally allocating queries across heterogeneous models remains poorly understood. In this paper, we formulate a robust, offline query-planning problem that minimizes total query cost subject to statewise error constraints which guarantee reliability for every possible ground-truth label. We first establish that this problem is NP-hard via a reduction from the minimum-weight set cover problem. To overcome this intractability, we develop a surrogate by combining a union bound decomposition of the multi-class error into pairwise comparisons with Chernoff-type concentration bounds. The resulting surrogate admits a closed-form, multiplicatively separable expression in the query counts and is guaranteed to be feasibility-preserving. We further show that the surrogate is asymptotically tight at the optimization level: the ratio of surrogate-optimal cost to true optimal cost converges to one as error tolerances shrink, with an explicit rate of $O\left(\log\log(1/α_{\min}) / \log(1/α_{\min})\right)$. Finally, we design an asymptotic fully polynomial-time approximation scheme (AFPTAS) that returns a surrogate-feasible query plan within a $(1+\varepsilon)$ factor of the surrogate optimum.

---

## 论文详细总结（自动生成）

这篇论文《Multi-LLM Query Optimization》探讨了在多大语言模型（LLM）环境下，如何通过数学优化手段实现成本最低且可靠性最高的查询分配策略。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在实际应用中（如医疗诊断、法律审查、内容分类），通常会并行调用多个 LLM 并聚合其结果以提高准确性。
*   **核心矛盾**：不同的 LLM 在处理不同类别的任务时表现（判别力）不同，且每个模型的 API 调用成本、延迟各异。
*   **研究问题**：如何针对一个未知的真实标签，在多个异构 LLM 之间分配查询次数（Query Allocation），以在满足预设的分类准确率（可靠性）约束下，使总查询成本最小化？
*   **研究动机**：现有的多模型集成多采用启发式方法，缺乏严谨的理论框架来平衡成本与状态相关的错误率约束。

### 2. 方法论
论文提出了一套从理论证明到算法实现的完整框架：
*   **问题建模**：将查询规划建模为一个鲁棒离线优化问题。目标函数是最小化总成本 $\sum c_m r_m$，约束条件是对于每一个可能的真实标签 $y$，其分类错误率 $P_e(y; r)$ 必须小于给定的容限 $\alpha_y$。
*   **复杂度证明**：通过将“最小权重集合覆盖问题”（Minimum-Weight Set Cover）归约为本问题，证明了该优化问题是 **NP-hard** 的。
*   **Chernoff 代理模型（Surrogate）**：
    *   由于精确计算 MAP（最大后验概率）错误率在计算上不可行，论文利用**联合界（Union Bound）**将多类误差分解为两两类别的比较。
    *   引入 **Chernoff 亲和因子（Chernoff affinity factor）** 来捕捉每个模型对特定类别对的判别力。
    *   构建了一个闭式的、乘法可分的指数界限作为代理约束，确保满足代理约束的方案一定满足原始误差约束。
*   **渐近紧致性证明**：证明了当误差容限 $\alpha$ 趋于 0 时，代理模型的最优成本与真实最优成本的比值收敛于 1。
*   **算法实现（AFPTAS）**：设计了一种“渐近全多项式时间近似方案”。该算法通过对 Chernoff 参数进行网格离散化，并结合动态规划（类似背包问题递归），在多项式时间内找到 $(1+\varepsilon)$ 近似最优解。

### 3. 实验设计
*   **实验性质**：**本文是一篇纯理论证明型论文**。
*   **场景模拟**：论文通过数学推导和构造性证明（如在 NP-hard 证明中构造特定的判别模型和成本结构）来验证其理论。
*   **Benchmark**：其对比基准是理论上的“真实最优解”（True Optimum）。
*   **对比方法**：主要对比了原始不可解的整数规划问题与提出的 Chernoff 代理模型之间的成本差距。
*   **注意**：文中未包含基于真实世界数据集（如 MMLU、GSM8K 等）的实测性能对比，重点在于算法的理论保证和收敛速率。

### 4. 资源与算力
*   **算力说明**：论文**未提及**任何具体的 GPU 型号、数量或训练时长。
*   **原因**：由于该研究侧重于优化算法的计算复杂度和近似比分析，不涉及大规模模型的训练或微调，其算法（Algorithm 1）在普通 CPU 上即可运行。

### 5. 实验数量与充分性
*   **理论验证**：论文提供了详尽的数学证明（见 36 页的附录），涵盖了从 NP 困难性到算法近似比的所有核心命题。
*   **充分性评价**：从理论计算机科学和运筹学的角度看，论证非常充分。它不仅给出了算法，还给出了误差收敛的显式速率 $O(\log \log(1/\alpha) / \log(1/\alpha))$。
*   **局限性**：缺乏实证数据的支持，无法直观观察在特定 LLM（如 GPT-4 vs. Claude 3）组合下的实际成本节省比例。

### 6. 主要结论与发现
*   **难解性**：多 LLM 查询优化本质上是组合优化问题，具有内在的计算难度。
*   **代理有效性**：基于 Chernoff 界的代理模型不仅计算简便，而且在“高可靠性要求”（小误差容限）的场景下几乎是无损的。
*   **可扩展性**：提出的 AFPTAS 算法说明，尽管问题本身是 NP-hard，但在类别数量 $L$ 固定时，可以高效地找到接近最优的查询计划。

### 7. 优点
*   **理论严谨**：为多模型部署提供了坚实的数学基础，填补了离线查询规划的理论空白。
*   **鲁棒性**：约束条件针对“每个类别”而非“平均准确率”，这对于医疗、法律等不容许特定类别出现高频错误的领域至关重要。
*   **通用性**：方法论不依赖于特定的 LLM 架构，只要能估计出模型的条件概率分布 $p(x|y)$ 即可适用。

### 8. 不足与局限
*   **参数依赖**：算法假设决策者已知每个 LLM 的条件概率分布（即混淆矩阵），在实践中获取这些精确概率可能需要大量的先验测试。
*   **非启发式/非自适应**：该研究关注的是“离线”规划（一次性决定买多少次查询），未考虑根据模型中间输出动态调整后续查询的“在线/自适应”策略。
*   **缺乏实证**：没有在真实 LLM API 环境下进行成本和准确率的消融实验，难以评估理论增益在噪声环境下的鲁棒性。
*   **类别规模限制**：算法复杂度随类别数量 $L$ 指数级增长，虽然对于小规模分类任务有效，但难以直接应用于超大规模标签集的任务。

（完）
