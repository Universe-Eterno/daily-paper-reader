---
title: "POLCA: Stochastic Generative Optimization with LLM"
title_zh: POLCA：基于大语言模型的随机生成式优化
authors: "Xuanfei Ren, Allen Nie, Tengyang Xie, Ching-An Cheng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14769v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: LLM 作为复杂系统的生成式优化器
tldr: 本研究针对LLM提示词及多轮智能体等复杂系统优化中的人工迭代难题，将其形式化为随机生成式优化问题。提出POLCA框架，利用大模型作为优化器，通过优先级队列平衡探索与利用，并结合ε-Net机制与元学习总结器应对反馈噪声和随机性。实验证明POLCA在多个基准测试中均优于现有算法，且具备理论收敛性，显著提升了优化效率与鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决复杂LLM系统优化过程中人工迭代成本高以及反馈噪声、随机行为带来的不确定性挑战。
method: 采用优先级队列管理候选方案，结合ε-Net维持参数多样性，并利用LLM总结历史试验进行元学习优化。
result: 在智能体优化、代码翻译及CUDA内核生成等多样化基准测试中，POLCA展现出卓越的样本效率和鲁棒性。
conclusion: POLCA是一个可扩展且理论完备的框架，能有效处理随机环境下的生成式优化任务，显著超越当前最先进算法。
---

## 摘要
优化复杂系统（从大语言模型提示词到多轮智能体）传统上需要耗费大量人力的手动迭代。我们将这一挑战形式化为一个随机生成式优化问题，其中生成式语言模型充当优化器，在数值奖励和文本反馈的引导下寻找最优系统。我们提出了基于局部上下文聚合的优先级优化（POLCA），这是一个可扩展的框架，旨在处理优化过程中的随机性（如噪声反馈、采样小批量和随机系统行为），同时有效管理解空间的无约束扩张。POLCA 维护一个优先级队列来管理探索与利用之间的权衡，系统地追踪候选解及其评估历史。为了提高效率，我们集成了 ε-Net 机制以维持参数多样性，并使用大语言模型总结器在历史试验中进行元学习。我们从理论上证明了 POLCA 在随机性条件下能够收敛至近优候选解。我们在多种基准测试上评估了该框架，包括 τ-bench、HotpotQA（智能体优化）、VeriBench（代码翻译）和 KernelBench（CUDA 内核生成）。实验结果表明，POLCA 实现了鲁棒、样本高效且时间高效的性能，在确定性和随机性问题上均持续优于现有最先进算法。本工作的代码库已在 https://github.com/rlx-lab/POLCA 公开。

## Abstract
Optimizing complex systems, ranging from LLM prompts to multi-turn agents, traditionally requires labor-intensive manual iteration. We formalize this challenge as a stochastic generative optimization problem where a generative language model acts as the optimizer, guided by numerical rewards and text feedback to discover the best system. We introduce Prioritized Optimization with Local Contextual Aggregation (POLCA), a scalable framework designed to handle stochasticity in optimization -- such as noisy feedback, sampling minibatches, and stochastic system behaviors -- while effectively managing the unconstrained expansion of solution space. POLCA maintains a priority queue to manage the exploration-exploitation tradeoff, systematically tracking candidate solutions and their evaluation histories. To enhance efficiency, we integrate an $\varepsilon$-Net mechanism to maintain parameter diversity and an LLM Summarizer to perform meta-learning across historical trials. We theoretically prove that POLCA converges to near-optimal candidate solutions under stochasticity. We evaluate our framework on diverse benchmarks, including $τ$-bench, HotpotQA (agent optimization), VeriBench (code translation) and KernelBench (CUDA kernel generation). Experimental results demonstrate that POLCA achieves robust, sample and time-efficient performance, consistently outperforming state-of-the-art algorithms in both deterministic and stochastic problems. The codebase for this work is publicly available at https://github.com/rlx-lab/POLCA.

---

## 论文详细总结（自动生成）

### 论文总结：POLCA —— 基于大语言模型的随机生成式优化

#### 1. 核心问题与整体含义（研究动机和背景）
优化复杂系统（如 LLM 提示词、多轮对话智能体、高性能代码）传统上依赖专家的人工迭代。虽然近期出现了利用 LLM 作为优化器的“生成式优化”方法，但面临两大核心挑战：
*   **随机性（Stochasticity）：** 评估反馈往往存在噪声（如 LLM-as-a-Judge 的主观性）、小批量采样带来的方差，以及智能体本身行为的不确定性。
*   **搜索空间扩张：** LLM 优化器容易生成大量语义重复的候选方案，导致评估成本随迭代线性增长，难以扩展。
**POLCA** 旨在将此过程形式化为“随机生成式优化问题”，通过系统化的框架解决随机环境下的效率与鲁棒性问题。

#### 2. 方法论：核心思想与关键技术
POLCA 框架（Prioritized Optimization with Local Contextual Aggregation）包含以下核心组件：
*   **优先级队列存储（Priority Queue Memory）：** 维护所有候选方案及其评估历史。利用经验均值（或理论分析中的 UCB 分数）作为优先级，平衡对高性能方案的“利用”和对不确定方案的“探索”。
*   **$\varepsilon$-Net 语义过滤机制：** 利用嵌入向量（Embedding）计算候选方案间的语义距离。只有当新方案与现有方案的距离大于阈值 $\varepsilon$ 时才被接受。这在理论上限制了需要评估的独特方案总数，防止了冗余计算。
*   **LLM 总结器（Summarizer）：** 这是一个元学习步骤。总结器分析历史中的成功与失败案例，提取“元梯度”（Meta-gradients），为优化器提供全局上下文，类似于数值优化中的动量（Momentum）机制。
*   **算法流程：** 采样小批量数据 $\rightarrow$ 选择高优先级方案评估 $\rightarrow$ 总结历史 $\rightarrow$ 提出新方案 $\rightarrow$ 语义过滤 $\rightarrow$ 评估并更新队列。

#### 3. 实验设计
论文在四个具有代表性的基准测试上验证了 POLCA，涵盖了不同来源的随机性：
*   **$\tau$-bench：** 优化工具使用智能体的提示词（涉及小批量采样和智能体随机性）。
*   **HotpotQA：** 优化多步推理提示词（涉及大规模数据集采样）。
*   **VeriBench：** 将 Python 翻译为 Lean 4 形式化代码（涉及随机的 LLM 评估器反馈）。
*   **KernelBench：** 优化 CUDA 内核代码（确定性环境，测试框架的通用性）。
*   **对比方法：** 包括 **DSPy**（顺序修订）、**GEPA**（基于帕累托前沿的进化）、**OpenEvolve**（AlphaEvolve 的开源实现）。

#### 4. 资源与算力
*   **模型使用：** 采用了 Gemini 2.0 Flash、Gemini 1.5 Flash-lite、Claude 3.5 Sonnet 和 Claude 3.7 Sonnet 作为骨干模型或优化器。
*   **嵌入模型：** 使用了 `text-embedding-004` 和 `gemini-embedding-001`。
*   **硬件：** KernelBench 实验在 **NVIDIA L40S GPU** 上运行。
*   **Token 消耗：** 附录详细列出了 Token 使用量（例如 $\tau$-bench 100 次迭代约消耗 3100 万 Input Tokens）。

#### 5. 实验数量与充分性
*   **实验规模：** 针对每个基准测试进行了多轮独立运行（$\tau$-bench 使用 6 个随机种子，其他通常为 3 个），并报告了均值和标准差。
*   **消融实验：** 充分验证了 $\varepsilon$-Net 和总结器的贡献，并对 $\varepsilon$ 的敏感度进行了深入分析。
*   **客观性：** 论文不仅对比了最终得分，还对比了“评估步骤”（时间效率）和“指标调用次数”（计算效率），并探讨了为什么简单的回归模型无法替代其经验均值机制。实验设计全面且公平。

#### 6. 主要结论与发现
*   **性能卓越：** POLCA 在所有基准测试中均显著优于 GEPA 和 OpenEvolve，尤其在随机性较强的任务中优势更大。
*   **收敛性：** 理论证明并实验证实，POLCA 能够收敛至近优解，且对优化器的随机失败具有鲁棒性。
*   **效率提升：** $\varepsilon$-Net 有效减少了 30%-50% 的冗余评估，显著降低了优化成本。
*   **泛化能力：** 在 $\tau$-bench 中，POLCA 优化的提示词在未见过的测试任务上也表现出良好的泛化性能。

#### 7. 优点（亮点）
*   **理论与实践结合：** 提供了严谨的收敛性证明，同时在复杂的现实任务（如 CUDA 优化）中表现出色。
*   **处理随机性的系统性：** 不同于以往假设评估是确定性的方法，POLCA 专门为噪声环境设计。
*   **可扩展性：** 通过语义过滤解决了生成式优化中搜索空间爆炸的顽疾。

#### 8. 不足与局限
*   **依赖嵌入质量：** $\varepsilon$-Net 的效果高度依赖于 Embedding 模型捕捉语义差异的能力。
*   **超参数调节：** 虽然对 $\varepsilon$ 有一定的鲁棒性，但在不同领域可能仍需微调以平衡多样性与精度。
*   **优化器假设：** 理论分析假设 LLM 优化器具有一定的改进概率（Assumption 1），若模型能力极弱，框架效果会受限。
*   **计算成本：** 维护优先级队列和进行语义过滤虽然节省了评估成本，但增加了管理开销和 Embedding 调用。

（完）
