---
title: Novelty Adaptation Through Hybrid Large Language Model (LLM)-Symbolic Planning and LLM-guided Reinforcement Learning
title_zh: 通过混合大语言模型（LLM）-符号规划与LLM引导的强化学习实现新颖性适应
authors: "Hong Lu, Pierrick Lorang, Timothy R. Duggan, Jivko Sinapov, Matthias Scheutz"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11351v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: LLM引导的强化学习和奖励函数生成
tldr: 针对开放世界中自主智能体难以应对新颖物体的问题，本文提出一种结合大语言模型（LLM）、符号规划与强化学习（RL）的神经符号架构。该方法利用LLM的常识推理能力识别缺失的算子，辅助符号规划器生成方案，并自动编写奖励函数引导RL学习新算子的控制策略，显著提升了机器人在动态环境中的适应能力。
selection_source: fresh_fetch
motivation: 传统符号规划器在面对缺乏对应操作算子的新颖物体时，无法生成有效的执行计划。
method: 提出一种集成架构，利用LLM识别缺失算子、辅助符号规划并编写奖励函数，通过强化学习训练新算子的控制策略。
result: 该方法在连续机器人领域的算子发现与算子学习任务中均优于现有最先进的方法。
conclusion: 结合LLM的常识推理与符号规划、强化学习，能有效解决自主智能体在动态环境下的新颖性适应问题。
---

## 摘要
在动态的开放世界环境中，自主智能体经常会遇到阻碍其寻找实现目标计划的新颖性。具体而言，当机器人的规划领域缺乏使其能够与环境中新颖物体进行适当交互的算子（operators）时，传统的符号规划器将无法生成计划。我们提出了一种神经符号架构，该架构集成了符号规划、强化学习和大语言模型（LLM），以学习如何处理新颖物体。特别地，我们利用 LLM 的常识推理能力来识别缺失的算子，利用符号 AI 规划器生成计划，并编写奖励函数以引导强化学习智能体学习新识别算子的控制策略。在连续机器人领域，我们的方法在算子发现和算子学习方面均优于现有最先进的方法。

## Abstract
In dynamic open-world environments, autonomous agents often encounter novelties that hinder their ability to find plans to achieve their goals. Specifically, traditional symbolic planners fail to generate plans when the robot's planning domain lacks the operators that enable it to interact appropriately with novel objects in the environment. We propose a neuro-symbolic architecture that integrates symbolic planning, reinforcement learning, and a large language model (LLM) to learn how to handle novel objects. In particular, we leverage the common sense reasoning capability of the LLM to identify missing operators, generate plans with the symbolic AI planner, and write reward functions to guide the reinforcement learning agent in learning control policies for newly identified operators. Our method outperforms the state-of-the-art methods in operator discovery as well as operator learning in continuous robotic domains.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合大语言模型（LLM）、符号规划与强化学习（RL）的神经符号架构，旨在解决自主机器人在开放世界中遇到新颖物体（Novelty）时，因缺乏相应动作定义（Operators）而导致规划失败的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的任务与运动规划（TAMP）依赖于预定义的符号域（如 PDDL）。当环境中出现新物体（如一个从未见过的抽屉或盖子）时，机器人因缺乏与之交互的“算子”（Operator）而无法生成通向目标的计划。
*   **研究动机**：现有的“算子发现”方法通常依赖随机探索，在连续动作空间中效率极低。作者希望利用 LLM 蕴含的常识推理能力来“发明”缺失的符号算子，并自动生成奖励函数来指导底层控制策略的学习。

### 2. 方法论
该论文提出了一个“规划-学习-执行”（Plan-Learn-Execute）循环，包含两个核心组件：
*   **混合 LLM-符号规划（Search-and-Prompt）**：
    *   当符号规划器报错无法找到路径时，系统进入广度优先搜索。在每个状态下，调用 LLM（GPT-o3）根据当前环境描述建议一个缺失的算子（包含前提条件和效果）。
    *   使用“自我一致性”（Self-consistency）技术采样多个建议并取多数。
    *   符号规划器验证加入该新算子后是否能打通通往目标的路径。
*   **LLM 引导的子目标学习（LLM-Guided RL）**：
    *   将新算子的“效果”分解为有序的子目标序列。
    *   利用 LLM（GPT-o4-mini）为每个子目标编写多个候选的稠密奖励函数（Python 代码）。
    *   **遗传算法启发式剪枝**：并行启动多个 PPO 强化学习智能体，每个智能体使用不同的奖励函数。定期评估并剔除表现最差（成功率最低）的候选函数，最终保留最优策略作为该算子的执行器。

### 3. 实验设计
*   **实验场景**：在 `mimicgen` 模拟环境中的四个连续机器人操纵任务：
    1.  **厨房（简单）**：新物体“锅盖”挡住了锅，需学习移开盖子。
    2.  **螺母组装（中等）**：新物体“圆柱销”，需学习从销上取下螺母。
    3.  **咖啡机-盒子（中等）**：咖啡荚在“盒子”里，需学习从盒中取出。
    4.  **咖啡机-抽屉（困难）**：咖啡荚在关闭的“抽屉”里，需先后学习“开抽屉”和“取物”。
*   **Benchmark 与对比方法**：
    *   **Operator Discovery (OD)**：传统的基于随机探索的算子发现方法。
    *   **LEAGUE-Sparse (LS)**：仅在完成所有效果后给予稀疏奖励的基准。
    *   **Reward Machine (RM)**：仅提供子目标奖金（Bonus）但不含 LLM 生成的稠密奖励。

### 4. 资源与算力
*   **硬件环境**：Ubuntu 20.04 系统，配备 24 核 Intel Core i9-12900K CPU 和 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：
    *   算子发现（OD）基准测试上限为 100 万个时间步（Timesteps）。
    *   LLM 引导的 RL 智能体在每 20 万个时间步进行一次奖励函数剪枝。
    *   使用了 Stable Baselines3 库实现的 PPO 算法。

### 5. 实验数量与充分性
*   **实验规模**：每个领域均运行了 **10 个随机种子**。
*   **充分性**：实验涵盖了从简单到极难（随机探索几乎不可能成功）的梯度。通过 Wilcoxon 符号秩检验进行了统计显著性分析（p < 0.05）。
*   **客观性**：对比了多种消融版本（如无稠密奖励的 RM），证明了 LLM 生成的代码在加速收敛方面的关键作用。

### 6. 主要结论与发现
*   **算子识别**：混合 LLM-符号规划器能准确识别所有测试域中缺失的算子，而传统 OD 方法在复杂任务（如抽屉任务）中完全失败。
*   **学习效率**：LLM 引导的稠密奖励函数显著提升了 RL 的成功率（多数任务达到 90% 以上），且比稀疏奖励快数倍。
*   **鲁棒性**：通过并行运行多个奖励候选并剪枝，有效解决了 LLM 生成错误代码或低效奖励函数的问题。

### 7. 优点
*   **神经符号集成**：巧妙地将 LLM 的常识（用于结构化定义）、符号规划的逻辑性（用于全局搜索）和 RL 的控制能力（用于底层执行）结合在一起。
*   **自动化程度高**：实现了从发现问题（规划失败）到解决问题（学习新技能）的全自动闭环。
*   **剪枝机制**：引入遗传算法思路处理 LLM 的幻觉或代码错误，增强了系统的实用性。

### 8. 不足与局限
*   **感知假设**：假设机器人已经拥有能够检测新物体状态（如 `open(drawer)`）的谓词检测器，但在现实中，谓词的自动发现（Predicate Invention）也是一个巨大挑战。
*   **模拟环境限制**：目前仅在仿真环境中验证，尚未在真实物理机器人上处理视觉噪声和动力学差异。
*   **成本考虑**：频繁调用 GPT-o3 等高级模型会产生较高的 API 调用成本和推理延迟。

（完）
