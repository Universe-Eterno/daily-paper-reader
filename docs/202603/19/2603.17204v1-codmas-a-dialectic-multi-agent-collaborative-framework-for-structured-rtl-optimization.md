---
title: "CODMAS: A Dialectic Multi-Agent Collaborative Framework for Structured RTL Optimization"
title_zh: CODMAS：一种用于结构化 RTL 优化的辩证多智能体协作框架
authors: "Che-Ming Chang, Prashanth Vijayaraghavan, Ashutosh Jadhav, Charles Mackin, Vandana Mukherjee, Hsinyu Tsai, Ehsan Degan"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17204v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 用于结构化RTL优化的多智能体大模型框架
tldr: 针对电子设计自动化（EDA）中寄存器传输级（RTL）代码优化的复杂性，本文提出CODMAS框架。该框架通过辩证多智能体协作，结合结构化推理、领域感知代码生成和确定性评估，实现自动化的RTL优化。核心包含阐述者和假设伙伴两个辩证智能体，协同代码生成与评估智能体，在流水线和时钟门控任务上显著提升了PPA性能并降低了错误率，为复杂芯片设计优化提供了新路径。
selection_source: fresh_fetch
motivation: 传统的RTL代码优化过程高度依赖人工经验且效率低下，亟需自动化的智能化方案来提升芯片的功耗、性能和面积（PPA）指标。
method: 提出一种基于辩证推理的多智能体框架，利用阐述者和假设伙伴进行逻辑推演，并配合领域感知代码生成与评估智能体执行优化。
result: "实验表明，该框架在流水线优化中降低了约25%的关键路径延迟，在时钟门控中降低了约22%的功耗，且功能正确性显著优于基准模型。"
conclusion: 结构化的多智能体辩证推理能够显著增强自动化RTL优化的效果，并展现出向更复杂电路设计任务扩展的巨大潜力。
---

## 摘要
优化寄存器传输级（RTL）代码是电子设计自动化（EDA）中提高功耗、性能和面积（PPA）的关键步骤。我们提出了 CODMAS（通过辩证多智能体系统进行的协作优化），这是一个将结构化辩证推理与领域感知代码生成及确定性评估相结合的框架，旨在实现 RTL 优化的自动化。CODMAS 的核心是两个辩证智能体：受“小黄鸭调试法”启发的阐述者（Articulator），负责阐明逐步转换计划并揭示潜在假设；以及假设伙伴（Hypothesis Partner），负责预测结果并调和预期与实际行为之间的偏差，以指导有针对性的改进。这些智能体指导领域特定编码智能体（DCA）生成架构感知的 Verilog 修改，并指导代码评估智能体（CEA）验证语法、功能和 PPA 指标。我们引入了 RTLOPT，这是一个包含 120 个 Verilog 三元组（未优化、已优化、测试平台）的基准测试集，用于流水线化和时钟门控转换。在专有和开源大语言模型（LLM）上，与强提示词和智能体基准相比，CODMAS 在流水线化方面实现了约 25% 的关键路径延迟降低，在时钟门控方面实现了约 22% 的功耗降低，同时减少了功能和编译失败。这些结果表明，结构化多智能体推理可以显著增强自动化 RTL 优化，并扩展到更复杂的设计和更广泛的优化任务。

## Abstract
Optimizing Register Transfer Level (RTL) code is a critical step in Electronic Design Automation (EDA) for improving power, performance, and area (PPA). We present CODMAS (Collaborative Optimization via a Dialectic Multi-Agent System), a framework that combines structured dialectic reasoning with domain-aware code generation and deterministic evaluation to automate RTL optimization. At the core of CODMAS are two dialectic agents: the Articulator, inspired by rubber-duck debugging, which articulates stepwise transformation plans and exposes latent assumptions; and the Hypothesis Partner, which predicts outcomes and reconciles deviations between expected and actual behavior to guide targeted refinements. These agents direct a Domain-Specific Coding Agent (DCA) to generate architecture-aware Verilog edits and a Code Evaluation Agent (CEA) to verify syntax, functionality, and PPA metrics. We introduce RTLOPT, a benchmark of 120 Verilog triples (unoptimized, optimized, testbench) for pipelining and clock-gating transformations. Across proprietary and open LLMs, CODMAS achieves ~25% reduction in critical path delay for pipelining and ~22% power reduction for clock gating, while reducing functional and compilation failures compared to strong prompting and agentic baselines. These results demonstrate that structured multi-agent reasoning can significantly enhance automated RTL optimization and scale to more complex designs and broader optimization tasks.

---

## 论文详细总结（自动生成）

### CODMAS：一种用于结构化 RTL 优化的辩证多智能体协作框架

#### 1. 核心问题与整体含义（研究动机和背景）
在集成电路设计中，寄存器传输级（RTL）代码的优化是提升芯片功耗、性能和面积（PPA）指标的关键环节。传统的优化过程高度依赖资深工程师的手工调整，既耗时又容易出错。虽然现有的 AI 技术在逻辑综合和布局布线方面取得了进展，但在处理硬件描述语言（HDL）的架构级优化（如流水线化和时钟门控）时，现有的大语言模型（LLM）往往缺乏对设计意图的深刻理解，且难以保证生成代码的功能正确性。本文旨在提出一种自动化的框架，通过多智能体协作和结构化推理来解决 RTL 优化的复杂性问题。

#### 2. 方法论：核心思想与关键技术
CODMAS 框架的核心思想是模拟人类工程师的辩证思维过程，建立一个闭环的迭代优化系统。
*   **辩证推理智能体（Dialectic Agents）：**
    *   **阐述者（Articulator）：** 受“小黄鸭调试法”启发，负责将优化目标分解为具体的步骤计划，揭示设计假设，并为后续步骤提供语义支架。
    *   **假设伙伴（Hypothesis Partner）：** 负责预测优化后的 PPA 收益和潜在的功能风险。当评估结果与预期不符时，它会修正假设并引导针对性的改进。
*   **执行智能体（Executor Agents）：**
    *   **领域特定编码智能体（DCA）：** 利用领域知识注入器（DKI），结合数据流图（DFG）和少样本示例，生成架构感知的 Verilog 代码。
    *   **代码评估智能体（CEA）：** 调用确定性的 EDA 工具（如 Icarus Verilog 进行仿真，Yosys 进行综合），提供语法、功能和 PPA 指标的反馈报告。
*   **算法流程：** 系统从原始 RTL 出发，通过阐述者规划、假设伙伴预测、DCA 生成代码、CEA 评估反馈，形成一个迭代循环，直到达到优化目标或迭代上限。

#### 3. 实验设计
*   **数据集：** 引入了 **RTLOPT** 基准测试集，包含 120 组 Verilog 三元组（未优化代码、人工优化代码、测试平台），涵盖了流水线化（70组）和时钟门控（50组）两种核心优化任务。
*   **对比方法：**
    *   **基础提示词方法：** Zero-Shot Prompting。
    *   **推理增强方法：** CoDes、ReAct、Reflexion。
    *   **专用框架：** LLM-VeriPPA。
*   **评估模型：** 包括专有模型（GPT-4o, GPT-3.5-turbo）和开源模型（Llama-3, DeepSeek-v2.5, Granite-34B, CodeLLaMA-34B）。
*   **评估指标：** 面积（A）、关键路径延迟（T）、功耗（P）以及失败率（FR，包括语法、功能和 PPA 违规）。

#### 4. 资源与算力
*   **硬件环境：** 实验在一台配备 64 核 CPU 和 **NVIDIA A100 GPU** 的服务器上运行。
*   **软件工具：** 使用了开源 EDA 工具链，包括 Icarus Verilog（编译器/仿真器）、GTKWave（波形查看）和 Yosys（综合与时序分析）。
*   **训练/运行：** 论文未提及模型微调（Training），主要侧重于推理（Inference）和多智能体交互。每组实验重复运行 5 次以确保统计显著性。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 6 种不同的 LLM 进行了详尽测试，涵盖了 120 个设计案例。
*   **消融实验：** 专门设计了消融实验来验证辩证智能体（DA）、领域知识注入（DKI）和评估智能体（CEA）的贡献。
*   **迭代分析：** 探讨了迭代次数（1-8次）对优化效果的影响。
*   **充分性评价：** 实验设计较为充分，通过多模型对比、统计学检验（p < 0.01）和消融分析，客观地证明了框架的有效性。

#### 6. 主要结论与发现
*   **性能提升：** 在 GPT-4o 上，CODMAS 使流水线任务的关键路径延迟降低了约 **25.5%**，使时钟门控任务的功耗降低了约 **21.7%**。
*   **可靠性增强：** 相比于基准方法，CODMAS 显著降低了失败率（通常保持在 30% 以下，而基准方法常超过 50%）。
*   **模型普适性：** 即使是较弱的开源模型，在 CODMAS 框架下也能获得显著的性能增益，证明了结构化推理对模型能力的补强作用。
*   **迭代价值：** 前 3 次迭代通常能获得最大的优化收益，随后收益趋于平缓或出现波动。

#### 7. 优点与亮点
*   **辩证推理架构：** 将“规划”与“预测/诊断”角色分离，有效避免了单智能体容易陷入的思维定式。
*   **确定性反馈：** 引入真实的 EDA 工具链作为评估环路，确保了优化建议的可验证性和实用性。
*   **新基准测试集：** RTLOPT 填补了 RTL 优化领域缺乏高质量、可综合、带测试平台的开源数据集的空白。
*   **领域知识结合：** 通过数据流图（DFG）等硬件特有信息指导 LLM，使其生成的代码更符合电路架构逻辑。

#### 8. 不足与局限
*   **优化类别有限：** 目前仅覆盖了流水线和时钟门控，尚未扩展到重定时（Retiming）、资源共享或状态机重构等更复杂的优化。
*   **规模扩展性：** 对于超大规模的工业级设计，频繁调用综合工具可能会带来巨大的计算开销和时间成本。
*   **验证深度：** 依赖于测试平台（Testbench）进行功能验证，而非穷尽式的形式验证（Formal Verification），可能遗漏极端情况下的错误。
*   **数据集规模：** 120 组数据虽然在同类研究中已属可观，但相对于复杂的芯片设计空间仍显不足。

（完）
