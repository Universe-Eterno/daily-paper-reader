---
title: Efficient and Interpretable Multi-Agent LLM Routing via Ant Colony Optimization
title_zh: 基于蚁群算法的高效且可解释的多智能体大语言模型路由
authors: "Xudong Wang, Chaoning Zhang, Jiaquan Zhang, Chenghao Li, Qigan Sun, Sung-Ho Bae, Peng Wang, Ning Xie, Jie Zou, Yang Yang, Hengtao Shen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12933v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 通过蚁群算法启发式进行大模型路由
tldr: 针对多智能体系统（MAS）推理成本高、延迟大且缺乏透明度的问题，本文提出AMRO-S路由框架。该框架利用微调的小语言模型进行意图推理，并引入受蚁群算法启发的信息素专家机制来优化路径选择，同时采用异步更新机制降低延迟。实验证明，AMRO-S在提升路由效率和质量成本权衡的同时，通过结构化信息素模式实现了路由过程的可解释性。
selection_source: fresh_fetch
motivation: 现有的多智能体路由策略受限于高昂的LLM选择器成本、静态策略的局限性以及动态负载下的低透明度。
method: 采用小模型进行语义意图推理，并结合任务特定的信息素专家机制与质量门控异步更新来优化路由决策。
result: 在多个基准测试中，AMRO-S显著提升了质量与成本的权衡表现，并展现出良好的高并发处理能力。
conclusion: AMRO-S通过结合仿生算法与轻量化模型，为构建高效、可解释且可扩展的多智能体路由系统提供了有效路径。
---

## 摘要
大语言模型（LLM）驱动的多智能体系统（MAS）在复杂推理和工具使用方面展现出强大的能力，而异构智能体池进一步拓宽了质量与成本之间的权衡空间。尽管取得了这些进展，但实际部署往往受到高推理成本、延迟和有限透明度的限制，这阻碍了可扩展且高效的路由。现有的路由策略通常依赖于昂贵的基于 LLM 的选择器或静态策略，并且在动态负载和混合意图下对语义感知路由的可控性有限，往往导致性能不稳定和资源利用效率低下。为了解决这些局限性，我们提出了 AMRO-S，一种用于多智能体系统（MAS）的高效且可解释的路由框架。AMRO-S 将 MAS 路由建模为语义条件的路径选择问题，通过三个关键机制增强路由性能：首先，它利用经过有监督微调（SFT）的小语言模型进行意图推理，为每个查询提供低开销的语义接口；其次，它将路由记忆分解为特定任务的信息素专家，减少跨任务干扰并优化混合工作负载下的路径选择；最后，它采用质量门控异步更新机制，将推理与学习解耦，在不增加延迟的情况下优化路由。在五个公共基准测试和高并发压力测试上的广泛实验表明，AMRO-S 在强路由基准之上持续改进了质量与成本的权衡，同时通过结构化的信息素模式提供了可追溯的路由证据。

## Abstract
Large Language Model (LLM)-driven Multi-Agent Systems (MAS) have demonstrated strong capability in complex reasoning and tool use, and heterogeneous agent pools further broaden the quality--cost trade-off space. Despite these advances, real-world deployment is often constrained by high inference cost, latency, and limited transparency, which hinders scalable and efficient routing. Existing routing strategies typically rely on expensive LLM-based selectors or static policies, and offer limited controllability for semantic-aware routing under dynamic loads and mixed intents, often resulting in unstable performance and inefficient resource utilization. To address these limitations, we propose AMRO-S, an efficient and interpretable routing framework for Multi-Agent Systems (MAS). AMRO-S models MAS routing as a semantic-conditioned path selection problem, enhancing routing performance through three key mechanisms: First, it leverages a supervised fine-tuned (SFT) small language model for intent inference, providing a low-overhead semantic interface for each query; second, it decomposes routing memory into task-specific pheromone specialists, reducing cross-task interference and optimizing path selection under mixed workloads; finally, it employs a quality-gated asynchronous update mechanism to decouple inference from learning, optimizing routing without increasing latency. Extensive experiments on five public benchmarks and high-concurrency stress tests demonstrate that AMRO-S consistently improves the quality--cost trade-off over strong routing baselines, while providing traceable routing evidence through structured pheromone patterns.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient and Interpretable Multi-Agent LLM Routing via Ant Colony Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）驱动的多智能体系统（MAS）规模扩大，如何在异构智能体池中为每个查询选择最优执行路径成为瓶颈。
*   **研究动机**：现有的路由策略要么依赖昂贵的 LLM 选择器（增加延迟和成本），要么使用静态规则（难以应对动态负载和复杂意图）。
*   **核心挑战**：如何在保证输出质量的前提下，降低推理成本和延迟，并提升路由决策的透明度（可解释性）。
*   **整体含义**：本文提出 **AMRO-S** 框架，将 MAS 路由建模为受蚁群算法（ACO）启发的语义条件路径选择问题，旨在实现高效、低成本且可解释的智能体调度。

### 2. 方法论：核心思想与关键技术
AMRO-S 将 MAS 路由抽象为分层有向图上的路径搜索，核心包含三个机制：
*   **语义感知路由（SLM Router）**：使用经过有监督微调（SFT）的轻量级小语言模型（如 Llama-3.2-1B）作为意图推理接口。它将用户查询映射为任务分布权重，为后续路由提供低开销的语义引导。
*   **任务特定信息素专家（Pheromone Specialists）**：借鉴蚁群算法，为不同任务（如数学、代码、通用）维护独立的信息素矩阵。通过“查询条件融合”技术，根据 SLM 输出的权重动态组合信息素，减少跨任务干扰，优化混合负载下的路径选择。
*   **质量门控异步进化（Quality-Gated Asynchronous Evolution）**：
    *   **推理阶段**：仅执行路径采样和智能体调用，不产生额外开销。
    *   **学习阶段**：在后台异步运行，利用轻量级 LLM-Judge 作为“质量门控”，仅对高质量的成功轨迹进行信息素强化，实现持续在线优化而不增加服务延迟。

### 3. 实验设计
*   **数据集**：涵盖 5 个公共基准测试：GSM8K（小学数学）、MMLU（综合知识）、MATH（竞赛数学）、HumanEval（代码生成）、MBPP（Python 编程）。
*   **智能体池**：包含 gpt-4o-mini, gemini-1.5-flash, claude-3.5-haiku, llama-3.1-70b 等异构模型。
*   **对比方法**：
    *   **单模型基线**：Vanilla GPT-4o, Claude-3.5-Sonnet 等。
    *   **链式推理**：CoT, ToT, GoT, AoT。
    *   **多智能体基线**：LLM-Debate, GPTSwarm, AFlow。
    *   **路由方法**：RouteLLM, RouterDC, MasRouter。
*   **集成测试**：将 AMRO-S 插入现有的 MAS 框架（MacNet, GPTSwarm, HEnRY）验证其通用性。

### 4. 资源与算力
*   **硬件**：使用单台 **NVIDIA A100 (80GB) GPU**。
*   **训练时长**：语义路由器的 SFT 训练通常在 **30 分钟内** 完成。
*   **模型规模**：路由器使用 1B-1.5B 参数的小模型，显著降低了部署门槛。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括 5 个数据集的性能对比、针对 3 个现有框架的集成实验、高并发压力测试（从 20 到 1000 个并发进程）以及详尽的消融实验。
*   **充分性与公平性**：
    *   **统一预算**：在对比实验中严格限制了最大交互轮数和智能体调用次数，确保性能提升源于路由逻辑而非计算量堆砌。
    *   **消融实验**：分别验证了 SFT、不同路由器骨干、路由策略对结果的影响，逻辑严密。
    *   **压力测试**：展示了在极端负载下系统的稳定性，证明了方法的实用性。

### 6. 主要结论与发现
*   **性能领先**：AMRO-S 在 5 个基准测试上的平均得分达到 87.83，超过了最强的多智能体路由基线 MasRouter（85.93）。
*   **成本效益**：在集成到现有框架时，AMRO-S 在提升准确率的同时降低了推理成本（Token 使用量）。
*   **高并发优势**：在 1000 并发压力测试下，AMRO-S 实现了 **4.7 倍的加速**，且准确率保持稳定，而传统加权轮询（WRR）方法在同等负载下准确率大幅下降。
*   **可解释性**：通过可视化收敛后的信息素热图，可以清晰观察到系统针对不同任务（如代码 vs 数学）自动发现的协作拓扑结构。

### 7. 优点
*   **仿生学结合**：巧妙地将蚁群算法的信息素机制引入 LLM 路由，解决了动态环境下的路径优化问题。
*   **低开销语义接口**：利用 SFT-SLM 替代昂贵的 LLM 选择器，极大地降低了路由本身的延迟和成本。
*   **解耦设计**：异步更新机制确保了系统可以“边做边学”，而不影响实时响应速度。
*   **透明度**：信息素矩阵为黑盒 LLM 系统提供了结构化的、可追溯的决策依据。

### 8. 不足与局限
*   **图结构限制**：目前模型基于预定义的“分层有向图”，对于非线性、高度动态变化的智能体拓扑结构的适应性有待进一步验证。
*   **质量门控依赖**：异步更新依赖于 LLM-Judge 的判断，如果 Judge 模型本身存在偏见或错误，可能会导致信息素的错误强化。
*   **冷启动问题**：虽然有离线预热，但在全新领域任务上的初始探索阶段可能仍存在性能波动。

（完）
