---
title: Cluster-Aware Attention-Based Deep Reinforcement Learning for Pickup and Delivery Problems
title_zh: 针对取送货问题的聚类感知注意力深度强化学习
authors: "Wentao Wang, Lifeng Han, Guangyu Zou"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.10053v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 针对车辆路径规划变体的深度强化学习
tldr: 针对取送货问题（PDP）中节点成对约束和空间聚类特性，本文提出CAADRL框架。该框架通过聚类感知编码器结合全局与簇内注意力，并利用动态双解码器平衡簇内路径与簇间转移。实验表明，CAADRL在处理聚类分布问题时表现优异，且推理速度显著快于现有的神经协作搜索方法，为解决复杂PDP提供了高效的归纳偏置。
selection_source: fresh_fetch
motivation: 现有的深度强化学习方法在处理具有空间聚类特性的PDP问题时，往往难以兼顾约束处理效率与推理速度。
method: 提出一种结合聚类感知编码与动态双解码器的层次化强化学习框架，通过学习门控平衡簇内路由与簇间转移。
result: 在聚类和均匀分布的PDP基准测试中，该方法在保持高竞争力的同时，推理时间大幅低于现有的神经协作搜索基准。
conclusion: 显式建模聚类结构为神经PDP求解器提供了有效且高效的归纳偏置，显著提升了模型在复杂空间布局下的求解性能。
---

## 摘要
取送货问题 (PDP) 是车辆路径问题 (VRP) 的一种基础且具有挑战性的变体，其特点是紧密耦合的取送货对、先后顺序约束以及通常表现出聚类特征的空间布局。现有的深度强化学习 (DRL) 方法要么在扁平图上对所有节点建模，依靠隐式学习来强制执行约束；要么通过推理时的协作搜索获得强大性能，但代价是巨大的延迟。在本文中，我们提出了 CAADRL（聚类感知注意力深度强化学习），这是一个通过聚类感知编码和分层解码显式利用 PDP 实例多尺度结构的 DRL 框架。编码器基于 Transformer，结合了全局自注意力和针对仓库、取货及送货节点的簇内注意力，生成的嵌入既具有全局信息量，又具有局部角色感知能力。基于这些嵌入，我们引入了一个带有可学习门控的动态双解码器，在每一步平衡簇内路由和簇间转换。该策略采用 POMO 风格的策略梯度方案进行端到端训练，每个实例使用多个对称展开。在合成的聚类和均匀 PDP 基准测试上的实验表明，CAADRL 在聚类实例上达到或优于强大的最先进基准模型，并且在均匀实例上（特别是随着问题规模增加）保持高度竞争力。至关重要的是，我们的方法在实现这些结果的同时，推理时间显著低于神经协作搜索基准，这表明显式建模聚类结构为神经 PDP 求解器提供了一种有效且高效的归纳偏置。

## Abstract
The Pickup and Delivery Problem (PDP) is a fundamental and challenging variant of the Vehicle Routing Problem, characterized by tightly coupled pickup--delivery pairs, precedence constraints, and spatial layouts that often exhibit clustering. Existing deep reinforcement learning (DRL) approaches either model all nodes on a flat graph, relying on implicit learning to enforce constraints, or achieve strong performance through inference-time collaborative search at the cost of substantial latency. In this paper, we propose \emph{CAADRL} (Cluster-Aware Attention-based Deep Reinforcement Learning), a DRL framework that explicitly exploits the multi-scale structure of PDP instances via cluster-aware encoding and hierarchical decoding. The encoder builds on a Transformer and combines global self-attention with intra-cluster attention over depot, pickup, and delivery nodes, producing embeddings that are both globally informative and locally role-aware. Based on these embeddings, we introduce a Dynamic Dual-Decoder with a learnable gate that balances intra-cluster routing and inter-cluster transitions at each step. The policy is trained end-to-end with a POMO-style policy gradient scheme using multiple symmetric rollouts per instance. Experiments on synthetic clustered and uniform PDP benchmarks show that CAADRL matches or improves upon strong state-of-the-art baselines on clustered instances and remains highly competitive on uniform instances, particularly as problem size increases. Crucially, our method achieves these results with substantially lower inference time than neural collaborative-search baselines, suggesting that explicitly modeling cluster structure provides an effective and efficient inductive bias for neural PDP solvers.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CAADRL**（聚类感知注意力深度强化学习）的框架，旨在高效解决取送货问题（PDP）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：取送货问题（PDP）要求车辆在满足“先取后送”的严格顺序约束下，最小化总行驶距离。
*   **研究动机**：
    *   **空间聚类特性**：现实中的取货点和送货点往往呈现空间聚类特征（如工业区取货，住宅区送货），但现有模型多将节点视为扁平图，未能显式利用这种多尺度空间结构。
    *   **效率与性能的权衡**：现有的神经求解器要么依赖隐式学习约束（性能有限），要么依赖推理时的迭代搜索（如 NCS），导致推理延迟较高。
    *   **归纳偏置缺失**：缺乏一种能够同时处理局部簇内路径优化和全局簇间转移的架构设计。

### 2. 方法论：CAADRL 框架
核心思想是通过**聚类感知编码**和**分层双解码器**，将 PDP 的空间结构作为归纳偏置引入模型。
*   **聚类感知编码器 (Cluster-Aware Encoder)**：
    *   **双重注意力机制**：在每一层 Transformer 中并行运行“全局自注意力”和“簇内注意力”。
    *   **簇内注意力**：使用结构化掩码（Cluster Mask），限制节点仅关注同类节点（如取货点仅看取货点），从而提取角色感知的局部特征。
    *   **特征融合**：将全局背景与局部角色特征融合，生成更具表达力的节点嵌入。
*   **动态双解码器 (Dynamic Dual-Decoder)**：
    *   **分层决策**：设计两个解码管道，一个负责“簇内路由”（局部战术），一个负责“簇间转换”（全局战略）。
    *   **可学习门控 (Gating Mechanism)**：引入一个门控网络输出概率 $p_{stay}$，动态决定当前步骤是留在当前簇内继续作业，还是跳转到另一个簇。
*   **训练算法**：
    *   采用 **POMO (Policy Optimization with Multiple Optima)** 强化学习方案。
    *   利用问题的对称性，从不同起始节点进行多个并行展开（Rollouts），以共享基准线减少梯度方差，提高训练稳定性。

### 3. 实验设计
*   **数据集/场景**：
    *   **聚类分布 (Clustered)**：取货点和送货点分别服从不同的正态分布，形成明显的空间簇。
    *   **均匀分布 (Uniform)**：所有节点在平面内随机均匀分布，用于测试模型的泛化性。
    *   **规模**：涵盖 PDP10、PDP20、PDP40、PDP80（数字代表总节点数）。
*   **基准方法 (Benchmarks)**：
    *   **NCS (Neural Collaborative Search)**：目前最强的神经协作搜索方法（含迭代改进）。
    *   **Heter-AM**：基于异构注意力的编码-解码模型。
    *   **POMO**：原始的 vanilla 神经构造方法。

### 4. 资源与算力
*   **硬件**：实验在 **vGPU-32GB** 显卡上进行。
*   **训练细节**：
    *   训练 800 个 Epoch，Batch Size 为 512。
    *   使用 Adam 优化器，学习率固定为 $1 \times 10^{-4}$。
    *   嵌入维度为 128，编码器包含 6 层双重注意力层。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比实验：在 4 种规模、2 种分布下进行了详尽对比。
    *   **消融实验**：分别验证了“聚类感知编码器”和“双解码器”的贡献，证明了两者结合的优越性。
    *   **泛化性实验**：测试了跨规模泛化（如 PDP100 模型直接测试 PDP200/300/500）。
*   **充分性评价**：实验设计较为全面，不仅对比了路径长度（Obj.），还对比了推理时间（Time），并考虑了不同采样预算（Greedy vs Sample 12800）下的表现，结果客观公平。

### 6. 主要结论与发现
*   **聚类场景优势显著**：在聚类分布下，CAADRL 在中大规模问题（PDP40/80）上优于 NCS 和 Heter-AM。
*   **推理效率极高**：作为纯构造式方法，CAADRL 的推理速度比需要迭代改进的 NCS 快数倍，且性能相当甚至更好。
*   **泛化能力强**：即使在没有明显聚类的“均匀分布”下，CAADRL 依然保持了极高的竞争力，且在大规模（PDP80-Uniform）上表现出更好的鲁棒性。
*   **规模可扩展性**：模型展现了良好的零样本（Zero-shot）跨规模迁移能力，能处理比训练规模大数倍的问题。

### 7. 优点与亮点
*   **创新的架构设计**：显式建模“簇内”与“簇间”决策，符合人类专家解决此类问题的直觉。
*   **高效的归纳偏置**：通过简单的掩码和双解码结构，成功将复杂的空间先验知识植入 DRL 模型。
*   **平衡性好**：在保证构造速度的同时，通过采样策略达到了接近搜索类算法的解质量。

### 8. 不足与局限
*   **场景单一化**：主要针对静态单车 PDP 问题，未深入探讨带时间窗（TW）、多车（Fleet）或动态需求等更复杂的变体。
*   **聚类假设依赖**：虽然在均匀分布下表现尚可，但其核心优势高度依赖于空间聚类的存在；若现实场景中节点分布极度破碎，双解码器的优势可能会减弱。
*   **合成数据局限**：实验完全基于合成数据集，缺乏在真实物流轨迹数据集上的验证。

（完）
