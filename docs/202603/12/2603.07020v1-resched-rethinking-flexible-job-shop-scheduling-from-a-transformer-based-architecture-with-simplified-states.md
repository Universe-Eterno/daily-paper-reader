---
title: "RESCHED: Rethinking Flexible Job Shop Scheduling from a Transformer-based Architecture with Simplified States"
title_zh: RESCHED：基于简化状态 Transformer 架构的柔性作业车间调度重新思考
authors: "Xiangjie Xiao, Cong Zhang, Wen Song, Zhiguang Cao"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07020v1"
tags: ["query:nco"]
score: 10.0
evidence: 基于Transformer架构的柔性作业车间调度
tldr: 针对柔性作业车间调度问题（FJSP），现有深度强化学习方法普遍依赖复杂的手工特征和图神经网络。本文提出ReSched框架，通过重新审视MDP模型，将状态空间简化为仅4个核心特征，并采用改进的Transformer架构。实验证明，ReSched在FJSP上优于现有SOTA方法，且在JSSP和FFSP等变体中展现出极强的泛化能力，显著降低了建模复杂度并提升了通用性。
selection_source: fresh_fetch
motivation: 旨在解决现有FJSP神经调度方法中状态表示过于复杂（依赖20多个手工特征）且模型架构缺乏通用性的问题。
method: 通过子问题视角将状态空间压缩至4个基本特征，并利用带有轻量级调度优化设计的Transformer块进行建模。
result: ReSched在FJSP上超越了传统规则和最先进的DRL方法，并在JSSP和FFSP任务中取得了极具竞争力的泛化表现。
conclusion: 研究证明了简化状态表示与通用Transformer架构相结合在处理复杂调度问题时的有效性和优越的泛化潜力。
---

## 摘要
柔性作业车间调度问题（FJSP）的神经方法，特别是基于深度强化学习（DRL）的方法，近年来受到了广泛关注。然而，现有方法依赖于复杂的特征工程状态表示（通常需要 20 多个手工特征）和偏向图的神经架构。为了降低建模复杂度并提出更具泛化性的 FJSP 框架，我们引入了 ReSched，这是一个极简的 DRL 框架，它重新思考了调度建模和模型设计。首先，通过重新审视 FJSP 的马尔可夫决策过程（MDP）建模，我们将状态空间压缩为仅四个核心特征，并通过基于子问题的视角消除了历史依赖。其次，我们采用了带有点积注意力的 Transformer 模块，并结合了针对调度任务定制的三个轻量且有效的架构改进。大量实验表明，ReSched 在 FJSP 上优于经典的派工规则和最先进的 DRL 方法。此外，ReSched 还能很好地泛化到作业车间调度问题（JSSP）和柔性流水车间调度问题（FFSP），在与专门为这些变体设计的神经基准模型的对比中取得了具有竞争力的性能。

## Abstract
Neural approaches to the Flexible Job Shop Scheduling Problem (FJSP), particularly those based on deep reinforcement learning (DRL), have gained growing attention in recent years. However, existing methods rely on complex feature-engineered state representations (i.e., often requiring more than 20 handcrafted features) and graph-biased neural architectures. To reduce modeling complexity and advance a more generalizable framework for FJSP, we introduce \textsc{ReSched}, a minimalist DRL framework that rethinks both the scheduling formulation and model design. First, by revisiting the Markov Decision Process (MDP) formulation of FJSP, we condense the state space to just four essential features, eliminating historical dependencies through a subproblem-based perspective. Second, we employ Transformer blocks with dot-product attention, augmented by three lightweight but effective architectural modifications tailored to scheduling tasks. Extensive experiments show that \textsc{ReSched} outperforms classical dispatching rules and state-of-the-art DRL methods on FJSP. Moreover, \textsc{ReSched} also generalizes well to the Job Shop Scheduling Problem (JSSP) and the Flexible Flow Shop Scheduling Problem (FFSP), achieving competitive performance against neural baselines specifically designed for these variants.

---

## 论文详细总结（自动生成）

以下是对论文《RESCHED: Rethinking Flexible Job Shop Scheduling from a Transformer-based Architecture with Simplified States》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：柔性作业车间调度（FJSP）是制造和物流领域的核心优化问题。现有的深度强化学习（DRL）方法普遍存在两个痛点：一是**状态表示过于复杂**，依赖超过20个手工设计的特征，且包含大量冗余的历史信息；二是**架构偏向性强**，多采用图神经网络（GNN），在捕捉长程依赖和复杂非局部交互方面存在局限。
*   **核心目标**：提出一个极简且通用的框架 **ReSched**，通过简化状态表示和引入改进的 Transformer 架构，降低建模复杂度并提升模型在不同调度变体（如 JSSP, FFSP）中的泛化能力。

### 2. 方法论
*   **核心思想**：将调度过程视为马尔可夫决策过程（MDP），通过“子问题”视角消除历史依赖，仅保留对当前决策至关重要的最小状态集。
*   **关键技术细节**：
    *   **极简状态表示**：仅使用 **4个核心特征**：1) 工序可用时间；2) 机器可用时间；3) 加工耗时（Edge feature）；4) 最小加工耗时（代理特征）。
    *   **图结构优化**：采用后向工序依赖（O2O）和工序-机器连接（O2M），通过跳跃连接让工序直接感知作业级的未来约束。
    *   **双分支 Transformer 架构**：
        *   **工序分支（RoPE）**：引入旋转位置编码（RoPE）来建模作业内部的工序顺序，无需额外参数即可增强位置感知。
        *   **机器分支（Edge-in Attention）**：将边特征（加工耗时）直接嵌入注意力机制的 Value 向量中，而非仅仅调整权重。
        *   **自基跨注意力（Self-based Cross-attention）**：针对工序与机器数量严重失衡（通常 > 10:1）的问题，让机器节点在聚合信息时也关注自身表示，防止信号稀释。
*   **算法流程**：采用 REINFORCE 算法进行训练（以证明架构本身的优越性），奖励函数定义为估计的下界完工时间（Makespan）的负增量。

### 3. 实验设计
*   **数据集与场景**：
    *   **FJSP**：使用合成数据集 SD1、SD2 进行训练，在 Brandimarte 和 Hurink 标准基准集上测试。
    *   **泛化性测试**：在未针对性训练的情况下，直接在 JSSP（Taillard, DMU 基准集）和 FFSP 场景下评估。
*   **Benchmark 与对比方法**：
    *   **传统规则**：FIFO, SPT, MWKR 等派工规则。
    *   **神经方法**：HGNN, DANIEL, DOAGNN (FJSP); L2D, RL-GNN (JSSP); MatNet (FFSP)。
    *   **精确求解器/启发式**：OR-Tools (CP-SAT), 2SGA。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX A40 GPU** 进行训练和评估。
*   **训练细节**：模型包含 2 层 Transformer 块，隐藏维度 128。在 SD1-10x5 规模上训练约需 30 分钟。训练周期通常为 2000 epoch，每个 epoch 包含 1000 个实例。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 10x5 到 100x20 等多种规模的实例。
*   **充分性**：
    *   **多维度评估**：包括分布内测试、跨规模泛化测试、跨问题变体（JSSP/FFSP）测试。
    *   **消融实验**：针对 RoPE、相对时间表示、跨注意力机制等 6 个关键组件进行了详尽的消融研究。
    *   **鲁棒性**：通过 4 个不同随机种子验证了结果的稳定性（波动小于 1%）。
*   **客观性**：对比了 SOTA 神经方法和强力非学习基准（如 OR-Tools），实验设计较为公正。

### 6. 主要结论与发现
*   **性能领先**：ReSched 在 FJSP 的多个基准测试中刷新了 SOTA 记录，尤其在复杂数据集 SD2 上优势明显。
*   **极强的泛化力**：仅在 10x10 的 JSSP 实例上训练，其在 100x20 大规模实例上的表现甚至优于专门为 JSSP 设计且在同规模训练的模型。
*   **特征冗余验证**：通过对基准模型 DANIEL 的实验发现，移除一半以上的特征并不会降低性能，证明了 ReSched 极简特征设计的合理性。

### 7. 优点
*   **极简主义**：证明了无需复杂的特征工程，仅靠合理的 MDP 建模和强大的通用架构即可解决复杂调度问题。
*   **架构创新**：针对调度问题的结构特性（如工序-机器失衡、边特征重要性）对 Transformer 进行了有效的轻量化改造。
*   **高效率**：推理速度快，且支持 KV Cache 理论加速，适合实时调度场景。

### 8. 不足与局限
*   **反馈机制缺失**：目前模型主要建模了工序到机器的交互，缺乏显式的机器到工序（M2O）反馈机制。
*   **代理特征依赖**：虽然使用了“最小加工耗时”作为特征，但在极端分布的实例中，这种简化可能导致信息丢失。
*   **算法选择**：为了对比公平主要使用了 REINFORCE，虽然也测试了 PPO 版本，但尚未在大规模工业实时动态调度（Dynamic Scheduling）中进行验证。

（完）
