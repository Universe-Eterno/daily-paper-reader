---
title: "Revisiting Tree Search for LLMs: Gumbel and Sequential Halving for Budget-Scalable Reasoning"
title_zh: 重新审视大语言模型的树搜索：面向预算可扩展推理的 Gumbel 与顺序减半算法
authors: "Leonid Ugadiarov, Yuri Kuratov, Aleksandr Panov, Alexey Skrynnik"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21162v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: AlphaZero风格的树搜索增强大语言模型的推理能力
tldr: 针对大语言模型在推理任务中使用AlphaZero式树搜索时出现的性能随预算增加而下降的“缩放失效”问题，本文提出了ReSCALE框架。该方法通过引入Gumbel采样和顺序减半（Sequential Halving）机制替代传统的Dirichlet噪声和PUCT选择，成功恢复了推理性能的单调增长。在GSM8K和Game24任务上，ReSCALE显著提升了高预算下的准确率，证明了其在预算可扩展推理中的有效性。
selection_source: fresh_fetch
motivation: 传统的AlphaZero式树搜索在LLM推理中存在预算增加反而导致性能下降的缩放失效问题。
method: 提出ReSCALE框架，利用Gumbel采样和顺序减半机制优化搜索过程，无需重新训练模型即可实现性能提升。
result: "在GSM8K和Game24基准测试中，ReSCALE在基准方法性能退化的预算水平下分别达到了58.4%和85.3%的准确率。"
conclusion: 顺序减半机制是恢复LLM树搜索预算可扩展性的关键，使推理性能随计算资源投入而稳定提升。
---

## 摘要
神经树搜索是一种强大的决策算法，广泛应用于博弈和基于模型的强化学习等复杂领域。近期的研究已将 AlphaZero 风格的树搜索应用于增强大语言模型（LLM）在推理阶段的推理能力，但我们发现这种方法存在扩展失效的问题：在 GSM8K 和 Game24 任务中，准确率会随着搜索预算的增加而下降。在本文中，我们提出了 ReSCALE，这是对 Gumbel AlphaZero MCTS 的一种改进，它利用 Gumbel 采样和顺序减半（Sequential Halving）取代了狄利克雷噪声和 PUCT 选择，在不改变模型或其训练过程的情况下恢复了单调扩展性。在基准模型性能退化的预算水平下，ReSCALE 在 GSM8K 上达到了 58.4%，在 Game24 上达到了 85.3%。消融实验证实，顺序减半是性能提升的主要驱动因素。

## Abstract
Neural tree search is a powerful decision-making algorithm widely used in complex domains such as game playing and model-based reinforcement learning. Recent work has applied AlphaZero-style tree search to enhance the reasoning capabilities of Large Language Models (LLMs) during inference, but we find that this approach suffers from a scaling failure: on GSM8K and Game24, accuracy drops as the search budget increases. In this paper, we present ReSCALE, an adaptation of Gumbel AlphaZero MCTS that replaces Dirichlet noise and PUCT selection with Gumbel sampling and Sequential Halving, restoring monotonic scaling without changes to the model or its training. ReSCALE reaches 58.4\% on GSM8K and 85.3\% on Game24 at budgets where the baseline degrades. Ablations confirm that Sequential Halving is the primary driver of the improvement.

---

## 论文详细总结（自动生成）

这是一份关于论文《Revisiting Tree Search for LLMs: Gumbel and Sequential Halving for Budget-Scalable Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然大语言模型（LLM）在推理任务上取得了进展，但仍难以处理复杂的多步规划问题。目前常用 AlphaZero 风格的蒙特卡洛树搜索（MCTS）来增强 LLM 的推理能力，通过增加推理时的计算量（Test-time compute）来换取准确率。
*   **核心问题**：作者发现传统的 AlphaZero 式树搜索在 LLM 推理中存在**“缩放失效”（Scaling Failure）**现象。即在 GSM8K 和 Game24 等任务中，当搜索预算（模拟次数或 Token 数量）增加到一定程度后，准确率反而会停滞甚至下降（类似于博弈论中的“前瞻病态”现象）。
*   **整体含义**：本文旨在寻找一种能够随计算预算增加而稳定提升性能（即具备单调缩放特性）的树搜索算法。

### 2. 方法论：核心思想与关键技术
作者提出了 **ReSCALE** 框架，其核心是将 Gumbel AlphaZero 的思想引入 LLM 解码过程，主要改进包括：
*   **动作空间定义**：采用“句子级”动作，以换行符（DELIM）作为动作边界，降低树的深度。
*   **根节点选择机制（核心改进）**：
    *   **Gumbel 采样**：取代了传统的狄利克雷噪声，通过 Gumbel 噪声实现无重复采样，并提供策略改进保证。
    *   **顺序减半（Sequential Halving）**：这是解决缩放失效的关键。它将总预算 $N$ 分配给多个轮次，每轮运行一定数量的模拟后，根据“Gumbel 分数 + 价值函数”淘汰表现较差的一半动作，将资源集中在最有潜力的候选路径上。
*   **非根节点选择**：使用改进的策略 $\pi'$，结合了先验概率和**混合价值近似（Mixed Value Approximation）**。该近似值在价值网络的预测值与已访问兄弟节点的经验均值之间进行插值，防止模型过度信任不准确的价值估计。
*   **价值网络**：基于 Llama2-7B 骨干网络添加 MLP 头，使用蒙特卡洛回报（MC Return）进行训练。

### 3. 实验设计
*   **数据集/场景**：
    *   **GSM8K**：数学应用题推理。
    *   **Game24**：数学规划任务（给定4个数字，通过加减乘除得到24）。
*   **基准模型（Backbone）**：Llama2-7B。
*   **对比方法（Benchmarks）**：
    *   **AlphaZero MCTS**：传统的基于 PUCT 和狄利克雷噪声的树搜索。
    *   **Best-of-N**：采样 $N$ 个完整路径并使用价值模型重排序。
*   **实验设置**：将预算分为 Small、Medium、Large 三个等级（基于 Token 消耗量），观察准确率随预算的变化曲线。

### 4. 资源与算力
*   **模型训练**：对 Llama2-7B 进行了 3 个 Epoch 的监督微调（SFT），随后训练价值网络 3 个 Epoch。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到在 Large 预算下，GSM8K 的单题 Token 消耗量达到了 16K-18K。

### 5. 实验数量与充分性
*   **实验规模**：在两个具有代表性的推理任务上进行了测试，涵盖了从低到高的多种预算配置。
*   **消融实验**：专门针对 Gumbel 噪声和顺序减半机制进行了消融，结果显示**顺序减半是性能提升的最主要驱动力**（移除它会导致准确率下降 4.7%）。
*   **客观性**：使用了多个随机种子进行实验，并报告了均值和标准差，实验设计较为严谨，对比基准（AlphaZero 风格 MCTS）是目前该领域的主流方法。

### 6. 主要结论与发现
*   **恢复了单调缩放**：ReSCALE 成功解决了 AlphaZero 在高预算下的性能退化问题。在 GSM8K 上达到 58.4%，在 Game24 上达到 85.3%，显著优于基准方法。
*   **失效原因分析**：传统 AlphaZero MCTS 容易过度开发（Over-exploit）少数节点，导致搜索树极度不平衡。
*   **搜索设计的关键性**：根节点的动作选择逻辑（如顺序减半）对于 LLM 树搜索的效率和最终表现至关重要。

### 7. 优点
*   **无需重训模型**：ReSCALE 是一种推理侧（Inference-only）的改进，不需要改变基础 LLM 或其训练目标，具有很强的通用性。
*   **解决痛点**：直接针对“计算量增加反而效果变差”这一反直觉的病态现象提出了有效的工程解决方案。
*   **理论支撑**：引入了多臂老虎机（MAB）领域成熟的“最佳臂识别”算法（顺序减半），使 LLM 搜索更具理论依据。

### 8. 不足与局限
*   **计算成本高昂**：虽然 ReSCALE 提升了效率，但树搜索本身相比标准解码仍需消耗巨大的 Token 预算（数千甚至上万 Token 换取一个答案）。
*   **任务覆盖面**：实验主要集中在数学和逻辑规划任务，尚未在更广泛的开放式生成或多模态任务上验证。
*   **模型规模限制**：实验仅基于 Llama2-7B，未探讨在更大规模模型（如 70B 或更高）上这种缩放失效现象是否同样严重。
*   **硬件信息缺失**：缺乏具体的算力消耗报告，不便于其他研究者评估复现成本。

（完）
