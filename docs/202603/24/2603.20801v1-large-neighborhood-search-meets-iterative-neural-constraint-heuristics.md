---
title: Large Neighborhood Search meets Iterative Neural Constraint Heuristics
title_zh: 大邻域搜索与迭代神经约束启发式算法的结合
authors: "Yudong W. Xu, Wenhao Li, Scott Sanner, Elias B. Khalil"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20801v1"
tags: ["query:nco"]
score: 10.0
evidence: 将神经约束启发式算法应用于大邻域搜索
tldr: 本研究探讨了迭代神经启发式算法与大邻域搜索（LNS）的内在联系，将神经约束满足方法ConsFormer适配为包含破坏与修复算子的LNS框架。通过引入预测引导的破坏算子，该方法在数独、图着色和最大割任务上显著提升了性能。研究揭示了随机破坏结合贪婪修复在寻找高质量解方面的优越性，为优化神经启发式算法提供了结构化的设计框架。
selection_source: fresh_fetch
motivation: 旨在利用大邻域搜索（LNS）的结构化框架来改进和优化现有的迭代神经约束满足启发式算法。
method: 将ConsFormer分解为破坏和修复算子，引入基于模型内部评分的预测引导破坏策略，并对比了贪婪与采样两种修复解码方式。
result: 实验表明适配后的LNS程序在多个组合优化任务上均显著优于原始神经模型，且在寻找高质量可行解方面更具竞争力。
conclusion: LNS是设计和提升迭代神经方法的有效框架，其中随机破坏与贪婪修复的组合被证明是提升求解质量的关键模式。
---

## 摘要
神经网络正越来越多地被用作约束满足问题的启发式方法。这些神经方法通常是循环的，学习如何迭代地优化候选赋值。在这项工作中，我们明确了此类迭代神经启发式算法与大邻域搜索（LNS）之间的联系，并将现有的神经约束满足方法 ConsFormer 适配为 LNS 过程。我们将生成的神经 LNS 分解为两个标准组件：破坏（destroy）和修复（repair）算子。在破坏端，我们实例化了几个经典启发式算法，并引入了利用模型内部评分来选择邻域的新型预测引导算子。在修复端，我们将 ConsFormer 用作神经修复算子，并将原始的基于采样的解码器与选择最可能赋值的贪婪解码器进行了比较。通过对数独、图着色和最大割（MaxCut）问题的实证研究，我们发现将神经启发式算法适配为 LNS 过程相比其原始设置带来了显著提升，并增强了其相对于经典和神经基准算法的竞争力。我们进一步观察到跨任务的一致设计模式：随机破坏算子优于贪婪算子，而贪婪修复在寻找单个高质量可行赋值方面比基于采样的修复更有效。这些发现强调了 LNS 是一个有用的视角和设计框架，可用于构建和改进迭代神经方法。

## Abstract
Neural networks are being increasingly used as heuristics for constraint satisfaction. These neural methods are often recurrent, learning to iteratively refine candidate assignments. In this work, we make explicit the connection between such iterative neural heuristics and Large Neighborhood Search (LNS), and adapt an existing neural constraint satisfaction method-ConsFormer-into an LNS procedure. We decompose the resulting neural LNS into two standard components: the destroy and repair operators. On the destroy side, we instantiate several classical heuristics and introduce novel prediction-guided operators that exploit the model's internal scores to select neighborhoods. On the repair side, we utilize ConsFormer as a neural repair operator and compare the original sampling-based decoder to a greedy decoder that selects the most likely assignments. Through an empirical study on Sudoku, Graph Coloring, and MaxCut, we find that adapting the neural heuristic to an LNS procedure yields substantial gains over its vanilla settings and improves its competitiveness with classical and neural baselines. We further observe consistent design patterns across tasks: stochastic destroy operators outperform greedy ones, while greedy repair is more effective than sampling-based repair for finding a single high-quality feasible assignment. These findings highlight LNS as a useful lens and design framework for structuring and improving iterative neural approaches.

---

## 论文详细总结（自动生成）

以下是对论文《Large Neighborhood Search meets Iterative Neural Constraint Heuristics》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何提升神经约束启发式算法在处理大规模或分布外（OOD）约束满足问题（CSP）时的泛化能力和求解质量。
*   **研究动机**：
    *   传统的单次（One-shot）神经预测方法计算预算固定，难以应对复杂多变的实例。
    *   新兴的迭代神经方法（如 ConsFormer）通过多次精炼解来提高性能，其本质过程与经典优化算法中的**大邻域搜索（LNS）**高度相似。
    *   **整体含义**：本文旨在明确迭代神经方法与 LNS 之间的联系，通过 LNS 的结构化框架（破坏+修复）来重新设计和增强神经求解器。

### 2. 核心方法论
论文提出了 **ConsFormer-LNS** 框架，将神经模型 ConsFormer 重新解释为 LNS 过程中的组件：
*   **核心思想**：将迭代过程分解为“破坏算子（Destroy）”选择变量子集，以及“修复算子（Repair）”利用 Transformer 模型重新赋值。
*   **关键技术细节**：
    *   **破坏算子（Destroy）**：
        *   **经典启发式**：随机移除、基于冲突的贪婪/随机最差移除（Worst Removal）、基于约束结构的关联移除（Related Removal）。
        *   **预测引导（新提出）**：利用模型内部信号。**梯度引导移除**利用损失函数对 Logits 的梯度来识别对冲突贡献最大的变量；**置信度余量移除**利用模型输出概率分布的 Top-2 差值来识别模型最不确定的变量。
    *   **修复算子（Repair）**：
        *   **随机采样**：使用 Gumbel-Softmax 从模型预测分布中采样（原始 ConsFormer 做法）。
        *   **贪婪解码**：直接选择概率最大的值。
*   **算法流程**：从初始赋值开始，迭代执行：1. 破坏算子选定变量子集；2. 修复算子（Transformer）对选定变量生成新赋值；3. 更新当前解并重复。

### 3. 实验设计
*   **数据集/场景**：
    *   **数独（Sudoku）**：包含 SATNet（分布内）和 RRN（更难的 OOD 实例）。
    *   **图着色（Graph Coloring）**：5 色和 10 色任务，测试集包含比训练集节点数更多的 OOD 图。
    *   **最大割（MaxCut）**：在 GSET 基准数据集上评估，节点数从 800 到 10,000 不等。
*   **对比方法（Benchmarks）**：
    *   **经典求解器**：OR-Tools（SOTA 约束编程求解器）。
    *   **神经基准**：原始 ConsFormer、IRED、Recurrent Transformer、ANYCSP、ECO-DQN 等。

### 4. 资源与算力
*   **硬件**：实验在单台 **NVIDIA H100 GPU** 节点上运行。
*   **训练细节**：作者为每种“破坏+修复”算子组合分别重新训练了模型，以确保公平性。
*   **时间限制**：推理阶段根据任务设置了不同的时间限制（如 MaxCut 为 180s，图着色为 10s）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大类组合优化问题，每类问题下细分了多种规模和分布（分布内 vs OOD）。
*   **消融实验**：系统地对比了 5 种破坏算子（每种含贪婪/随机变体）与 2 种修复算子的组合，实验矩阵非常完整。
*   **客观性**：通过 OOD 测试验证了泛化性，并与工业级求解器 OR-Tools 进行直接对比，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **LNS 框架的有效性**：将 ConsFormer 适配为 LNS 过程后，在所有基准测试中均显著优于原始迭代设置。
*   **随机性至关重要**：在破坏算子中，**随机化变体**（如随机最差移除）一致优于纯贪婪变体，后者容易陷入局部最优。
*   **修复策略的权衡**：**贪婪修复**在寻找单个高质量可行解方面优于采样修复，尽管采样在中间步骤的平均约束满足率可能更高。
*   **性能突破**：ConsFormer-LNS 在 OOD 数独任务上达到了 91.8% 的胜率（原为 85.8%），并在 10 色图着色 OOD 任务上超越了 OR-Tools。

### 7. 优点（亮点）
*   **理论联系实际**：首次明确了迭代神经启发式算法与经典 LNS 之间的对等关系，为神经求解器的设计提供了坚实的运筹学理论支撑。
*   **创新算子**：提出了利用模型内部梯度和置信度信号的破坏算子，展示了深度学习模型特有的优势。
*   **泛化能力强**：实验重点关注 OOD 表现，证明了该框架在处理未见过的更大规模问题时的鲁棒性。

### 8. 不足与局限
*   **算子选择的复杂性**：实验显示没有单一的破坏算子在所有任务中都表现最优，这增加了实际应用中选择算子的难度。
*   **训练成本**：由于需要针对不同算子组合重新训练，对于大规模工业应用，其训练时间和资源消耗可能较高。
*   **与经典求解器的差距**：虽然在某些 OOD 任务上领先，但在部分标准测试（如 5 色图着色）中仍略逊于高度优化的 OR-Tools。

（完）
