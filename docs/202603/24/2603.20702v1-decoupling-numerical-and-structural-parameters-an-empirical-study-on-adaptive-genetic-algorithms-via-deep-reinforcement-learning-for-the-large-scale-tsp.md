---
title: "Decoupling Numerical and Structural Parameters: An Empirical Study on Adaptive Genetic Algorithms via Deep Reinforcement Learning for the Large-Scale TSP"
title_zh: 解耦数值与结构参数：基于深度强化学习的自适应遗传算法在大规模旅行商问题中的实证研究
authors: "Hongyu Wang, Yuhan Jing, Yibing Shi, Enjin Zhou, Haotian Zhang, Jialong Shi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20702v1"
tags: ["query:nco"]
score: 10.0
evidence: 深度强化学习用于大规模旅行商问题和遗传算法
tldr: "本研究探讨了进化算法中数值参数与结构参数对可扩展性的不同影响。通过构建双层深度强化学习（DRL）框架，利用Recurrent PPO智能体动态调节遗传算法参数，并以大旅行商问题（TSP）为基准进行实证研究。结果显示，该方法在大型实例上将最优性差距降低了约45%。消融实验表明，结构参数的动态调整是防止算法停滞和跳出局部最优的关键，而数值参数仅提供局部优化。"
selection_source: fresh_fetch
motivation: 旨在探究进化算法中数值参数与结构参数在提升算法可扩展性方面是否具有同等重要性。
method: 提出一种基于Recurrent PPO的双层深度强化学习框架，用于解耦并动态调节遗传算法的数值与结构参数。
result: "实验表明该方法在大型TSP实例上显著优于静态基准，将最优性差距降低了约45%。"
conclusion: 结构可塑性是防止算法陷入局部最优的决定性因素，未来算法设计应优先考虑动态结构重构而非精细的概率调整。
---

## 摘要
合理的参数配置是进化算法（EAs）成功的先决条件。尽管已经提出了各种自适应策略，但所有控制维度是否对算法的可扩展性贡献均等仍是一个悬而未决的问题。为了研究这一点，我们将控制变量分为数值参数（如交叉率和变异率）和结构参数（如种群规模和算子切换），并假设它们发挥着不同的作用。本文提出了一项实证研究，利用双层深度强化学习（DRL）框架来解耦并分析这两个维度对旅行商问题（TSP）的影响。我们采用循环 PPO（Recurrent PPO）智能体来动态调节这些参数，将 DRL 模型视为揭示进化动力学的探针。实验结果证实了该方法的有效性：学习到的策略优于静态基准，在最大的测试实例（rl5915）上将最优性差距缩小了约 45%。在此验证框架的基础上，我们的消融分析揭示了一个基本见解：虽然数值微调提供了局部细化，但结构可塑性是防止停滞并促进跳出局部最优的关键因素。这些发现表明，未来的自动化算法设计应优先考虑动态结构重构，而非细粒度的概率调整。为便于复现，源代码可在 https://github.com/StarDream1314/DRLGA-TSP 获取。

## Abstract
Proper parameter configuration is a prerequisite for the success of Evolutionary Algorithms (EAs). While various adaptive strategies have been proposed, it remains an open question whether all control dimensions contribute equally to algorithmic scalability. To investigate this, we categorize control variables into numerical parameters (e.g., crossover and mutation rates) and structural parameters (e.g., population size and operator switching), hypothesizing that they play distinct roles. This paper presents an empirical study utilizing a dual-level Deep Reinforcement Learning (DRL) framework to decouple and analyze the impact of these two dimensions on the Traveling Salesman Problem (TSP). We employ a Recurrent PPO agent to dynamically regulate these parameters, treating the DRL model as a probe to reveal evolutionary dynamics. Experimental results confirm the effectiveness of this approach: the learned policies outperform static baselines, reducing the optimality gap by approximately 45% on the largest tested instance (rl5915). Building on this validated framework, our ablation analysis reveals a fundamental insight: while numerical tuning offers local refinement, structural plasticity is the decisive factor in preventing stagnation and facilitating escape from local optima. These findings suggest that future automated algorithm design should prioritize dynamic structural reconfiguration over fine-grained probability adjustment. To facilitate reproducibility, the source code is available at https://github.com/StarDream1314/DRLGA-TSP

---

## 论文详细总结（自动生成）

这篇论文针对进化算法（EA）在大规模组合优化问题中的参数配置难题，提出了一种解耦数值参数与结构参数的深度强化学习（DRL）自适应框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在进化算法中，数值参数（如交叉率、变异率）和结构参数（如种群规模、算子切换）对算法可扩展性的贡献是否相等？
*   **研究动机**：
    *   传统的自适应参数控制多关注数值概率的微调，忽视了算法结构的动态调整（结构可塑性）。
    *   静态的算法结构在面对大规模、复杂地形的问题时容易陷入局部最优。
    *   旨在通过 DRL 作为一个“探测器”，揭示进化动力学中不同维度参数的真实影响。

### 2. 方法论：DRLGA 框架
论文提出了 **DRLGA（Dual-Level Deep Reinforcement Learning Genetic Algorithm）**，采用双层架构：
*   **上层控制器（Controller）**：
    *   **核心算法**：采用 **Recurrent PPO**（结合 LSTM），以处理进化过程中的非马尔可夫特性（即当前最优决策依赖于历史改进趋势）。
    *   **状态空间**：包含 4 个静态特征（规模、分布等）和 11 个动态特征（停滞计数、种群方差、当前 gap 等）。
    *   **动作空间**：解耦为数值维度（交叉率、变异率）和结构维度（种群规模、算子模式、锦标赛规模）。
*   **下层执行器（Worker）**：
    *   **高性能 GA**：使用 **Numba JIT** 加速，负责执行具体的搜索任务。
    *   **结构化模式**：定义了三种模式：Mode 0（初始探索，2-opt）、Mode 1（强化探索，Double-Bridge 扰动）、Mode 2（精细收敛，纯局部优化）。
    *   **灾难性重置**：当长期停滞时触发，以恢复种群多样性。

### 3. 实验设计
*   **数据集**：使用 TSPLIB 中的标准实例，涵盖小规模（kroA100）、中规模（lin318 等）到大规模（rl5915，5915个城市）。
*   **基准方法（Benchmark）**：
    *   **Static GA**：经过人工调优的固定参数高性能遗传算法。
*   **对比实验**：
    *   **性能对比**：在不同规模的 TSP 实例上对比 DRLGA 与 Static GA 的最优性差距（Optimality Gap）。
    *   **消融实验**：分别固定交叉率（Fixed CR）、变异率（Fixed MR）、算子（Fixed Op）和种群规模（Fixed Pop），观察性能退化情况。

### 4. 资源与算力
*   **硬件环境**：使用高性能计算节点，配备 **64 核 AMD EPYC 7452 CPU** (@2.35GHz) 和 DDR4 内存。
*   **训练细节**：
    *   在 64 个并行环境中训练了 **500 万个时间步（timesteps）**。
    *   使用了 Adam 优化器，初始学习率为 $3 \times 10^{-4}$。
    *   **未明确提到 GPU 使用情况**，考虑到 GA 和 Numba 的特性，该实验可能主要依赖高性能 CPU 并行计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   训练集涵盖 50 到 1200 个城市的合成 TSP 实例。
    *   测试集包含 5 个大规模 TSPLIB 实例进行性能对比，以及 5 个不同规模实例进行消融研究。
*   **充分性与公平性**：
    *   每个实验独立重复运行 **30 次**并取均值，以消除随机性影响。
    *   基准方法（Static GA）使用了与 DRLGA 相同的 Numba 加速内核，确保了对比的公平性。
    *   通过 **Zero-Shot（零样本）泛化**测试（在 1200 规模训练，在 5915 规模测试），验证了策略的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：DRLGA 显著优于静态基准，在最大实例 rl5915 上将最优性差距降低了约 **45%**。
*   **核心发现（解耦见解）**：
    *   **结构可塑性是决定性因素**：动态调整算子模式和种群规模是防止算法停滞、跳出局部最优的关键。
    *   **数值参数起辅助作用**：交叉率和变异率的微调主要影响局部收敛速度，对解决算法停滞的贡献较小。
*   **智能行为**：智能体学会了“逆退火”行为（在后期增加变异率以应对多样性危机）和资源饱和策略（早期最大化种群规模）。

### 7. 优点
*   **理论深度**：首次系统性地解耦并量化了数值与结构参数在 DRL 驱动的 EA 中的不同作用。
*   **泛化能力强**：实现了“小规模训练，大规模求解”，在超出训练规模 5 倍的实例上依然表现稳健。
*   **工程实现高效**：利用 Numba JIT 显著提升了底层 GA 的运行效率，使 DRL 与启发式算法的深度结合更具实用性。

### 8. 不足与局限
*   **计算开销**：DRLGA 的推理时间约为静态 GA 的 **2 倍**，虽然精度更高，但在极端追求速度的场景下存在劣势。
*   **问题覆盖面**：目前仅在 TSP 问题上进行了验证，虽然提到未来会扩展到 VRP 等约束问题，但目前尚缺乏跨问题的通用性证明。
*   **黑盒特性**：尽管通过参数轨迹进行了一定分析，但 DRL 学习到的复杂控制逻辑在理论上仍缺乏完全的可解释性。

（完）
