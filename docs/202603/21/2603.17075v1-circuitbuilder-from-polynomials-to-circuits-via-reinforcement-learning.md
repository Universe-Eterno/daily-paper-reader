---
title: "CircuitBuilder: From Polynomials to Circuits via Reinforcement Learning"
title_zh: CircuitBuilder：通过强化学习从多项式构建电路
authors: "Weikun K. Zhang, Rohan Pandey, Bhaumik Mehta, Kaijie Jin, Naomi Morato, Archit Ganapule, Michael Ruofan Zeng, Jarod Alper"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17075v1"
tags: ["query:nco"]
score: 8.0
evidence: 用于算术电路发现的强化学习
tldr: 本研究受自动证明生成和Valiant猜想启发，探讨了利用强化学习自动发现高效算术电路来计算多项式的问题。研究者将该问题建模为单人博弈，并对比了PPO+MCTS和SAC两种算法。实验表明，SAC在双变量目标上表现优异，而PPO+MCTS在三变量及更复杂实例上展现出更好的扩展性和持续改进能力，为研究自我改进搜索策略提供了可验证的紧凑环境。
selection_source: fresh_fetch
motivation: 旨在通过自动发现高效算术电路来探索自动证明生成和Valiant的VP与VNP猜想。
method: 将电路合成建模为单人博弈，并采用AlphaZero风格的训练框架，对比了PPO结合蒙特卡洛树搜索（MCTS）与SAC算法。
result: SAC在双变量多项式上成功率最高，而PPO+MCTS在处理三变量及更难的实例时表现出更强的扩展性。
conclusion: 多项式电路合成是一个紧凑且易于验证的领域，非常适合用于研究和开发自我改进的搜索策略。
---

## 摘要
受自动证明生成和 Valiant 的 VP vs. VNP 猜想启发，我们研究了发现高效算术电路（使用加法门和乘法门）以计算多项式的问题。我们将该问题建模为一个单人游戏，其中强化学习（RL）智能体尝试在固定的操作步数内构建电路。我们实现了一个 AlphaZero 风格的训练循环，并比较了两种方法：结合蒙特卡洛树搜索的近端策略优化（PPO+MCTS）和软演员-评论家算法（SAC）。SAC 在双变量目标上取得了最高的成功率，而 PPO+MCTS 可扩展至三变量，并在更困难的实例上表现出稳步提升。这些结果表明，多项式电路合成是研究自我改进搜索策略的一个紧凑且可验证的场景。

## Abstract
Motivated by auto-proof generation and Valiant's VP vs. VNP conjecture, we study the problem of discovering efficient arithmetic circuits to compute polynomials, using addition and multiplication gates. We formulate this problem as a single-player game, where an RL agent attempts to build the circuit within a fixed number of operations. We implement an AlphaZero-style training loop and compare two approaches: Proximal Policy Optimization with Monte Carlo Tree Search (PPO+MCTS) and Soft Actor-Critic (SAC). SAC achieves the highest success rates on two-variable targets, while PPO+MCTS scales to three variables and demonstrates steady improvement on harder instances. These results suggest that polynomial circuit synthesis is a compact, verifiable setting for studying self-improving search policies.

---

## 论文详细总结（自动生成）

以下是对论文《CircuitBuilder: From Polynomials to Circuits via Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何自动发现计算特定多项式的最简算术电路（即使用最少数量的加法门和乘法门）。
*   **研究动机**：
    *   **代数复杂性理论**：该问题是 Valiant 提出的 VP vs. VNP 猜想（代数版的 P vs. NP）的核心，理解特定多项式（如永久项）的电路复杂度具有重大理论意义。
    *   **自动证明生成**：寻找最短电路可以看作是寻找最短数学证明的简化版，是研究强化学习（RL）在数学发现领域应用的理想实验场。
    *   **搜索空间挑战**：电路构建的搜索空间随步数呈指数级增长，传统穷举法在复杂多项式面前失效。

### 2. 方法论
*   **核心思想**：将电路构建建模为**单人马尔可夫决策过程（MDP）**，智能体通过顺序添加逻辑门来构建电路，目标是匹配目标多项式且步数最少。
*   **关键技术细节**：
    *   **状态编码**：使用**图神经网络（GNN，具体为 GCNConv）**编码当前的电路 DAG（有向无环图），并结合目标多项式的特征向量。
    *   **架构设计**：采用 **Transformer Decoder** 作为核心，将图嵌入和目标嵌入作为输入，输出策略（动作概率）和价值估计。
    *   **算法一：PPO + MCTS (CircuitBuilder)**：结合了近端策略优化（PPO）和蒙特卡洛树搜索（MCTS）。利用 MCTS 引导探索，并使用 PPO 稳定策略更新。
    *   **算法二：SAC (Soft Actor-Critic)**：一种离策（Off-policy）算法，通过最大化熵来鼓励智能体在稀疏奖励环境下进行探索。
    *   **训练策略**：包含监督预训练（从已知最优电路学习）、强化学习微调以及从易到难的课程学习。

### 3. 实验设计
*   **数据集/场景**：在有限域 $F_5$ 上生成多项式。
*   **Benchmark**：构建了名为“Game-board”的 DAG，枚举了所有在固定步数（$C=4, 5, 6$）内可达的多项式，并筛选出具有多个最优路径的“有趣”多项式作为挑战目标。
*   **对比维度**：
    *   变量数量：$n=2$ vs. $n=3$。
    *   电路复杂度（门数量）：$C=5$ vs. $C=6$。
    *   算法对比：PPO+MCTS vs. SAC。

### 4. 资源与算力
*   **硬件环境**：使用 AWS EC2 云实例，配备单块 **NVIDIA Tesla T4 GPU (16 GB)**。
*   **辅助工具**：部分代码库由 GitHub Copilot 辅助生成。
*   **说明**：文中未明确给出具体的训练总时长，但实验是在 200 个迭代（Iteration）内完成的。

### 5. 实验数量与充分性
*   **实验规模**：针对不同变量数和复杂度组合进行了多组实验，每个检查点使用 1000 个未见过的测试回合进行评估。
*   **充分性**：实验设计较为客观，对比了成功率、平均奖励和策略熵等多个指标。虽然目前仅限于小规模电路（最高 6 个门），但作为探索 RL 在代数搜索中可行性的初步研究，实验覆盖了难度梯度的核心变量。

### 6. 主要结论与发现
*   **SAC 擅长简单任务**：在双变量（$n=2$）场景下，SAC 表现优于 PPO+MCTS，成功率最高达到约 58%。
*   **PPO+MCTS 扩展性更强**：当变量增加到 $n=3$ 时，SAC 性能大幅下降，而 PPO+MCTS 表现出更好的稳健性和持续改进能力，证明了显式搜索（MCTS）在处理高维搜索空间时的必要性。
*   **难度瓶颈**：增加变量数量（$n$）带来的搜索难度提升远大于增加电路深度（$C$）。

### 7. 优点与亮点
*   **创新建模**：将抽象的代数复杂性问题转化为可交互的 RL 游戏环境，具有高度的可验证性。
*   **结构化表示**：结合 GNN 处理图结构和 Transformer 处理序列决策，能够有效捕捉电路的拓扑特征。
*   **数据生成策略**：通过“Game-board”生成具有多条最优路径的目标，强制智能体学习通用的构建策略而非死记硬背。

### 8. 不足与局限
*   **规模受限**：实验仅限于极小规模的电路（$C \le 6$），尚未触及 VP vs. VNP 猜想中涉及的大规模复杂多项式。
*   **输入限制**：目前仅支持变量输入，未引入任意常数系数，这限制了其处理更广泛多项式家族的能力。
*   **训练稳定性**：在 $n=3, C=6$ 的复杂任务中，PPO+MCTS 出现了性能退化，显示出在大规模搜索空间下算法仍存在不稳定性。

（完）
