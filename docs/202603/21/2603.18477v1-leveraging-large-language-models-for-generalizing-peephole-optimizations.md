---
title: Leveraging Large Language Models for Generalizing Peephole Optimizations
title_zh: 利用大语言模型泛化窥孔优化
authors: "Chunhao Liao, Hongxu Xu, Xintong Zhou, Zhenyang Xu, Chengnian Sun"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18477v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大模型驱动的优化规则生成与泛化
tldr: 窥孔优化是现代编译器优化的核心，但将具体优化实例泛化为通用规则通常需要大量人工和领域知识。本文提出LPG框架，利用大语言模型（LLM）的语义抽象和推理能力，结合形式化验证确保规则的正确性与收益。LPG通过闭环工作流实现符号常量、结构及约束的自动泛化，在LLVM真实案例评估中表现优异，显著超越了现有的程序合成工具Hydra，为自动化编译器优化提供了高效的新途径。
selection_source: fresh_fetch
motivation: 现有的窥孔优化泛化方法受限于搜索空间爆炸、泛化能力不足以及对多样化指令域支持有限的问题。
method: LPG框架利用LLM进行多维度的规则泛化，并集成语法校验、语义验证和收益检查的闭环反馈机制以确保规则质量。
result: 35的优势大幅领先现有工具Hydra。
conclusion: 结合LLM的探索性推理与形式化分析的严谨性，可以有效解决编译器优化规则自动泛化中的复杂性挑战。
---

## 摘要
窥孔优化是现代优化编译器的核心组件。它将特定的指令重写为语义等价但更高效的形式。在实践中，创建新的窥孔优化通常从具体的优化实例开始，并需要将其提升为更通用的重写规则，以匹配更广泛的指令模式。这一泛化步骤对优化效果至关重要，但也非常困难：生成既正确又足够通用的规则通常需要大量的体力劳动和领域专业知识。现有方法（如 Hydra）尝试通过程序合成来自动化这一任务，但它们的泛化能力往往受到搜索空间爆炸、泛化不足以及对多样化指令域支持有限的限制。我们提出了 LPG（大语言模型辅助的窥孔优化泛化），这是一个利用大语言模型（LLM）来泛化窥孔优化的框架。LPG 的设计动机源于以下观察：LLM 擅长语义抽象和探索性推理，而形式化分析对于确保生成的规则是可靠且有收益的至关重要。基于此，LPG 采用了一种闭环工作流，整合了 LLM 驱动的符号常量泛化、结构泛化、约束松弛以及位宽/精度泛化，并结合了来自语法验证、语义验证和收益检查的反馈。我们在源自 LLVM 生态系统的真实世界窥孔优化问题上评估了 LPG。总体而言，LPG 成功泛化了 102 个优化中的 90 个。在与 Hydra 直接可比的整数子集上，LPG 泛化了 81 个优化中的 74 个，而 Hydra 仅泛化了 35 个。

## Abstract
Peephole optimizations are a core component of modern optimizing compilers. It rewrites specific instruction into semantically equivalent but more efficient forms. In practice, creating a new peephole optimization often starts from a concrete optimization instance and requires lifting it into a more general rewrite rule that matches a wider range of instruction patterns. This generalization step is critical to optimization effectiveness, but it is also difficult: producing rules that are both correct and sufficiently general typically demands substantial manual effort and domain expertise. Existing approaches such as Hydra attempt to automate this task with program synthesis, but their generalization capability is often limited by search-space explosion, under-generalization, and restricted support for diverse instruction domains.   We present LPG, large language model aided peephole optimization generalization, a framework that uses large language models (LLMs) to generalize peephole optimizations. The design of LPG is motivated by the observation that LLMs are effective at semantic abstraction and exploratory reasoning, while formal analyses are necessary to ensure that generated rules are sound and profitable. Based on this observation, LPG adopts a closed-loop workflow that integrates LLM-driven symbolic constant generalization, structural generalization, constraint relaxation, and bitwidth/precision generalization with feedback from syntactic validation, semantic verification, and profitability checking.   We evaluate LPG on real-world peephole optimization issues drawn from the LLVM ecosystem. Overall, LPG successfully generalizes 90 out of 102 optimizations. On the integer-focused subset that is directly comparable to Hydra, LPG generalizes 74 out of 81 optimizations, whereas Hydra generalizes 35.

---

## 论文详细总结（自动生成）

### 论文总结：利用大语言模型泛化窥孔优化 (LPG)

#### 1. 核心问题与研究动机
*   **核心问题**：窥孔优化（Peephole Optimization）是编译器的核心，但将一个具体的优化实例（Concrete Instance）泛化为通用的重写规则（General Rule）极其困难。
*   **研究动机**：
    *   **人工泛化难**：需要深厚的领域知识，且极易引入语义不一致的 Bug。
    *   **现有工具局限**：如 Hydra 等基于程序合成（Program Synthesis）的工具，面临搜索空间爆炸、泛化程度不足（Under-generalization）以及仅支持整数运算（不支持浮点和内存指令）等问题。
*   **整体含义**：本文提出 **LPG** 框架，旨在结合 LLM 的语义推理能力与形式化验证的严谨性，实现跨指令域（整数、浮点、内存）的自动化优化规则泛化。

#### 2. 方法论
LPG 采用**闭环工作流**，将 LLM 作为“提议者”，将形式化工具作为“验证者”。核心步骤包括：
*   **核心思想**：利用 LLM 推断程序意图并提出泛化假设，通过 Alive2 进行语义等价性验证，通过 llvm-mca 进行性能收益评估。
*   **四个泛化阶段**：
    1.  **符号常量泛化**：将具体数值（如 63, 8）替换为符号变量（$C_1, C_2$），并由 LLM 推导必要的先决条件（Precondition）。
    2.  **结构泛化**：抽象固定的表达式结构，用受限的符号值替换子表达式，消除对特定语法树的依赖。
    3.  **约束松弛**：通过 LLM 尝试移除冗余预设、削弱约束条件或移除不必要的指令标志（如 `nsw`），以扩大规则适用范围。
    4.  **位宽/精度泛化**：将规则从特定位宽（如 i32）扩展到任意位宽或不同的浮点精度。
*   **关键技术细节**：
    *   **递归位宽/精度缩减**：当验证工具（Alive2）因状态空间过大超时时，LPG 会自动降低位宽进行验证。
    *   **严格弱化测试**：通过双向细化测试（Refinement Test）确保松弛后的约束确实比原约束更宽松。

#### 3. 实验设计
*   **数据集**：
    *   **HydraDataset**：包含 40 个来自 Hydra 原始论文的整数优化案例。
    *   **LPGDataset**：作者新收集的 62 个真实 LLVM GitHub Issue 案例（包含整数、浮点和内存指令），且报告时间晚于模型知识截止日期，以防数据泄露。
*   **Benchmark 与对比方法**：
    *   主要对比对象是 **Hydra**（目前最先进的基于合成的泛化工具）。
    *   评估指标包括：泛化成功率、规则的通用性（是否比对比方法生成的规则更广）。

#### 4. 资源与算力
*   **硬件环境**：使用了一台配备 Intel Xeon Gold 5217 CPU (32 核) 和 376 GB RAM 的服务器。
*   **模型使用**：
    *   默认模型为 **Gemini 1.5 Pro**。
    *   对比实验中使用了 **Qwen 2.5** 系列（30B 和 235B 的 Instruct 及 Thinking 版本）。
*   **算力说明**：文中未提及具体的 GPU 训练时长，因为 LPG 主要是调用现成 LLM 的 API 或进行推理，核心计算开销在于 SMT 求解（Alive2 验证过程）。

#### 5. 实验数量与充分性
*   **实验规模**：共测试了 102 个真实案例，涵盖了整数（81 个）、浮点（16 个）和内存（5 个）三大领域。
*   **消融实验**：详细分析了四个泛化阶段各自的贡献度，并对比了不同规模和推理能力的 LLM 对结果的影响。
*   **客观性与公平性**：
    *   通过使用模型知识截止日期之后的 Issue 确保了评估的真实性。
    *   对 Hydra 进行了公平对比，使用了其公开实现并在相同数据集上运行。
    *   引入了 `llvm-mca` 确保生成的规则不仅语义正确，而且在硬件上确实有性能收益。

#### 6. 主要结论与发现
*   **性能卓越**：LPG 成功泛化了 90/102 个案例。在整数领域，LPG 泛化了 74 个，远超 Hydra 的 35 个。
*   **更强的通用性**：在双方都成功的案例中，LPG 在 22 个案例中生成了比 Hydra 更通用的规则。
*   **跨领域能力**：LPG 证明了 LLM 可以处理复杂的浮点语义（如处理 NaN、符号零等），这是传统合成工具难以企及的。
*   **推理模型优势**：带有“Thinking”过程的模型（如 Qwen-235B-Thinking）在处理复杂逻辑约束时显著优于普通指令微调模型。

#### 7. 优点
*   **突破领域限制**：首次将自动化泛化扩展到浮点和内存指令域。
*   **闭环验证**：结合了形式化验证（Alive2）和性能模拟（llvm-mca），保证了生成规则的工业级可靠性。
*   **语义理解**：相比于 Hydra 的贪婪搜索，LPG 能理解“PowerOfTwo”或“位掩码”等高层语义，从而发现更深层的泛化规律。

#### 8. 不足与局限
*   **浮点边缘案例**：在极少数涉及 IEEE-754 极细微语义（如特定的舍入模式或复杂的浮点内在函数组合）时，LPG 仍可能失败。
*   **验证工具依赖**：LPG 的上限受限于底层验证工具（如 Alive2）的能力，如果验证器不支持某些指令，LPG 也无法确认规则的正确性。
*   **随机性风险**：尽管有闭环校验，但 LLM 本身的随机性可能导致在不同运行中生成的规则质量存在波动。

（完）
