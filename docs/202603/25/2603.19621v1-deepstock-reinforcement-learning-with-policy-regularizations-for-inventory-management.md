---
title: "DeepStock: Reinforcement Learning with Policy Regularizations for Inventory Management"
title_zh: DeepStock：基于策略正则化的强化学习库存管理
authors: "Yaqi Xie, Xinru Hao, Jiaxi Liu, Will Ma, Linwei Xin, Lei Cao, Yidong Zhang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19621v1"
tags: ["query:nco"]
score: 8.0
evidence: 带有策略正则化的深度强化学习用于库存管理
tldr: 本文针对深度强化学习（DRL）在库存管理中对超参数高度敏感的问题，提出了DeepStock框架。该框架通过引入基于经典库存理论（如“基准库存”）的策略正则化，显著加速了超参数调优并提升了模型性能。该方法已在阿里巴巴天猫平台全面部署，实验证明正则化能有效改善多种DRL算法在复杂库存场景下的表现。
selection_source: fresh_fetch
motivation: 深度强化学习在库存管理应用中常因超参数敏感性高而难以稳定训练和优化。
method: 在DRL训练过程中引入基于经典库存管理概念（如Base Stock）的策略正则化项。
result: 该方法在天猫平台实现了全量部署，并显著提升了超参数调优效率和最终库存管理性能。
conclusion: 将经典库存理论作为正则化约束能有效增强DRL算法在实际供应链场景中的实用性和鲁棒性。
---

## 摘要
深度强化学习（DRL）为训练能够利用大数据和计算能力的库存策略提供了一种通用方法。然而，现成的 DRL 实现效果参差不齐，往往受到训练过程中超参数高度敏感性的困扰。在本文中，我们展示了通过引入基于“基准库存”（Base Stock）等经典库存概念的策略正则化，可以显著加速超参数调优，并提升多种 DRL 方法的最终性能。我们报告了在阿里巴巴电商平台天猫上 100% 部署带有策略正则化的 DRL 的详细细节。此外，我们还进行了广泛的合成实验，结果表明策略正则化重塑了关于哪种 DRL 方法最适合库存管理的定论。

## Abstract
Deep Reinforcement Learning (DRL) provides a general-purpose methodology for training inventory policies that can leverage big data and compute. However, off-the-shelf implementations of DRL have seen mixed success, often plagued by high sensitivity to the hyperparameters used during training. In this paper, we show that by imposing policy regularizations, grounded in classical inventory concepts such as "Base Stock", we can significantly accelerate hyperparameter tuning and improve the final performance of several DRL methods. We report details from a 100% deployment of DRL with policy regularizations on Alibaba's e-commerce platform, Tmall. We also include extensive synthetic experiments, which show that policy regularizations reshape the narrative on what is the best DRL method for inventory management.

---

## 论文详细总结（自动生成）

以下是对论文《DeepStock: Reinforcement Learning with Policy Regularizations for Inventory Management》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：库存管理是运筹学（OR）的核心问题。虽然深度强化学习（DRL）在处理高维上下文数据方面具有潜力，但在实际应用中面临两大挑战：一是**超参数敏感性极高**，导致调优耗时且难以收敛；二是**黑盒特性**，决策缺乏可解释性。
*   **核心问题**：如何利用经典的库存理论（如基准库存策略）来约束和引导 DRL 的学习过程，从而提高训练效率、性能和模型的可解释性。
*   **整体含义**：本文提出了 **DeepStock** 框架，通过引入“策略正则化”（Policy Regularizations），将人类直觉和 OR 理论融入神经网络结构，实现了在阿里巴巴天猫平台 100% 的全量部署。

### 2. 核心方法论
论文的核心思想不是改变 DRL 算法本身，而是通过**动作重映射（Action Remapping）**来约束策略网络的输出形式：
*   **基准库存正则化（Base Stock Regularization）**：
    *   公式：$\pi(I_t, x_t) = \max\{\mu_{Base}(I_t, x_t) - \text{tot}(I_t), 0\}$。
    *   细节：神经网络 $\mu_{Base}$ 不再直接预测下单量，而是预测“目标库存水平”。下单量等于目标水平减去当前总库存（含在途）。这保证了决策符合“缺多少补多少”的库存直觉。
*   **系数正则化（Coefficient Regularization）**：
    *   公式：$\pi(I_t, x_t) = \mu_{Coeff}(I_t, x_t)^\top \text{feat}(x_t)$。
    *   细节：网络输出一组系数，作用于预定义的特征（如历史需求、预测需求）。这确保了下单量与需求特征之间存在正向关联。
*   **组合正则化（Both）**：结合上述两者，网络学习如何根据上下文调整特征权重，并以基准库存的形式输出。
*   **底层算法**：支持 DDPG、PPO 和可微模拟器（DS）等多种 DRL 框架。

### 3. 实验设计
*   **数据集/场景**：
    1.  **合成数据**：包括独立分布（INDEP）、自回归过程（AR(1)）和 IID 分布，用于验证算法在不同需求模式下的收敛性。
    2.  **阿里巴巴离线数据**：包含 50,000 条 SKU 轨迹（2024 年 7-9 月），特征维度达 190 维。
    3.  **真实世界部署**：在天猫 B2C 平台覆盖超过 100 万个“SKU-仓库”组合。
*   **Benchmark（基准）**：
    *   无正则化的原生 DRL（DDPG, PPO）。
    *   可微模拟器（DS，亚马逊等公司采用的方法）。
    *   阿里巴巴原有的“预测后优化”（Predict-then-optimize）启发式算法。
*   **对比指标**：缺货率（SR）、库存周转时间（TT）以及标准的欠载/过载损失（$l_{bh}$）。

### 4. 资源与算力
*   **合成实验**：使用单张 NVIDIA RTX A4500 GPU，12 vCPUs，62 GB RAM。
*   **离线实验（阿里巴巴规模）**：
    *   **训练时长**：在阿里巴巴内部环境下，单个超参数试验（5万 SKU × 90天 × 190维特征）大约需要 **24 小时**。
    *   **调优过程**：每个方法进行了 10 次连续的超参数搜索试验。
*   **部署规模**：管理超过 100 万个 SKU-仓库对，支持全球范围内的库存决策。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单的合成环境到极其复杂的工业级场景。合成实验中进行了 50 次超参数配置搜索，并对前 5 名进行了 5 个随机种子的重复实验。
*   **充分性**：实验设计非常充分。不仅对比了不同 DRL 算法，还通过消融实验验证了正则化的有效性。
*   **客观性**：通过“双重差分法”（DiD）和反事实分析评估真实部署效果，证明了在降低周转时间的同时未增加缺货率，具有很强的说服力。

### 6. 主要结论与发现
1.  **正则化加速调优**：策略正则化显著降低了 DRL 对超参数的敏感性，在有限的调优次数下能获得远优于原生 DRL 的效果。
2.  **传统 DRL 逆袭 DS**：在引入正则化后，传统的 DDPG 和 PPO 表现优于目前流行的可微模拟器（DS），因为 DS 容易在小样本下过拟合且缺乏跨时序学习能力。
3.  **统一模型（Meta-learning）**：正则化允许训练一个“统一策略”覆盖所有 SKU（从爆款到长尾），无需像以前那样进行 SKU 分组训练。
4.  **商业价值**：在天猫全量部署后，库存周转时间显著下降（部分类别下降达 4 天），预计每年节省数千万人民币的资金成本。

### 7. 优点
*   **理论与实践结合**：成功将 OR 领域的经典结构（Base Stock）转化为深度学习的归纳偏置（Inductive Bias）。
*   **工业级落地**：展示了 DRL 在超大规模电商环境下的全量部署细节，具有极高的工程参考价值。
*   **鲁棒性**：通过正则化解决了 DRL 难以在生产环境中稳定训练的痛点。

### 8. 不足与局限
*   **细节缺失**：受限于企业机密，部分实现细节（如具体的奖励权重、特征工程细节）未完全披露。
*   **模型假设**：训练过程基于简化的库存动力学模型（如确定性提前期），虽然实测鲁棒，但在极端不确定环境下的表现仍需更多探讨。
*   **计算成本**：尽管正则化减少了调优次数，但单次大规模 DRL 训练的绝对耗时（24小时/次）对于某些实时性要求极高的场景可能仍然偏重。

（完）
