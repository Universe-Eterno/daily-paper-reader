---
title: A Learning Method with Gap-Aware Generation for Heterogeneous DAG Scheduling
title_zh: 一种面向异构 DAG 调度的具有间隙感知生成能力的学习方法
authors: "Ruisong Zhou, Haijun Zou, Li Zhou, Chumin Sun, Zaiwen Wen"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23249v1"
tags: ["query:nco"]
score: 9.0
evidence: 用于DAG调度的端到端强化学习框架
tldr: 针对异构环境下的DAG调度难题，本文提出WeCAN强化学习框架。该框架采用单次前向传播设计，利用加权交叉注意力机制建模任务与资源池的兼容性，并针对传统列表调度中存在的生成间隙问题，通过序空间分析提出了一种带跳过规则的高效生成方法。实验表明，WeCAN在保持与启发式算法相当的推理速度下，显著提升了调度性能。
selection_source: fresh_fetch
motivation: 异构资源环境下的DAG调度需要兼顾环境适应性与生成速度，且现有生成机制常因搜索空间受限而存在性能间隙。
method: 提出WeCAN框架，采用加权交叉注意力编码器和单次前向传播设计，并引入基于序空间分析的跳过扩展生成规则以消除调度间隙。
result: 在计算图和TPC-H基准测试中，WeCAN的完工时间优于现有强基准，且推理速度远快于多轮神经调度器。
conclusion: 该研究通过理论分析与架构创新，证明了在不增加计算开销的前提下，通过优化生成机制可有效提升异构DAG调度的性能。
---

## 摘要
在异构环境中对有向无环图（DAG）进行高效调度，由于资源容量和依赖关系的限制，具有很大的挑战性。在实践中，对具有不同资源池和任务类型的环境的适应性需求，以及快速生成调度方案的要求，使这些挑战变得更加复杂。我们提出了 WeCAN，这是一个用于异构 DAG 调度的端到端强化学习框架，它解决了任务-资源池兼容性系数以及生成引起的优化间隙问题。它采用两阶段单次通过设计：单次前向传播产生任务-资源池评分和全局参数，随后通过生成映射构建调度方案，无需重复的网络调用。其加权交叉注意力编码器对受兼容性系数门控的任务-资源池交互进行建模，并且对环境波动具有规模无关性。此外，广泛使用的列表调度映射可能会因可达性受限而产生生成引起的优化间隙。我们引入了一种序空间分析方法，通过可行调度顺序来表征生成映射的可达集，解释了生成引起间隙背后的机制，并给出了消除间隙的充分条件。在这些条件的指导下，我们设计了一种带有解析参数化递减跳过规则的跳过扩展实现，在保持单次通过效率的同时扩大了可达顺序集。在计算图和真实世界 TPC-H DAG 上的实验表明，与强基准相比，该方法改进了完工时间，推理时间与经典启发式算法相当，且快于多轮神经调度器。

## Abstract
Efficient scheduling of directed acyclic graphs (DAGs) in heterogeneous environments is challenging due to resource capacities and dependencies. In practice, the need for adaptability across environments with varying resource pools and task types, alongside rapid schedule generation, complicates these challenges. We propose WeCAN, an end-to-end reinforcement learning framework for heterogeneous DAG scheduling that addresses task--pool compatibility coefficients and generation-induced optimality gaps. It adopts a two-stage single-pass design: a single forward pass produces task--pool scores and global parameters, followed by a generation map that constructs schedules without repeated network calls. Its weighted cross-attention encoder models task--pool interactions gated by compatibility coefficients, and is size-agnostic to environment fluctuations. Moreover, widely used list-scheduling maps can incur generation-induced optimality gaps from restricted reachability. We introduce an order-space analysis that characterizes the reachable set of generation maps via feasible schedule orders, explains the mechanism behind generation-induced gaps, and yields sufficient conditions for gap elimination. Guided by these conditions, we design a skip-extended realization with an analytically parameterized decreasing skip rule, which enlarges the reachable order set while preserving single-pass efficiency. Experiments on computation graphs and real-world TPC-H DAGs demonstrate improved makespan over strong baselines, with inference time comparable to classical heuristics and faster than multi-round neural schedulers.

---

## 论文详细总结（自动生成）

### 论文总结：用于异构 DAG 调度的间隙感知生成学习方法 (WeCAN)

#### 1. 核心问题与整体含义
本文针对**异构环境下的有向无环图 (DAG) 任务调度问题**展开研究。核心挑战在于：
*   **异构性复杂：** 不同资源池的容量不同，且任务与资源池之间存在“兼容性系数”（即同一任务在不同池中的执行效率不同）。
*   **效率与质量的权衡：** 现有的神经调度器要么采用多轮推理（速度慢），要么采用单次通过（Single-pass）配合列表调度（List Scheduling），但后者由于搜索空间受限，存在无法触达最优解的“生成诱导优化间隙”（Generation-induced optimality gap）。
*   **环境适应性：** 调度器需要能够处理任务数量和资源池规模波动的动态环境。

#### 2. 方法论
论文提出了 **WeCAN** 框架，其核心思想是将调度分为“单次神经网络编码”和“间隙感知调度生成”两个阶段：
*   **加权交叉注意力编码器 (WeCA)：** 专门设计用于建模任务与资源池的交互。通过将兼容性系数作为门控机制引入注意力机制，使模型能够识别最适合任务的资源池，且该结构对资源池数量不敏感（Size-agnostic）。
*   **最长路径导向图神经网络 (LDDGNN)：** 利用 DAG 中的最长有向距离（LDD）来捕捉任务间的长程依赖关系，增强结构特征提取。
*   **序空间分析 (Order-space Analysis)：** 论文从理论上证明了传统列表调度无法覆盖所有可行的调度顺序。为此提出了 **OCO (Order-covering optimal)** 准则，作为消除优化间隙的充分条件。
*   **跳过扩展生成算法 (Skip-extended Generation)：** 在生成阶段引入“主动跳过（Skip）”动作（即在有就绪任务时选择等待，以预留资源给更重要的后续任务）。为了保持单次通过的效率，设计了一个**解析参数化的递减跳过评分规则**，无需在调度过程中重复调用神经网络。

#### 3. 实验设计
*   **数据集：**
    *   **TPC-H 数据集：** 模拟真实工业查询的 DAG，涵盖 30 到 200 个 DAG 组（任务数从 275 到 2000+ 不等）。
    *   **计算图数据集：** 包含机器学习编译器中的典型结构，如层级图（Layer Graphs）、Erdős-Rényi 图和随机块模型（SBM）。
*   **Benchmark (基准方法)：**
    *   **启发式算法：** CP (关键路径), SFT, MOPNR, Tetris, HEFT, PEFT, IPPTS。
    *   **神经调度算法：** PPO-BiHyb (双层强化学习), One-Shot (单次通过基准), ScheduleNet, HGN-Two-Stage。
*   **对比维度：** 完工时间 (Makespan)、推理时间 (Inference Time)、泛化能力（跨规模、跨环境波动）。

#### 4. 资源与算力
*   **硬件设施：** 使用了内部集群，配备 **NVIDIA Tesla A100 (80 GB)** GPU，以及 Intel Xeon Silver 4310 CPU (12核) 和 192GB 内存。
*   **训练时长：** 论文详细列出了不同任务规模的训练时间。例如，TPC-H-30 约需 6.6 小时，TPC-H-100 约需 26 小时，计算图任务约需 13-19 小时。
*   **推理效率：** WeCAN 在 GPU 上的推理时间极短（如 TPC-H-30 仅需 0.15s），且在纯 CPU 环境下依然保持高效。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **主实验：** 在多个标准基准上验证了 Makespan 的领先地位。
*   **消融实验：** 分别验证了 WeCA 架构、LDDGNN 模块以及“跳过动作”设计的有效性。
*   **重负载测试：** 专门构建了包含“重型任务”的场景，证明了跳过机制在消除优化间隙方面的关键作用。
*   **泛化性实验：** 测试了模型在资源池数量增加、容量变化、任务类型改变等 8 种环境波动下的鲁棒性。
*   **公平性：** 对比方法均经过了参数对齐，且报告了多次运行的均值和标准差。

#### 6. 主要结论与发现
*   **性能卓越：** WeCAN 在 TPC-H 上比最强启发式算法提升了约 11.7% 的性能，比现有神经调度器提升了约 7.3%。
*   **速度优势：** 推理速度与经典启发式算法相当，比多轮推理的神经调度器快 10-100 倍。
*   **理论验证：** 证明了“递减跳过评分”规则在不增加计算开销的前提下，能有效消除列表调度的结构性缺陷。
*   **泛化能力强：** 在小规模数据上训练的模型可以直接应用于大规模任务（如 2000 节点），且性能依然优于基准。

#### 7. 优点
*   **理论深度：** 引入序空间几何分析，为调度算法的“可达性”提供了严谨的数学解释。
*   **架构创新：** WeCA 解决了异构兼容性编码的难题，且保持了单次通过的高效性。
*   **实用性高：** 兼顾了高质量解和极速推理，非常适合需要实时响应的云数据中心调度场景。

#### 8. 不足与局限
*   **离线设定：** 目前主要针对离线调度（所有任务信息预先已知），对于任务动态到达的在线（Online）场景讨论较少。
*   **通信开销：** 实验中未深入探讨异构节点间的通信延迟对调度的影响（虽然模型框架理论上可以扩展）。
*   **随机性依赖：** 在 Sampling 模式下需要一定数量的采样（如 64 或 256 次）才能达到最优性能，这在极度严苛的实时场景下可能存在权衡。

（完）
