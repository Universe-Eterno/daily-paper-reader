---
title: "L2O-CCG: Adversarial Learning with Set Generalization for Adaptive Robust Optimization"
title_zh: L2O-CCG：面向自适应鲁棒优化的具有集合泛化能力的对抗学习
authors: "Zhiyi Zhou, Ján Drgoňa, Yury Dvorkin"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22548v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 针对自适应鲁棒优化子问题的学习优化方法
tldr: 针对两阶段自适应鲁棒优化（ARO）中对抗子问题计算效率低且泛化性差的挑战，本文提出L2O-CCG框架。该框架将学习优化（L2O）引入约束与列生成（CCG）算法，通过神经网络近似追索值函数，并利用预训练的近端梯度映射实现跨不确定性集合几何形状的泛化。研究不仅提供了分布外收敛界限，还在建筑HVAC管理任务中验证了其高效性和鲁棒性。
selection_source: fresh_fetch
motivation: 传统的ARO对抗子问题在处理非凹追索函数和多变的不确定性集合时存在计算瓶颈且缺乏泛化能力。
method: 提出一种双层学习框架，结合神经网络近似值函数与预训练的近端梯度步长映射，实现无需重新训练的跨集合泛化。
result: 建立了不确定性集合偏移下的分布外收敛界限，并在HVAC管理任务中展示了优于传统求解器的性能。
conclusion: L2O-CCG通过结合深度学习与传统优化算法，显著提升了复杂鲁棒优化问题的求解效率和对环境变化的适应性。
---

## 摘要
两阶段自适应鲁棒优化（ARO）中的对抗子问题旨在识别最坏情况下的不确定性实现，是主要的计算瓶颈。当追索值函数（recourse value function）是非凹的且不确定性集合在不同应用场景间发生偏移时，这一困难会进一步加剧。现有方法通常利用值函数或不确定性集合几何结构的特定结构假设来重新表述该子问题，但当这些假设失效或部署时几何结构发生变化时，其性能会下降。为了应对这一挑战，我们提出了 L2O-CCG，这是一个双层框架，能够将结构感知的对抗求解器集成到约束与列生成（CCG）算法中。作为一种实例化，我们开发了一种可泛化的对抗学习方法，该方法使用学习到的近端梯度优化器取代了基于求解器的对抗搜索，该优化器无需重新训练即可在不同的不确定性集合几何结构间进行泛化。其中，内层神经网络利用离线数据近似追索值函数，而外层预训练映射则为近端梯度方案生成与迭代相关的步长。我们还建立了不确定性集合参数偏移下的分布外（OOD）收敛界限，展示了学习优化器的轨迹偏差如何受不确定性集合偏移的限制。我们在建筑暖通空调（HVAC）管理任务上展示了 L2O-CCG 方法的性能。

## Abstract
The adversarial subproblem in two-stage adaptive robust optimization (ARO), which identifies the worst-case uncertainty realization, is a major computational bottleneck. This difficulty is exacerbated when the recourse value function is non-concave and the uncertainty set shifts across applications. Existing approaches typically exploit specific structural assumptions on the value function or the uncertainty set geometry to reformulate this subproblem, but degrade when these assumptions are violated or the geometry changes at deployment. To address this challenge, we propose L2O-CCG, a bi-level framework that enables the integration of structure-aware adversarial solvers within the constraint-and-column generation (CCG) algorithm. As one instantiation, we develop a generalizable adversarial learning method, which replaces solver-based adversarial search with a learned proximal gradient optimizer that can generalize across uncertainty set geometries without retraining. Here, an inner-level neural network approximates the recourse value function from offline data, while an outer-level pre-trained mapping generates iteration-dependent step sizes for a proximal gradient scheme. We also establish out-of-distribution convergence bounds under uncertainty set parameter shifts, showing how the trajectory deviation of the learned optimizer is bounded by the uncertainty set shift. We illustrate performance of the L2O-CCG method on a building HVAC management task.

---

## 论文详细总结（自动生成）

以下是对论文《L2O-CCG: Adversarial Learning with Set Generalization for Adaptive Robust Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在两阶段自适应鲁棒优化（ARO）中，寻找“最坏情况”的不确定性实现（即对抗子问题）是主要的计算瓶颈。当追索值函数（Recourse Value Function）非凹且不确定性集合（Uncertainty Set）的几何形状在部署时发生偏移（Distribution Shift）时，传统方法和现有的机器学习方法往往难以兼顾计算效率、全局最优性和泛化能力。
*   **研究动机**：现有的学习方法（如 Neur2RO）通常将神经网络嵌入 MILP 求解，计算开销随网络规模增长，且一旦不确定性集合的参数（如预算、协方差）改变，往往需要重新训练或重新构建模型。本文旨在开发一种既快又能跨几何形状泛化的对抗求解框架。

### 2. 方法论
*   **核心思想**：提出 **L2O-CCG** 框架，将“学习优化”（Learning-to-Optimize, L2O）引入传统的“约束与列生成”（CCG）算法中。通过解耦“步长搜索策略”与“集合约束处理”，实现对未知不确定性集合的泛化。
*   **关键技术细节**：
    *   **双层学习结构**：
        *   **内层（代理模型）**：使用双头神经网络（Set-Encoder 架构）分别近似追索问题的目标值（$\hat{Q}_{obj}$）和可行性（$\hat{Q}_{fea}$）。
        *   **外层（学习优化器）**：使用基于 LSTM 的学习优化器，通过算法展开（Algorithm Unrolling）生成迭代步长。
    *   **集合无关泛化（Set-Agnostic Generalization）**：学习优化器仅接收梯度特征，而不直接接收集合参数。集合约束通过**解析近端算子（Proximal Operator）**强制执行。这意味着同一套学习到的优化策略可以应用于盒子（Box）、多面体（Polyhedral）、椭圆（Ellipsoidal）甚至高斯混合模型（GMM）等不同形状的集合。
    *   **算法流程**：1. 主问题求解得到决策；2. 学习优化器在不确定性集合内进行多点随机初始化的梯度搜索，找到候选最坏场景；3. 精确求解器验证该场景并反馈给主问题，直至收敛。
*   **理论支撑**：建立了分布外（OOD）收敛界限，证明了优化轨迹的偏差受限于训练与测试集合间近端算子的差异。

### 3. 实验设计
*   **应用场景**：建筑暖通空调（HVAC）管理任务（鲁棒模型预测控制，MPC）。
*   **不确定性集合**：测试了四种几何形状：Box、Polyhedral、Ellipsoidal、GMM（非凸）。
*   **Benchmark 与对比方法**：
    *   **标准 CCG**：使用多点采样+精确 QP 评估作为基准（Gold Standard）。
    *   **Neur2RO**：基于神经网络 MILP 重构的最新学习方法。
*   **实验设置**：分为分布内（In-Distribution）性能评估和分布外（OOD）泛化评估（改变预算 $\Gamma$、形状参数 $\omega$ 或旋转协方差）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-10875H CPU，NVIDIA GeForce RTX 2060 GPU (6 GB VRAM)。
*   **软件环境**：Python 3.10, PyTorch 2.2, Gurobi 13.0。
*   **算力说明**：神经网络训练在 GPU 上完成，优化子问题在 CPU 上求解。文中强调了推理速度（比传统方法快 10-100 倍），但未明确给出具体的总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对四种不同的不确定性几何形状分别进行了实验，涵盖了从凸集到非凸集的多种情况。
*   **充分性**：实验不仅对比了求解质量（Verified Objective）和计算时间，还详细分析了 OOD 情况下的性能退化情况。
*   **客观性**：通过多点随机初始化的方式增强了 L2O 搜索的鲁棒性，并使用精确验证步骤确保了最终结果的可比性，实验设计较为公平。

### 6. 主要结论与发现
*   **计算加速**：L2O-CCG 相比传统 CCG 实现了 **10-100 倍** 的加速。
*   **泛化能力强**：在 OOD 场景下，L2O-CCG 的性能显著优于 Neur2RO。由于其解耦设计，即使不确定性集合发生旋转或缩放，优化器依然能保持较低的误差。
*   **求解质量**：在处理非凸（GMM）或复杂约束（椭圆）时，L2O-CCG 往往能比基于 MILP 的方法找到更好的局部最优解，且迭代次数更少。

### 7. 优点
*   **模块化与灵活性**：将 L2O 与近端算子结合，解决了深度学习模型在约束优化中难以处理变动约束的痛点。
*   **理论与实践结合**：不仅提供了高效的算法，还给出了严谨的 OOD 收敛性证明。
*   **即插即用**：无需针对新的集合几何形状重新训练模型，具有极高的工程实用价值。

### 8. 不足与局限
*   **局部最优风险**：作为一种基于梯度的启发式搜索，尽管有随机重启机制，但在极端复杂的非凸景观中仍可能陷入局部最优，导致 CCG 迭代次数增加。
*   **代理模型依赖**：算法性能高度依赖于内层神经网络对追索值函数的近似精度，若代理模型误差过大，会引导优化器走向错误的搜索方向。
*   **应用范围限制**：目前仅在 HVAC 这一特定物理系统上验证，对于更高维度或具有离散追索决策的 ARO 问题的适用性尚待进一步验证。

（完）
