---
title: Self-Supervised Graph Neural Networks for Optimal Substation Reconfiguration
title_zh: 用于最优变电站重构的自监督图神经网络
authors: "Antoine Martinez, Balthazar Donon, Louis Wehenkel, a Efthymios Karangelos"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23401v1"
tags: ["query:nco"]
score: 9.0
evidence: 用于变电站重构优化（混合整数线性规划）的图神经网络
tldr: "本研究针对电力系统中的最优变电站重构（OSR）问题，提出了一种基于自监督学习的图神经网络（GNN）方法。OSR旨在通过改变拓扑结构来缓解拥塞，但传统的混合整数线性规划（MILP）求解速度慢，难以满足实时决策需求。本文将OSR建模为摊销优化问题，利用GNN直接优化目标函数。在12变电站系统的测试中，该模型在推理速度大幅提升的同时，实现了10.2%的交换容量提升，接近MILP的15.2%，为电网实时调度提供了高效的新方案。"
selection_source: fresh_fetch
motivation: 传统的混合整数线性规划方法在解决最优变电站重构问题时计算耗时过长，无法满足电网实时决策的需求。
method: 提出一种自监督学习的图神经网络模型，将重构问题转化为摊销优化任务，通过直接优化目标函数来学习拓扑调整策略。
result: "在小型电网测试中，该模型实现了10.2%的平均交换容量提升，虽然略低于MILP的15.2%，但计算速度提升了数个数量级。"
conclusion: 自监督图神经网络在保持较高优化性能的同时显著降低了计算开销，是实现电网实时拓扑优化的有效途径。
---

## 摘要
改变输电系统拓扑是减少拥塞或增加交换容量的一种高效且低成本的手段。寻找变电站内最优开关状态的问题被称为最优变电站重构（OSR），可以建模为混合整数线性规划（MILP）。目前最先进的优化技术计算时间过长，使其在实时决策中不切实际。与此同时，深度学习提供了一个极具前景的视角，其计算时间大幅缩短，但代价是昂贵的训练阶段以及缺乏最优性保证。在这项工作中，我们将 OSR 建模为一个摊销优化（Amortized Optimization）问题，其中图神经网络（GNN）模型（我们的数据为图结构）以自监督的方式进行训练，以优化目标函数。我们将该方法应用于一个包含 12 个变电站的小规模系统中，旨在最大化两个区域之间的交换容量。训练完成后，与全连接配置相比，我们的 GNN 模型平均将交换容量提高了 10.2%，而传统的 MILP 求解器虽然实现了 15.2% 的平均提升，但其计算时间要高出几个数量级。

## Abstract
Changing the transmission system topology is an efficient and costless lever to reduce congestion or increase exchange capacities. The problem of finding the optimal switch states within substations is called Optimal Substation Reconfiguration (OSR), and may be framed as a Mixed Integer Linear Program (MILP). Current state-of-the-art optimization techniques come with prohibitive computing times, making them impractical for real-time decision-making. Meanwhile, deep learning offers a promising perspective with drastically smaller computing times, at the price of an expensive training phase and the absence of optimality guarantees. In this work, we frame OSR as an Amortized Optimization problem, where a Graph Neural Network (GNN) model -- our data being graphs -- is trained in a self-supervised way to improve the objective function. We apply our approach to the maximization of the exchange capacity between two areas of a small-scale 12-substations system. Once trained, our GNN model improves the exchange capacity by 10.2% on average compared to the all connected configuration, while a classical MILP solver reaches an average improvement of 15.2% with orders-of-magnitude larger computing times.

---

## 论文详细总结（自动生成）

### 论文总结：用于最优变电站重构的自监督图神经网络

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在满足实时性要求的前提下，找到电力系统中变电站内部开关的最优配置状态（即最优变电站重构，OSR），以最大化区域间的交换容量或缓解电网拥塞。
*   **研究背景**：
    *   传统的拓扑优化方法通常建模为混合整数线性规划（MILP），虽然精确但计算复杂度极高，无法满足实时决策需求。
    *   现有的深度学习方法多采用监督学习，依赖大量预先计算的最优解标签，而生成这些标签本身在计算上是不可行的。
    *   强化学习（RL）在处理高维动作空间时存在收敛困难和泛化性差的问题。
*   **整体含义**：本文提出一种无需标签的自监督学习框架，利用图神经网络（GNN）的强大表征能力，在极短时间内提供接近最优的拓扑决策方案。

#### 2. 方法论
*   **核心思想**：将 OSR 建模为**摊销优化（Amortized Optimization）**问题，通过自监督方式训练 GNN，使其直接输出能够最小化目标函数（或最大化交换容量）的开关状态评分。
*   **关键技术细节**：
    *   **H2MG 建模**：将电网视为“超异构多重图”（Hyper Heterogeneous Multi Graphs），能够处理变电站内部复杂的连接关系及动态变化的拓扑结构。
    *   **连续代理优化**：将离散的二进制开关变量 $y$ 松弛为连续的实值评分 $z$，通过玻尔兹曼分布和 KL 散度构建可微的代理目标函数。
    *   **梯度估计器**：为了解决直接蒙特卡洛（MC）采样导致的训练不稳定问题，提出了两种改进方案：
        1.  **过滤蒙特卡洛（Filtered MC）**：过滤掉得分过高的差决策，防止其干扰梯度方向。
        2.  **记忆表（Memory Table）**：记录每个样本在训练过程中发现的历史最佳决策，引导模型向其靠拢。
    *   **集成模型（Ensemble）**：结合上述两种估计器的优点，取两者中性能更好的决策作为最终输出。
    *   **H2MGNODE 架构**：利用神经常微分方程（NODE）在图上进行连续的信息传递，增强了模型对不同规模和结构电网的鲁棒性。

#### 3. 实验设计
*   **场景与数据集**：
    *   使用一个包含 **12 个变电站** 的测试系统（涉及两种不同类型的变电站结构）。
    *   生成了四个数据集：大训练集（850k 样本）、小训练集（10k）、验证集（100k）和测试集（10k）。通过随机注入噪声、改变热限制和断开线路来模拟多样的运行工况。
*   **Benchmark（基准）**：
    *   **All Closed**：所有开关均闭合的初始状态。
    *   **MILP Solver**：使用 XPRESS 求解器在限制条件下（最多开启 6 个开关，10 分钟时限）获得的最优解。
*   **对比方法**：
    *   **GNN Supervised**：在小规模数据集上进行的监督学习（模仿 MILP 求解器）。
    *   **GNN Filtered MC / Memory Table / Ensemble**：本文提出的三种自监督变体。

#### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA V100S GPU** 进行 GNN 的前向和后向计算，以及 **8 核 Intel Xeon Gold 6226R CPU** 用于样本探索和梯度估计。
*   **训练时长**：每个模型的训练耗时约 **7 到 10 天**（迭代 120k 至 180k 次）。
*   **推理速度**：训练完成后，GNN 处理一个 batch（8 个样本）仅需约 **100 毫秒**，而 MILP 求解器通常需要 10 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：针对 12 节点系统进行了详尽的测试，包括交换容量提升、开关开启数量分布、开关使用频率等多个维度的分析。
*   **充分性与客观性**：
    *   每个 GNN 模型都使用了 **3 个不同的随机种子** 进行重复实验，并给出了标准差，体现了实验的严谨性。
    *   对比了监督学习和传统优化算法，证明了自监督方法在无需标签的情况下能超越监督学习的效果。
    *   **局限性**：实验目前仅限于 12 节点的微型系统，尚未在大规模实际电网上展示其性能。

#### 6. 主要结论与发现
*   **性能表现**：集成模型（Ensemble）平均将交换容量提升了 **10.2%**，显著优于监督学习（5.55%），并达到了 MILP 求解器（15.2%）约三分之二的性能水平。
*   **效率优势**：GNN 的推理速度比 MILP 快了数个数量级，完全满足实时调度需求。
*   **行为特征**：自监督模型倾向于开启更少的开关（平均 1-2 个）来实现优化，表现出比 MILP（平均 6 个）更“节俭”的策略。

#### 7. 优点
*   **无需标签**：解决了电力系统优化中“最优解标签”获取成本极高的问题。
*   **拓扑感知**：采用 H2MGNODE 架构，能够自然地处理电网的异构性和拓扑多变性。
*   **实时性极佳**：将复杂的组合优化问题转化为毫秒级的神经网络推理。
*   **开源贡献**：提供了开源工具包 `EnerGNN`，便于社区复现和扩展。

#### 8. 不足与局限
*   **规模限制**：目前的验证仅在 12 节点系统上完成，大规模系统的组合爆炸问题可能对自监督探索效率提出更高挑战。
*   **训练成本**：虽然推理快，但训练过程（7-10 天）相对漫长且计算资源消耗较大。
*   **最优性差距**：与 MILP 相比仍有约 5% 的性能差距，对于追求极致经济效益的场景可能仍需改进。
*   **探索效率**：在更大规模的系统中，随机采样可能难以发现有效的拓扑动作，需要更高效的探索策略。

（完）
