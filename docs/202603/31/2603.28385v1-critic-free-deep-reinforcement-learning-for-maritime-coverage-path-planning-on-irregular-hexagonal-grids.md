---
title: Critic-Free Deep Reinforcement Learning for Maritime Coverage Path Planning on Irregular Hexagonal Grids
title_zh: 不规则六边形网格上海事覆盖路径规划的无评论员深度强化学习
authors: "Carlos S. Sepúlveda, Gonzalo A. Ruz"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28385v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 基于Transformer指针策略的神经组合优化任务
tldr: 针对复杂海域的覆盖路径规划（CPP）难题，本文提出一种基于六边形网格的深度强化学习框架。该方法将CPP建模为神经组合优化任务，利用Transformer指针策略和无评论员（Critic-free）的GRPO算法，有效解决了长时程规划中的价值估计不稳定问题。在合成海域测试中，该方法在路径长度、转向次数及成功率上均显著优于传统启发式算法，且推理速度极快，适用于实时部署。
selection_source: fresh_fetch
motivation: 传统覆盖路径规划方法在处理具有不规则海岸线和岛屿的复杂海域时，存在分解困难或计算成本过高的问题。
method: 采用基于Transformer的指针策略进行自回归路径构建，并引入组相对策略优化（GRPO）算法，通过样本间对比代替价值函数估计。
result: "在1000个未知环境中实现了99.0%的哈密顿成功率，路径长度缩短7%，转向次数减少24%，且单次推理时间低于50毫秒。"
conclusion: 该研究证明了无评论员强化学习在解决复杂几何约束下的海域覆盖规划问题上的高效性与实时应用潜力。
---

## 摘要
海事监视任务（如搜救和环境监测）依赖于在广阔且几何形状复杂的区域内高效分配传感资产。传统的覆盖路径规划（CPP）方法依赖于分解技术，这些技术在处理不规则海岸线、岛屿和禁区时面临挑战，或者需要针对每个实例进行计算成本高昂的重新规划。我们提出了一种深度强化学习（DRL）框架，用于解决不规则海域六边形网格表示上的 CPP 问题。与传统方法不同，我们将该问题建模为神经组合优化任务，其中基于 Transformer 的指针策略以自回归方式构建覆盖路径。为了克服长时程路由问题中价值估计的不稳定性，我们实现了一种无评论员（Critic-Free）的组相对策略优化（GRPO）方案。该方法通过对采样轨迹进行实例内比较来估计优势，而不是依赖于价值函数。在 1,000 个未见过的合成海事环境上的实验表明，训练后的策略实现了 99.0% 的哈密顿成功率，是最佳启发式方法（46.0%）的两倍以上，同时生成的路径比最接近的基准缩短了 7%，航向变化减少了 24%。所有三种推理模式（贪婪、随机采样和带有 2-opt 优化的采样）在笔记本电脑 GPU 上处理每个实例的时间均低于 50 毫秒，证实了实时机载部署的可行性。

## Abstract
Maritime surveillance missions, such as search and rescue and environmental monitoring, rely on the efficient allocation of sensing assets over vast and geometrically complex areas. Traditional Coverage Path Planning (CPP) approaches depend on decomposition techniques that struggle with irregular coastlines, islands, and exclusion zones, or require computationally expensive re-planning for every instance. We propose a Deep Reinforcement Learning (DRL) framework to solve CPP on hexagonal grid representations of irregular maritime areas. Unlike conventional methods, we formulate the problem as a neural combinatorial optimization task where a Transformer-based pointer policy autoregressively constructs coverage tours. To overcome the instability of value estimation in long-horizon routing problems, we implement a critic-free Group-Relative Policy Optimization (GRPO) scheme. This method estimates advantages through within-instance comparisons of sampled trajectories rather than relying on a value function. Experiments on 1,000 unseen synthetic maritime environments demonstrate that a trained policy achieves a 99.0% Hamiltonian success rate, more than double the best heuristic (46.0%), while producing paths 7% shorter and with 24% fewer heading changes than the closest baseline. All three inference modes (greedy, stochastic sampling, and sampling with 2-opt refinement) operate under 50~ms per instance on a laptop GPU, confirming feasibility for real-time on-board deployment.

---

## 论文详细总结（自动生成）

这篇论文针对复杂海域环境下的覆盖路径规划（CPP）问题，提出了一种结合神经组合优化（NCO）与强化学习的新型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：海事监视（如搜救、环境监测）需要在包含不规则海岸线、岛屿和禁区的复杂区域内规划高效的传感器覆盖路径。
*   **传统局限**：传统的“牛耕式”（Boustrophedon）分解方法在处理非凸多边形时会导致区域过度碎片化，增加无效航行和剧烈转向；而基于精确优化的方法（如MILP）计算成本高，且无法在不同任务间泛化。
*   **核心目标**：开发一种能够泛化到未知几何形状、兼顾路径长度与运动平滑性、且具备实时推理能力的自动化规划工具。

### 2. 方法论
*   **环境表示**：采用**不规则六边形网格**对海域进行离散化。相比正方形网格，六边形具有等距邻居特性和更低的各向异性，更符合传感器圆型覆盖范围的几何特性。
*   **核心架构**：
    *   **Transformer指针策略**：采用编码器-解码器结构。编码器提取节点几何特征和全局图信息；解码器通过注意力机制自回归地选择下一个访问节点。
    *   **动态动作掩码（Action Masking）**：在Softmax前应用掩码，强制排除已访问节点、非邻居节点及在未完成覆盖前排除返回基地的动作，确保路径的拓扑可行性。
*   **关键技术：无评论员的GRPO算法**：
    *   针对长时程路由问题中价值函数（Critic）估计不稳定的问题，引入了**组相对策略优化（Group-Relative Policy Optimization, GRPO）**。
    *   **核心思想**：不再学习价值函数，而是对同一个地图实例并行采样 $G$ 条轨迹，通过这组轨迹的相对收益来估计“优势”（Advantage），从而稳定训练梯度。
*   **早期死胡同检测（BFS）**：在每一步利用广度优先搜索检测当前位置是否还能到达所有未访问节点并返回基地。若检测到“死胡同”，立即终止并施加惩罚，解决了稀疏奖励下的信用分配难题。

### 3. 实验设计
*   **数据集**：合成生成了 10,000 个具有挑战性的海事环境（8,000 训练，1,000 验证，1,000 测试），涵盖凸多边形、狭长通道、多岛屿等形态。
*   **基准方法（Benchmarks）**：对比了 **13 种经典启发式算法**，分为六大类：
    1.  线性扫描类（Boustrophedon等）
    2.  交错扫描类
    3.  轮廓/螺旋类
    4.  生成树覆盖类（STC）
    5.  局部图搜索类（Warnsdorff算法）
    6.  空间填充曲线类（Morton Z-order）
*   **评估指标**：哈密顿成功率（HSR，即不重复访问且全覆盖）、覆盖完成率（CCR）、路径长度、转向次数、推理延迟。

### 4. 资源与算力
*   **硬件环境**：AMD Ryzen 9 5900HX CPU，32GB RAM，**NVIDIA GeForce RTX 3070 Laptop GPU (8GB)**。
*   **训练时长**：总计执行约 34 个 Epoch，总耗时约 **440 小时**。
*   **模型规模**：约 205 万个可训练参数。

### 5. 实验数量与充分性
*   **实验规模**：在 1,000 个完全未见的测试实例上进行了评估，样本量充足。
*   **对比公平性**：所有方法均在相同的六边形图结构和约束条件下运行。
*   **消融与模式对比**：对比了“贪婪解码”、“16样本并行采样（BoK16）”以及“采样+2-opt局部优化”三种推理模式，展示了性能随计算量增加的提升曲线。实验设计客观且覆盖了从可行性到质量的多个维度。

### 6. 主要结论与发现
*   **极高的成功率**：提出的 RL-BoK16+2opt 方法实现了 **99.0% 的哈密顿成功率**，而表现最好的启发式算法（Warnsdorff）仅为 46.0%。
*   **路径质量优异**：在相同覆盖任务下，RL 生成的路径比最强基准缩短了 **7.4%**，转向次数减少了 **24%**。
*   **实时性强**：尽管使用了复杂的 Transformer 模型，但在 GPU 加速下，单实例推理时间（含采样和优化）仅为 **32.2 毫秒**，远快于海事平台的决策周期。

### 7. 优点
*   **算法创新**：成功将 GRPO 引入路径规划领域，解决了传统 Actor-Critic 架构在复杂组合优化任务中难以收敛的问题。
*   **几何感知**：通过在奖励函数中加入连续转向惩罚，使模型学会了生成符合船舶/无人机动力学特性的平滑路径。
*   **鲁棒性**：模型展现了极强的泛化能力，能够处理训练集中未出现的极端复杂拓扑结构。

### 8. 不足与局限
*   **严格约束的脆弱性**：模型追求“哈密顿路径”（零重复访问），但在极少数极端复杂的瓶颈地形下，这种严格约束可能导致无解。实际应用中可能需要允许少量的重复访问以换取 100% 的覆盖保证。
*   **数据偏差风险**：实验完全基于合成数据集，尚未在真实海图（包含水深、洋流、动态障碍物等）上进行验证。
*   **算力需求**：虽然推理很快，但训练过程耗时较长（440小时），对于计算资源受限的团队来说门槛较高。

（完）
