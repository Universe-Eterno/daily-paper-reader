---
title: Evolutionary Discovery of Reinforcement Learning Algorithms via Large Language Models
title_zh: 通过大语言模型进化发现强化学习算法
authors: "Alkis Sygkounas, Amy Loutfi, Andreas Persson"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28416v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 利用大模型作为生成式变异算子发现强化学习算法
tldr: 本研究提出了一种利用大语言模型（LLM）作为进化变异算子的框架，旨在自动发现强化学习算法。通过在可执行的更新规则空间中进行搜索，并排除传统的Actor-Critic和时序差分等机制，该方法成功发现了具有竞争力的非标准学习规则。结合LLM驱动的超参数精炼阶段，所发现的算法在多个基准测试中表现优于或等同于SAC、PPO等经典算法，展示了自动化算法设计的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的强化学习算法依赖人工设计且结构固定，限制了对更高效、非标准学习机制的探索。
method: 采用基于LLM的进化框架REvolve，直接搜索可执行的更新规则，并利用LLM进行后期的超参数范围精炼。
result: 在多个Gymnasium基准测试中，自动发现的算法达到了与SAC、PPO、DQN和A2C等主流基准算法相当的性能。
conclusion: 该研究证明了利用LLM进行算法级进化搜索的可行性，能够有效发现新颖且高性能的强化学习训练程序。
---

## 摘要
强化学习算法由其学习更新规则定义，这些规则通常是人工设计且固定的。我们提出了一个进化框架，通过直接搜索实现完整训练过程的可执行更新规则来发现强化学习算法。该方法基于 REvolve，这是一个利用大语言模型作为生成式变异算子的进化系统，并将其从奖励函数发现扩展到算法发现。为了促进非标准学习规则的出现，搜索排除了诸如演员-评论家（actor-critic）结构、时序差分（temporal-difference）损失和价值自助法（value bootstrapping）等规范机制。由于强化学习算法对内部标量参数高度敏感，我们引入了一个进化后细化阶段，由大语言模型为每个进化出的更新规则提出可行的超参数范围。通过在多个 Gymnasium 基准测试上进行完整训练运行的端到端评估，所发现的算法相对于既有的基准算法（包括 SAC、PPO、DQN 和 A2C）取得了具有竞争力的性能。

## Abstract
Reinforcement learning algorithms are defined by their learning update rules, which are typically hand-designed and fixed. We present an evolutionary framework for discovering reinforcement learning algorithms by searching directly over executable update rules that implement complete training procedures. The approach builds on REvolve, an evolutionary system that uses large language models as generative variation operators, and extends it from reward-function discovery to algorithm discovery. To promote the emergence of nonstandard learning rules, the search excludes canonical mechanisms such as actor--critic structures, temporal-difference losses, and value bootstrapping. Because reinforcement learning algorithms are highly sensitive to internal scalar parameters, we introduce a post-evolution refinement stage in which a large language model proposes feasible hyperparameter ranges for each evolved update rule. Evaluated end-to-end by full training runs on multiple Gymnasium benchmarks, the discovered algorithms achieve competitive performance relative to established baselines, including SAC, PPO, DQN, and A2C.

---

## 论文详细总结（自动生成）

以下是对论文《Evolutionary Discovery of Reinforcement Learning Algorithms via Large Language Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的强化学习（RL）算法（如 PPO、SAC）其核心更新规则通常由人工设计且固定。虽然自动化设计已应用于架构搜索和超参数调优，但直接发现全新的、可执行的训练逻辑（更新规则）仍具挑战，因为该搜索空间是离散且高度敏感的程序结构。
*   **核心问题**：能否利用大语言模型（LLM）作为进化算法的变异算子，在不依赖传统 RL 机制（如时序差分 TD、Actor-Critic 结构）的前提下，自动发现高性能且新颖的强化学习算法？

### 2. 论文提出的方法论
*   **核心思想**：将 RL 算法发现建模为在可执行代码空间上的进化搜索。利用 LLM 生成、修改和组合代码片段，通过在多个环境上的实际训练表现作为适应度（Fitness）进行筛选。
*   **关键技术细节**：
    *   **REvolve 框架扩展**：将原本用于奖励函数发现的 REvolve 系统扩展到更新规则（Loss 函数及训练逻辑）的发现。
    *   **禁忌搜索约束**：显式禁止 LLM 使用 Actor-Critic、TD 误差和价值自助法（Value Bootstrapping），迫使系统探索非标准的学习机制。
    *   **变异算子**：
        *   **宏观变异（Macro-mutation）**：利用 LLM 重写算法的一个语义完整组件。
        *   **多样性感知交叉（Crossover）**：结合两个父代算法，并引入基于编辑距离（Levenshtein Distance）的惩罚项，防止种群多样性崩溃。
    *   **LLM-HPO（后进化超参数优化）**：进化结束后，利用 LLM 为选出的算法建议合理的超参数范围，并进行小规模网格/随机搜索以发挥算法最大潜力。

### 3. 实验设计
*   **实验场景**：使用了 Gymnasium 平台。
    *   **训练环境（用于计算适应度）**：CartPole-v1, MountainCar-v0, Acrobot-v1, LunarLander-v3, HalfCheetah-v5。
    *   **测试环境（用于评估泛化性）**：Walker2d, InvertedPendulum, Reacher, Swimmer, Pusher。
*   **对比基准（Benchmarks）**：PPO, A2C, DQN, SAC。
*   **评估流程**：每个候选算法在 5 个环境上分别运行 5 个随机种子，取训练过程中的最高评估回报作为得分，并进行归一化处理。

### 4. 资源与算力
*   **硬件设施**：使用了 **4 台 NVIDIA A100 (40GB)** GPU。
*   **训练时长**：单个进化代（Generation）大约需要 **30 小时** 的墙上时间（Wall-clock time）。
*   **总规模**：实验运行了 10 代，每代每个岛屿产生 24 个新候选算法。

### 5. 实验数量与充分性
*   **实验规模**：进行了多轮独立进化运行（使用 GPT-5.2 和 Claude 4.5 Opus 作为算子）。
*   **消融实验**：
    *   针对多样性权重 $\alpha$ 进行了消融，证明了结构正则化对搜索稳定性的必要性。
    *   针对发现的算法（如 CG-FPD）进行了“加入价值自助法”的消融，验证了其核心机制的独立性。
*   **客观性与公平性**：所有算法（包括基准和进化出的算法）均使用相同的 MLP 策略架构（256x256）和 Adam 优化器，确保性能差异仅源于更新规则本身。

### 6. 论文的主要结论与发现
*   **成功发现新算法**：发现了两种高性能非标准算法：
    *   **CG-FPD**：通过蒸馏来自学习到的潜空间动力学模型的短时域规划信号来训练策略。
    *   **DF-CWP-CP**：利用可微的世界模型规划，结合置信度加权和可控性先验进行优化。
*   **性能表现**：在排除传统 RL 核心机制的情况下，进化出的算法在多个任务上达到了与 PPO、SAC 等成熟算法相当甚至更优的水平。
*   **LLM 的作用**：LLM 能够理解复杂的代码逻辑并提出连贯的改进建议，是处理离散算法搜索空间的有效工具。

### 7. 优点
*   **突破性尝试**：打破了 RL 算法必须依赖 TD 或策略梯度的固有思维，证明了“规划蒸馏”等机制的有效性。
*   **端到端自动化**：从代码生成到超参数精炼实现了全流程自动化，减少了人工干预。
*   **多样性管理**：引入编辑距离来引导交叉算子，有效解决了进化算法中常见的种群同质化问题。

### 8. 不足与局限
*   **算力成本极高**：由于每个候选算法都需要完整的训练运行来评估，大规模搜索的成本令普通研究者难以负担。
*   **创新深度受限**：虽然禁忌了传统机制，但 LLM 生成的代码往往是现有模型预测控制（MPC）或蒸馏思想的重新组合，尚未发现完全颠覆性的数学原理。
*   **泛化风险**：虽然在 10 个环境上表现良好，但在更复杂、高维或稀疏奖励的任务中，这些非标准算法的稳定性仍需进一步验证。

（完）
