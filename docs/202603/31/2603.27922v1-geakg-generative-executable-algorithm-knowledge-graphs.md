---
title: "GEAKG: Generative Executable Algorithm Knowledge Graphs"
title_zh: GEAKG：生成式可执行算法知识图谱
authors: "Camilo Chacón Sartori, José H. García, Andrei Voicu Tomut, Christian Blum"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27922v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大模型合成的可执行算法知识图谱
tldr: 针对算法设计中过程性知识难以复用和表示的问题，本文提出生成式可执行算法知识图谱（GEAKG）。该框架将算法算子作为节点，通过大模型合成拓扑结构，并利用蚁群优化算法学习算子组合模式。GEAKG具有可执行性和跨领域迁移能力，在神经架构搜索和组合优化等任务中表现优异，实现了算法经验的显式表示与零样本迁移，为自动化算法设计提供了新路径。
selection_source: fresh_fetch
motivation: 传统的知识图谱主要处理陈述性知识，难以有效表示、学习和复用算法设计中的过程性知识。
method: 提出一种由大模型合成算子、蚁群算法优化组合模式的三层架构知识图谱，使算法步骤变为可执行的图遍历过程。
result: 在神经架构搜索和组合优化（如从旅行商问题迁移至调度问题）的实验中，该框架展示了强大的零样本跨领域迁移能力。
conclusion: GEAKG证明了算法专业知识可以被显式地表示为可执行的知识图谱，并能在不同领域间实现有效的学习与迁移。
---

## 摘要
在解决问题的算法背景下，过程性知识（即算法设计和算子组合的诀窍）在代码中保持隐性，在运行之间丢失，并且必须针对每个新领域重新设计。知识图谱（KG）已被证明在组织陈述性知识方面非常有效，但目前的知识图谱范式在将过程性知识表示为可执行、可学习的图结构方面支持有限。我们引入了“生成式可执行算法知识图谱”（GEAKG），这是一类知识图谱，其节点存储可执行算子，其边编码学习到的组合模式，且其遍历过程可生成解决方案。GEAKG 是生成式的（拓扑结构和算子由大语言模型合成）、可执行的（每个节点都是可运行的代码）且可迁移的（学习到的模式可以跨领域进行零样本泛化）。该框架在引擎层面是领域无关的：相同的三层架构和基于蚁群优化（ACO）的学习引擎可以跨领域实例化，并通过可插拔的本体（RoleSchema）进行参数化。两个案例研究（不共享任何特定领域的框架代码）为该框架假设提供了具体证据：(1) 在两个表格基准测试上的 70 个跨数据集迁移对中进行神经架构搜索；(2) 组合优化，其中在旅行商问题上学习到的知识可以零样本迁移到调度和分配领域。综上所述，结果表明算法专业知识可以作为可执行知识图谱被显式表示、学习和迁移。

## Abstract
In the context of algorithms for problem solving, procedural knowledge -- the know-how of algorithm design and operator composition -- remains implicit in code, lost between runs, and must be re-engineered for each new domain. Knowledge graphs (KGs) have proven effective for organizing declarative knowledge, yet current KG paradigms provide limited support for representing procedural knowledge as executable, learnable graph structures. We introduce \textit{Generative Executable Algorithm Knowledge Graphs} (GEAKG), a class of KGs whose nodes store executable operators, whose edges encode learned composition patterns, and whose traversal generates solutions. A GEAKG is \emph{generative} (topology and operators are synthesized by a Large Language Model), \emph{executable} (every node is runnable code), and \emph{transferable} (learned patterns generalize zero-shot across domains). The framework is domain-agnostic at the engine level: the same three-layer architecture and Ant Colony Optimization (ACO)-based learning engine can be instantiated across domains, parameterized by a pluggable ontology (\texttt{RoleSchema}). Two case studies -- sharing no domain-specific framework code -- provide concrete evidence for this framework hypothesis: (1)~Neural Architecture Search across 70 cross-dataset transfer pairs on two tabular benchmarks, and (2)~Combinatorial Optimization, where knowledge learned on the Traveling Salesman Problem transfers zero-shot to scheduling and assignment domains. Taken together, the results support that algorithmic expertise can be explicitly represented, learned, and transferred as executable knowledge graphs.

---

## 论文详细总结（自动生成）

### GEAKG：生成式可执行算法知识图谱论文总结

#### 1. 核心问题与整体含义
*   **研究动机**：传统的知识图谱（KG）主要存储“陈述性知识”（即实体间的关系），而算法设计中的“过程性知识”（即如何组合算子、何时探索或开发等诀窍）通常隐含在代码中，难以在不同领域间复用。
*   **核心问题**：如何将算法的过程性知识显式化、结构化，并使其具备可执行性和跨领域迁移能力？
*   **整体含义**：论文提出了 **GEAKG** 框架，通过大语言模型（LLM）合成算法拓扑和算子，利用蚁群优化（ACO）学习组合模式，将算法专家经验转化为可迁移的知识图谱快照。

#### 2. 方法论
*   **核心思想**：将算法执行过程建模为在知识图谱上的遍历。图的节点是抽象的“角色”（Role），边代表算子的转换逻辑，边权重（信息素）代表组合的有效性。
*   **三层架构**：
    *   **L0 层（拓扑层）**：由 LLM 根据领域本体（RoleSchema）生成算法的结构骨架（如：初始化 -> 局部搜索 -> 扰动）。
    *   **L1 层（算子层）**：由 LLM 为每个角色生成具体的可执行代码（遵循 AFO 原则，即始终从原始基准生成，减少变异漂移）。
    *   **L2 层（学习层）**：利用 ACO 在离线阶段通过多次运行学习边权重（信息素）和符号规则（如停滞阈值）。
*   **关键技术**：
    *   **RoleSchema（本体）**：定义了领域的抽象角色和转换规则，是实现“领域无关”引擎的关键。
    *   **符号执行器（Symbolic Executor）**：在线部署阶段，无需调用 LLM，直接根据 L2 层的权重和规则在图上进行符号化推理和执行。
    *   **领域绑定（Domain Binding）**：通过极简的接口（如 `evaluate`, `valid`）将抽象图与具体问题解耦。

#### 3. 实验设计
*   **场景一：神经架构搜索（NAS）**
    *   **数据集**：NAS-Bench-Graph（GNN 架构）、NAS-Bench-201（CNN 细胞）。
    *   **Benchmark**：70 个跨数据集迁移对（如从 Cora 学习到的知识迁移到 Photo 数据集）。
    *   **对比方法**：随机搜索（Random Search）、正则化进化算法（RegEvo）、贝叶斯优化（BO）。
*   **场景二：组合优化（Combinatorial Optimization）**
    *   **数据集**：TSP（旅行商问题，作为源领域）、JSSP（作业车间调度）、QAP（二次分配问题）。
    *   **对比方法**：LLaMEA（纯代码进化方法）、经典启发式算法（SPT, LPT, Gilmore-Lawler）、迭代局部搜索（ILS）。

#### 4. 资源与算力
*   **模型使用**：使用了 GPT-5.2 (gpt-5.2-0111)、GPT-4o-mini 以及本地运行的 Qwen2.5-14B（通过 Ollama）。
*   **算力细节**：
    *   论文未明确说明具体的 GPU 型号和数量，但强调了 **Qwen2.5-14B 可以在本地消费级硬件上运行**。
    *   **Token 预算**：离线构建阶段通常消耗 10k 到 50k tokens。
    *   **运行耗时**：在线部署阶段（符号执行）在 0.1s 到 1.8s 之间，且 **运行时 LLM token 消耗为零**。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   NAS 实验涵盖了 70 组迁移对，每组进行 10 次独立运行。
    *   组合优化实验在 TSP、JSSP、QAP 的数十个标准实例上进行了测试，每个实例运行 15 次。
*   **充分性与公平性**：
    *   进行了多模型梯队测试（从最强 API 到本地小模型），验证了框架的鲁棒性。
    *   包含消融实验，对比了“纯 GEAKG”与“嵌入代码进化算子的混合 GEAKG”。
    *   实验设计较为客观，通过统计显著性检验（Wilcoxon 符号秩检验）验证了结果。

#### 6. 主要结论与发现
*   **跨领域迁移有效性**：在 TSP 上学习到的搜索策略（L2 知识）可以零样本迁移到 JSSP 和 QAP，在大型实例上表现优于非迁移基准。
*   **NAS 性能**：在 70 个迁移对中，GEAKG 引导的搜索 100% 优于随机搜索，且在 89% 的情况下具有统计显著性。
*   **零成本部署**：一旦离线学习完成，在线执行不再依赖 LLM，极大地降低了推理成本和延迟。
*   **鲁棒性**：相比于直接生成完整代码的方法（如 LLaMEA），GEAKG 的图结构约束使其在面对较弱的 LLM（如 14B 模型）时仍能保持 100% 的代码有效性。

#### 7. 优点
*   **知识显性化**：将“算法诀窍”从隐性代码提取为可解释、可可视化的知识图谱。
*   **领域无关引擎**：通过 RoleSchema 实现了高度的通用性，同一套引擎可处理 NAS 和组合优化两个截然不同的领域。
*   **高效复用**：通过“快照（Snapshot）”机制，实现了算法经验的持久化和零样本迁移。
*   **符号与神经结合**：结合了 LLM 的生成能力和符号系统的可靠性/可解释性。

#### 8. 不足与局限
*   **本体设计依赖人工**：RoleSchema（角色定义和转换规则）目前仍需专家手动设计，耗时约数小时。
*   **离线学习限制**：目前的学习过程是离线的，在线执行阶段缺乏动态自适应能力。
*   **实验覆盖范围**：组合优化目前仅限于基于排列（Permutation）的表示，尚未扩展到连续空间或混合整数规划。
*   **性能上限**：虽然迁移效果好，但在特定领域内可能仍无法完全超越经过数十年人工精调的最尖端（SOTA）专用求解器。

（完）
