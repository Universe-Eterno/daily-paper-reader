---
title: "DSevolve: Enabling Real-Time Adaptive Scheduling on Dynamic Shop Floor with LLM-Evolved Heuristic Portfolios"
title_zh: DSevolve：利用大语言模型演化的启发式组合实现动态车间的实时自适应调度
authors: "Jin Huang, Jie Yang, XinLei Zhou, Qihao Liu, Liang Gao, Xinyu Li"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27628v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 大模型演化的启发式组合用于自适应调度
tldr: 针对动态制造环境中调度策略需实时调整的问题，本文提出DSevolve框架。该框架利用大语言模型离线演化出具有质量多样性的调度规则组合，并通过探针指纹机制和前瞻模拟在在线调度中实现秒级响应的自适应规则切换。在500个真实工业实例上的实验表明，该方法在性能上显著优于现有的自动启发式设计、遗传编程及深度强化学习方法，为智能车间调度提供了高效且可部署的方案。
selection_source: fresh_fetch
motivation: 现有的LLM驱动的自动启发式设计框架通常只演化出单一规则，难以应对机器故障和新订单到达等动态车间环境中的实时变化。
method: 提出DSevolve框架，通过多角色种子设定和拓扑感知算子构建多样化规则库，并结合探针指纹机制与快速前瞻模拟实现实时自适应调度。
result: 在500个动态柔性作业车间调度实例上的评估显示，DSevolve的性能超越了最先进的AHD框架、经典规则、遗传编程和深度强化学习。
conclusion: DSevolve通过结合LLM的离线演化能力与在线自适应选择机制，为复杂动态工业环境下的实时智能调度提供了一种实用且高效的解决方案。
---

## 摘要
在动态制造环境中，机器故障和新订单到达等干扰不断改变最优调度策略，使得自适应规则选择变得至关重要。现有的基于大语言模型（LLM）的自动启发式设计（AHD）框架倾向于演化出单一的精英规则，无法满足这种自适应需求。为解决这一问题，我们提出了 DSevolve，这是一个工业调度框架，它在离线状态下演化出高质量且多样化的调度规则组合，并在在线状态下以秒级响应时间进行自适应部署。多角色种子设定和拓扑感知进化算子产生了一个由 MAP-Elites 特征空间索引的行为多样化规则库。在每次干扰事件发生时，基于探测的指纹识别机制会表征当前车间状态，从离线知识库中检索高质量候选规则，并通过快速前瞻模拟选择最佳规则。在基于真实工业数据生成的 500 个动态柔性作业车间实例上的评估结果表明，DSevolve 优于最先进的 AHD 框架、经典调度规则、遗传规划和深度强化学习，为智能车间调度提供了一种实用且可部署的解决方案。

## Abstract
In dynamic manufacturing environments, disruptions such as machine breakdowns and new order arrivals continuously shift the optimal dispatching strategy, making adaptive rule selection essential. Existing LLM-powered Automatic Heuristic Design (AHD) frameworks evolve toward a single elite rule that cannot meet this adaptability demand. To address this, we present DSevolve, an industrial scheduling framework that evolves a quality-diverse portfolio of dispatching rules offline and adaptively deploys them online with second-level response time. Multi-persona seeding and topology-aware evolutionary operators produce a behaviorally diverse rule archive indexed by a MAP-Elites feature space. Upon each disruption event, a probe-based fingerprinting mechanism characterizes the current shop floor state, retrieves high-quality candidate rules from an offline knowledge base, and selects the best one via rapid look-ahead simulation. Evaluated on 500 dynamic flexible job shop instances derived from real industrial data, DSevolve outperforms state-of-the-art AHD frameworks, classical dispatching rules, genetic programming, and deep reinforcement learning, offering a practical and deployable solution for intelligent shop floor scheduling.

---

## 论文详细总结（自动生成）

以下是对论文《DSevolve: Enabling Real-Time Adaptive Scheduling on Dynamic Shop Floor with LLM-Evolved Heuristic Portfolios》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在现代制造业中，车间调度直接影响吞吐量和交付率。动态环境（如机器故障、新订单到达）要求调度系统具备秒级响应的自适应能力。
*   **核心问题**：
    *   **传统方法局限**：经典启发式规则（HDR）固定且通用性差；深度强化学习（DRL）泛化性弱且缺乏可解释性；遗传编程（GP）受限于预定义的算子集。
    *   **现有 LLM-AHD 缺陷**：目前基于大语言模型（LLM）的自动启发式设计（AHD）框架（如 FunSearch, EoH）通常演化出单一的“精英规则”，但在动态波动的生产状态下，没有单一规则能始终保持最优。
*   **研究动机**：开发一个能离线演化出“高质量且多样化”规则库，并能在在线干扰发生时实时检索并切换最优规则的框架。

### 2. 方法论：核心思想与关键技术
DSevolve 采用“离线演化多样化规则库”与“在线自适应检索部署”解耦的架构。
*   **特征空间构建**：定义了一个三维行为特征空间（Load Skewness 负载偏斜度、Waiting Ratio 等待比例、Diversity 行为多样性），利用 **MAP-Elites** 范式将规则库索引化。
*   **多角色种子设定 (Multi-Persona Seeding)**：使用 7 种正交的调度哲学（如极端贪婪、负载均衡、全局规划、期限追逐等）初始化 LLM 提示词，确保种群从第一代起就具备广泛的行为覆盖。
*   **拓扑感知演化算子**：
    *   **距离最大化交叉**：选择特征空间中距离最远的两个父代进行杂交，鼓励行为互补。
    *   **双轨变异**：结合“精英变异”（局部优化）与“对比变异”（向特征空间未开发区域推送），防止过早收敛。
*   **探针指纹机制 (Probe Fingerprinting)**：
    *   当动态事件发生时，利用轻量级的 SPT（最短加工时间）规则进行快速虚拟仿真。
    *   提取 6 维状态指纹（包含瞬时负载密度、柔性度以及仿真后的瓶颈严重程度、等待比例等）。
*   **在线检索与前瞻模拟**：基于指纹从知识库检索相似案例，提取候选规则集，通过极速前瞻模拟选出当前状态下 Makespan 最小的规则执行。

### 3. 实验设计
*   **数据集**：基于真实工业数据构建的 500 个动态柔性作业车间调度（DFJSSP）实例，涵盖机器故障和随机订单。分为简单（S1）、中等（S2）、困难（S3）三个等级。
*   **对比方法**：
    *   **AHD 框架**：EoH, ReEvo, HSEvo。
    *   **经典规则**：SPT, LPT, SRM, SSO, LSO。
    *   **学习/进化方法**：遗传规划（GP）、深度强化学习（DRL/PPO）。
*   **LLM 配置**：使用了 Qwen-Plus, DeepSeek-V3, GPT-4o-mini 三种模型进行验证。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和数量。
*   **训练/演化时长**：提到 LLM 驱动的演化是一个“昂贵的离线过程”，而在线部署要求“秒级响应”。在附录中提到 DRL 基准方法的训练限制在 5 小时内，但未详述 DSevolve 演化所需的总 CPU/GPU 小时数。

### 5. 实验数量与充分性
*   **实验规模**：使用了 500 个测试实例，规模较大，具有统计学意义。
*   **充分性**：
    *   **交叉验证**：在三种不同的 LLM 上运行，证明了框架的稳健性。
    *   **消融实验**：分别验证了“多角色种子”和“特征空间引导”的有效性，证明了每个组件对最终性能的贡献。
    *   **通用性测试**：将“探针检索机制”应用于其他 AHD 框架（如 EoH），均带来了性能提升，证明了该机制的普适价值。
*   **客观性**：对比了从经典规则到最先进的 AI 驱动方法，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能领先**：DSevolve 在所有难度场景下均显著优于现有的 AHD 框架、DRL 和 GP。在困难场景（S3）下，比最强基准 EoH 的平均完工时间进一步降低。
*   **多样性价值**：通过 MAP-Elites 维持的规则库比单一精英规则更能适应动态干扰。
*   **检索有效性**：基于探针指纹的实例自适应检索比静态选择（Top-k 规则）更有效，能精准匹配当前车间状态。

### 7. 优点（亮点）
*   **质量-多样性平衡**：首次将 MAP-Elites 引入 LLM 启发式演化，解决了 LLM 容易陷入局部最优的问题。
*   **工业实用性**：生成的规则是可读的代码，具有高可解释性，且在线检索和模拟在秒级完成，符合实际工厂部署需求。
*   **解耦设计**：离线演化与在线检索的解耦，使得系统可以不断通过离线计算积累知识，而不影响在线生产。

### 8. 不足与局限
*   **特征定义依赖人工**：行为特征空间（3D）和指纹特征（6D）仍依赖专家经验定义，可能无法完全捕捉所有复杂的调度特征。
*   **计算开销**：离线构建知识库时，需要对所有存档规则在校准实例上进行评估，计算量随规则库规模线性增长。
*   **目标单一**：目前主要针对 Makespan（完工时间）进行优化，未充分讨论多目标调度（如能耗、交货期延误、成本等）的权衡。

（完）
