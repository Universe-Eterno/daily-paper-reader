---
title: Dynamic resource matching in manufacturing using deep reinforcement learning
title_zh: 基于深度强化学习的制造业动态资源匹配
authors: "Saunak Kumar Panda, Yisha Xiang, Ruiqi Liu"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27066v1"
tags: ["query:nco"]
score: 8.0
evidence: 深度强化学习用于动态资源匹配
tldr: 本研究针对制造业中需求与产能的动态匹配问题，将其建模为多周期、多对多的序列决策过程。为应对大规模状态空间及建模动态转移的挑战，提出了一种基于模型无关的深度强化学习方法。通过引入领域知识惩罚和可行性约束，改进了传统算法的收敛性与动作合规性，并开发了DKDDPG算法。实验证明，该方法在奖励获取和计算效率上均显著优于传统强化学习算法。
selection_source: fresh_fetch
motivation: 旨在解决制造业资源匹配中由于大规模状态空间和难以显式建模的动态转移导致的决策难题。
method: 提出一种融合领域知识惩罚项与可行性约束的改进型深度确定性策略梯度算法（DKDDPG）。
result: 在小规模和大规模实验中，DKDDPG 均在奖励值、收敛速度和时间效率上优于传统 DDPG 及其他强化学习算法。
conclusion: 将领域知识与深度强化学习相结合，能有效提升复杂动态资源匹配问题的求解性能和实用性。
---

## 摘要
匹配在广泛行业的资源逻辑分配中发挥着重要作用。匹配的优势在制造业中日益得到认可，特别是产能共享近年来受到了广泛关注。本文考虑了制造业资源中需求-产能类型的动态匹配问题。我们将多周期、多对多的制造业资源匹配问题建模为一个序贯决策过程。所构建的制造业资源匹配问题涉及巨大的状态和动作空间，且准确建模各类需求的联合分布并不现实。为了解决维数灾难以及显式建模转移概率的困难，我们采用无模型深度强化学习方法来寻找最优匹配策略。此外，为了解决 Q-learning 中由最大值算子引起的初始偏差估计所导致的不可行动作和收敛缓慢问题，我们在传统 Q-learning 算法中引入了两种惩罚：一种是基于先验策略的领域知识惩罚，另一种是符合供需约束的不可行性惩罚。我们建立了领域知识引导的 Q-learning 收敛性的理论结果，为小规模问题提供了性能保证。对于大规模问题，我们进一步将改进的方法注入深度确定性策略梯度（DDPG）算法中，称之为领域知识引导的 DDPG（DKDDPG）。在包括小规模和大规模实验的计算研究中，DKDDPG 始终优于传统的 DDPG 和其他强化学习算法，获得了更高的奖励，并在时间和训练轮数方面表现出更高的效率。

## Abstract
Matching plays an important role in the logical allocation of resources across a wide range of industries. The benefits of matching have been increasingly recognized in manufacturing industries. In particular, capacity sharing has received much attention recently. In this paper, we consider the problem of dynamically matching demand-capacity types of manufacturing resources. We formulate the multi-period, many-to-many manufacturing resource-matching problem as a sequential decision process. The formulated manufacturing resource-matching problem involves large state and action spaces, and it is not practical to accurately model the joint distribution of various types of demands. To address the curse of dimensionality and the difficulty of explicitly modeling the transition dynamics, we use a model-free deep reinforcement learning approach to find optimal matching policies. Moreover, to tackle the issue of infeasible actions and slow convergence due to initial biased estimates caused by the maximum operator in Q-learning, we introduce two penalties to the traditional Q-learning algorithm: a domain knowledge-based penalty based on a prior policy and an infeasibility penalty that conforms to the demand-supply constraints. We establish theoretical results on the convergence of our domain knowledge-informed Q-learning providing performance guarantee for small-size problems. For large-size problems, we further inject our modified approach into the deep deterministic policy gradient (DDPG) algorithm, which we refer to as domain knowledge-informed DDPG (DKDDPG). In our computational study, including small- and large-scale experiments, DKDDPG consistently outperformed traditional DDPG and other RL algorithms, yielding higher rewards and demonstrating greater efficiency in time and episodes.

---

## 论文详细总结（自动生成）

这篇论文针对制造业中的动态资源匹配问题，提出了一种结合领域知识的深度强化学习方法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着云制造和产能共享模式的兴起，如何高效匹配制造需求与生产产能成为核心挑战。
*   **核心问题**：制造业资源匹配具有**多对多（Many-to-Many）**、**多周期动态性**、**高维状态/动作空间**以及**需求不确定性**等特征。
*   **研究动机**：传统的精确算法（如线性规划）难以应对大规模动态环境；标准强化学习（如Q-learning）在处理复杂约束时收敛缓慢，且容易产生初始估计偏差。

### 2. 方法论
*   **核心思想**：将匹配问题建模为马尔可夫决策过程（MDP），并提出**领域知识引导（Domain Knowledge-Informed）**的改进策略，通过在奖励函数和更新规则中引入惩罚项来加速收敛并保证可行性。
*   **关键技术细节**：
    *   **MDP建模**：状态为各类型待处理的需求量；动作为需求与产能的匹配矩阵；奖励基于匹配收益减去惩罚。
    *   **领域知识惩罚（$g_\pi$）**：引入先验策略（如单周期最优解）作为引导，惩罚偏离先验策略的动作，缓解Q-learning的初始偏差。
    *   **不可行性惩罚（$u, v$）**：针对违反需求上限或产能上限的动作施加比例惩罚，将约束优化问题转化为无约束形式。
    *   **DKDDPG算法**：将改进的Q更新规则集成到深度确定性策略梯度（DDPG）框架中。在Critic网络的损失函数中加入正则化项，并对正则化参数 $\beta$ 进行动态调度（从强引导逐渐过渡到自主探索）。
*   **理论保证**：论文严格证明了在固定 $\beta$ 和动态调度 $\beta_t$ 两种情况下，领域知识引导的Q-learning算法均能收敛至最优解。

### 3. 实验设计
*   **实验场景**：模拟了不同规模的制造业匹配任务，涵盖水平差异化（基于距离/成本）和垂直差异化（基于质量/等级）两种奖励结构。
*   **数据集**：采用合成数据，需求分布设为离散均匀分布，产能固定。
*   **Benchmark与对比方法**：
    *   **小规模（2x2到5x5）**：对比线性规划（LP）精确解、标准Q-learning、DKQL（带惩罚的Q-learning）、DQN、DPG、DDPG。
    *   **大规模（10x10到30x30）**：主要对比标准DDPG与提出的DKDDPG。
*   **评价指标**：平均Q值估计、收敛时间、收敛所需轮数（Episodes）、样本外测试的累积奖励。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Intel Xeon 4核（8逻辑处理器）** 的工作站上运行。
*   **算力说明**：文中未明确提到使用GPU，考虑到DDPG的规模，可能主要依赖CPU或标准工作站算力。
*   **训练时长**：设置了最大运行时间为 **150,000秒**（约41.6小时）。

### 5. 实验数量与充分性
*   **实验规模**：针对每个问题规模（如2x2, 10x10等）均测试了5个不同的随机实例。
*   **充分性评价**：实验设计较为充分。不仅涵盖了从小规模到大规模的跨度，还进行了样本内训练对比和样本外（Out-of-sample）泛化性评估。通过与精确解对比验证了准确性，通过与多种RL变体对比验证了改进模块的有效性。

### 6. 主要结论与发现
*   **效率提升**：在大规模问题中，DKDDPG 比标准 DDPG 的收敛速度快约 **30%**，所需训练轮数减少约 **36%**。
*   **性能优越**：DKDDPG 获得的平均Q值显著高于标准DDPG（在某些案例中高出数倍），说明领域知识引导能有效避免陷入局部最优。
*   **可行性**：引入不可行性惩罚后，模型能快速学会遵守产能和需求的物理约束。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还给出了收敛性的数学证明，增强了算法的可靠性。
*   **冷启动优化**：利用单周期最优解作为先验知识，解决了强化学习在复杂组合优化问题中初期探索效率低下的“顽疾”。
*   **动作空间处理**：通过Softmax输出与状态缩放的转换机制，巧妙处理了高维连续动作空间与离散约束之间的矛盾。

### 8. 不足与局限
*   **提前假设较多**：假设提前期（Lead time）为单周期，未考虑更复杂的生产延迟情况。
*   **中心化限制**：目前模型基于中心化控制，未探讨多智能体分布式决策（Decentralized）的场景。
*   **数据分布单一**：实验主要基于均匀分布的需求，对于长尾分布或极度波动的现实需求分布验证不足。
*   **成本建模简化**：未显式考虑匹配过程中的固定成本或切换成本，仅以收益最大化为目标。

（完）
