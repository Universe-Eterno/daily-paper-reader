---
title: "Beyond One-Size-Fits-All: Adaptive Subgraph Denoising for Zero-Shot Graph Learning with Large Language Models"
title_zh: 超越“一刀切”：基于大语言模型的零样本图学习自适应子图去噪
authors: "Fengzhi Li, Liang Zhang, Yuan Zuo, Ruiqing Zhao, YanSong Liu, Yunfei Ma, Fanyu Meng, Junlan Feng"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02938v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大模型与图推理的结合
tldr: 针对零样本图学习中子图提取策略单一且包含大量噪声的问题，本文提出GraphSSR框架。该框架通过“采样-选择-推理”（SSR）流水线实现自适应子图去噪，并结合SSR-SFT数据合成策略与两阶段强化学习（SSR-RL）来优化LLM的推理路径。GraphSSR能自主过滤无关邻居，显著提升了LLM在处理复杂图结构时的泛化能力与预测准确性。
selection_source: fresh_fetch
motivation: 现有的基于LLM的图推理方法采用通用的子图提取策略，导致引入大量结构噪声，干扰了模型的感受野和预测效果。
method: 提出了包含SSR流水线、SSR-SFT微调策略以及两阶段强化学习（SSR-RL）的GraphSSR框架，实现自适应的子图去噪与推理。
result: 该方法能够动态调整子图提取过程，通过精简且去噪的子图结构实现了更准确的零样本图学习预测。
conclusion: GraphSSR通过自适应去噪机制克服了传统方法在图推理中的局限性，为提升LLM在图领域任务的泛化性能提供了有效方案。
---

## 摘要
由于数据稀缺以及传统图神经网络（GNN）无法泛化到未见领域或标签空间，零样本设置下的图任务仍然是一个重大挑战。虽然最近的进展已转向利用大语言模型（LLM）作为预测器来增强 GNN，但这些方法通常面临跨模态对齐问题。最近的一种范式（如 Graph-R1）通过采用纯文本格式并利用基于 LLM 的图推理，克服了上述架构依赖性，展示了改进的零样本泛化能力。然而，它采用了一种与任务无关的“一刀切”子图提取策略，这不可避免地引入了显著的结构噪声（即无关的邻居和边），从而扭曲了 LLM 的感受野并导致次优预测。为了解决这一局限性，我们引入了 GraphSSR，这是一个专为基于 LLM 的零样本图推理中的自适应子图提取和去噪而设计的新颖框架。具体而言，我们提出了 SSR 流水线，它通过“采样-选择-推理”（Sample-Select-Reason）过程，根据特定上下文动态定制子图提取，使模型能够自主过滤掉与任务无关的邻居，并克服“一刀切”问题。为了内化这种能力，我们开发了 SSR-SFT，这是一种数据合成策略，可生成高质量的 SSR 风格图推理轨迹，用于 LLM 的有监督微调。此外，我们提出了 SSR-RL，这是一个两阶段强化学习框架，旨在显式调节所提出的 SSR 流水线中用于自适应子图去噪的采样和选择操作。通过结合真实性增强和去噪增强的强化学习，我们引导模型使用精简且去噪的子图进行推理，从而实现准确预测。

## Abstract
Graph-based tasks in the zero-shot setting remain a significant challenge due to data scarcity and the inability of traditional Graph Neural Networks (GNNs) to generalize to unseen domains or label spaces. While recent advancements have transitioned toward leveraging Large Language Models (LLMs) as predictors to enhance GNNs, these methods often suffer from cross-modal alignment issues. A recent paradigm (i.e., Graph-R1) overcomes the aforementioned architectural dependencies by adopting a purely text-based format and utilizing LLM-based graph reasoning, showing improved zero-shot generalization. However, it employs a task-agnostic, one-size-fits-all subgraph extraction strategy, which inevitably introduces significant structural noise--irrelevant neighbors and edges--that distorts the LLMs' receptive field and leads to suboptimal predictions. To address this limitation, we introduce GraphSSR, a novel framework designed for adaptive subgraph extraction and denoising in zero-shot LLM-based graph reasoning. Specifically, we propose the SSR pipeline, which dynamically tailors subgraph extraction to specific contexts through a "Sample-Select-Reason" process, enabling the model to autonomously filter out task-irrelevant neighbors and overcome the one-size-fits-all issue. To internalize this capability, we develop SSR-SFT, a data synthesis strategy that generates high-quality SSR-style graph reasoning traces for supervised fine-tuning of LLMs. Furthermore, we propose SSR-RL, a two-stage reinforcement learning framework that explicitly regulates sampling and selection operations within the proposed SSR pipeline designed for adaptive subgraph denoising. By incorporating Authenticity-Reinforced and Denoising-Reinforced RL, we guide the model to achieve accurate predictions using parsimonious, denoised subgraphs for reasoning.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **GraphSSR** 的新颖框架，旨在解决大语言模型（LLM）在零样本图学习（Zero-Shot Graph Learning）中因“一刀切”的子图提取策略而引入结构噪声的问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 LLM 的图推理方法（如 Graph-R1）通常采用固定的 $k$ 步邻居提取策略。这种“一刀切”的方法会引入大量与任务无关的节点和边（结构噪声），干扰 LLM 的感受野，导致推理链条断裂或产生错误预测。
*   **研究动机**：在零样本场景下，模型无法通过特定任务的训练来识别噪声。因此，需要一种能够根据具体节点和任务上下文，**自适应地提取并去噪子图**的机制，使模型能专注于最关键的结构信息。

### 2. 方法论
GraphSSR 框架由三个核心部分组成：
*   **SSR 流水线（Sample-Select-Reason）**：
    *   **采样（Sample）**：针对目标任务，模型首先采样一组多样化的候选子图（如仅中心节点、语义密集子图等），探索不同的结构视角。
    *   **选择（Select）**：模型自主评估候选子图的质量，剔除含噪子图，选择最“纯净”且相关的结构。
    *   **推理（Reason）**：在去噪后的子图基础上执行高保真推理，生成最终预测。
*   **SSR-SFT（有监督微调）**：
    *   利用强大的教师模型（如 DeepSeek-R1）合成高质量的 SSR 风格推理轨迹。
    *   引入多维过滤策略：验证子图真实性、评估结构多样性（基于能量指标）、检查选择一致性及答案正确性。
*   **SSR-RL（两阶段强化学习）**：
    *   采用 **GRPO（群体相对策略优化）** 算法。
    *   **第一阶段：真实性增强 RLVR**。通过可验证奖励（真实性、一致性、正确性）减少采样和选择过程中的幻觉。
    *   **第二阶段：去噪增强 RLVR**。引入基于子图大小的奖励机制，在保证正确的前提下，奖励更精简（去噪更彻底）的子图，显式惩罚结构噪声。

### 3. 实验设计
*   **数据集与场景**：涵盖社交网络、知识图谱、引用网络、电子商务和网页链接等 5 个领域。具体包括 **Cora、WikiCS、Products、FB15K237**（这些数据集在训练阶段被严格排除，以测试零样本能力）。
*   **任务类型**：零样本节点分类和链接预测。
*   **对比方法**：
    *   **图专用模型**：OFA, GraphGPT, UniGraph, ZeroG, LLaGA, GOFA, Graph-R1。
    *   **通用大推理模型**：DeepSeek-R1 (14B/Full), Qwen3-14B, Ministral-3-14B。

### 4. 资源与算力
*   **硬件环境**：使用了由 **8 台 NVIDIA H100 GPU** 组成的集群。
*   **训练细节**：
    *   **基座模型**：DeepSeek-R1-distilled-Qwen2.5-14B。
    *   **SFT 阶段**：微调 3 个 epoch，学习率 $1.0 \times 10^{-5}$，Batch Size 为 64。
    *   **RL 阶段**：训练 4 个 epoch，学习率 $1.0 \times 10^{-6}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了主实验，涵盖了从 2 类到 237 类不等的任务难度。
*   **消融实验**：对比了“无 SSR 流水线”、“无 RL”、“无真实性增强 RL”、“无去噪增强 RL”等变体，验证了各模块的必要性。
*   **敏感性分析**：针对去噪强度超参数 $\lambda$ 进行了参数敏感性测试。
*   **定性分析**：提供了详细的推理轨迹案例分析和子图规模统计分析。
*   **评价**：实验设计全面，对比了当前最先进的基线模型，且严格遵循零样本设置，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：GraphSSR 在多数任务上达到了 SOTA 水平，尤其在类别多、噪声大的复杂数据集（如 Products）上优势更明显。
*   **去噪的价值**：实验证明“信息多并不等于推理好”。GraphSSR 选取的子图规模远小于原始子图，但准确率更高，证明了去除结构噪声的关键作用。
*   **RL 的协同效应**：两阶段 RL 缺一不可，第一阶段保证“做对”，第二阶段引导“精简”。

### 7. 优点
*   **范式创新**：首次将零样本图推理重新表述为“采样-选择-推理”过程，打破了传统的固定子图输入模式。
*   **自适应性强**：模型能够根据任务需求自主决定关注哪些邻居，具有极强的灵活性。
*   **可解释性**：通过显式的选择步骤和推理链条，使图推理过程更加透明。

### 8. 不足与局限
*   **计算开销**：SSR 流水线涉及多次子图采样和长文本推理，推理阶段的 Token 消耗和延迟可能高于传统方法。
*   **过度剪枝风险**：参数敏感性实验显示，如果去噪惩罚过重（$\lambda$ 过大），模型可能会丢失关键语义信息，导致性能下降。
*   **对教师模型的依赖**：SFT 数据的质量高度依赖于 DeepSeek-R1 等顶级模型的合成能力，在极度冷门的图领域可能存在合成偏差。

（完）
