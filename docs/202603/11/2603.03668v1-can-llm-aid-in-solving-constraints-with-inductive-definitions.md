---
title: Can LLM Aid in Solving Constraints with Inductive Definitions?
title_zh: LLM 能否辅助求解带有归纳定义的约束？
authors: "Weizhi Feng, Shidong Shen, Jiaxiang Liu, Taolue Chen, Fu Song, Zhilin Wu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03668v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 将大模型与约束求解器结合处理归纳定义
tldr: "针对包含归纳定义（如递归、抽象数据类型）的约束求解难题，本文提出一种结合大语言模型（LLM）与符号求解器的神经符号方法。通过结构化提示词引导LLM生成辅助引理，并由求解器迭代验证其有效性。实验表明，该方法显著提升了现有SMT和CHC求解器的性能，在相关证明任务上的求解率提高了约25%，展示了LLM在辅助形式化验证方面的巨大潜力。"
selection_source: fresh_fetch
motivation: 现有的SMT和CHC求解器在处理涉及归纳定义和抽象数据类型的复杂约束时，往往难以自动生成必要的辅助引理。
method: 提出一种神经符号框架，利用LLM迭代生成引理猜想，并由符号求解器验证其正确性及对证明目标的辅助作用。
result: "在包含代数数据类型和递推关系的基准测试中，该方法成功帮助现有求解器多解决了约25%的证明任务。"
conclusion: 研究证明了LLM的启发式推理能力可以与符号求解器的严谨性互补，是解决归纳推理约束难题的有效途径。
---

## 摘要
求解涉及归纳（又称递归）定义的约束具有挑战性。最先进的 SMT/CHC 求解器和一阶逻辑证明器对求解此类约束的支持有限，特别是当涉及抽象数据类型等内容时。在这项工作中，我们利用结构化提示引导大语言模型（LLMs）生成对这些归纳定义进行推理所必需的辅助引理。我们进一步提出了一种神经符号方法，将 LLM 与约束求解器协同集成：LLM 迭代地生成猜想，而求解器则检查其有效性以及对于证明目标的有用性。我们在包含源自代数数据类型和递推关系的约束的多样化基准测试集上评估了我们的方法。实验结果表明，我们的方法可以改进最先进的 SMT 和 CHC 求解器，多解决了约 25% 涉及归纳定义的证明任务，证明了其有效性。

## Abstract
Solving constraints involving inductive (aka recursive) definitions is challenging. State-of-the-art SMT/CHC solvers and first-order logic provers provide only limited support for solving such constraints, especially when they involve, e.g., abstract data types. In this work, we leverage structured prompts to elicit Large Language Models (LLMs) to generate auxiliary lemmas that are necessary for reasoning about these inductive definitions. We further propose a neuro-symbolic approach, which synergistically integrates LLMs with constraint solvers: the LLM iteratively generates conjectures, while the solver checks their validity and usefulness for proving the goal. We evaluate our approach on a diverse benchmark suite comprising constraints originating from algebrai data types and recurrence relations. The experimental results show that our approach can improve the state-of-the-art SMT and CHC solvers, solving considerably more (around 25%) proof tasks involving inductive definitions, demonstrating its efficacy.

---

## 论文详细总结（自动生成）

这篇论文探讨了利用大语言模型（LLM）辅助求解带有归纳定义（Inductive Definitions）的逻辑约束问题。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在程序验证中，归纳定义（如代数数据类型 ADT 和递归定义函数 RDF）无处不在。虽然现有的 SMT（可满足性模理论）求解器和一阶逻辑证明器已尝试支持归纳推理，但在处理复杂属性时，往往因为无法自动生成必要的**辅助引理（Auxiliary Lemmas）**而失败。
*   **核心问题**：如何有效地结合 LLM 的启发式推理能力和符号求解器的严谨性，自动生成并验证辅助引理，从而解决传统逻辑方法难以处理的归纳约束难题。

### 2. 论文提出的方法论
论文提出了一种名为 **LLM4Ind** 的神经符号（Neuro-symbolic）方法，其核心是一个“生成-过滤-验证”的迭代循环：
*   **核心思想**：将 LLM 作为引理生成器，将 SMT 求解器作为验证器，通过递归的方式构建证明树。
*   **关键技术流程**：
    1.  **查询阶段（Query）**：设计了两种结构化提示策略：
        *   **策略1：等式推理（Equational Reasoning）**：引导 LLM 模仿人类证明过程，进行步进式的项重写，在推导受阻处生成猜想。
        *   **策略2：项重写与泛化（Generalization）**：引导 LLM 简化目标、加强结论或通过替换公共子项来泛化属性。
    2.  **过滤阶段（Filter）**：利用求解器快速剔除语法错误、与目标相同或与公理冲突（不一致）的无效猜想。
    3.  **验证阶段（Validate）**：
        *   首先检查“公理 + 猜想”是否足以证明目标属性。
        *   若满足，则将猜想作为新的子目标，递归地调用求解流程进行证明，确保引理本身的正确性。

### 3. 实验设计
*   **数据集/基准测试**：共使用了 706 个证明任务，涵盖四个主流基准集：
    *   **StandardDT**：标准的 ADT 推理任务。
    *   **StandardDTLIA**：结合了 ADT 和线性整数算术（LIA）的任务。
    *   **AutoproofBM**：更复杂的递归函数等价性验证。
    *   **IndBen**：涵盖列表、树等多种数据结构的代表性任务。
*   **对比方法**：
    *   **cvc5**（最先进的 SMT 求解器，内置归纳推理模块）。
    *   **Vampire**（领先的一阶逻辑定理证明器）。
    *   **Racer**（针对 CHC 约束的求解器）。

### 4. 资源与算力
*   **硬件环境**：Intel Xeon Platinum 8368Q CPU (76核, 2.60 GHz)，1024 GiB RAM。
*   **算力消耗**：论文未提及模型训练（属于 Inference-only 研究）。实验中每个任务的超时限制为 1200 秒。
*   **LLM 成本**：使用 Qwen 模型运行全量基准测试约消耗 1150 万个 token，成本约为 4 美元。

### 5. 实验数量与充分性
*   **实验规模**：在 706 个任务上进行了大规模测试，并针对不同 LLM（Qwen3-235B, DeepSeek-v3.2, Gemini 2.5, GPT-5）进行了横向对比。
*   **消融实验**：详细对比了“有无过滤器”以及“结构化提示 vs. 简单提示”的效果。
*   **鲁棒性测试**：针对 LLM 的随机性，每个设置运行了 3 次，并测试了不同采样温度（0.1 到 1.3）的影响。
*   **评价**：实验设计全面、客观，充分考虑了 LLM 的随机性和不同后端求解器的兼容性。

### 6. 论文的主要结论与发现
*   **性能提升**：LLM4Ind 显著优于所有基准工具，比最强的传统求解器多解决了约 **25%** 的任务（解决了 525/706，而 cvc5 为 293/706）。
*   **提示词的重要性**：结构化提示策略（等式推理和泛化）的效果远好于简单提示。
*   **过滤器的价值**：过滤器能有效减少无效尝试，降低 token 消耗并缩短求解时间。
*   **模型鲁棒性**：该方法在不同主流 LLM 上均表现出一致的改进效果，证明了其通用性。

### 7. 优点
*   **神经符号协同**：成功将 LLM 的“直觉”与 SMT 的“严谨”结合，解决了引理生成的搜索空间爆炸问题。
*   **递归证明机制**：不仅生成引理，还递归地证明引理，保证了最终证明结果的逻辑完备性。
*   **提示工程深入**：提出的两种策略紧扣归纳推理的本质（重写与泛化），具有很强的启发性。

### 8. 不足与局限
*   **时间开销**：由于涉及多次 LLM 调用和递归求解，平均求解时间（约 100s）远高于传统求解器（约 3-20s）。
*   **依赖性**：高度依赖 LLM 的 API 可用性和推理能力，且存在一定的 token 成本。
*   **输入限制**：目前主要针对 SMTLIB2 格式，对于更复杂的程序级验证场景（如直接处理 C/Rust 代码）仍需前端转换。

（完）
