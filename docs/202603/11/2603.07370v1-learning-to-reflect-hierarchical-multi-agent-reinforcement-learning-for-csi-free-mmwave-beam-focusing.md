---
title: "Learning to Reflect: Hierarchical Multi-Agent Reinforcement Learning for CSI-Free mmWave Beam-Focusing"
title_zh: 学习反射：用于无 CSI 毫米波波束聚焦的分层多智能体强化学习
authors: "Hieu Le, Oguz Bedir, Mostafa Ibrahim, Jian Tao, Sabit Ekin"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07370v1"
tags: ["query:nco"]
score: 8.0
evidence: 针对组合动作空间的强化学习
tldr: 针对毫米波系统中可重构智能表面（RIS）面临的信道状态信息（CSI）估计开销大和维度爆炸问题，本文提出了一种分层多智能体强化学习（HMARL）框架。该框架采用“无CSI”范式，利用位置数据代替导频估计，并通过高层分配与底层优化的两级架构实现高效控制。实验表明，该方法在显著提升信号强度的同时，具备极佳的扩展性和对定位误差的鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决毫米波RIS部署中CSI估计带来的巨大开销以及中心化优化在处理大规模动作空间时的维度爆炸难题。
method: 提出一种基于位置信息的HMARL框架，结合MAPPO算法与CTDE范式，将控制问题分解为高层用户分配和底层焦点优化两个抽象层。
result: 实验显示该框架比中心化基准提升了2.81-7.94 dB的RSSI，且在用户密度翻倍或存在0.5米定位误差时仍能保持高效稳定。
conclusion: 该研究通过消除CSI开销并实现高精度波束聚焦，为智能毫米波环境提供了一种具有实际应用潜力的可扩展解决方案。
---

## 摘要
可重构智能表面有望改变无线环境，但实际部署受到信道状态信息（CSI）估计的高昂开销以及集中式优化中固有的维度爆炸的阻碍。本文提出了一种分层多智能体强化学习（HMARL）框架，用于控制毫米波（mmWave）系统中的机械可重构反射表面。我们引入了一种“无 CSI”范式，用易于获取的用户定位数据取代了基于导频的信道估计。为了管理巨大的组合动作空间，所提出的架构在集中式训练分布式执行（CTDE）范式下利用了多智能体近端策略优化（MAPPO）。该架构将控制问题分解为两个抽象层级：用于用户到反射器分配的高层控制器，以及用于低层焦点优化的分布式低层控制器。全面的射线追踪评估表明，该框架相比集中式基准实现了 2.81-7.94 dB 的 RSSI 提升，且性能优势随着系统复杂度的增加而扩大。可扩展性分析显示，该系统保持了持续的效率，即使在用户密度翻倍的情况下，也表现出极小的单用户性能下降和稳定的总功率利用率。此外，鲁棒性验证确认了该框架在不同反射器孔径尺寸（45-99 个瓦片）下的可行性，并证明了在高达 0.5 米的定位误差下性能下降平缓。通过消除 CSI 开销并保持高保真波束聚焦，本研究确立了 HMARL 作为智能毫米波环境的一种实用解决方案。

## Abstract
Reconfigurable Intelligent Surfaces promise to transform wireless environments, yet practical deployment is hindered by the prohibitive overhead of Channel State Information (CSI) estimation and the dimensionality explosion inherent in centralized optimization. This paper proposes a Hierarchical Multi-Agent Reinforcement Learning (HMARL) framework for the control of mechanically reconfigurable reflective surfaces in millimeter-wave (mmWave) systems. We introduce a "CSI-free" paradigm that substitutes pilot-based channel estimation with readily available user localization data. To manage the massive combinatorial action space, the proposed architecture utilizes Multi-Agent Proximal Policy Optimization (MAPPO) under a Centralized Training with Decentralized Execution (CTDE) paradigm. The proposed architecture decomposes the control problem into two abstraction levels: a high-level controller for user-to-reflector allocation and decentralized low-level controllers for low-level focal point optimization. Comprehensive ray-tracing evaluations demonstrate that the framework achieves 2.81-7.94 dB RSSI improvements over centralized baselines, with the performance advantage widening as system complexity increases. Scalability analysis reveals that the system maintains sustained efficiency, exhibiting minimal per-user performance degradation and stable total power utilization even when user density doubles. Furthermore, robustness validation confirms the framework's viability across varying reflector aperture sizes (45-99 tiles) and demonstrates graceful performance degradation under localization errors up to 0.5 m. By eliminating CSI overhead while maintaining high-fidelity beam-focusing, this work establishes HMARL as a practical solution for intelligent mmWave environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Reflect: Hierarchical Multi-Agent Reinforcement Learning for CSI-Free mmWave Beam-Focusing》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在毫米波（mmWave）通信中，可重构智能表面（RIS）的实际部署面临两大瓶颈：一是**信道状态信息（CSI）估计开销巨大**（导频开销随反射单元数量呈指数级增长）；二是**中心化优化面临维度爆炸**（难以同时协调大量反射单元和多用户分配）。
*   **研究动机**：传统的电子式 RIS 依赖复杂的射频电路和精确的相位控制。本文转向研究**机械可重构金属反射器**，并提出一种“无 CSI”范式，利用易于获取的**用户位置信息**代替难以获取的电磁信道参数，以实现低开销、高增益的波束聚焦。

### 2. 方法论
*   **核心思想**：将复杂的反射控制问题分解为**分层多智能体强化学习（HMARL）**架构，采用“集中式训练、分布式执行（CTDE）”范式。
*   **关键技术细节**：
    *   **两级分层架构**：
        *   **高层分配器（High-level Allocator）**：负责全局决策，根据用户位置将特定的反射器段分配给特定用户（离散组合优化）。
        *   **底层执行器（Low-level Controllers）**：每个反射器段作为独立智能体，通过调整“焦点（Focal Point）”坐标来优化反射角度（连续空间优化）。
    *   **焦点抽象（Focal Point Abstraction）**：不直接控制成百上千个瓦片的角度，而是控制一个虚拟焦点，通过几何关系推导各瓦片的法向量，将动作空间维度降低了 5 倍以上。
    *   **MAPPO 算法**：使用多智能体近端策略优化算法，结合全局评论家（Global Critic）解决多智能体环境中的非平稳性问题。
    *   **几何兼容性矩阵**：引入先验几何知识（距离和角度）作为启发式引导，加速高层分配器的收敛速度。

### 3. 实验设计
*   **场景与工具**：使用 NVIDIA Sionna 射线追踪引擎和 Blender 构建真实的 60 GHz 室内会议室环境，考虑了混凝土、木材、大理石等材料的电磁特性。
*   **Benchmark（基准方法）**：
    *   **No Allocator**：传统的中心化 PPO 算法（不分层）。
    *   **No Compat**：不带几何兼容性矩阵的分层架构（消融实验）。
    *   **Random**：随机分配用户与反射器。
*   **对比维度**：RSSI（接收信号强度指示）增益、收敛速度、用户密度扩展性、硬件孔径大小、定位误差鲁棒性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到使用了 Adam 优化器，学习率为 $2.0 \times 10^{-4}$，训练周期为 3200 个 Episode，每个 Episode 包含 100 个时间步。代码已在 GitHub 开源。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **收敛性实验**：对比了 2 用户和 4 用户场景下的四种方法。
    *   **消融实验**：验证了分层架构和兼容性矩阵的必要性。
    *   **扩展性实验**：测试了用户密度翻倍对性能的影响。
    *   **鲁棒性实验**：测试了 4 种不同的反射器尺寸（45-99 瓦片）、5 种不同的奖励函数公式，以及 6 种不同等级的定位误差（0-3 米）。
*   **充分性评价**：实验设计非常全面且客观。通过射线追踪模拟真实物理环境，并针对定位误差这一实际部署中的核心痛点进行了专项测试，验证了方案的实用性。

### 6. 主要结论与发现
*   **性能提升**：相比中心化基准方法，该框架实现了 **2.81–7.94 dB** 的 RSSI 提升，且系统越复杂（用户越多），分层架构的优势越明显。
*   **高效扩展**：当用户密度翻倍时，单用户性能仅下降 1.39 dB，总系统功率保持稳定，证明了良好的线性扩展性。
*   **鲁棒性强**：系统能容忍高达 **0.5 米** 的定位误差而保持性能平稳，这符合现有 WiFi/UWB 定位技术的精度范围。
*   **硬件优化**：发现反射器尺寸存在边际效应，81 瓦片（9x9）是性能与成本的最佳平衡点。

### 7. 优点
*   **消除 CSI 依赖**：通过位置感知实现“无 CSI”控制，极大地降低了毫米波系统的信令开销。
*   **维度压缩**：焦点抽象和分层架构巧妙地解决了大规模 RIS 控制中的维度爆炸问题。
*   **实用性高**：机械反射器方案避开了复杂射频电路，且对频率不敏感（宽频带适用）。
*   **收敛加速**：引入几何兼容性矩阵作为归纳偏置，使复杂组合优化问题的收敛速度提升了约 20%-30%。

### 8. 不足与局限
*   **机械延迟**：机械重构反射器的调整速度（毫秒级）远慢于电子式 RIS（微秒级），可能不适用于极高移动性的场景。
*   **定位依赖**：虽然对误差有一定容忍度，但系统高度依赖定位基础设施，在定位信号缺失的环境下无法工作。
*   **NLOS 局限**：研究主要集中在非视距（NLOS）覆盖增强，未深入探讨视距（LOS）与 NLOS 频繁切换时的动态协调。
*   **干扰模型简化**：在多用户干扰处理上，主要依赖空间隔离，未深入探讨极高密度下的多用户同频干扰抑制。

（完）
