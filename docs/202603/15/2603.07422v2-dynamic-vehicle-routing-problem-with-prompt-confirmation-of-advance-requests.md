---
title: Dynamic Vehicle Routing Problem with Prompt Confirmation of Advance Requests
title_zh: 具有预约请求即时确认功能的动态车辆路径问题
authors: "Amutheezan Sivagnanam, Ayan Mukhopadhyay, Samitha Samaranayake, Abhishek Dubey, Aron Laszka"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07422v2"
tags: ["query:nco"]
score: 8.0
evidence: 解决具有取送货约束的动态车辆路径问题
tldr: 本研究针对按需交通服务中预约请求需即时确认且需持续优化路径的现实需求，提出了一种结合即时确认与持续优化的动态车辆路径规划新框架。该方法通过快速插入搜索实现即时反馈，并利用随时算法进行路径持续改进，同时引入强化学习训练的非近视目标函数以最大化服务请求量。在真实微循环公交数据集上的实验表明，该方法在保证即时确认的同时，显著提升了服务效率和请求接受率。
selection_source: fresh_fetch
motivation: 现有的动态车辆路径算法无法在提供即时预约确认的同时，保证对已接受请求的持续路径优化和服务承诺。
method: 提出一种集成快速插入搜索与随时算法的新框架，并利用强化学习训练非近视目标函数来指导路径优化。
result: 在真实微循环公交数据集上的实验证明，该方法在实现即时确认的前提下，显著增加了服务的请求总数。
conclusion: 该研究成功解决了预约式动态车辆路径问题中的即时性与优化性矛盾，为提升城市微循环公交服务效率提供了有效方案。
---

## 摘要
运营按需运输服务的交通机构必须实时响应乘客的出行请求，这涉及解决具有取送约束的动态车辆路径问题。基于与公共交通机构的讨论，我们观察到一个先前工作尚未解决的现实问题：当行程被提前预订时（例如，出行请求在要求的取件时间前几小时到达），机构需要即时确认是否可以接受该请求，并确保已接受的请求能按承诺提供服务。最先进的计算方法要么提供即时确认但缺乏对已接受请求进行持续优化和改进路径的能力，要么提供持续优化但无法保证服务所有已接受的请求。为了填补这一空白，我们引入了一种具有即时确认和持续优化的动态车辆路径问题的新型问题表述。我们为该车辆路径问题提出了一种新型计算方法，该方法将用于即时确认的快速插入搜索与用于持续优化的随时算法（anytime algorithm）相结合。为了最大化服务的请求数量，我们使用强化学习训练了一个非近视（non-myopic）目标函数，该函数引导插入算法和随时算法走向最优的非近视解。我们在美国一家公共交通机构的真实微循环公交（microtransit）数据集上评估了我们的计算方法，结果表明，与现有方法相比，我们提出的方法在提供即时确认的同时，显著增加了服务的请求数量。

## Abstract
Transit agencies that operate on-demand transportation services have to respond to trip requests from passengers in real time, which involves solving dynamic vehicle routing problems with pick-up and drop-off constraints. Based on discussions with public transit agencies, we observe a real-world problem that is not addressed by prior work: when trips are booked in advance (e.g., trip requests arrive a few hours in advance of their requested pick-up times), the agency needs to promptly confirm whether a request can be accepted or not, and ensure that accepted requests are served as promised. State-of-the-art computational approaches either provide prompt confirmation but lack the ability to continually optimize and improve routes for accepted requests, or they provide continual optimization but cannot guarantee serving all accepted requests. To address this gap, we introduce a novel problem formulation of dynamic vehicle routing with prompt confirmation and continual optimization. We propose a novel computational approach for this vehicle routing problem, which integrates a quick insertion search for prompt confirmation with an anytime algorithm for continual optimization. To maximize the number requests served, we train a non-myopic objective function using reinforcement learning, which guides both the insertion and the anytime algorithms towards optimal, non-myopic solutions. We evaluate our computational approach on a real-world microtransit dataset from a public transit agency in the U.S., demonstrating that our proposed approach provides prompt confirmation while significantly increasing the number of requests served compared to existing approaches.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dynamic Vehicle Routing Problem with Prompt Confirmation of Advance Requests》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**按需交通服务（如微循环公交、残疾人专用交通）中的动态车辆路径问题（DVRP）**。
*   **研究动机**：在现实场景中，乘客通常会提前几小时预约。交通机构面临两难：要么为了用户体验**即时确认**（但一旦确认就难以更改路径，导致效率低下）；要么为了效率**延迟确认**（持续优化路径，但乘客无法提前确定行程）。
*   **核心问题**：如何设计一种算法，既能在请求到达后的几秒钟内给出“接受或拒绝”的**确定性答复**，又能在请求间隔期间**持续优化**车辆路径，以最大化长期的服务请求接受率。

### 2. 方法论
论文提出了一种结合“快速决策”与“后台优化”的混合框架，并利用强化学习（RL）引导决策。
*   **核心思想**：将问题建模为马尔可夫决策过程（MDP）。
*   **两阶段处理流程**：
    1.  **即时确认（Prompt Confirmation）**：采用**快速插入算法**。当新请求到达，在当前路径中搜索所有可能的插入位置。如果存在可行解，则根据 RL 训练的 $Q$ 函数决定是否接受。
    2.  **持续优化（Continual Optimization）**：采用**随时算法（Anytime Algorithm）**。在两个请求到达的空隙时间，利用模拟退火（Simulated Annealing）不断尝试交换、移动、平移或反转路径节点，以提升整体方案的 $Q$ 值。
*   **关键技术细节**：
    *   **非近视目标函数**：使用 RL 训练一个 $Q$ 函数，评估当前路径状态对未来请求的承载潜力（而非仅仅看当前的行驶距离）。
    *   **特征表示**：将复杂的路径状态转化为固定长度的特征向量，包括总空闲时间、时空可用性网格等。
    *   **训练策略**：先通过监督学习（模仿简单启发式策略）进行预训练，再通过 Q-Learning 进行强化学习微调。

### 3. 实验设计
*   **数据集**：
    1.  **真实微循环公交数据**：来自美国某中型城市（2022-2023年），包含真实的请求分布和路网。
    2.  **NYC Taxi 数据**：经典的纽约出租车数据集，经过改造以模拟预约请求。
*   **基准方法（Benchmarks）**：
    *   **Google OR-Tools**：工业级 VRP 求解器。
    *   **Rolling Horizon (RH)**：基于滚动时域的时间分解方法。
    *   **MC VRP**：基于蒙特卡洛树搜索的非近视算法。
*   **对比指标**：拒绝率（Rejection Rate）、确认延迟（Confirmation Time）。

### 4. 资源与算力
*   **硬件环境**：服务器配备 2 个 AMD EPYC 7763 (64核) CPU，1TB RAM，以及 **2 个 NVIDIA RTX A5000 (24GB) GPU**。
*   **训练时长**：
    *   监督预训练（80万条经验）：MLP 架构每轮约 15 秒，KAN 架构约 45 秒。
    *   收敛时间：MLP 约 3 分钟，CNN 约 12 分钟，KAN 约 97 分钟。
*   **推理/决策耗时**：确认决策在 **0.2s 至 1s** 内完成，远快于 RH 和 MC VRP。

### 5. 实验数量与充分性
*   **实验规模**：针对两个数据集各选取了 5 个完整的运营日（Episode）进行测试，涵盖了上千个请求。
*   **消融实验**：
    *   测试了不同神经网络架构（MLP, CNN, KAN）的影响。
    *   测试了随时算法运行时间（从 5s 到 60s 不等）对拒绝率的改善效果。
    *   对比了使用 RL 目标函数与简单启发式目标函数的性能差异。
*   **充分性评价**：实验设计较为全面，不仅对比了传统求解器，还通过消融实验验证了各组件的贡献。使用了统计显著性检验（p-value < 5%）证明结果的客观性。

### 6. 主要结论与发现
*   **性能卓越**：在微循环公交数据集上，该方法将拒绝率降低至约 **1%**，显著优于所有基准方法。
*   **即时性强**：能够在 1 秒内给出确认结果，满足现实应用中“即时反馈”的要求。
*   **持续优化的价值**：实验证明，利用请求之间的空隙时间进行路径重组，能显著提升系统对未来请求的接纳能力。
*   **架构表现**：MLP 和新型的 KAN（Kolmogorov-Arnold Networks）在处理此类结构化特征时表现略优于 CNN。

### 7. 优点
*   **实用性极高**：直接解决了公共交通机构在预约服务中的痛点（即时确认 vs 路径灵活性）。
*   **算法融合创新**：巧妙地将确定性的插入搜索与随机性的随时优化算法结合，并用 RL 统一了目标函数。
*   **非近视决策**：通过学习未来的可能性，避免了贪婪算法导致的局部最优。

### 8. 不足与局限
*   **公平性考虑不足**：论文提到请求在司机之间的分配可能不均（标准差较大），虽然最大化了服务率，但可能引起司机端的不满。
*   **依赖分布假设**：RL 的效果依赖于训练数据与测试数据在请求分布上的一致性，若城市出行模式发生突变，模型可能需要重新训练。
*   **计算资源需求**：虽然推理快，但训练过程（尤其是生成百万级模拟经验）需要较强的算力支持。
*   **应用限制**：目前主要针对固定车队的微循环场景，对于像 Uber/Lyft 那样规模巨大且车辆动态加入/退出的场景，其状态空间的复杂度和特征提取方式可能面临挑战。

（完）
