---
title: "UniHetCO: A Unified Heterogeneous Representation for Multi-Problem Learning in Unsupervised Neural Combinatorial Optimization"
title_zh: UniHetCO：无监督神经组合优化中多问题学习的统一异构表示
authors: "Kien X. Nguyen, Ilya Safro"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11456v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 无监督神经组合优化框架
tldr: 针对无监督神经组合优化（NCO）中现有方法难以跨问题类别统一学习的局限，本文提出UniHetCO框架。该框架通过统一的异构图表示，将问题结构、目标函数和线性约束编码为单一输入，并结合梯度范数动态加权方案解决多任务学习中的梯度失衡。实验证明，该方法在多种约束问题上表现优异，具备强大的跨问题迁移能力，并能有效加速传统求解器的热启动。
selection_source: fresh_fetch
motivation: 现有的无监督神经组合优化方法通常针对特定问题设计，缺乏能够同时处理多种不同约束问题的统一表示和学习框架。
method: 提出一种基于异构图的统一表示方法，将约束二次规划问题编码为单一输入，并利用梯度范数动态加权优化多问题学习过程。
result: 在四个约束问题类别上的实验显示，该方法达到了最先进的无监督基准水平，并展现出卓越的跨问题适应性和求解器加速效果。
conclusion: UniHetCO证明了通过统一异构表示实现多问题无监督学习的可行性，为通用神经组合优化求解器的开发提供了新思路。
---

## 摘要
无监督神经组合优化（NCO）通过在没有地面真值解的情况下训练基于学习的求解器，直接最小化实例目标和约束违反，为监督方法提供了一种极具吸引力的替代方案。然而，对于图节点子集选择问题（例如最大团和最大独立集），现有的无监督方法通常专门针对单一问题类别，并依赖于特定问题的代理损失，这阻碍了在统一框架内进行跨类别的学习。在这项工作中，我们提出了 UniHetCO，这是一种用于基于约束二次规划的组合优化的统一异构图表示，它将问题结构、目标项和线性约束编码在单个输入中。这种公式化方法使得能够使用统一的无标签目标在多个问题类别上训练单个模型。为了提高多问题学习下的稳定性，我们采用了一种基于梯度范数的动态加权方案，以缓解类别间的梯度不平衡。在多个数据集和四个受限问题类别上的实验表明，该方法与最先进的无监督 NCO 基准相比具有竞争力的性能，展现了强大的跨问题适应潜力，并在严格的时间限制下为商业经典求解器提供了有效的热启动。

## Abstract
Unsupervised neural combinatorial optimization (NCO) offers an appealing alternative to supervised approaches by training learning-based solvers without ground-truth solutions, directly minimizing instance objectives and constraint violations. Yet for graph node subset-selection problems (e.g., Maximum Clique and Maximum Independent Set), existing unsupervised methods are typically specialized to a single problem class and rely on problem-specific surrogate losses, which hinders learning across classes within a unified framework. In this work, we propose UniHetCO, a unified heterogeneous graph representation for constrained quadratic programming-based combinatorial optimization that encodes problem structure, objective terms, and linear constraints in a single input. This formulation enables training a single model across multiple problem classes with a unified label-free objective. To improve stability under multi-problem learning, we employ a gradient-norm-based dynamic weighting scheme that alleviates gradient imbalance among classes. Experiments on multiple datasets and four constrained problem classes demonstrate competitive performance with state-of-the-art unsupervised NCO baselines, strong cross-problem adaptation potential, and effective warm starts for a commercial classical solver under tight time limits.

---

## 论文详细总结（自动生成）

以下是对论文《UniHetCO: A Unified Heterogeneous Representation for Multi-Problem Learning in Unsupervised Neural Combinatorial Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
无监督神经组合优化（NCO）虽然无需地面真值（Ground-truth）即可训练，但现有方法通常针对特定问题（如最大独立集）设计专门的代理损失函数，难以在统一框架下同时处理多种不同的组合优化问题。
**研究动机：**
*   **通用性缺失：** 现有的无监督方法通常是“单任务”的，无法直接跨问题类别学习。
*   **表示不统一：** 不同问题的约束和目标函数差异巨大，缺乏一种能够同时编码结构、目标和约束的统一输入表示。
*   **梯度失衡：** 在多问题联合训练时，不同问题的目标函数尺度差异会导致梯度不平衡，使得模型偏向于优化某些特定问题。

### 2. 方法论
UniHetCO 提出了一种将约束二次规划（QP）问题转化为**统一异构图表示**的方法，并结合无监督损失进行训练。

*   **核心思想：** 将组合优化问题编码为包含变量节点、约束节点以及多种边类型的异构图，从而将不同的 CO 问题简化为统一的节点选择任务。
*   **关键技术细节：**
    1.  **异构图表示：**
        *   **变量节点（Variable Nodes）：** 代表决策变量 $x_i$。
        *   **约束节点（Constraint Nodes）：** 将线性不等式约束 $Ax \le b$ 中的每一行编码为一个节点。
        *   **三种边类型：** 包含原始图结构边（Problem Graph）、目标函数耦合边（Objective Graph，捕捉二次项 $Q$ 和线性项 $c$）以及变量-约束关联边（Constraint Hypergraph）。
    2.  **统一无监督损失：** 基于 QUBO（二次无约束二进制优化）公式，将目标函数和约束违反项（Penalty）合并为一个可微的代理损失函数。
    3.  **动态加权方案（GradNorm）：** 为了解决多问题学习中的梯度失衡，引入了基于梯度范数的动态权重调整。通过实时缩放各问题的损失贡献，确保没有任何一个问题类别主导参数更新。
    4.  **模型架构：** 使用异构图神经网络（包含 GINConv 和 GraphConv 层）提取特征，最后通过全连接层输出每个节点被选中的概率。

### 3. 实验设计
*   **问题类别：** 最大独立集 (MIS)、最大团 (MC)、最小顶点覆盖 (MVC)、最小支配集 (MDS)。
*   **数据集：**
    *   社交网络数据集：IMDB, COLLAB, Twitter。
    *   合成基准：RB200。
    *   结构多样化数据集：SparseSuit Matrix Collection（包含电力网络、材料科学等领域的真实图结构）。
*   **基准方法（Benchmarks）：**
    *   无监督 NCO 基准：EGN, Meta-EGN。
    *   传统算法：贪心算法（Greedy）。
    *   精确求解器：Gurobi v12.03（设置不同时间限制：0.2s, 1s, 2s, 4s）。
*   **对比设置：** 分为单问题设置（Single-problem）和多问题联合训练设置（Multi-problem），并评估了跨问题泛化能力（Zero-shot 和 Fine-tuning）。

### 4. 资源与算力
*   **硬件：** 使用了单台配备 **80GB 显存的 NVIDIA A100 GPU**。
*   **训练参数：**
    *   单问题模式：Batch size 64，训练 50 epochs。
    *   多问题模式：每个问题类别 Batch size 32，训练 100 epochs。
    *   优化器：RMSprop，学习率为 0.001。
*   **时长：** 文中未明确给出总训练时长，但提到了推理时间（通常在 0.01s 到 0.2s 之间）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 5 个不同性质的数据集和 4 种经典的 NP-hard 问题，实验组数较多。
*   **消融与变体：** 对比了 UniHetCO-ERM（经验风险最小化）、UniHetCO-SW（静态加权）和 UniHetCO-DW（动态加权）三种变体，验证了动态加权的必要性。
*   **客观性：** 引入了 Gurobi 作为 Oracle（最优解参考），并使用了近似比（Approximation Ratio）作为统一衡量标准。实验设计考虑了结构相似和结构差异巨大的两种场景，评估较为全面。

### 6. 主要结论与发现
*   **性能对标：** UniHetCO 在单问题设置下达到了与 SOTA（如 Meta-EGN）相当的水平，在某些挑战性数据集（如 RB200）上甚至有所超越。
*   **多问题学习可行性：** 证明了使用单一模型同时解决四种不同约束问题的可行性，虽然性能略低于专门针对单一问题训练的模型，但差距较小。
*   **跨问题迁移：** 模型展现了一定的零 shot 迁移能力，且通过极少量的微调（Fine-tuning）即可快速适应新问题类别。
*   **求解器加速：** 将 UniHetCO 的预测作为 Gurobi 的热启动（Warm-start）输入，能显著提升求解器在极短时间限制（0.2s）内的解质量。

### 7. 优点
*   **统一性：** 首次提出通过异构图统一表示目标和约束，实现了真正的“通用型”无监督 NCO 求解器。
*   **硬件友好：** 基于 QUBO 的公式与量子处理器或专用 CMOS 加速器具有天然的映射关系。
*   **鲁棒性：** 引入动态梯度平衡技术，有效解决了多任务学习中常见的训练不稳定问题。

### 8. 不足与局限
*   **计算开销：** 对于非局部约束（Non-local constraints），异构图中的约束节点和边会显著增加图的规模，导致消息传递的计算成本上升，限制了在大规模稠密约束问题上的扩展性。
*   **性能权衡：** 多问题联合训练的模型在绝对精度上仍略逊于针对特定问题高度优化的单任务模型。
*   **超参数敏感：** 尽管有动态加权，但 QUBO 目标项与惩罚项之间的相对比例（$\lambda$）仍可能对最终收敛产生影响。

（完）
