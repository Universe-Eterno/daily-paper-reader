---
title: On the PLS-Completeness of $k$-Opt Local Search for the Traveling Salesman Problem
title_zh: 关于旅行商问题 $k$-Opt 局部搜索的 PLS-完备性研究
authors: "Sophia Heimann, Hung P. Hoang, Stefan Hougardy"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11270v1"
tags: ["query:nco"]
score: 8.0
evidence: 旅行商问题的k-Opt局部搜索算法
tldr: 本研究探讨了旅行商问题（TSP）中 k-Opt 局部搜索算法的计算复杂性。针对 Krentel 早期证明中 k 值过大（需大于1000）且存在逻辑漏洞的问题，本文提供了首个严谨的 PLS-完备性证明。通过优化构造，研究成功将 k 的下限大幅降低至 15，且结论同时适用于一般 TSP 和度量 TSP，解决了该领域长期悬而未决的开放性问题。
selection_source: fresh_fetch
motivation: 现有的 k-Opt 局部搜索 PLS-完备性证明存在逻辑缺陷，且要求的 k 值过大，缺乏严谨性。
method: 采用严谨的理论分析与构造方法，重新审视了 k-Opt 邻域在 TSP 问题中的搜索复杂性。
result: 成功证明了当 k ≥ 15 时，k-Opt 局部搜索在一般和度量 TSP 中均具有 PLS-完备性。
conclusion: 该成果大幅降低了 k-Opt 完备性所需的 k 值，填补了理论空白，深化了对局部搜索算法复杂性的理解。
---

## 摘要
$k$-Opt 算法是针对旅行商问题的一种局部搜索算法。该算法从初始路径开始，通过迭代地将路径中至多 $k$ 条边替换为相同数量的边，以获得更优的路径。Krentel (FOCS 1989) 证明了具有 $k$-Opt 邻域的旅行商问题对于 PLS（多项式时间局部搜索）类是完备的。然而，他的证明要求 $k \gg 1000$ 且存在

## Abstract
The $k$-Opt algorithm is a local search algorithm for the traveling salesman problem. Starting with an initial tour, it iteratively replaces at most $k$ edges in the tour with the same number of edges to obtain a better tour. Krentel (FOCS 1989) showed that the traveling salesman problem with the $k$-Opt neighborhood is complete for the class PLS (polynomial time local search). However, his proof requires $k \gg 1000$ and has a substantial gap. We provide the first rigorous proof for the PLS-completeness and at the same time drastically lower the value of $k$ to $k \geq 15$, addressing an open question by Monien, Dumrauf, and Tscheuschner (ICALP 2010). Our result holds for both the general and the metric traveling salesman problem.

---

## 论文详细总结（自动生成）

这篇论文是关于理论计算机科学中局部搜索复杂性的重要研究，重点探讨了旅行商问题（TSP）中经典的 $k$-Opt 算法的 PLS-完备性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：证明对于多大的 $k$，TSP 的 $k$-Opt 局部搜索算法是 **PLS-完备（Polynomial Time Local Search Complete）** 的。
*   **研究背景**：
    *   $k$-Opt 是解决 TSP 最常用的启发式算法之一，通过迭代替换至多 $k$ 条边来改进路径。
    *   Krentel (1989) 曾声称证明了该问题的 PLS-完备性，但其要求的 $k$ 值极大（$k > 1000$），且证明过程中存在一个重大漏洞（假设权重无穷大的边不会出现在局部最优解中，但未给出严谨证明）。
    *   Monien 等人在 2010 年提出了一个开放性问题：能否在 $k \ll 1000$ 的情况下证明其 PLS-完备性？
*   **整体含义**：本文提供了首个严谨的证明，填补了 Krentel 证明中的漏洞，并将 $k$ 的下限从 1000 以上大幅降低至 **$k \ge 15$**。

### 2. 核心方法论
论文通过从 **最大割（Max-Cut/Flip）问题** 到 **TSP/$k$-Opt** 的 **紧凑 PLS-归约（Tight PLS-reduction）** 来实现证明：
*   **核心思想**：构造一个特殊的 TSP 实例，使得该实例中的“标准路径（Standard Tours）”与最大割问题的解一一对应，且最大割中的一次节点翻转（Flip）对应 TSP 中的一次 $k$-Opt 交换。
*   **关键技术细节**：
    *   **奇偶小部件（Parity Gadgets）**：分为“严格”和“灵活”两种，用于模拟最大割中边的权重贡献。
    *   **异或小部件（XOR Gadgets）**：用于确保在一次 $k$-Opt 交换中，只能模拟最大割中的一个节点翻转。
    *   **非边权重分配（Weight Assignment for Non-edges）**：这是补全 Krentel 证明漏洞的关键。作者定义了一套复杂的优先级规则和权重函数，确保任何包含非边（即不在构造图中的边）的路径都不是局部最优解。
    *   **紧凑归约**：证明了两个问题的转移图（Transition Graph）在标准解空间上是同构的。

### 3. 实验设计
*   **研究性质**：本论文属于**纯理论计算机科学（TCS）**论文，不涉及实证实验、数据集测试或软件运行。
*   **验证方式**：通过严谨的数学引理和定理证明（如 Lemma 17, 18 证明转移对应关系，Lemma 26 证明非边不在局部最优解中）来验证结论。
*   **对比对象**：对比的是 Krentel (1989) 的原始证明以及后续关于 $k$ 值的非正式推测（如有人曾私下声称 $k=8$ 或 $k=6$ 可行，但从未给出书面证明）。

### 4. 资源与算力
*   **算力消耗**：**无**。该研究不涉及任何模型训练、数值模拟或大规模计算。
*   **工具**：主要依赖逻辑推理、图论分析和组合优化理论。

### 5. 实验数量与充分性
*   **理论充分性**：论文通过 22 页的严谨推导，涵盖了从小部件构造、路径映射到权重分配的所有细节。
*   **客观性**：证明过程遵循计算复杂性理论的标准范式，结论具有数学上的确定性。

### 6. 主要结论与发现
*   **PLS-完备性**：当 $k \ge 15$ 时，TSP/$k$-Opt 是 PLS-完备的。这意味着在该邻域内寻找局部最优解在计算上是困难的（除非 PLS $\subseteq$ P）。
*   **适用范围**：该结论同时适用于**一般 TSP** 和满足三角不等式的**度量 TSP（Metric TSP）**。
*   **紧凑性**：该归约是“紧凑的”，这意味着最大割问题的所有指数级搜索路径特征都会遗传给 TSP/$k$-Opt。

### 7. 优点
*   **严谨性**：首次解决了困扰该领域 30 多年的 Krentel 证明漏洞问题。
*   **大幅优化**：将 $k$ 值从 1000+ 降低到 15，极大地接近了实际应用中常用的 $k$ 值。
*   **通用性**：权重分配方案具有创新性，为证明其他局部搜索问题的完备性提供了参考。

### 8. 不足与局限
*   **$k$ 值仍有下降空间**：虽然 15 已经很小，但论文提到目前尚不清楚 $k < 15$（例如 $k=3$ 或 $k=6$）时是否仍然是 PLS-完备的。
*   **构造复杂性**：为了保证严谨性，证明中使用的图构造非常复杂，难以直观想象其在真实 TSP 实例中的表现。
*   **应用限制**：PLS-完备性关注的是最坏情况复杂度，并不直接否定 $k$-Opt 在实际工业场景中的高效性。

（完）
