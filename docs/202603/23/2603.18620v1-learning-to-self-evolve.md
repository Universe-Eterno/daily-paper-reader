---
title: Learning to Self-Evolve
title_zh: 学习自我演化
authors: "Xiaoyin Chen, Canwen Xu, Yite Wang, Boyi Liu, Zhewei Yao, Yuxiong He"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18620v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 用于LLM测试时自演化和上下文优化的强化学习框架
tldr: 本研究提出“学习自我进化”（LSE）框架，通过强化学习训练大语言模型在测试时自主优化上下文。不同于以往依赖模型固有推理能力的方法，LSE将多步进化简化为单步RL目标，并结合树引导进化循环。实验表明，经过LSE训练的4B模型在Text-to-SQL和MMLU任务上超越了GPT-5等强模型驱动的策略及现有提示优化方法，证明了将自我进化视为可学习技能的有效性。
selection_source: fresh_fetch
motivation: 现有模型在测试时自我进化主要依赖其固有的推理能力，而缺乏针对该进化过程的显式训练。
method: 提出LSE框架，将多步进化问题简化为单步强化学习目标，并结合树引导的进化循环来优化上下文编辑。
result: 4B参数的LSE模型在Text-to-SQL和MMLU任务上表现优于GPT-5、Claude 3.5驱动的策略以及TextGrad等提示优化方法。
conclusion: 研究证明了将自我进化视为一种可学习的技能是有效的，且训练出的策略具有良好的跨模型迁移能力。
---

## 摘要
我们介绍了“学习自我演化”（Learning to Self-Evolve, LSE），这是一个强化学习框架，旨在训练大语言模型（LLM）在测试时改进自身的上下文。我们将 LSE 置于测试时自我演化的场景中，模型通过对已见问题的反馈迭代地优化其上下文，从而在处理新问题时表现得更好。现有方法完全依赖于模型的固有推理能力，从未针对该任务进行显式训练。LSE 将多步演化问题简化为单步强化学习目标，其中每次上下文编辑都根据下游性能的提升获得奖励。我们将该目标与树引导的演化循环相结合。在 Text-to-SQL 生成（BIRD）和通用问答（MMLU-Redux）任务上，使用 LSE 训练的 4B 参数模型优于由 GPT-5 和 Claude Sonnet 4.5 驱动的自我演化策略，也优于包括 GEPA 和 TextGrad 在内的提示优化方法，并且无需额外训练即可迁移以指导其他模型。我们的结果强调了将自我演化视为一种可学习技能的有效性。

## Abstract
We introduce Learning to Self-Evolve (LSE), a reinforcement learning framework that trains large language models (LLMs) to improve their own contexts at test time. We situate LSE in the setting of test-time self-evolution, where a model iteratively refines its context from feedback on seen problems to perform better on new ones. Existing approaches rely entirely on the inherent reasoning ability of the model and never explicitly train it for this task. LSE reduces the multi-step evolution problem to a single-step RL objective, where each context edit is rewarded by the improvement in downstream performance. We pair this objective with a tree-guided evolution loop. On Text-to-SQL generation (BIRD) and general question answering (MMLU-Redux), a 4B-parameter model trained with LSE outperforms self-evolving policies powered by GPT-5 and Claude Sonnet 4.5, as well as prompt optimization methods including GEPA and TextGrad, and transfers to guide other models without additional training. Our results highlight the effectiveness of treating self-evolution as a learnable skill.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Self-Evolve》（学习自我演化）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：目前的 LLM 在部署后通常是静态的，无论解决多少问题，其策略和上下文都保持不变，无法像人类一样从经验中积累知识。
*   **核心问题**：现有的“测试时自我演化”方法（如提示优化）主要依赖模型的固有推理能力（即零样本或少样本提示），而没有显式地训练模型如何根据反馈来改进自身。
*   **整体含义**：本文提出 **LSE (Learning to Self-Evolve)** 框架，旨在通过强化学习（RL）将“自我演化”训练成模型的一种显式技能，使模型能够根据历史表现的反馈，在测试阶段迭代地重写和优化自身的指令/上下文。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将复杂的多步演化轨迹简化为单步强化学习目标。模型接收当前上下文和性能摘要，生成改进后的上下文。
*   **关键技术细节**：
    *   **单步 RL 目标**：为了降低信用分配（Credit Assignment）的难度，LSE 专注于单次编辑的质量。
    *   **改进型奖励（Improvement-Based Reward）**：奖励函数定义为 $r_{LSE} = \bar{R}(c_{new}) - \bar{R}(c_{old})$。这种“增量奖励”消除了初始上下文质量带来的偏差，迫使模型专注于“如何改进”而非“维持现状”。
    *   **树引导演化（Tree-Guided Evolution）**：在测试时，不使用简单的线性链式更新，而是维护一个演化树。利用 **UCB（置信上限）算法** 选择节点进行扩展，允许模型在发现效果不佳时进行回溯和分支探索，避免陷入局部最优。
    *   **训练流程**：使用 Qwen3-4B-Instruct 作为基础模型，通过在训练集上生成的演化轨迹进行离线强化学习训练。

### 3. 实验设计
*   **数据集/场景**：
    *   **Text-to-SQL**：使用 **BIRD** 基准测试，涵盖金融、毒理学、代码库等 5 个不同领域的数据库。
    *   **通用问答**：使用 **MMLU-Redux**，选择了 10 个学科（如商业伦理、哲学、解剖学等）。
*   **Benchmark 与对比方法**：
    *   **强模型策略**：使用 **GPT-5** 和 **Claude Sonnet 4.5** 作为自我演化策略（即让这些强模型来优化 4B 模型的提示词）。
    *   **提示优化方法**：对比了 **GEPA**（反射式提示演化）和 **TextGrad**（文本梯度下降）。
    *   **Action Model**：所有实验统一使用 Qwen3-4B-Instruct 作为执行任务的模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 `verl` 强化学习框架，学习率为 $1 \times 10^{-5}$，每批次采样 32 个节点，每个节点生成 4 个 rollout，训练 4 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个大类任务、15 个细分领域进行了测试。
*   **消融实验**：
    *   **奖励设计**：对比了 LSE 增量奖励与标准 GRPO 组内奖励的效果。
    *   **搜索策略**：对比了 UCB 树搜索与线性链式演化的效果。
    *   **迁移性测试**：验证了 LSE 训练的 4B 模型能否指导更强的模型（如 Arctic-Text2SQL-R1-7B）。
*   **充分性评价**：实验设计较为全面，不仅对比了最先进的闭源模型，还对比了专门的提示优化算法，并验证了跨模型的迁移能力，客观性较强。

### 6. 主要结论与发现
*   **小模型胜大模型**：经过 LSE 训练的 **4B 参数模型**，在自我演化任务上的表现超过了未经过针对性训练的 **GPT-5** 和 **Claude Sonnet 4.5**。
*   **增量奖励的必要性**：实验证明，基于性能提升（Delta）的奖励比基于绝对分数的奖励能提供更清晰的训练信号，性能提升约 4.3%。
*   **树搜索的优势**：UCB 树搜索比线性演化平均提升了约 2.2%-2.4% 的准确率，有效防止了因单次错误编辑导致的性能崩溃。
*   **可迁移性**：LSE 训练出的演化策略具有通用性，可以有效提升其他不同架构或经过 RL 微调的模型的性能。

### 7. 优点
*   **创新视角**：将自我演化视为一种可学习的“元技能”，而非仅仅依赖模型的涌现能力。
*   **高效性**：证明了通过针对性训练，极小规模的模型也能在特定元任务上超越顶级闭源模型。
*   **鲁棒性设计**：引入树搜索和 UCB 机制，解决了提示优化中常见的“越改越烂”和不可逆转的问题。

### 8. 不足与局限
*   **单步简化风险**：虽然单步 RL 降低了训练难度，但可能忽略了需要多步规划才能实现的复杂演化策略。
*   **领域局限**：目前主要在 Text-to-SQL 和选择题上验证，对于更开放、无客观评价指标的任务（如创意写作）可能难以应用。
*   **演化范围有限**：目前仅限于修改指令（Instruction），未涉及工具调用、外部记忆库或模型参数的动态调整。
*   **训练成本**：虽然推理成本低，但训练阶段需要生成大量的演化轨迹作为数据，这在扩展到更多领域时可能存在成本挑战。

（完）
