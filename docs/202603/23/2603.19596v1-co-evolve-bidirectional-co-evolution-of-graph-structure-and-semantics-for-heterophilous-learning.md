---
title: "CO-EVOLVE: Bidirectional Co-Evolution of Graph Structure and Semantics for Heterophilous Learning"
title_zh: CO-EVOLVE：面向异质性学习的图结构与语义双向协同演化
authors: "Jinming Xing, Muhammad Shahzad"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19596v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大模型与图神经网络结合用于结构推理
tldr: 针对LLM与GNN结合中存在的双向误差传播、语义结构不一致及盲目对齐等挑战，本文提出CO-EVOLVE框架。该框架采用Gauss-Seidel交替优化策略，实现图拓扑与语义嵌入的双向协同演化：GNN为LLM提供结构上下文提示，LLM则为GNN构建动态语义图。通过引入冲突感知对比损失、自适应节点门控和不确定性对齐机制，该方法在异质图学习任务上取得了显著的性能提升。
selection_source: fresh_fetch
motivation: 现有的LLM与GNN结合方法通常依赖静态单向流水线，导致模型在处理异质图时容易受到误差传播和语义-结构冲突的影响。
method: 提出一种双视图协同演化框架，利用交替优化建立反馈环路，并结合冲突感知对比学习、自适应结构融合及不确定性门控一致性策略。
result: "实验结果显示，CO-EVOLVE在多个基准数据集上显著超越了现有SOTA模型，准确率和F1分数分别平均提高了9.07%和7.19%。"
conclusion: CO-EVOLVE通过动态互惠的演化机制有效解决了异质图学习中的语义与结构失调问题，证明了双向协同优化在图语义理解中的优越性。
---

## 摘要
大语言模型 (LLMs) 与图神经网络 (GNNs) 的集成有望将语义理解与结构推理统一起来，然而现有方法通常依赖于静态、单向的流水线。这些方法存在根本性的局限性：(1) 双向错误传播，即 LLM 中的语义幻觉或 GNN 中的结构噪声会永久性地污染下游模态，且缺乏补救机会；(2) 语义-结构不协调，特别是在异质性场景下，文本相似性与拓扑现实相矛盾；(3) “盲人领路”现象，即不加区分的对齐强制模型在不考虑不确定性的情况下相互模仿对方的错误。为了应对这些挑战，我们提出了 CO-EVOLVE，这是一个双视图协同演化框架，将图拓扑和语义嵌入视为动态、相互增强的隐变量。通过采用 Gauss-Seidel 交替优化策略，我们的框架建立了一个循环反馈回路：GNN 将结构上下文作为软提示 (Soft Prompts) 注入以引导 LLM，而 LLM 则构建有利的动态语义图来重构 GNN 的连接。我们引入了三项关键创新来稳定这一演化过程：(1) 一种硬结构冲突感知对比损失，通过扭曲语义流形以遵循高阶拓扑边界；(2) 一种自适应节点门控机制，动态融合静态和可学习结构以恢复缺失链接；(3) 一种不确定性门控一致性策略，实现元认知对齐，确保模型仅从置信的视图中学习。最后，一种熵感知自适应融合在推理过程中整合预测。在公开基准数据集上的广泛实验表明，CO-EVOLVE 显著优于最先进的基准模型，在准确率和 F1 分数上分别实现了 9.07% 和 7.19% 的平均提升。

## Abstract
The integration of Large Language Models (LLMs) and Graph Neural Networks (GNNs) promises to unify semantic understanding with structural reasoning, yet existing methods typically rely on static, unidirectional pipelines. These approaches suffer from fundamental limitations: (1) Bidirectional Error Propagation, where semantic hallucinations in LLMs or structural noise in GNNs permanently poison the downstream modality without opportunity for recourse; (2) Semantic-Structural Dissonance, particularly in heterophilous settings where textual similarity contradicts topological reality; (3) a Blind Leading the Blind phenomenon, where indiscriminate alignment forces models to mirror each other's mistakes regardless of uncertainty. To address these challenges, we propose CO-EVOLVE, a dual-view co-evolution framework that treats graph topology and semantic embeddings as dynamic, mutually reinforcing latent variables. By employing a Gauss-Seidel alternating optimization strategy, our framework establishes a cyclic feedback loop: the GNN injects structural context as Soft Prompts to guide the LLM, while the LLM constructs favorable Dynamic Semantic Graphs to rewire the GNN. We introduce three key innovations to stabilize this evolution: (1) a Hard-Structure Conflict-Aware Contrastive Loss that warps the semantic manifold to respect high-order topological boundaries; (2) an Adaptive Node Gating Mechanism that dynamically fuses static and learnable structures to recover missing links; (3) an Uncertainty-Gated Consistency strategy that enables meta-cognitive alignment, ensuring models only learn from the confident view. Finally, an Entropy-Aware Adaptive Fusion integrates predictions during inference. Extensive experiments on public benchmarks demonstrate that CO-EVOLVE significantly outperforms state-of-the-art baselines, achieving average improvements of 9.07% in Accuracy and 7.19% in F1-score.

---

## 论文详细总结（自动生成）

### 论文总结：CO-EVOLVE —— 面向异质性学习的图结构与语义双向协同演化

#### 1. 核心问题与研究动机
论文探讨了将大语言模型（LLM）与图神经网络（GNN）集成的挑战。现有方法（如 LLM-as-Encoder 或 GNN-as-Prefix）多采用**静态单向流水线**，存在三大局限：
*   **双向误差传播**：LLM 的语义幻觉或 GNN 的结构噪声会永久污染下游模态，缺乏纠错机制。
*   **语义-结构不协调**：在异质图（Heterophilous Graphs）中，文本相似的节点可能在拓扑上属于不同类别，导致模型误判。
*   **盲目对齐（Blind Alignment）**：不加区分地强制 LLM 和 GNN 输出一致，导致模型在不确定或错误时相互模仿（“盲人领路”）。

#### 2. 方法论
提出了 **CO-EVOLVE** 框架，将图拓扑和语义嵌入视为动态互动的隐变量，通过 **Gauss-Seidel 交替优化策略**实现双向协同演化。其核心组件包括：
*   **结构条件软提示（Structure-Conditioned Soft Prompting）**：利用 GNN 提取节点的全局结构特征，通过 MLP 投影为连续的“软提示”注入 LLM，赋予 LLM 拓扑感知能力，减少幻觉。
*   **自适应语义引导结构学习（Adaptive Semantic-Guided Structure Learning）**：利用 LLM 更新的语义嵌入构建动态语义图，并通过**节点自适应门控机制**融合原始静态图，实现动态“重连”，修复缺失链接。
*   **硬结构冲突感知损失（Hard-Structure Conflict-Aware Loss）**：引入个性化 PageRank（PPR）作为全局拓扑基准，识别“虚假语义朋友”（语义相似但拓扑疏远），通过对比学习强制语义流形尊重高阶结构边界。
*   **不确定性门控一致性（Uncertainty-Gated Consistency）**：基于预测分布的熵来衡量模型置信度，仅在“老师”模型置信度高时才进行 KL 散度对齐，过滤噪声。
*   **推理阶段**：采用多阶段顺序推理，结合结构上下文提取、语义推理、图重构及最终的熵感知自适应融合。

#### 3. 实验设计
*   **数据集**：使用了三个具有文本属性的真实世界数据集：**Reddit**（社交网络）、**Instagram**（社交网络）和 **WikiCS**（维基百科引用网络）。
*   **基准方法（Baselines）**：对比了 10 种方法，涵盖 GNN-only（GCN, GAT）、LM-only（Sent-BERT, RoBERTa）以及 Graph-LLM 混合模型（TAPE, ZeroG, LLaGA, InstructGLM, FLAG, Patton）。
*   **测试场景**：
    1.  标准节点分类基准测试。
    2.  **虚假语义朋友压力测试**：通过篡改 10%-30% 的文本属性模拟语义噪声。
    3.  **缺失结构链接压力测试**：通过删除 10%-30% 的关键结构边模拟结构稀疏性。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 (80GB)** GPU 上运行。
*   **模型配置**：LLM 使用了 **Llama-3.2-1B**，配合 **LoRA**（rank=4）进行参数高效微调；GNN 使用了 2 层 GCN。
*   **训练细节**：使用了 AdamW 优化器，LLM 学习率为 $1 \times 10^{-4}$，GNN 为 $5 \times 10^{-3}$。文中未明确说明总训练时长，但提到了采用交替优化步骤。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度评估，包括主实验、消融实验（针对四个核心组件）、超参数敏感性分析（针对邻域大小 $k$、冲突阈值 $\alpha$ 和 $\epsilon$）。
*   **充分性与客观性**：通过引入“语义篡改”和“边删除”两种极端噪声环境，验证了模型在非理想情况下的鲁棒性。消融实验清晰地展示了每个模块的增益，实验设计较为全面且对比公平。

#### 6. 主要结论与发现
*   **性能提升**：CO-EVOLVE 在所有数据集上均表现优异，准确率平均提升 **9.07%**，F1 分数提升 **7.19%**。
*   **鲁棒性强**：在 30% 语义噪声下，其性能下降远小于 LLaGA 等基准模型；在 30% 边删除情况下，依然能通过语义信息有效恢复结构。
*   **协同效应**：实验证明 LLM 和 GNN 的预测熵在训练过程中同步下降并趋于一致，验证了双向反馈环路的稳定性。

#### 7. 优点
*   **打破静态范式**：通过交替优化实现了真正的双向纠错，解决了单向流水线的误差累积问题。
*   **异质性优化**：专门针对异质图设计了冲突感知损失，有效处理了语义与结构不一致的矛盾。
*   **元认知对齐**：引入不确定性门控，避免了模型在不确定时的盲目模仿，增强了训练稳定性。

#### 8. 不足与局限
*   **计算开销**：虽然使用了轻量级 LLM 和 LoRA，但交替优化和多阶段推理（涉及多次 GNN 和 LLM 前向传播）在超大规模图上的计算成本和延迟可能较高。
*   **超参数依赖**：冲突感知损失和结构学习模块涉及多个阈值（如 $\alpha, \epsilon, k$），虽然有敏感性分析，但在新数据集上可能需要精细调优。
*   **模型规模限制**：实验主要基于 1B 规模的 LLM，尚未探讨该框架在更大规模模型（如 7B 或 70B）上的扩展性及收益递减情况。

（完）
