---
title: "Offline Decision Transformers for Neural Combinatorial Optimization: Surpassing Heuristics on the Traveling Salesman Problem"
title_zh: 用于神经组合优化的离线决策 Transformer：在旅行商问题上超越启发式算法
authors: "Hironori Ohigashi, Shinichiro Hamada"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25241v1"
tags: ["query:nco"]
score: 10.0
evidence: 用于TSP和神经组合优化的决策Transformer
tldr: 针对旅行商问题（TSP）等组合优化难题，本文提出一种基于离线强化学习的决策Transformer框架。该方法通过整合指针网络处理可变动作空间，并利用分位数回归进行乐观的收益条件建模，直接从启发式算法数据集中学习。实验证明，该模型不仅能模仿现有算法，还能通过合成策略超越其训练所用的四种经典启发式算法，展示了离线强化学习在提升组合优化性能方面的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的在线强化学习在组合优化中难以部署且未充分利用现有的启发式算法知识。
method: 结合指针网络处理节点选择的动态动作空间，并采用分位数回归对目标收益进行乐观估计。
result: 实验结果显示，该方法在TSP问题上生成的路径质量始终优于其训练数据来源的四种经典启发式算法。
conclusion: 离线强化学习能够有效整合并超越现有领域知识，为解决复杂的组合优化问题提供了新路径。
---

## 摘要
像旅行商问题（TSP）这样的组合优化问题在工业界至关重要，但属于 NP 困难问题。神经组合优化已展现出潜力，但其对在线强化学习（RL）的依赖阻碍了部署，且未能充分利用数十年的算法知识。我们通过应用离线强化学习框架——决策 Transformer（Decision Transformer）来解决这些局限性，直接从启发式解的数据集中学习更优策略；其目标不仅是模仿，而是综合并超越这些启发式算法。具体而言，我们（i）集成了一个指针网络（Pointer Network）以处理与实例相关的、可变的节点选择动作空间，以及（ii）采用 expectile 回归对待获得回报（Return-to-Go）进行乐观调节，这对于最优值差异巨大的实例至关重要。实验表明，我们的方法始终能产生比其训练所用的四种经典启发式算法更高质量的路径，证明了离线强化学习在释放并超越现有领域知识中所蕴含性能方面的潜力。

## Abstract
Combinatorial optimization problems like the Traveling Salesman Problem are critical in industry yet NP-hard. Neural Combinatorial Optimization has shown promise, but its reliance on online reinforcement learning (RL) hampers deployment and underutilizes decades of algorithmic knowledge. We address these limitations by applying the offline RL framework, Decision Transformer, to learn superior strategies directly from datasets of heuristic solutions; it aims to not only to imitate but to synthesize and outperform them. Concretely, we (i) integrate a Pointer Network to handle the instance-dependent, variable action space of node selection, and (ii) employ expectile regression for optimistic conditioning of Return-to-Go, which is crucial for instances with widely varying optimal values. Experiments show that our method consistently produces higher-quality tours than the four classical heuristics it is trained on, demonstrating the potential of offline RL to unlock and exceed the performance embedded in existing domain knowledge.

---

## 论文详细总结（自动生成）

这是一份关于论文《Offline Decision Transformers for Neural Combinatorial Optimization: Surpassing Heuristics on the Traveling Salesman Problem》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的神经组合优化（NCO）方法大多依赖**在线强化学习（Online RL）**，这在实际工业部署中面临数据获取成本高、环境模拟难等挑战，且未能充分利用人类数十年积累的启发式算法知识。
*   **研究动机**：探索是否可以利用**离线强化学习（Offline RL）**框架，直接从现有的启发式算法数据集中学习，不仅实现对启发式算法的模仿，更通过策略合成实现超越。
*   **整体含义**：本文将决策 Transformer（Decision Transformer, DT）引入旅行商问题（TSP），旨在证明离线学习可以作为一种强大的工具，从次优的领域知识中提取并进化出更优的解决方案。

### 2. 方法论
*   **核心思想**：将 TSP 求解建模为序列建模问题，利用 DT 的条件生成能力，根据目标收益（Return-to-Go, RTG）预测下一步访问的节点。
*   **关键技术细节**：
    *   **集成指针网络（Pointer Network）**：标准 DT 的动作空间是固定的，而 TSP 的动作空间（节点选择）随实例和步骤动态变化。本文在 Transformer 解码器后引入指针机制，通过注意力权重选择节点，并使用节点嵌入向量而非索引作为输入，解决了动作空间的语义一致性问题。
    *   **乐观 RTG 预测（Expectile Regression）**：由于不同 TSP 实例的最优路径长度差异巨大，设定统一的 RTG 目标不现实。模型增加了一个预测头，利用 **Expectile 回归（分位数回归的一种变体）** 预测实例可能达到的最高收益。
    *   **损失函数**：结合了动作预测的交叉熵损失（$L_{CE}$）和 RTG 预测的 Expectile 损失（$L^\alpha_2$）。通过设置超参数 $\alpha > 0.5$（如 0.99），鼓励模型进行“乐观”预测，即瞄准数据集中的最佳轨迹。

### 3. 实验设计
*   **数据集**：2D 欧几里得 TSP 实例，规模包括 $N=20, 50, 100$。每个规模包含 100 万个训练实例。
*   **训练数据来源（Benchmark）**：使用了四种经典的启发式算法生成训练轨迹：
    1.  最近邻算法（Nearest Neighbor, NN）
    2.  最近插入法（Nearest Insertion, NI）
    3.  最远插入法（Farthest Insertion, FI）
    4.  模拟退火算法（Simulated Annealing, SA）
*   **对比方法**：
    *   原始启发式算法（Original Heuristics）
    *   行为克隆（Behavior Cloning, BC）：即不带 RTG 条件的监督学习。
    *   本文提出的 DT 模型。
*   **评价指标**：相对于精确最优解的**最优性差距（Optimality Gap, %）**。

### 4. 资源与算力
*   **硬件设备**：单台服务器，配备 1 张 **NVIDIA GeForce RTX 4090 (24GB VRAM)** 和 Intel Xeon Silver 4314 CPU。
*   **训练时长**：
    *   $N=20$：约 19 小时。
    *   $N=50$：约 1 天 3 小时。
    *   $N=100$：约 1 天 23 小时。
*   **推理速度**：单个实例预测耗时在 0.63 到 1.10 秒之间。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种问题规模和 4 种启发式数据源进行了交叉验证，总计进行了数十组完整的训练与测试。
*   **消融实验**：
    *   对比了固定 RTG（如设为 0 或均值）与预测 RTG 的效果。
    *   研究了 Expectile 回归中 $\alpha$ 参数的影响（0.5, 0.7, 0.99）。
    *   探讨了推理时手动增加 RTG 偏移量（Offset）对性能的进一步提升。
*   **充分性评价**：实验设计较为全面，涵盖了从简单贪心到复杂随机搜索的多种数据源，验证了模型在不同质量数据下的鲁棒性，结果具有客观性。

### 6. 主要结论与发现
*   **超越启发式**：DT 模型在所有测试场景下均显著优于其训练所用的启发式算法。特别是在 SA 数据集上，性能提升接近一倍。
*   **RTG 的关键作用**：带 RTG 条件的 DT 普遍优于行为克隆（BC），证明了目标条件引导模型进行了“策略缝合”，即从多个次优轨迹中组合出更优路径。
*   **乐观预测至关重要**：使用 Expectile 回归（$\alpha=0.99$）预测 RTG 效果最好，说明瞄准数据集中的“上限”而非“平均水平”是离线学习成功的关键。

### 7. 优点
*   **创新性结合**：成功将 DT 框架适配到具有动态动作空间的组合优化问题中。
*   **实用性强**：无需复杂的在线环境模拟，直接利用现有的算法库即可训练，降低了 NCO 的应用门槛。
*   **性能卓越**：证明了离线模型不仅能“学会”算法，还能“优化”算法。

### 8. 不足与局限
*   **泛化性限制**：实验仅限于 2D 欧几里得 TSP，尚未在具有复杂约束（如带时间窗的车辆路径问题）或其他类型的组合优化问题上验证。
*   **对数据多样性的依赖**：在动作模式极其单一的数据集（如 NN）上，DT 的提升空间有限，说明模型性能受限于训练数据的探索程度。
*   **RTG 预测尚不完美**：实验显示手动增加 RTG 偏移量能进一步提升效果，暗示目前的自动预测机制在处理大规模实例时仍显保守。

（完）
