---
title: "Learning to Staff: Offline Reinforcement Learning and Fine-Tuned LLMs for Warehouse Staffing Optimization"
title_zh: 学习人员配置：用于仓库人员配置优化的离线强化学习与微调大语言模型
authors: "Kalle Kujanpää, Yuying Zhu, Kristina Klinkner, Shervin Malmasi"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24883v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 离线强化学习和微调LLM用于仓库人员配置优化
tldr: "本研究探讨了优化半自动化仓库分拣系统实时人员配置的机器学习方法。研究对比了两种路径：一是利用离线强化学习训练基于 Transformer 的策略，在详细历史数据上实现了 2.4% 的吞吐量提升；二是利用大语言模型处理抽象的人类可读状态，通过监督微调和直接偏好优化（DPO）使其性能达到或超过历史基准。研究证明了离线强化学习在特定任务上的高效性，以及 LLM 在人机交互决策支持中的潜力。"
selection_source: fresh_fetch
motivation: 旨在解决半自动化仓库分拣系统中实时人员配置决策的优化问题，以提升运营效率并降低成本。
method: 对比了基于 Transformer 的离线强化学习策略与经过 SFT 和 DPO 微调的大语言模型在模拟环境中的表现。
result: "离线强化学习将吞吐量提高了 2.4%，而微调后的 LLM 在处理抽象状态描述时达到或略微超过了历史基准。"
conclusion: 离线强化学习擅长处理特定任务架构，而 LLM 则能提供人类可读的决策支持并灵活整合管理者偏好。
---

## 摘要
我们研究了用于优化半自动化仓库分拣系统中实时人员配置决策的机器学习方法。运营决策可以在不同的抽象层级上得到支持，并具有不同的权衡。我们在匹配的模拟环境中评估了两种方法。首先，我们利用详细的历史状态表示，通过离线强化学习训练了定制的基于 Transformer 的策略，在学习型模拟器中相比历史基准实现了 2.4% 的吞吐量提升。在高业务量的仓库运营中，这种规模的改进意味着显著的成本节约。其次，我们探索了在抽象的、人类可读的状态描述上运行的大语言模型（LLM）。这些模型非常适合仓库经理利用高层级运营摘要所做的决策。我们系统地比较了提示技术、自动提示优化和微调策略。虽然仅靠提示被证明是不够的，但监督微调结合基于模拟器生成偏好的直接偏好优化（DPO），在手工构建的模拟器中实现了与历史基准持平或略高的性能。我们的研究结果表明，这两种方法都为人工智能辅助的运营决策提供了可行路径。离线强化学习在特定任务架构方面表现出色，而大语言模型支持人类可读的输入，并可与能够纳入经理偏好的迭代反馈循环相结合。

## Abstract
We investigate machine learning approaches for optimizing real-time staffing decisions in semi-automated warehouse sortation systems. Operational decision-making can be supported at different levels of abstraction, with different trade-offs. We evaluate two approaches, each in a matching simulation environment. First, we train custom Transformer-based policies using offline reinforcement learning on detailed historical state representations, achieving a 2.4% throughput improvement over historical baselines in learned simulators. In high-volume warehouse operations, improvements of this size translate to significant savings. Second, we explore LLMs operating on abstracted, human-readable state descriptions. These are a natural fit for decisions that warehouse managers make using high-level operational summaries. We systematically compare prompting techniques, automatic prompt optimization, and fine-tuning strategies. While prompting alone proves insufficient, supervised fine-tuning combined with Direct Preference Optimization on simulator-generated preferences achieves performance that matches or slightly exceeds historical baselines in a hand-crafted simulator. Our findings demonstrate that both approaches offer viable paths toward AI-assisted operational decision-making. Offline RL excels with task-specific architectures. LLMs support human-readable inputs and can be combined with an iterative feedback loop that can incorporate manager preferences.

---

## 论文详细总结（自动生成）

这篇论文由亚马逊（Amazon）的研究团队发表于 ICLR 2026 研讨会，探讨了如何利用机器学习优化仓库分拣系统中的实时人员配置决策。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在半自动化仓库中，经理需要根据吞吐量、缓冲区水平和排队长度，实时重新分配工人以消除瓶颈。
*   **核心挑战**：
    *   **复杂动力学**：并行流水线通过共享缓冲区相互影响，具有高度非线性的状态依赖关系。
    *   **决策成本**：频繁移动工人会产生适应成本和生产力损失。
    *   **动作空间巨大**：工人数量多，可能的分配组合呈指数级增长。
*   **研究动机**：探索两种不同抽象层级的 AI 辅助决策路径：一种是基于详细状态的**离线强化学习（Offline RL）**，另一种是基于人类可读摘要的**微调大语言模型（LLM）**。

### 2. 方法论
论文提出了两种平行的技术路线：

#### A. 离线强化学习（针对详细状态）
*   **核心架构（TGNN）**：开发了一种**Transformer 图神经网络（Transformer-GNN）**。它将时空注意力分解：先进行空间注意力（位置与流水线之间），再进行时间注意力。这解决了标准 Transformer 处理高维时空数据时计算复杂度过高（二次方增长）的问题。
*   **动作空间分解**：为了处理组合爆炸，模型让每个工人独立决定“留守”或“移动到特定目的地”，通过设置概率阈值（<0.1 才移动）来限制不必要的搬迁。
*   **算法**：采用 Actor-Critic 架构，并结合行为正则化（Behavioral Regularization）以防止策略偏离历史数据分布。

#### B. 微调大语言模型（针对抽象状态）
*   **核心思想**：将系统状态（如各线吞吐量、缓冲区百分比）转化为结构化文本，让 LLM 生成 JSON 格式的调配建议。
*   **关键技术**：
    *   **SFT（监督微调）**：在历史人类决策数据上进行指令微调。
    *   **迭代 DPO（直接偏好优化）**：利用模拟器生成“成对”的动作对比（动作 A 产生的吞吐量高于动作 B），通过 DPO 训练模型学习偏好。这种迭代过程模拟了经理的反馈循环。

### 3. 实验设计
*   **数据集**：使用亚马逊真实的仓库运营数据，包含 31,366 个班次（约 50 万次状态转换），涵盖数月跨多个物理站点的 42,000 小时数据。
*   **基准（Benchmark）**：
    *   **历史人类决策（Human Decisions）**：在模拟器中重放人类当时的实际操作。
    *   **无移动基准（No-move Baseline）**：保持初始配置不变。
*   **对比方法**：
    *   **RL 侧**：行为克隆（BC）、微调行为克隆（BC-FT）、离线 Actor-Critic。
    *   **LLM 侧**：Zero-shot 提示、思维链（CoT）、Self-Refine、自动提示优化（OPRO）、SFT、SFT+DPO。

### 4. 资源与算力
*   **离线 RL**：论文强调 TGNN 架构显著提高了效率，所有模型均可在**单张 GPU** 上运行，训练时间缩短了 95%。
*   **LLM**：
    *   提示实验使用了 **Qwen2.5-32B-Instruct**。
    *   微调实验（SFT/DPO）由于计算限制使用了较小的 **Qwen2.5-14B-Instruct**。
*   **具体型号**：文中未明确指出具体的 GPU 型号（如 A100 或 H100），但提到了计算受限是选择较小模型微调的原因。

### 5. 实验数量与充分性
*   **实验规模**：基于数万个真实班次的大规模数据集，具有很强的统计说服力。
*   **消融与鲁棒性**：
    *   针对 RL，在两个不同的学习型模拟器上取平均值以防止过拟合。
    *   针对 LLM，系统比较了从简单提示到复杂迭代优化（DPO）的多种变体。
    *   所有结果均报告了 95% 的置信区间。
*   **客观性**：作者诚实地指出，由于 RL 和 LLM 使用的状态抽象层级不同，无法在同一模拟器下直接横向对比，而是各自与人类基准对标。

### 6. 主要结论与发现
*   **离线 RL 表现最优**：离线 Actor-Critic 在学习型模拟器中比人类基准提升了 **2.4%** 的吞吐量。
*   **LLM 提示词工程不足**：仅靠 Prompting（即使是经过优化的）表现均差于人类基准（约低 2%）。
*   **微调是 LLM 的关键**：经过 SFT+DPO 微调后的 LLM 能够匹配并略微超过人类基准（+0.6%）。
*   **DPO 的价值**：DPO 有效教会了 LLM “什么时候不该动”，解决了 SFT 容易导致过度频繁调配工人的问题。

### 7. 优点
*   **架构创新**：TGNN 成功解决了大规模工业场景下时空数据处理的计算瓶颈。
*   **人机协作导向**：设计初衷是辅助而非取代经理，LLM 的输出具有可读性，方便经理理解和干预。
*   **闭环潜力**：提出的迭代 DPO 框架可以直接将模拟器反馈替换为真实的经理反馈，具备持续进化的潜力。

### 8. 不足与局限
*   **模拟器偏差（Sim-to-Real Gap）**：策略可能学习到了模拟器特有的动力学特征，在真实物理环境中的表现可能存在折扣。
*   **模型规模限制**：由于算力限制，微调阶段使用了较小的模型（14B），可能限制了 LLM 的上限。
*   **静态假设**：研究假设系统动力学是平稳的，未考虑设备故障或极端需求波动等非平稳因素。
*   **评估差异**：RL 和 LLM 在不同的模拟环境下评估，削弱了两条路线直接对比的严谨性。

（完）
