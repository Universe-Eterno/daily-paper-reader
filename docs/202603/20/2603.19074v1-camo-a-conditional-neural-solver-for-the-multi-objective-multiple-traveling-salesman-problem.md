---
title: "CAMO: A Conditional Neural Solver for the Multi-objective Multiple Traveling Salesman Problem"
title_zh: CAMO：一种用于多目标多旅行商问题的条件神经求解器
authors: "Fengxiaoxiao Li, Xiao Mao, Mingfeng Fan, Yifeng Zhang, Yi Li, Tanishq Duhan, Guillaume Sartoretti"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19074v1"
tags: ["query:nco"]
score: 10.0
evidence: 多目标多旅行商问题的神经求解器
tldr: 针对多目标多旅行商问题（MOMTSP）中多智能体协作与多目标权衡的双重挑战，本文提出了CAMO神经求解器。该方法通过条件编码器将偏好向量融合进实例表示，并利用协作解码器通过交替选择智能体和节点来自动回归地构建路径。实验证明，CAMO在逼近帕累托前沿方面优于传统启发式和现有神经方法，且在不同规模问题上展现出良好的泛化能力和实际应用潜力。
selection_source: fresh_fetch
motivation: 现有的学习方法在处理多智能体协调与多目标权衡结合的复杂优化问题时存在局限性。
method: 提出一种包含条件编码器和协作解码器的神经求解器，并采用基于REINFORCE的混合分布训练策略以提升泛化性。
result: CAMO在逼近帕累托前沿的质量上显著优于现有的神经和传统启发式算法，并成功在真实机器人平台上进行了验证。
conclusion: CAMO为解决多目标多智能体路径规划问题提供了一个高效、可泛化且具有实际应用价值的深度学习框架。
---

## 摘要
机器人系统通常需要一组机器人共同访问多个目标，同时优化相互竞争的目标，例如总旅行成本和完工时间。这一场景可以建模为多目标多旅行商问题（MOMTSP）。尽管基于学习的方法在单智能体 TSP 和多目标 TSP 变体上表现出强大的性能，但它们很少同时解决多智能体协作和多目标权衡带来的挑战，而这两者引入了双重复杂性。为了填补这一空白，我们提出了 CAMO，这是一种用于 MOMTSP 的条件神经求解器，它可以在不同数量的目标、智能体和偏好向量之间进行泛化，并生成帕累托前沿（PF）的高质量近似。具体而言，CAMO 由一个将偏好融合到实例表示中的条件编码器（以实现对多目标权衡的显式控制）以及一个通过交替进行智能体选择和节点选择来协调所有智能体并自回归地构建多智能体路径的协作解码器组成。为了进一步提高泛化能力，我们使用基于 REINFORCE 的目标函数在混合的问题规模分布上对 CAMO 进行训练。广泛的实验表明，CAMO 的性能优于神经启发式算法和传统启发式算法，实现了更接近的 PF 近似。此外，消融实验结果验证了 CAMO 关键组件的贡献，在移动机器人平台上的真实世界测试证明了其实际应用价值。

## Abstract
Robotic systems often require a team of robots to collectively visit multiple targets while optimizing competing objectives, such as total travel cost and makespan. This setting can be formulated as the Multi-Objective Multiple Traveling Salesman Problem (MOMTSP). Although learning-based methods have shown strong performance on the single-agent TSP and multi-objective TSP variants, they rarely address the combined challenges of multi-agent coordination and multi-objective trade-offs, which introduce dual sources of complexity. To bridge this gap, we propose CAMO, a conditional neural solver for MOMTSP that generalizes across varying numbers of targets, agents, and preference vectors, and yields high-quality approximations to the Pareto front (PF). Specifically, CAMO consists of a conditional encoder to fuse preferences into instance representations, enabling explicit control over multi-objective trade-offs, and a collaborative decoder that coordinates all agents by alternating agent selection and node selection to construct multi-agent tours autoregressively. To further improve generalization, we train CAMO with a REINFORCE-based objective over a mixed distribution of problem sizes. Extensive experiments show that CAMO outperforms both neural and conventional heuristics, achieving a closer approximation of PFs. In addition, ablation results validate the contributions of CAMO's key components, and real-world tests on a mobile robot platform demonstrate its practical applicability.

---

## 论文详细总结（自动生成）

以下是对论文《CAMO: A Conditional Neural Solver for the Multi-objective Multiple Traveling Salesman Problem》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效解决**多目标多旅行商问题（MOMTSP）**。该问题要求一组机器人访问多个目标，同时优化相互竞争的目标（如最小化总路径长度 Min-Sum 和最小化最大完工时间 Min-Max）。
*   **研究背景**：
    *   MOMTSP 具有双重复杂性：**多智能体协作**（任务分配与顺序规划）与**多目标权衡**（帕累托前沿 PF 的逼近）。
    *   传统进化算法（MOEAs）计算成本高、实时性差，难以应对动态任务。
    *   现有神经求解器大多针对单智能体或单目标场景，难以同时处理多机器人协调和连续的偏好插值。

### 2. 方法论
CAMO 采用基于注意力机制的编码器-解码器架构，核心思想是通过偏好向量显式控制目标权衡，并利用协作机制构建路径。
*   **条件编码器 (Conditional Encoder)**：
    *   **条件注意力 (CA)**：通过特征线性变换（FWA）将偏好向量 $\lambda$ 注入节点嵌入，使模型能感知不同目标的权重。
    *   **门控聚合 (GA)**：替代传统的残差连接，通过门控机制调节信息流，增强表示能力。
*   **协作解码器 (Collaborative Decoder)**：
    *   **智能体选择模块**：基于全局图信息和各智能体的当前状态（坐标、已行驶距离），动态决定下一个行动的智能体。
    *   **节点选择模块**：为选定的智能体从剩余未访问节点中挑选最优目标。
    *   这种“交替选择”机制允许模型处理变动数量的智能体和节点。
*   **训练策略**：
    *   使用 **REINFORCE** 强化学习算法。
    *   采用 **Tchebycheff (TC) 分解法**将多目标奖励标量化。
    *   **混合规模训练**：在训练过程中随机采样不同数量的节点（20-100）和智能体（2-4），以提升跨规模泛化性。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成数据**：在单位正方形内随机生成的实例，涵盖 N20A2 到 N200A6 等多种规模。
    *   **基准测试**：使用 TSPLIB 标准库实例（如 ulysses22, kroA150 等）进行跨分布验证。
    *   **真实验证**：在包含 3 台移动机器人和 50 个任务节点的物理平台上进行实测。
*   **对比基准 (Benchmarks)**：
    *   **传统算法**：MOEA/D, MOGLS, NSGA-II, NSGA-III（均配备了针对 TSP 优化的局部搜索算子）。
    *   **神经方法**：MO-PARCO（将最先进的多智能体神经求解器 PARCO 扩展为多目标版本）。
*   **评价指标**：超体积（Hypervolume, HV）、Gap（与最优结果的差距）、运行时间（Time）。

### 4. 资源与算力
*   **硬件环境**：NVIDIA Ampere **A100 GPU** 和 AMD EPYC 7742 CPU。
*   **训练细节**：
    *   训练 400 个 epoch，每个 epoch 包含 100,000 个实例。
    *   Batch size 为 64，学习率为 $10^{-4}$。
    *   **训练时长**：文中对比显示，采用混合采样策略的 CAMO 训练耗时约 **70.42 小时**，比固定规模训练（168.62 小时）效率更高。

### 5. 实验数量与充分性
*   **实验规模**：
    *   基础对比：3 种同分布规模。
    *   泛化测试：5 种未见过的更大规模（最高至 200 节点、6 智能体）。
    *   跨分布测试：10 个 TSPLIB 经典实例。
    *   消融实验：针对 GA 模块、CA 机制和采样策略做了 3 组对比。
*   **充分性评价**：实验设计非常全面，不仅对比了经典的进化算法，还对比了最新的神经方法。通过 TSPLIB 和物理机器人实验，验证了模型在分布偏移和实际应用中的鲁棒性，实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：CAMO 在所有测试规模上的 HV 指标均优于传统 MOEAs 和 MO-PARCO，Gap 显著更小。
*   **效率极高**：在处理 100 节点问题时，CAMO 仅需不到 1 分钟，而传统算法需要 4 小时以上。
*   **泛化能力强**：模型在未见过的更大规模问题和不同分布的 TSPLIB 实例上依然保持领先，证明了混合规模训练的有效性。
*   **实时性**：能够根据不同的偏好向量实时生成对应的帕累托解。

### 7. 优点
*   **架构创新**：协作解码器巧妙解决了多智能体任务分配中的动态协调问题。
*   **偏好感知**：条件注意力机制实现了对多目标权衡的精细化控制。
*   **训练高效**：混合分布训练策略不仅提升了泛化性，还缩短了总训练时间。
*   **闭环验证**：从理论模型到仿真实验，再到物理机器人部署，形成了完整的验证闭环。

### 8. 不足与局限
*   **目标维度**：实验主要集中在双目标优化，对于三目标及以上（Many-objective）的性能表现尚待验证。
*   **约束限制**：未考虑更复杂的现实约束，如机器人的电池电量限制、节点访问的时间窗或避障动态。
*   **目标不平衡**：论文提到未来需进一步研究如何处理多智能体场景中常见的目标量级不平衡问题。

（完）
