---
title: A common parallel framework for LLP combinatorial problems
title_zh: 一种用于 LLP 组合问题的通用并行框架
authors: "David Ribeiro Alves, Vijay K. Garg"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13147v1"
tags: ["query:nco"]
score: 8.0
evidence: 解决最短路径等组合优化问题的并行框架
tldr: 针对传统无锁并行算法在处理组合优化问题时需编写特定同步代码的局限，本文提出了通用无锁运行时框架 LLP-FW。该框架基于格线性谓词（LLP）理论，通过并行推进所有禁用的局部状态直至获得解，开发者仅需定义禁用检查和推进规则。实验表明，LLP-FW 在 SSSP、稳定匹配、作业调度等七类典型问题上表现优异，多数情况下性能可媲美甚至超越手工调优的定制化方案。
selection_source: fresh_fetch
motivation: 传统无锁并行算法需要针对特定问题编写复杂的同步代码，缺乏通用性且开发难度大。
method: 提出通用无锁运行时框架 LLP-FW，通过并行推进格线性谓词（LLP）中的禁用局部状态来求解组合优化问题。
result: 在 SSSP、BFS、稳定匹配等七类问题上，LLP-FW 的性能在多数情况下优于或等同于手工优化的定制方案。
conclusion: LLP-FW 为多种组合优化问题提供了一个高效且通用的并行求解框架，显著简化了并行算法的开发流程。
---

## 摘要
传统的用于组合优化问题（如最短路径、稳定匹配和作业调度）的无锁并行算法要求程序员编写特定于问题的例程和同步代码。我们提出了一种通用的无锁运行时 LLP-FW，它可以通过并行推进所有禁忌局部状态（forbidden local states）直到解出现，从而解决所有可以表述为格线性谓词（Lattice-Linear Predicate）的组合优化问题。唯一特定于问题的代码是禁忌性检查（forbiddenness check）的定义和推进（advancement）的定义。我们展示了 LLP-FW 可以解决几种不同的组合优化问题，例如单源最短路径 (SSSP)、广度优先搜索 (BFS)、稳定婚姻、作业调度、传递闭包、并行归约和 0-1 背包问题。我们将 LLP-FW 与这七个问题的经过手动调优的定制解决方案进行了对比，结果表明它在大多数情况下都具有竞争优势。

## Abstract
Traditional lock-free parallel algorithms for combinatorial optimization problems, such as shortest paths, stable matching, and job scheduling require programmers to write problem-specific routines and synchronization code. We propose a general-purpose lock-free runtime, LLP-FW that can solve all combinatorial optimization problems that can be formulated as a Lattice-Linear Predicate by advancing all forbidden local states in parallel until a solution emerges. The only problem-specific code is a definition of the forbiddenness check and a definition of the advancement. We show that LLP-FW can solve several different combinatorial optimization problems, such as Single Source Shortest Paths (SSSP), Breadth-First Search (BFS), Stable Marriage, Job Scheduling, Transitive Closure, Parallel Reduction, and 0-1 Knapsack. We compare LLP-FW against hand-tuned, custom solutions for these seven problems and show that it compares favorably in the majority of cases.

---

## 论文详细总结（自动生成）

### 论文总结：用于 LLP 组合问题的通用并行框架 (LLP-FW)

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的并行组合优化算法（如最短路径、稳定匹配等）通常是“问题特定”的。开发者必须为每个问题编写复杂的同步代码、定制数据结构和调优策略，这导致了大量的重复工程工作，且难以复用并发控制的优化成果。
*   **研究动机**：探索是否能将“检测禁用状态并并行推进”这一模式提取为一个通用的、无锁的运行时框架，使得开发者只需定义问题的数学属性，而无需处理底层的并发细节。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：基于**格线性谓词（Lattice-Linear Predicate, LLP）**理论。将组合优化问题建模为一个解的格（Lattice），如果当前状态不满足谓词（即非法），则必然存在至少一个“禁用（Forbidden）”维度。通过并行且单调地推进这些禁用状态，系统最终会收敛到最小可行解。
*   **关键技术细节**：
    *   **解耦设计**：开发者只需实现 `is_forbidden`（检查状态是否非法）和 `advance`（如何更新状态）两个核心函数。
    *   **无锁运行时**：利用原子操作（如 Compare-and-Swap, CAS）确保状态更新的单调性和正确性，无需使用传统的锁机制。
    *   **调度策略（Worklists）**：框架提供了多种工作列表管理策略，包括共享工作包（Shared Work Bag）、每线程私有工作包（Per-Thread Work Bag, PTWB）、桶调度（Buckets）和块状 FIFO（PTCF），以适应不同问题的拓扑结构。
    *   **固定状态备忘录**：通过位图记录已达到最终状态的元素，避免重复检查，显著提升深层依赖问题的效率。

#### 3. 实验设计
*   **覆盖问题**：单源最短路径 (SSSP)、广度优先搜索 (BFS)、稳定婚姻 (SM)、作业调度 (Job Scheduling)、传递闭包 (Transitive Closure)、并行归约 (Reduction)、0-1 背包问题 (Knapsack)。
*   **数据集**：
    *   **图算法**：路网（Asia_rand）、幂律图（Kronecker/RMAT）、引用网络、合成 DAG。
    *   **其他**：随机生成的偏好列表（稳定婚姻）、合成作业依赖图、随机权重的背包项。
*   **Benchmark 对比**：
    *   SSSP：对比经过调优的 $\Delta$-stepping 算法。
    *   稳定婚姻：对比并行 Gale-Shapley 算法。
    *   传递闭包：对比并行 Floyd-Warshall 算法。
    *   其他：对比各领域标准的并行或顺序基准实现。

#### 4. 资源与算力
*   **硬件环境**：AWS c7i.16xlarge 虚拟机。
*   **配置**：Intel Xeon Platinum 8375C CPU (2.3 GHz)，128 GB RAM，NVMe 存储。
*   **线程数**：实验覆盖了从 1 到 32 个线程的扩展性测试。
*   **软件**：使用 Rust 语言实现，利用 `rayon` 库进行线程管理，`criterion` 进行基准测试。

#### 5. 实验数量与充分性
*   **实验规模**：论文对 7 类完全不同的组合优化问题进行了深入评估，每类问题都包含多个不同规模和特征的数据集。
*   **消融实验**：针对不同的调度策略（Worklist strategies）进行了详细的对比实验，分析了不同策略对特定问题的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅展示了框架领先的场景（如稳定婚姻、路网 BFS），也诚实地分析了框架落后的场景（如并行归约、稠密图），并从理论层面解释了原因（如原子操作开销与前沿宽度）。

#### 6. 主要结论与发现
*   **性能表现**：在具有“窄前沿（Narrow Frontier）”和级联依赖的问题上表现极佳。例如，稳定婚姻问题比并行基准快 **246倍**，路网 BFS 快 **16倍**。
*   **决定因素**：**“禁用前沿（Forbidden Frontier）”的形状**是预测 LLP-FW 性能的最佳指标。窄而深的前沿有利于 LLP-FW 的无锁推进；宽而浅的前沿（如稠密图）则会导致原子操作开销过大。
*   **通用性代价**：在高度规则、受带宽限制的任务（如并行归约）中，LLP-FW 由于每步操作的原子开销，性能比定制化方案慢 4-5 倍。

#### 7. 优点
*   **高度抽象与复用**：实现了算法逻辑与并发调度的完全解耦，极大降低了并行算法的开发门槛。
*   **无锁高效**：通过精心设计的无锁数据结构，在处理不规则计算任务时展现出比传统同步机制更好的扩展性。
*   **理论支撑扎实**：基于成熟的格理论，保证了算法的收敛性和正确性。

#### 8. 不足与局限
*   **单线程开销**：由于必须使用原子操作和谓词检查，LLP-FW 在单线程下的绝对速度通常慢于非原子的顺序算法（约 2-10 倍）。
*   **稠密问题劣势**：在所有元素几乎同时变为“禁用”状态的稠密问题中，框架的协调开销会抵消并行收益。
*   **内存压力**：某些实现（如传递闭包）在处理大规模稠密矩阵时存在较高的内存消耗。

（完）
