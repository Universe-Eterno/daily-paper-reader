---
title: Deep Reinforcement Learning-Assisted Automated Operator Portfolio for Constrained Multi-objective Optimization
title_zh: 深度强化学习辅助的约束多目标优化自动算子组合
authors: "Shuai Shao, Ye Tian, Shangshang Yang, Xingyi Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16401v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 深度强化学习用于多目标优化中的算子选择
tldr: 针对约束多目标优化问题（CMOPs）中固定算子或单一算子选择策略通用性差、易陷入局部最优的问题，本文提出一种基于深度强化学习的自动化算子组合方法（CMOEA-AOP）。该方法通过学习种群状态与算子分配方案之间的映射，在每一代动态确定最优算子组合，有效提升了算法在不同基准测试中的收敛性、多样性和稳定性。
selection_source: fresh_fetch
motivation: 现有的约束多目标进化算法通常使用固定或单一算子，难以适应复杂的种群状态并充分利用评价函数。
method: 利用深度强化学习构建种群特征与算子组合之间的映射模型，根据收敛性和多样性的综合提升动态分配算子比例。
result: 在33个基准问题上的实验结果表明，该方法显著增强了现有算法的性能，并在不同问题上表现出更强的稳定性。
conclusion: 自动化算子组合策略能有效提升约束多目标优化问题的求解效率，为进化算法的自适应算子选择提供了新思路。
---

## 摘要
约束多目标优化问题（CMOPs）在从科学到工程领域的实际应用中具有重要意义。大多数现有的约束多目标进化算法（CMOEAs）通常始终采用固定的算子，这在处理各种 CMOPs 时表现出较差的通用性。因此，最近的一些研究集中在搜索过程中根据当前种群状态自适应地选择最佳算子。这些研究中提出的进化算法学习每个算子的价值，并为当前种群推荐价值最高的算子，导致每一代仅推荐单个算子，这可能导致陷入局部最优以及函数评估利用效率低下。为了解决算子自适应中的困境，本文提出了一种基于强化学习的自动算子组合方法，用于学习每一代的算子分配方案。该方法将当前种群的优化相关和约束相关特征视为状态，将种群收敛性和多样性的整体提升视为奖励，并将不同的算子组合视为动作。通过利用深度神经网络建立种群状态与预期累积奖励之间的映射模型，所提方法在进化过程中确定最优算子组合。通过将该方法嵌入到现有的 CMOEAs 中，开发了一种用于解决 CMOPs 的深度强化学习辅助自动算子组合进化算法，简称 CMOEA-AOP。对 33 个基准问题的实证研究表明，所提算法显著增强了 CMOEAs 的性能，并在不同的 CMOPs 中表现出更稳定的性能。

## Abstract
Constrained multi-objective optimization problems (CMOPs) are of great significance in the context of practical applications, ranging from scientific to engineering domains. Most existing constrained multi-objective evolutionary algorithms (CMOEAs) usually employ fixed operators all the time, which exhibit poor versatility in handling various CMOPs. Therefore, some recent studies have focused on adaptively selecting the best operators for the current population states during the search process. The evolutionary algorithms proposed in these studies learn the value of each operator and recommend the operator with the highest value for the current population, resulting in only a single operator being recommended at each generation, which can potentially lead to local optima and inefficient utilization of function evaluations. To address the dilemma in operator adaptation, this paper proposes a reinforcement learning-based automated operator portfolio approach to learn an allocation scheme of operators at each generation. This approach considers the optimization-related and constraint-related features of the current population as states, the overall improvement in population convergence and diversity as rewards, and different operator portfolios as actions. By utilizing deep neural networks to establish a mapping model between the population states and the expected cumulative rewards, the proposed approach determines the optimal operator portfolio during the evolutionary process. By embedding the proposed approach into existing CMOEAs, a deep reinforcement learning-assisted automated operator portfolio based evolutionary algorithm for solving CMOPs, abbreviated as CMOEA-AOP, is developed. Empirical studies on 33 benchmark problems demonstrate that the proposed algorithm significantly enhances the performance of CMOEAs and exhibits more stable performance across different CMOPs.

---

## 论文详细总结（自动生成）

### 论文总结：深度强化学习辅助的约束多目标优化自动算子组合 (CMOEA-AOP)

#### 1. 核心问题与整体含义（研究动机和背景）
约束多目标优化问题（CMOPs）要求在满足多个约束条件的同时，优化多个相互冲突的目标。现有的约束多目标进化算法（CMOEAs）主要面临两个困境：
*   **算子固定化**：大多数算法在整个搜索过程中使用固定的变异/交叉算子，缺乏对不同问题特征的适应性。
*   **单一选择策略的局限**：虽然近期有研究利用强化学习（RL）来选择算子，但通常每一代只推荐**单个**最优算子。这种“赢者通吃”的模式导致搜索范式过于单一，容易陷入局部最优，且无法充分利用单次迭代中的函数评估效率。
**研究动机**：通过深度强化学习（DRL）学习一种**算子组合（Portfolio）**方案，即在每一代动态分配不同算子的使用比例，以平衡探索（Exploration）与利用（Exploitation）。

#### 2. 论文提出的方法论
论文提出了 **CMOEA-AOP** 框架，其核心思想是将算子选择视为一个连续空间的决策问题：
*   **核心思想**：利用 DDPG（深度确定性策略梯度）算法，根据当前种群的状态，输出一组算子的比例分配（Action），从而在单次迭代中结合多种搜索范式。
*   **关键技术细节**：
    *   **状态（States）**：提取种群的四个维度特征：收敛性（各目标均值）、多样性（各目标方差）、可行性（平均约束违反程度 CV）以及进化阶段（当前评价次数占比）。
    *   **动作（Actions）**：输出一个连续向量，代表遗传算子（GA）、DE/rand/1 和 DE/best/1 三种算子的使用比例（通过 Softmax 归一化）。
    *   **奖励（Rewards）**：采用超体积（Hypervolume, HV）指标的改进值作为奖励信号，综合评价种群在收敛性和多样性上的提升。
    *   **算法流程**：Agent（包含 Actor 和 Critic 网络）与进化环境交互，将经验存入经验池，定期抽取 Batch 数据更新网络参数，动态指导下一代的算子分配。

#### 3. 实验设计
*   **数据集/场景**：共使用了 33 个标准基准问题，涵盖了不同难度的约束和目标空间特征：
    *   **CF 系列** (CF1–CF10)
    *   **LIR-CMOP 系列** (LIR-CMOP1–LIR-CMOP14，具有大面积不可行域)
    *   **DAS-CMOP 系列** (DAS-CMOP1–DAS-CMOP9，难度可调且具有可扩展性)
*   **对比方法（Benchmarks）**：
    *   经典及前沿 CMOEAs：**EMCMO**（多任务框架）、**Bico**（双向进化）、**AGEMOEA-II**（几何建模）、**TSTI**（三指标两阶段）。
    *   基于 RL 的算子选择算法：**DRLOS**（同样基于 EMCMO 但仅选择单一算子）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、显存数量）或具体的训练时长。
*   **软件环境**：实验基于 MATLAB 平台上的进化计算框架 **PlatEMO** 实现。由于涉及深度神经网络（DDPG）的在线训练，其计算开销通常高于传统进化算法。

#### 5. 实验数量与充分性
*   **实验规模**：在 33 个测试实例上进行了实验，每个算法独立运行 **30 次**。
*   **评价指标**：使用反转世代距离（IGD）作为主要性能指标。
*   **统计检验**：采用了 **Wilcoxon 秩和检验**（显著性水平 0.05），确保结果的统计显著性。
*   **消融实验**：对比了 CMOEA-AOP 与仅使用单一算子（GA, DE/rand, DE/best）的三个变体，验证了“算子组合”策略的有效性。
*   **充分性评价**：实验设计涵盖了多种类型的约束挑战，对比方法具有代表性，统计分析严谨，实验设计较为充分且客观。

#### 6. 主要结论与发现
*   **性能卓越**：CMOEA-AOP 在 33 个问题中的 23 个上取得了最优 IGD 值，显著优于 EMCMO 和 DRLOS 等对比算法。
*   **组合优于单一**：实验证明，动态分配算子比例比每代只选一个算子更能有效跳出局部最优，尤其在处理具有复杂、断裂可行域的问题（如 LIR-CMOP12）时表现更优。
*   **收敛速度快**：在相同评价次数下，CMOEA-AOP 的收敛曲线通常位于对比算法下方，显示出更高的搜索效率。

#### 7. 优点
*   **搜索范式创新**：打破了“单代单算子”的限制，通过连续动作空间实现了算子的精细化协同。
*   **特征表征全面**：状态空间同时考虑了优化目标和约束违反情况，使 Agent 能感知种群是否进入了可行域或陷入停滞。
*   **通用性强**：该算子组合模块可以作为插件嵌入到多种现有的 CMOEA 框架中（如文中嵌入到 EMCMO）。

#### 8. 不足与局限
*   **计算开销**：在线训练深度神经网络（Actor-Critic）会显著增加算法的运行时间，论文未对时间复杂度进行详细量化分析。
*   **算子库规模**：目前仅集成了三种基础算子（GA 和两种 DE 变体），对于更复杂的算子（如群体智能或生成模型算子）的扩展性有待验证。
*   **约束处理深度**：目前将所有约束违反量（CV）求和作为状态特征，忽略了不同约束函数之间的差异性，未来可考虑对每个约束进行单独建模。

（完）
