---
title: "Spend Less, Reason Better: Budget-Aware Value Tree Search for LLM Agents"
title_zh: 花费更少，推理更好：面向大语言模型智能体的预算感知价值树搜索
authors: "Yushu Li, Wenlong Deng, Jiajin Li, Xiaoxiao Li"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12634v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 基于LLM的价值树搜索用于多步推理
tldr: 针对LLM智能体在推理时过度消耗计算资源的问题，本文提出BAVT框架。这是一种无需训练的推理时搜索方法，通过动态搜索树和步级价值评估引导推理。其核心创新在于预算感知的节点选择机制和残差价值预测器，实现了从探索到利用的平滑过渡，并有效剪枝冗余步骤。实验证明，BAVT在低预算下表现优于4倍资源的基线方法，显著提升了推理效率。
selection_source: fresh_fetch
motivation: 现有的LLM推理增强方法往往忽略计算成本，导致在冗余步骤或错误路径上浪费大量Token和工具调用资源。
method: 提出一种无需训练的预算感知价值树（BAVT）框架，通过步级残差价值预测和动态预算调节机制来优化搜索路径。
result: 在多项多跳问答任务中，BAVT在极低预算下的表现甚至超过了消耗4倍资源的传统并行采样基线。
conclusion: 研究证明了智能的预算分配与管理在提升LLM智能体可靠性方面比单纯的暴力计算缩放更为有效。
---

## 摘要
测试时扩展（Test-time scaling）已成为提高大语言模型（LLM）智能体可靠性的主流范式，但目前的方法将计算视为一种充裕的资源，导致智能体在冗余步骤或死胡同轨迹上耗尽 Token 和工具预算。现有的预算感知方法要么需要昂贵的微调，要么依赖于粗粒度的轨迹级启发式方法，无法在执行过程中进行干预。我们提出了预算感知价值树（BAVT），这是一种无需训练的推理阶段框架，它在单个 LLM 主干网络中，通过步骤级价值估计引导，将多跳推理建模为动态搜索树。另一项关键创新是预算调节的节点选择机制，该机制利用剩余资源比例作为节点价值的自然缩放指数，在预算耗尽时，提供了一种从广泛探索到贪婪利用的有原则、无参数转换。为了应对 LLM 自我评估中众所周知的过度自信问题，BAVT 采用了残差价值预测器，对相对进展而非绝对状态质量进行评分，从而能够可靠地剪枝无信息或冗余的工具调用。我们进一步提供了理论收敛保证，证明在显式有限预算约束下，BAVT 以至少 $1-ε$ 的概率得出最终答案。在两个模型系列的四个多跳问答基准测试上的广泛评估表明，BAVT 始终优于并行采样基准。最值得注意的是，在严格的低预算约束下，BAVT 的表现超过了 4 倍资源分配下的基准性能，证明了智能预算管理在根本上优于暴力计算扩展。

## Abstract
Test-time scaling has become a dominant paradigm for improving LLM agent reliability, yet current approaches treat compute as an abundant resource, allowing agents to exhaust token and tool budgets on redundant steps or dead-end trajectories. Existing budget-aware methods either require expensive fine-tuning or rely on coarse, trajectory-level heuristics that cannot intervene mid-execution. We propose the Budget-Aware Value Tree (BAVT), a training-free inference-time framework that models multi-hop reasoning as a dynamic search tree guided by step-level value estimation within a single LLM backbone. Another key innovation is a budget-conditioned node selection mechanism that uses the remaining resource ratio as a natural scaling exponent over node values, providing a principled, parameter-free transition from broad exploration to greedy exploitation as the budget depletes. To combat the well-known overconfidence of LLM self-evaluation, BAVT employs a residual value predictor that scores relative progress rather than absolute state quality, enabling reliable pruning of uninformative or redundant tool calls. We further provide a theoretical convergence guarantee, proving that BAVT reaches a terminal answer with probability at least $1-ε$ under an explicit finite budget bound. Extensive evaluations on four multi-hop QA benchmarks across two model families demonstrate that BAVT consistently outperforms parallel sampling baselines. Most notably, BAVT under strict low-budget constraints surpasses baseline performance at $4\times$ the resource allocation, establishing that intelligent budget management fundamentally outperforms brute-force compute scaling.

---

## 论文详细总结（自动生成）

### 论文总结：Spend Less, Reason Better (BAVT)

#### 1. 核心问题与整体含义（研究动机和背景）
当前提升 LLM 智能体可靠性的主流方法是“测试时扩展”（Test-time scaling），即在推理阶段投入更多计算资源（如多次采样、搜索）。然而，现有方法通常假设资源无限，导致智能体在冗余步骤或死胡同路径上浪费大量的 Token 和工具调用预算。现有的预算管理方案要么需要昂贵的微调，要么只能在轨迹层面进行粗粒度干预。
**核心问题：** 如何在严格的计算预算约束下，通过精细化的步骤级控制，让智能体实现更高的任务准确率？

#### 2. 方法论：核心思想与关键技术
论文提出了 **预算感知价值树（BAVT）**，这是一个无需训练的推理时框架，包含三个核心支柱：
*   **测试时扩展树（Test-Time Scaling Tree）：** 将推理过程建模为动态搜索树，节点代表中间状态，边代表动作或工具调用。这允许智能体探索多条路径而非陷入单一死胡同。
*   **步级价值估计（Step-Level Value Estimation）：** 引入“残差价值预测器”。为了克服 LLM 自我评估过度自信的问题，该预测器不评分绝对状态，而是评分当前步骤带来的“边际信息增益”（即残差 $\Delta_t$）。
*   **预算感知节点扩展（Budget-Aware Expansion）：** 这是最核心的创新。系统根据剩余资源比例 $r_t$ 动态调整节点选择的概率分布。
    *   **公式逻辑：** 使用缩放指数 $\alpha_t = 1/r_t$。当预算充足时（$r_t \approx 1$），分布较平缓，鼓励广泛探索；当预算枯竭时（$r_t \to 0$），$\alpha_t$ 增大，分布变得尖锐，强制系统转向“贪婪利用”，优先扩展价值最高的节点并尽快生成答案。
*   **强制兜底机制（Backstop）：** 当预算即将耗尽且未找到答案时，强制系统基于当前最优状态合成最终答案。

#### 3. 实验设计
*   **数据集：** 四个复杂的多跳问答（Multi-hop QA）基准：HotpotQA、2WikiMultihopQA、MuSiQue 和 Bamboogle。这些任务必须通过多次工具调用获取外部信息才能完成。
*   **评估模型：** 两种不同架构的模型：GPT-OSS-20B（推理模型）和 Qwen3-30B-Instruct（指令微调模型）。
*   **基线方法（Benchmark）：** 预算约束下的“并行采样（Parallel Sampling）+ 多数投票”。在相同总预算下，基线尽可能多地运行独立轨迹。
*   **预算设置：** 分为低（5次工具调用）、中（10次）、高（20次）三个层级。

#### 4. 资源与算力
*   **训练算力：** 论文明确指出 BAVT 是 **Training-free（无需训练）** 的，因此没有模型训练阶段的算力消耗。
*   **推理环境：** 使用了 Inspect AI 评估框架，检索工具基于 2018 Wikipedia dump，使用 E5 模型作为向量检索器。
*   **成本分析：** 文中提供了详细的推理成本估算表，指出在多跳任务中，外部搜索 API 的调用成本（约占 90% 以上）远超 Token 成本，证明了减少冗余工具调用的必要性。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 个数据集、2 种模型系列、3 种预算梯度，并进行了详尽的消融实验。
*   **充分性：** 实验设计非常客观。通过在“相同预算”下对比 BAVT 与并行采样，证明了其优越性并非来自更多的计算量，而是来自更智能的资源分配。
*   **消融实验：** 分别验证了“树结构”、“价值评估”和“预算感知选择”三个组件的贡献，结果显示仅靠树结构甚至会降低性能，必须配合预算感知机制才能达到最优。

#### 6. 主要结论与发现
*   **智能管理胜过暴力缩放：** BAVT 在**低预算**（5次调用）下的表现经常超过基线在**高预算**（20次调用，4倍资源）下的表现。
*   **解决模型病态问题：** BAVT 有效缓解了推理模型的“路径依赖”和指令模型的“模式崩溃（Mode Collapse）”。
*   **理论保证：** 论文从数学上证明了 BAVT 在有限预算下以高概率收敛至终止答案。

#### 7. 优点
*   **即插即用：** 无需微调，适用于任何具备基本推理和工具调用能力的 LLM。
*   **动态自适应：** 节点选择机制在“探索”与“利用”之间提供了平滑且无参数的转换。
*   **残差评估：** 通过预测相对增益而非绝对分数，有效解决了 LLM 自评过高的问题。

#### 8. 不足与局限
*   **Critic 开销：** 每次步骤都需要调用 LLM 作为 Critic 进行评估，这本身会消耗一定的 Token 预算（虽然实验证明这种交换是值得的）。
*   **工具成本同质化假设：** 目前实验假设所有工具调用成本相同（$C_{tool}=1$），但在现实中，不同 API 的价格和延迟差异巨大。
*   **任务范围：** 目前主要集中在知识密集型问答，尚未在长程交互（如复杂的网页操作或计算机控制）中验证。

（完）
