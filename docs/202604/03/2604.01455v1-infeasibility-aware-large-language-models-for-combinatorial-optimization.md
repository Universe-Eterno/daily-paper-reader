---
title: Infeasibility Aware Large Language Models for Combinatorial Optimization
title_zh: 具备不可行性感知能力的大语言模型用于组合优化
authors: "Yakun Wang, Min Chen, Zeguan Wu, Junyu Liu, Sitao Zhang, Zhenwen Shao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01455v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 大语言模型用于组合优化及不可解性检测
tldr: 本研究针对LLM在组合优化中忽视不可解性检测的局限，提出一种不可解性感知框架。通过数学规划构建包含可行性证明和不可解性标签的数据集，对8B模型进行微调，并结合LLM引导的下游搜索。实验显示，该方法在准确率上显著优于GPT-5.2，并能使局部搜索速度提升2倍，为LLM解决NP-hard问题提供了新思路。
selection_source: fresh_fetch
motivation: 现有利用LLM解决组合优化问题的研究大多只关注可行解生成，缺乏对实例不可解性的有效识别能力。
method: 提出一种结合数学规划与不可解性筛选的框架，通过构建带证明标签的数据集微调LLM，并将其输出作为下游搜索的热启动。
result: "微调后的8B模型在准确率上比GPT-5.2提升了30%，且LLM引导的热启动使下游局部搜索速度提升了2倍。"
conclusion: 引入不可解性感知机制能显著增强LLM解决复杂组合优化问题的能力，并能有效加速传统优化算法的执行。
---

## 摘要
大语言模型（LLMs）正越来越多地被探索用于解决 NP 难组合优化问题，但现有方法大多强调可行实例的解生成，而未明确解决不可行性检测问题。我们提出了一种不可行性感知框架，该框架结合了可验证数据集构建、监督微调以及 LLM 辅助的下游搜索。针对子图嵌入（minor-embedding）问题，我们引入了一种新的数学规划公式以及可证明的零阶段不可行性筛选，从而能够大规模构建训练实例，这些实例被标记为带有结构化证明的可行实例或可证明的不可行实例。利用通过该精确优化流水线生成的训练数据，我们展示了一个 8B 参数的大语言模型可以经过微调，共同执行解生成和不可行性检测。我们进一步利用 LLM 的输出作为下游局部搜索的热启动，提供了一种即使在 LLM 输出不完美的情况下也能加速优化的实用方法。实验表明，我们的微调模型相比 GPT-5.2 将整体准确率提高了多达 30%；同时，与下游局部搜索中的从零开始相比，LLM 引导的热启动提供了高达 2 倍的加速。

## Abstract
Large language models (LLMs) are increasingly explored for NP-hard combinatorial optimization problems, but most existing methods emphasize feasible-instance solution generation and do not explicitly address infeasibility detection. We propose an infeasibility-aware framework that combines certifiable dataset construction, supervised fine-tuning, and LLM-assisted downstream search. For the minor-embedding problem, we introduce a new mathematical programming formulation together with provable zero-phase infeasibility screening, which enables scalable construction of training instances labeled either as feasible with structured certificates or as certifiably infeasible. Using training data generated through this exact optimization pipeline, we show that an 8B-parameter LLM can be fine-tuned to jointly perform solution generation and infeasibility detection. We further utilize LLM outputs as warm starts for downstream local search, providing a practical way to accelerate optimization even when the LLM outputs are imperfect. Experiments show that our fine-tuned model improves overall accuracy by up to 30\% over GPT-5.2; meanwhile LLM-guided warm starts provide up to $2\times$ speedup compared with starting from scratch in downstream local search.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何利用大语言模型（LLM）解决组合优化（CO）问题，并特别关注了现有研究中常被忽视的“不可行性检测”能力。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 求解组合优化问题的方法大多集中在生成可行解上，但在面对 **NP 难（NP-hard）** 问题时，往往无法准确识别实例是否“不可解”（Infeasible）。
*   **研究动机**：
    *   在实际应用（如量子计算中的子图嵌入 Minor-embedding）中，确定一个问题是否有解本身就是 NP 完全的。
    *   通用 LLM 在处理复杂约束时容易产生幻觉，且在面对难题时倾向于盲目预测“不可行”。
    *   缺乏高质量、带有不可行性证明（Certificates）的训练数据集。

### 2. 方法论
论文提出了一种**不可行性感知框架**，包含三个核心阶段：
*   **可验证数据集构建**：
    *   针对子图嵌入问题，提出了一种新的**整数规划（IP）公式**。
    *   引入**零阶段不可行性筛选（Zero-phase screening）**：利用图论属性（如度数约束、边预算等）预先排除明显不可行的实例，提高数据生成效率。
    *   使用精确求解器（如 Gurobi）生成带有结构化证明的标签（可行解或不可行声明）。
*   **监督微调（SFT）**：
    *   使用 **LoRA（低秩自适应）** 技术对 Llama-3.1-8B 模型进行微调。
    *   **统一输出格式**：模型直接输出“Yes + 解决方案”或“No”，不使用思维链（CoT），以提高推理效率并减少 Token 消耗。
*   **LLM 引导的下游搜索（热启动）**：
    *   **Best-of-N 采样**：通过多次采样和多数投票机制提高预测可靠性。
    *   **可行性跳转（Feasibility Jump, FJ）**：将 LLM 输出的（可能不完美的）解作为局部搜索算法的初始点（热启动），利用传统算法修复约束冲突。

### 3. 实验设计
*   **实验场景**：主要针对**子图嵌入（Minor-embedding）**问题，并扩展到 **k-着色（k-coloring）**问题。
*   **数据集**：构建了包含 10,000 个实例的平衡数据集（50% 可行，50% 不可行），涵盖多种图拓扑（ER、BA、WS 等）。
*   **Benchmark 与对比方法**：
    *   **前沿模型**：GPT-5.2、GPT-5.2-Reasoning、DeepSeek-V3.2、Llama-3.3-70B、Grok-4.1-Fast。
    *   **传统工具**：Gurobi（MILP 求解器）、minorminer（启发式算法）。
    *   **消融实验**：对比了从零开始搜索与 LLM 热启动搜索的性能。

### 4. 资源与算力
*   **模型规模**：主要使用 Llama-3.1-8B。
*   **训练框架**：使用了 **Unsloth** 和 TRL 库进行高效微调。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长，但提到了使用 LoRA 和 bf16 精度以降低内存成本。

### 5. 实验数量与充分性
*   **实验规模**：每个问题类型测试了 10,000 个实例，并进行了 **Best-of-8** 的多次采样实验。
*   **分布外（OOD）测试**：在未见过的图拓扑（如 Pegasus 图和随机块模型 SBM）上进行了测试，验证了模型的泛化能力。
*   **客观性**：通过平衡 SAT/UNSAT 实例比例，避免了模型通过简单预测“不可行”来刷高准确率的偏见。实验对比了当前最顶尖的商业和开源模型，具有较高的参考价值。

### 6. 主要结论与发现
*   **性能飞跃**：微调后的 8B 模型在子图嵌入任务上的准确率达到 99.9%，比 GPT-5.2 高出约 30%。
*   **消除偏见**：通用大模型（如 GPT-5.2）在处理复杂 CO 问题时存在严重的“负面偏见”，即在遇到复杂的可行实例时倾向于报错“不可行”，而微调模型表现更均衡。
*   **显著加速**：利用 LLM 输出作为热启动，使下游局部搜索算法（Feasibility Jump）的收敛速度提升了 **2 倍**。
*   **泛化性**：该框架在 3-着色问题上也表现优异，证明了其在不同 CO 任务中的通用性。

### 7. 优点
*   **闭环设计**：将精确求解器的严谨性（数据生成）与 LLM 的模式识别能力（快速预测）以及局部搜索的修复能力（后处理）有机结合。
*   **不可行性证明**：解决了 LLM 在 CO 领域长期存在的“只管找解，不管证无”的问题。
*   **实用性强**：即使 LLM 输出的解不完全正确，也能作为高质量的初始值加速传统求解器。

### 8. 不足与局限
*   **规模限制**：实验主要集中在中小规模图（节点数通常在 300 以内），对于超大规模工业级实例的性能尚待验证。
*   **数据依赖**：框架高度依赖精确求解器生成初始训练数据，对于那些连精确求解器都无法处理的超难问题，数据获取将成为瓶颈。
*   **Token 限制**：随着图规模增大，输入表示（边列表）可能会超出 LLM 的上下文窗口。

（完）
