---
title: When does learning pay off? A study on DRL-based dynamic algorithm configuration for carbon-aware scheduling
title_zh: 学习何时能带来回报？一项关于碳感知调度的基于 DRL 的动态算法配置研究
authors: "Andrea Mencaroni, Robbert Reijnen, Yingqian Zhang, Dieter Claeys"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01886v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 基于深度强化学习的调度动态算法配置
tldr: 本研究探讨了基于深度强化学习（DRL）的动态算法配置（DAC）在碳感知调度问题中的应用价值。针对DRL训练成本高的问题，研究通过在简单实例上训练并跨规模测试，验证了其泛化能力。结果显示，DRL在复杂和未见实例中显著优于静态调优基准，证明了其在动态环境下的投资回报和鲁棒性。
selection_source: fresh_fetch
motivation: 探究高成本的深度强化学习在动态算法配置中是否具备泛化性，以及其训练投入在何种条件下优于传统的静态参数调优。
method: 开发了一种DRL框架用于动态调整碳感知调度算法参数，仅在小型简单实例上训练，并与静态调优基准在不同复杂度的实例上进行对比。
result: DRL策略在简单实例上与静态调优表现相当，但在面对更复杂或分布偏移的实例时，其性能持续且显著地优于静态调优。
conclusion: DRL能学习到鲁棒且可泛化的控制策略，其跨实例的适应能力使得初始计算投入在复杂和多变的优化场景中非常值得。
---

## 摘要
深度强化学习（DRL）近期已成为动态算法配置（DAC）的一种极具前景的工具，使进化算法能够在线调整其参数，而非依赖于静态调优的配置。虽然 DRL 可以学习有效的控制策略，但其训练过程在计算上非常昂贵。如果学习到的策略具有泛化能力，允许训练成果在不同实例类型和问题规模之间迁移，那么这种成本可能是合理的。然而，对于现实世界的优化问题，这种承诺在实践中是否成立，以及在何种条件下学习投入能带来回报，目前尚不明确。在本研究中，我们在碳感知置换流水车间调度问题的背景下探讨了这一问题。我们开发了一个基于 DRL 的 DAC 框架，并仅在小型、简单的实例上进行训练。随后，我们将学习到的策略部署在相似以及更复杂的未见实例上，并将其性能与静态调优的基准进行比较，从而提供公平的对比。我们的研究结果表明，所提出的方法提供了一种强大的动态算法控制策略，可以有效地迁移到不同的未见问题实例中。值得注意的是，在与训练和调优过程中观察到的实例相似的简单且计算成本低的实例上，DRL 的表现与静态调优基准相当。然而，随着实例特征的差异化和计算复杂度的增加，DRL 学习到的策略持续优于静态调优。这些结果证实了 DRL 可以获得鲁棒且具有泛化能力的控制策略，这些策略在训练实例分布之外依然有效。这种跨实例类型的泛化能力使得初始的计算投入变得物有所值，特别是在静态调优难以适应不断变化的问题场景的情况下。

## Abstract
Deep reinforcement learning (DRL) has recently emerged as a promising tool for Dynamic Algorithm Configuration (DAC), enabling evolutionary algorithms to adapt their parameters online rather than relying on static tuned configurations. While DRL can learn effective control policies, training is computationally expensive. This cost may be justified if learned policies generalize, allowing the training effort to transfer across instance types and problem scales. Yet, for real-world optimization problems, it remains unclear whether this promise holds in practice and under which conditions the investment in learning pays off. In this work, we investigate this question in the context of the carbon-aware permutation flow-shop scheduling problem. We develop a DRL-based DAC framework and train it exclusively on small, simple instances. We then deploy the learned policy on both similar and more complex unseen instances and compare its performance against a static tuned baseline, which provides a fair point of comparison. Our findings show that the proposed method provides a strong dynamic algorithm control policy that can be effectively transferred to different unseen problem instances. Notably, on simple and cheap to compute instances, similar to those observed during training and tuning, DRL performs comparably with the statically tuned baseline. However, as instance characteristics diverge and computational complexities increase, the DRL-learned policy continuously outperforms static tuning. These results confirm that DRL can acquire robust and generalizable control policies which are effective beyond the training instance distributions. This ability to generalize across instance types makes the initial computational investment worthwhile, particularly in settings where static tuning struggles to adapt to changing problem scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《When does learning pay off? A study on DRL-based dynamic algorithm configuration for carbon-aware scheduling》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：进化算法（EAs）的性能高度依赖于参数配置。传统的静态调优（Static Tuning）耗时且无法适应搜索过程中的动态需求，而动态算法配置（DAC）虽能在线调整参数，但基于深度强化学习（DRL）的 DAC 训练成本极高。
*   **核心问题**：DRL 训练的高昂投入是否值得？具体而言，在“碳感知调度（CAS）”这一复杂的现实问题中，DRL 学习到的策略是否具备跨问题规模和跨实例类型的**泛化能力**？在什么条件下，DRL 的表现会优于传统的静态调优？

### 2. 方法论
*   **核心思想**：将碳感知置换流水车间调度（CAS-PFSP）的参数控制建模为马尔可夫决策过程（MDP），利用 DRL 代理在线动态调整备忘录算法（Memetic Algorithm, MA）的 7 个关键参数。
*   **关键技术细节**：
    *   **基础算法**：MA-CAS-PFSP，结合了遗传算法（GA）和局部搜索，旨在最小化生产过程中的碳排放。
    *   **状态空间（State）**：5 维归一化向量，包括当前最佳适应度、平均适应度、适应度变异系数（代表种群多样性）、剩余计算预算、停滞计数。
    *   **动作空间（Action）**：7 维连续向量，控制交叉率（$\xi$）、作业/停顿交叉概率（$\chi_j, \chi_p$）、变异概率（$\pi_j, \pi_p$）及变异步长（$\sigma_j, \sigma_p$）。
    *   **奖励函数（Reward）**：采用密集奖励方案，根据每代适应度的改进量进行计算，并使用平方项来加大对搜索后期（更难改进阶段）改进的奖励权重。
    *   **学习算法**：使用近端策略优化（PPO）算法进行训练。

### 3. 实验设计
*   **数据集**：共 10 个数据集（每组 50 个随机实例）。
    *   **已知类型（Known）**：4 组小型简单实例，用于 DRL 训练和静态调优。
    *   **未知类型（Unknown）**：6 组更复杂、机器数量更多（最高达 15 台机器）的大型实例，用于测试泛化性。
*   **对比基准（Benchmarks）**：
    1.  **MA Default**：使用文献推荐的默认固定参数。
    2.  **MA Tuned**：使用 Optuna 框架进行静态调优。为了公平竞争，调优的计算预算（400 万次算法迭代）与 DRL 的训练预算完全一致。
*   **对比方法**：将提出的 **MA-DRL-CAS-PFSP** 与上述两个基准在所有数据集上进行 10 次独立重复实验。

### 4. 资源与算力
*   **硬件环境**：11th Gen Intel Core i5-1145G7 (2.60 GHz)。
*   **训练时长**：训练过程持续了 400 万个步骤（即算法迭代次数）。
*   **算力说明**：论文未提及使用 GPU，鉴于使用的是 Stable-Baselines3 库且状态/动作空间维度较低，实验主要在 CPU 环境下完成。

### 5. 实验数量与充分性
*   **实验规模**：总计测试了 500 个不同的实例（10 个数据集 $\times$ 50 个实例），每个实例重复 10 次以消除随机性影响。
*   **公平性**：实验设计非常严谨，特别强调了 **“预算对等”**——DRL 的训练步数与静态调优的评估步数完全相同，确保了对比的客观性。
*   **统计显著性**：使用了 Wilcoxon 秩和检验（p < 0.05）来验证结果的显著性。

### 6. 主要结论与发现
*   **学习的回报取决于复杂度**：在简单且已知的实例上，DRL 与精心调优的静态参数表现持平，没有显著优势。
*   **泛化能力显著**：DRL 策略展现了极强的跨规模泛化性。尽管只在小型实例上训练，但在面对未见过的、更复杂的重型实例时，DRL 持续优于静态调优（提升幅度随复杂度增加而扩大，最高提升超过 3%）。
*   **投资价值**：DRL 的高昂初始训练成本可以通过其“一次训练，到处运行”的泛化能力得到补偿，尤其是在静态调优难以覆盖的大规模复杂场景中。

### 7. 优点
*   **实验设计的公平性**：通过对等预算解决了 DRL 研究中常见的“训练成本被忽视”的问题。
*   **现实意义强**：选择了碳感知调度这一具有时变目标函数的复杂问题，比标准的 CEC 基准函数更具说服力。
*   **泛化性验证**：明确区分了已知和未知实例，证明了 DRL 学习的是“搜索动力学”而非简单的实例特征。

### 8. 不足与局限
*   **算法单一性**：仅在一种备忘录算法（MA）框架下进行了测试，尚未验证该 DRL 策略在其他进化算法（如差分进化或粒子群算法）上的通用性。
*   **状态空间局限**：状态特征主要基于适应度统计，可能忽略了调度问题本身的结构化特征（如机器负载、作业相关性等）。
*   **跨类别泛化未知**：虽然实现了跨规模泛化，但尚未探讨在一个问题类别（如调度）训练的策略是否能直接迁移到另一个类别（如路由问题）。

（完）
