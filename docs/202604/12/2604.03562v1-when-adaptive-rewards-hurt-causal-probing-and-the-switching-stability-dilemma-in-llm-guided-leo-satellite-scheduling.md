---
title: "When Adaptive Rewards Hurt: Causal Probing and the Switching-Stability Dilemma in LLM-Guided LEO Satellite Scheduling"
title_zh: 当自适应奖励适得其反：LLM 引导的低轨卫星调度中的因果探测与切换-稳定性困境
authors: Yuanhang Li
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03562v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 大模型引导的卫星调度与深度强化学习奖励设计
tldr: 本研究探讨了低轨卫星调度中深度强化学习的奖励设计，挑战了动态奖励优于静态的直觉。通过引入单变量因果探测法，揭示了“切换-稳定性困境”：频繁变动的奖励权重会破坏PPO算法的价值函数收敛。实验对比了固定、规则、MLP及LLM方案，发现LLM因输出波动导致性能崩溃，而MLP表现最优。研究明确了LLM在通信系统中应侧重于意图理解而非直接参与高频奖励调节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03562v1/fig-001.webp\", \"caption\": \"Fig. 3. Outage weight trajectories for MLP (top) and FT-LLM (bottom) architects during generalization experiments (seed 42). The MLP produces stable weights (range 0.94–1.57), while the LLM oscillates between extreme values (0.004–0.999). Dashed gray line marks the transition to novel regimes at step 250k.\", \"page\": 6, \"index\": 1, \"width\": 506, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.03562v1/fig-002.webp\", \"caption\": \"Fig. 2. Mean sum rate comparison across architect variants. The MLP (M3) achieves the highest throughput on both known and novel regimes. The FT-LLM (M4) collapses on novel regimes due to weight oscillation (see Section V-H).\", \"page\": 4, \"index\": 2, \"width\": 511, \"height\": 282}]"
motivation: 验证在多波束卫星调度中，环境感知的动态奖励权重是否优于静态权重，并探究其对强化学习收敛的影响。
method: 引入单变量因果探测法分析奖励项敏感度，并对比评估了固定权重、MLP及微调LLM等四种奖励架构。
result: 静态权重性能显著优于波动的动态权重，LLM方案因权重振荡导致性能骤降，而MLP在已知和新场景下表现最稳。
conclusion: 强化学习需要准静态奖励信号以保证收敛，LLM在集成时应关注自然语言意图理解而非频繁的数值权重调整。
---

## 摘要
多波束低轨（LEO）卫星调度中深度强化学习（DRL）的自适应奖励设计，源于一种直觉：即感知场景的奖励权重应优于静态权重。我们系统地测试了这一直觉，并揭示了一个“切换-稳定性”困境：近乎恒定的奖励权重（342.1 Mbps）优于精心调优的动态权重（103.3+/-96.8 Mbps），因为 PPO 算法需要准平稳的奖励信号来实现价值函数的收敛。无论权重质量如何，权重的自适应调整都会因反复重启收敛过程而降低性能。为了理解特定权重为何重要，我们引入了一种单变量因果探测方法，通过独立扰动每个奖励项 +/-20%，并测量 5 万步后的 PPO 响应。探测结果揭示了反直觉的杠杆效应：在极地切换场景中，增加 20% 的切换惩罚可带来 +157 Mbps 的增益；在冷热更替场景中则带来 +130 Mbps 的增益。如果没有系统性的探测，人类专家或训练好的 MLP 无法获得这些发现。我们在已知和新颖的流量场景下评估了四种 MDP 架构变体（固定、基于规则、学习型 MLP、微调后的 LLM）。MLP 在已知场景下达到 357.9 Mbps，在新颖场景下达到 325.2 Mbps；而微调后的 LLM 性能则崩溃至 45.3+/-43.0 Mbps，这是由于权重振荡而非缺乏领域知识导致的——输出一致性而非知识储备，才是核心约束。我们的研究为通信系统中的 LLM-DRL 集成提供了基于实证的路线图，明确了 LLM 在何处具有不可替代的价值（自然语言意图理解），以及在何处使用更简单的方法即可满足需求。

## Abstract
Adaptive reward design for deep reinforcement learning (DRL) in multi-beam LEO satellite scheduling is motivated by the intuition that regime-aware reward weights should outperform static ones. We systematically test this intuition and uncover a switching-stability dilemma: near-constant reward weights (342.1 Mbps) outperform carefully-tuned dynamic weights (103.3+/-96.8 Mbps) because PPO requires a quasistationary reward signal for value function convergence. Weight adaptation-regardless of quality-degrades performance by repeatedly restarting convergence. To understand why specific weights matter, we introduce a single-variable causal probing method that independently perturbs each reward term by +/-20% and measures PPO response after 50k steps. Probing reveals counterintuitive leverage: a +20% increase in the switching penalty yields +157 Mbps for polar handover and +130 Mbps for hot-cold regimes-findings inaccessible to human experts or trained MLPs without systematic probing. We evaluate four MDP architect variants (fixed, rule-based, learned MLP, finetuned LLM) across known and novel traffic regimes. The MLP achieves 357.9 Mbps on known regimes and 325.2 Mbps on novel regimes, while the fine-tuned LLM collapses to 45.3+/-43.0 Mbps due to weight oscillation rather than lack of domain knowledge-output consistency, not knowledge, is the binding constraint. Our findings provide an empirically-grounded roadmap for LLM-DRL integration in communication systems, identifying where LLMs add irreplaceable value (natural language intent understanding) versus where simpler methods suffice.

---

## 论文详细总结（自动生成）

### 论文结构化总结：当自适应奖励适得其反

#### 1. 核心问题与整体含义（研究动机和背景）
随着低轨（LEO）卫星星座的扩张，多波束资源调度变得极其复杂。传统的深度强化学习（DRL）方法通常采用固定的奖励函数，但在实际应用中，卫星面临的流量场景（Regime）差异巨大（如城市高密度需求 vs 灾区突发需求）。
*   **研究动机**：学术界直觉认为，能够感知场景并动态调整的奖励权重应优于静态权重。
*   **核心问题**：引入大语言模型（LLM）来动态生成奖励权重是否真的能提升调度性能？在通信系统这种对数值精度和一致性要求极高的场景下，LLM-DRL 集成的瓶颈在哪里？

#### 2. 论文提出的方法论
论文提出了一个**三层自适应架构**，并引入了**单变量因果探测（Causal Probing）**方法：
*   **三层架构**：
    1.  **战略层（LLM）**：负责小时级的自然语言意图解析（如“优先保障应急通信”），转化为目标配置文件。
    2.  **战术层（MLP）**：负责分钟级的 KPI 监测与权重映射，利用 CUSUM 算法检测流量场景切换。
    3.  **操作层（PPO 代理）**：负责毫秒级的波束功率和带宽分配。
*   **核心思想**：解耦意图理解与数值优化。
*   **关键技术细节**：
    *   **因果探测**：对每个奖励项（总速率、中断数、切换惩罚等）独立进行 $\pm20\%$ 的扰动，运行 5 万步 PPO，构建奖励景观的实证因果图。
    *   **奖励函数公式**：$R_w = w_r \cdot \bar{R} - w_o \cdot O - w_s \cdot S - w_q \cdot Q + w_f \cdot F$（包含速率、中断、切换、队列、公平性五项）。

#### 3. 实验设计
*   **场景设置**：模拟 19 波束 Ka 频段 LEO 卫星，高度 600km。
*   **流量模式**：
    *   **已知场景**：城市（Urban）、海事（Maritime）、灾难（Disaster）、混合（Mixed）。
    *   **新颖场景（测试泛化性）**：IoT 爆发、极地切换、冷热更替。
*   **对比方法（Benchmark）**：
    1.  **M1 (Fixed)**：离线优化的静态权重。
    2.  **M2 (Rule-based)**：基于专家规则的阈值分类器。
    3.  **M3 (Learned MLP)**：在模拟数据上训练的 3 层神经网络。
    4.  **M4 (Fine-tuned LLM)**：经过 LoRA 微调的 Qwen3-4B 模型。

#### 4. 资源与算力
*   **硬件**：使用了 NVIDIA RTX 4060 GPU。
*   **模型细节**：Qwen3-4B 使用 4-bit NF4 量化，LoRA 微调（秩 16，33M 可训练参数）。
*   **推理延迟**：LLM 每次调用耗时约 14 秒，而 MLP 推理时间小于 1 毫秒。
*   **训练时长**：每个实验运行 50 万个时间步（500k steps）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了四种架构在已知和未知场景下的表现。
    *   进行了 8 个随机种子的扩展验证（针对切换-稳定性困境）。
    *   包含消融实验，验证了 MLP、规则和固定权重的性能阶梯。
    *   针对 LLM 的权重振荡进行了专门的轨迹分析。
*   **充分性**：实验设计较为严谨，通过多随机种子（42, 123, 456 等）减少了偶然性，且针对“为什么 LLM 会失败”做了深入的根因分析，具有较高的客观性。

#### 6. 主要结论与发现
*   **切换-稳定性困境（Switching-Stability Dilemma）**：这是论文最重要的发现。PPO 算法需要准平稳的奖励信号来估计价值函数。频繁或大幅度的权重调整（即使权重本身质量很高）会不断重启收敛过程，导致性能大幅下降。
*   **LLM 的局限性**：微调后的 LLM 在新场景下性能崩溃（仅 45.3 Mbps），主因是**权重振荡**。LLM 生成的数值权重变异系数（CV）比 MLP 高出 4.6 倍，这种不一致性是 DRL 集成的核心障碍。
*   **因果探测的价值**：发现“切换惩罚”权重对性能有超乎预期的杠杆作用（+157 Mbps），这是人类专家难以直观发现的。
*   **最优方案**：轻量级 MLP 在已知（357.9 Mbps）和未知场景（325.2 Mbps）中表现最稳健。

#### 7. 优点
*   **视角独特**：挑战了“动态奖励一定好”的固有认知，提出了深刻的稳定性理论解释。
*   **方法实用**：提出的“因果探测”法比完整训练快 10 倍，是 DRL 奖励调优的有力工具。
*   **架构合理**：三层架构合理分配了 LLM（语义理解）和 MLP（数值映射）的职责，具有工程落地参考价值。

#### 8. 不足与局限
*   **模型规模限制**：实验使用的 Qwen3-4B 规模较小，更大规模的模型（如 70B+）是否能通过更强的推理能力减少权重振荡尚不可知。
*   **环境简化**：卫星模拟器简化了波束间干扰和复杂的轨道动力学，可能与真实空间环境存在偏差。
*   **泛化场景有限**：虽然测试了 3 种新颖场景，但实际卫星通信中的干扰和衰落模式远比实验设置复杂。
*   **应用限制**：该结论主要针对 PPO 等对非平稳奖励敏感的算法，对于其他类型的强化学习算法是否适用仍需验证。

（完）
