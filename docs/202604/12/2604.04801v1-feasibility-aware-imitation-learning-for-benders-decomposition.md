---
title: Feasibility-Aware Imitation Learning for Benders Decomposition
title_zh: 针对 Benders 分解的可行性感知模仿学习
authors: "Bernard T. Agyeman, Zhe Li, Ilias Mitrai, Prodromos Daoutidis"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04801v1"
tags: ["query:nco-llm"]
score: 8.5
evidence: 用于混合整数优化中Benders分解的模仿学习框架
tldr: 本文针对混合整数优化中Benders分解主问题求解缓慢的瓶颈，提出一种感知可行性的模仿学习框架。该方法通过两阶段训练（行为克隆与可行性逻辑调整）预测主问题整数变量，并将其集成到带有显式可行性检查和限时求解器的分解框架中。该方法在保证算法有限收敛性的前提下，显著提升了求解效率并保持了高精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04801v1/fig-001.webp\", \"caption\": \"TABLE II COMPARISON BETWEEN CLASSICAL GBD AND LEARNING-AUGMENTED VARIANTS.\", \"page\": 7, \"index\": 1, \"width\": 1052, \"height\": 232}]"
motivation: 旨在解决Benders分解中主问题迭代求解耗时过长，以及现有模仿学习方法难以满足约束可行性的问题。
method: 采用结合行为克隆与可行性逻辑调整的训练策略，并在推理时利用限时求解器确保下界的有效性。
result: 实验表明，该方法在保持解准确性的同时，比现有的模仿学习加速方案具有更短的求解时间。
conclusion: 该研究证明了将可行性感知预测与传统分解算法结合，能有效加速复杂混合整数优化问题的求解。
---

## 摘要
混合整数优化问题广泛存在于各种控制应用中。Benders 分解是一种广泛使用的算法，通过将此类问题分解为一个混合整数主问题和一个连续子问题来求解。一个关键的计算瓶颈是在迭代过程中重复求解日益复杂的主问题。在本文中，我们提出了一种可行性感知的模仿学习框架，该框架在预测每次迭代中主问题整数变量值的同时，考虑了关于允许整数赋值的约束以及累积的 Benders 可行性割平面的可行性。智能体采用两阶段过程进行训练，该过程结合了行为克隆与基于可行性的 Logit 调整，从而使预测偏向于满足不断演化的割集赋值。智能体被部署在一个基于智能体的 Benders 分解框架中，该框架结合了显式的可行性检查以及求解器在限时内对有效下界的计算。由于下界在每次迭代中都经过验证，所提方法保留了有限收敛特性。对原型案例研究的应用表明，与现有的加速 Benders 分解的模仿学习方法相比，所提方法在保持求解准确性的同时，缩短了求解时间。

## Abstract
Mixed-integer optimization problems arise in a wide range of control applications. Benders decomposition is a widely used algorithm for solving such problems by decomposing them into a mixed-integer master problem and a continuous subproblem. A key computational bottleneck is the repeated solution of increasingly complex master problems across iterations. In this paper, we propose a feasibility-aware imitation learning framework that predicts the values of the integer variables of the master problem at each iteration while accounting for feasibility with respect to constraints governing admissible integer assignments and the accumulated Benders feasibility cuts. The agent is trained using a two-stage procedure that combines behavioral cloning with a feasibility-based logit adjustment to bias predictions toward assignments that satisfy the evolving cut set. The agent is deployed within an agent-based Benders decomposition framework that combines explicit feasibility checks with a time-limited solver computation of a valid lower bound. The proposed approach retains finite convergence properties, as the lower bound is certified at each iteration. Application to a prototypical case study shows that the proposed method improves solution time relative to existing imitation learning approaches for accelerating Benders decomposition, while preserving solution accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Feasibility-Aware Imitation Learning for Benders Decomposition》的结构化技术总结：

### 1. 核心问题与整体含义
**研究动机：**
混合整数非线性规划（MINLP）在控制和工程领域应用广泛，通常采用 Benders 分解（BD）求解。BD 的核心瓶颈在于需要反复求解整数主问题（Master Problem, MP），随着迭代增加，主问题中的割平面（Cuts）不断累积，导致求解耗时剧增。

**核心问题：**
现有的机器学习（ML）加速方法（如模仿学习或强化学习）直接预测主问题的解，但存在两个致命缺陷：
1. **可行性无法保证：** 预测的整数解可能违反主问题的约束或已生成的 Benders 可行性割平面。
2. **缺乏收敛性保证：** ML 预测无法提供有效的下界（Lower Bound），导致算法无法保证在有限步内收敛到全局最优。

### 2. 方法论
本文提出了一种**可行性感知模仿学习（Feasibility-Aware Imitation Learning）**框架，并将其集成到广义 Benders 分解（GBD）中。

**核心思想：**
*   **动作空间约束：** 预先筛选满足原始问题中纯二元约束的赋值集合 $Y_{pb}$，将预测任务转化为在该有限集合上的分类任务。
*   **两阶段训练策略：**
    1.  **阶段一（行为克隆）：** 使用专家数据（MIP 求解器的解）训练图神经网络（GNN），学习从主问题结构到最优整数解的映射。
    2.  **阶段二（可行性微调）：** 引入**可行性感知 Logit 调整**。根据当前迭代中违反可行性割平面的程度计算惩罚项 $s_j$，调整输出层的 Logit 值：$\tilde{\ell}_j = \ell_j - \omega s_j$。这使得模型在保持预测准确性的同时，倾向于选择满足割平面的解。
*   **智能体驱动的 GBD 算法：**
    *   **显式检查：** 预测解 $\hat{y}$ 需通过所有已累积可行性割平面的检查，否则退回到求解器。
    *   **限时求解验证：** 即使预测通过检查，也会调用 MIP 求解器并在严格的时间限制（$T_k$）内运行。此步骤有两个目的：一是利用预测解作为热启动（Warm-start）寻找更好的整数解；二是获取经过验证的有效下界（LBD），从而确保算法的**有限收敛性**。

### 3. 实验设计
*   **场景/数据集：** 采用了一个经典的 MINLP 原型案例（包含 5 个二元变量和 6 个连续变量），通过随机采样参数生成了 300 个不同的问题实例，产生约 2.4 万个训练数据点。
*   **Benchmark（基准）：**
    1.  **Classical GBD：** 传统的广义 Benders 分解（使用 Gurobi 求解主问题）。
    2.  **Independent IL：** 现有的模仿学习方法（Agyeman et al., 2024），其独立预测每个二元变量，不考虑约束相关性。
*   **对比指标：** 求解时间、迭代次数、预测解的精确匹配率（Exact Match）、可行性比例以及最终解的准确性。

### 4. 资源与算力
*   **硬件设施：** 实验在配备 Intel Core i7-14700 处理器（20 核）和 32 GB RAM 的机器上运行，操作系统为 Ubuntu 24.04。
*   **软件工具：** 使用 Gurobi 12.0.1 求解主问题，Ipopt 3.12.6 求解子问题，BONMIN 1.8.9 获取真实最优解。
*   **算力说明：** 文中未明确提到使用 GPU 进行训练，考虑到 GNN 处理的是小型二元变量图，CPU 训练在合理时间内是可行的。

### 5. 实验数量与充分性
*   **实验规模：** 训练集包含 23,409 个数据点，测试集包含 1,232 个实例（其中 877 个包含可行性割平面）。
*   **充分性评价：** 实验设计较为充分，包含了针对模型预测能力的静态评估（表 I）和针对算法运行表现的动态评估（表 II）。通过对比“独立预测”和“基准模型”，清晰地展示了“两阶段训练”和“动作空间约束”的增量贡献。然而，案例研究仅限于一个特定的小规模 MINLP 问题，其在更大规模问题上的泛化性有待验证。

### 6. 主要结论与发现
*   **可行性显著提升：** 所提方法在存在可行性割平面的情况下，预测解的可行率达到 **93.73%**，远高于 Independent IL 的 19.95%。
*   **求解加速：** 相比 Classical GBD，主问题的平均求解时间减少了 **65.3%**；相比 Independent IL，加速效果也提升了近 3 倍。
*   **精度与收敛：** 所有测试实例均 100% 找到了最优解，且迭代次数与传统方法一致，证明了该框架在加速的同时没有牺牲鲁棒性。

### 7. 优点
*   **理论完备性：** 通过集成限时求解器获取有效下界，解决了 ML 方法普遍存在的收敛性证明难题。
*   **结构感知：** 利用二元约束预减小动作空间，并使用 Logit 调整动态响应迭代过程中的割平面，设计巧妙且符合优化理论。
*   **热启动优势：** 将 ML 预测作为求解器的热启动输入，实现了 ML 与传统优化算法的有机结合。

### 8. 不足与局限
*   **扩展性风险：** 动作空间 $Y_{pb}$ 的大小随二元变量数量呈指数级增长。虽然文中提到 $N_c \ll 2^m$，但对于具有数百个整数变量的大规模问题，将其视为分类任务可能导致输出层过大。
*   **案例单一：** 仅在一个包含 5 个二元变量的小规模案例上进行了验证，缺乏对不同类型（如非凸、大规模稀疏）问题的广泛测试。
*   **超参数敏感性：** 惩罚参数 $\omega$ 和时间限制 $T_k$ 的设定可能需要针对不同问题进行繁琐的调优。

（完）
