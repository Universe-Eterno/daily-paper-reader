---
title: Semantic Evolution over Populations for LLM-Guided Automated Program Repair
title_zh: 基于种群语义演化的大语言模型引导自动程序修复
authors: "Cuong Chi Le, Minh Le-Anh, Cuong Duc Van, Tien N. Nguyen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02134v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 基于大语言模型的语义进化算法用于迭代优化
tldr: 针对大语言模型在自动程序修复中难以维持多样性、组合部分修复及利用结构化反馈等挑战，本文提出 EvolRepair 框架。该框架将 APR 建模为语义进化算法，利用 LLM 和执行反馈替代传统遗传算法的语法操作。通过将候选修复按行为分组并进行语义重组，EvolRepair 能够有效探索修复空间并合成更优方案，显著提升了修复效果。
selection_source: fresh_fetch
motivation: 现有的基于 LLM 的迭代修复方法在维持修复多样性、组合互补修复方案以及利用结构化失败信息方面存在局限。
method: 提出 EvolRepair 框架，将 LLM 与遗传算法结合，通过行为分组、语义重组和结构化反馈引导的进化策略来优化修复过程。
result: 实验结果表明，EvolRepair 在修复有效性上显著优于现有的基于 LLM 的自动程序修复方法。
conclusion: 结合群体语义进化与 LLM 的能力，可以更有效地解决复杂程序错误，为自动程序修复提供了新的搜索范式。
---

## 摘要
大语言模型（LLMs）近期在自动程序修复（APR）领域展现出巨大潜力，特别是通过生成并改进候选补丁的迭代细化方法。然而，最先进的基于 LLM 的迭代细化 APR 方法仍无法完全解决一些挑战，包括：在修复假设中保持有用的多样性、识别语义相关的修复族群、组合互补的部分修复、利用结构化的失败信息，以及逃离结构性缺陷的搜索区域。在本文中，我们提出了一种用于 APR 迭代细化的基于种群的语义演化框架，称为 EvolRepair，它将基于 LLM 的 APR 形式化为一种语义演化算法。EvolRepair 重新构建了经典 APR 遗传算法的搜索范式，但将其基于语法的算子替换为由 LLM 和结构化执行反馈驱动的语义感知组件。候选修复被组织成行为一致的组，使算法能够保持多样性，对修复族群进行推理，并通过重组种群中互补的修复见解来合成更强的候选方案。通过利用结构化的失败模式来引导搜索方向，EvolRepair 既能细化有前景的修复策略，也能在必要时转向替代的抽象方案。我们的实验表明，EvolRepair 与现有的基于 LLM 的 APR 方法相比，显著提高了修复效果。

## Abstract
Large language models (LLMs) have recently shown strong potential for automated program repair (APR), particularly through iterative refinement that generates and improves candidate patches. However, state-of-the-art iterative refinement LLM-based APR approaches cannot fully address challenges, including maintaining useful diversity among repair hypotheses, identifying semantically related repair families, composing complementary partial fixes, exploiting structured failure information, and escaping structurally flawed search regions. In this paper, we propose a Population-Based Semantic Evolution framework for APR iterative refinement, called EvolRepair, that formulates LLM-based APR as a semantic evolutionary algorithm. EvolRepair reformulates the search paradigm of classic genetic algorithm for APR, but replaces its syntax-based operators with semantics-aware components powered by LLMs and structured execution feedback. Candidate repairs are organized into behaviorally coherent groups, enabling the algorithm to preserve diversity, reason over repair families, and synthesize stronger candidates by recombining complementary repair insights across the population. By leveraging structured failure patterns to guide search direction, EvolRepair can both refine promising repair strategies and shift toward alternative abstractions when necessary. Our experiments show that EvolRepair substantially improves repair effectiveness over existing LLM-based APR approaches.

---

## 论文详细总结（自动生成）

以下是对论文《Semantic Evolution over Populations for LLM-Guided Automated Program Repair》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于大语言模型（LLM）的迭代式自动程序修复（APR）方法（如 REx, ChatRepair）通常采用单轨迹搜索或贪婪细化策略。这导致了以下局限：
    *   **缺乏多样性**：容易过早收敛到错误的修复方向。
    *   **难以组合修复**：无法将分布在不同候选补丁中的“部分正确”逻辑整合在一起。
    *   **反馈利用不足**：仅依赖简单的测试通过率，忽略了失败模式的结构化信息。
    *   **局部最优陷阱**：当初始修复假设存在结构性缺陷时，局部微调无法跳出错误框架。
*   **研究背景**：论文试图将经典的遗传算法（GA）思想与 LLM 的语义理解能力结合，通过“种群演化”而非“单兵作战”来解决复杂的程序修复任务。

### 2. 方法论：EvolRepair 框架
EvolRepair 将 APR 建模为一个**语义进化算法**，其核心流程如下：
*   **初始化**：利用 LLM 生成初始修复种群。
*   **行为分组（Behavioral Grouping）**：根据候选补丁通过的测试用例子集（行为特征），利用 Jaccard 相似度和凝聚层次聚类将补丁分组。这有助于识别具有相似修复策略的“家族”。
*   **跨组采样（Cross-Group Sampling）**：通过熵权法采样构建混合候选池，以促进不同行为区域之间的信息交换。
*   **群体级语义重组（Crossover）**：不同于传统的两亲本交叉，EvolRepair 让 LLM 同时观察整个候选池（包含多个补丁及其执行反馈），提取各方的正确逻辑并合成一个更强的补丁。
*   **语义变异（Semantic Mutation）**：根据测试失败的结构化报告（错误信息、预期输出等），利用 LLM 对单个候选补丁进行针对性细化。
*   **生存选择**：根据测试通过率、通过的测试数量及代码长度对种群进行排序，保留前 $N$ 个唯一补丁进入下一代。

### 3. 实验设计
*   **数据集**：采用从 **LiveCodeBench** 衍生出的 **SWE-Synth** 基准测试，包含 120 个编程问题的 326 个错误实现。这些错误是部分正确的（通过部分测试但未全过），具有较高的挑战性。
*   **对比方法（Baselines）**：
    *   **REx**：基于 Thompson 采样平衡探索与利用的迭代修复方法。
    *   **ChatRepair**：基于对话的迭代细化方法。
    *   **Naive Prompting**：直接提示修复。
*   **底层模型（Backbones）**：Llama 3.3 70B Instruct、Kimi K2 Instruct、DeepSeek V3.1。
*   **评价指标**：Pass@1、Pass@3、平均通过率（APR）、测试用例覆盖率（TCC）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **推理环境**：实验通过 **Fireworks AI** 托管的 API 进行推理，强调了推理成本的经济性。
*   **成本统计**：论文详细记录了每个错误程序的平均运行时间（约 50-500 秒，视模型而定）和平均 Token 成本（约 $0.01 - $0.03）。

### 5. 实验数量与充分性
*   **实验规模**：针对 326 个错误程序，每个方法独立运行 5 次以计算 Pass@k，实验数据量充足。
*   **消融实验**：对比了 5 种变体（如：成对交叉 vs 群体交叉、随机分组 vs 行为分组、无变异 vs 有变异等），验证了各组件的必要性。
*   **敏感性分析**：对进化代数、种群大小、分组数量等 6 个超参数进行了详细的敏感性测试。
*   **公平性**：通过限制总尝试次数（约 40 次）确保 EvolRepair 与基线方法在相同的 Prompt 预算下进行对比。

### 6. 主要结论与发现
*   **性能领先**：EvolRepair 在所有底层模型上均显著优于 REx 和 ChatRepair。例如，在 Llama 3.3 上，Pass@1 比最强基线提升了 4.91%。
*   **组合效应**：实验证明语义交叉算子能有效整合不同补丁的优点。在 Llama 3.3 中，约 61.7% 的交叉操作成功合成了包含多个父代独特正确行为的子代。
*   **跳出局部最优**：通过行为分组和群体推理，EvolRepair 能够覆盖更广泛的测试用例（更高的 TCC），并缩小“最佳个体”与“群体总能力”之间的差距（更小的 $\Delta$）。

### 7. 优点与亮点
*   **范式创新**：将传统的语法级遗传算法升级为 LLM 驱动的语义级进化算法，解决了传统 GA 盲目变异的问题。
*   **行为感知**：利用测试用例的通过情况进行分组，而非仅看分数，这使得模型能更精准地识别互补的修复方案。
*   **群体推理**：群体级重组（Population-level Recombination）充分利用了 LLM 的长上下文处理能力，实现了比成对交叉更高效的逻辑整合。

### 8. 不足与局限
*   **基准局限**：实验主要集中在算法类问题（LiveCodeBench），对于大型工业级软件系统（如包含复杂依赖和长调用链的项目）的修复效果尚待验证。
*   **过拟合风险**：虽然使用了行为签名，但本质上仍依赖测试套件作为唯一真理来源（Oracle），在测试用例不完备时可能产生过拟合补丁。
*   **延迟与成本**：相比单次提示，进化过程涉及多次迭代和种群评估，在实时性要求极高的场景下可能存在延迟。

（完）
