---
title: "GSR-GNN: Training Acceleration and Memory-Saving Framework of Deep GNNs on Circuit Graph"
title_zh: GSR-GNN：电路图上深层图神经网络的训练加速与省存框架
authors: "Yuebo Luo, Shiyang Li, Yifei Feng, Vishal Kancharla, Shaoyi Huang, Caiwen Ding"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27156v1"
tags: ["query:nco"]
score: 6.0
evidence: 用于电路图分析的深度图神经网络框架
tldr: "针对现代大规模电路图中深度图神经网络（GNN）训练面临的显存瓶颈和高昂计算成本，本文提出了GSR-GNN框架。该框架结合了可逆残差模块、分组稀疏非线性算子及优化的执行流水线，支持训练数百层的深度GNN。在显著降低显存占用（最高87.2%）和加速训练（超过30倍）的同时，保持了极高的预测精度，使深度GNN在EDA领域的大规模应用成为可能。"
selection_source: fresh_fetch
motivation: 深度GNN在电路分析中表现优于浅层模型，但受限于GPU显存和训练成本，难以扩展到大规模电路图。
method: 提出GSR-GNN框架，集成可逆残差模块、分组稀疏非线性算子及优化的执行流水线，以压缩节点嵌入并减少数据搬运。
result: "在电路图样本上实现了最高87.2%的显存缩减和超过30倍的训练加速，且预测质量几乎无损。"
conclusion: GSR-GNN有效解决了深度GNN在EDA任务中的效率与显存难题，为大规模电路分析提供了实用的深度学习方案。
---

## 摘要
图神经网络 (GNN) 在电路分析中展现出巨大的潜力，但扩展到现代大规模电路图时受到 GPU 显存和训练成本的限制，尤其是对于深层模型。我们重新审视了用于电路图的深层 GNN，并证明当其可训练时，其性能显著优于浅层架构，这促使我们开发一种高效的领域特定训练框架。我们提出了分组稀疏可逆 GNN (GSR-GNN)，它能够训练多达数百层的 GNN，同时降低计算和内存开销。GSR-GNN 将可逆残差模块与分组稀疏非线性算子相结合，在不牺牲任务相关信息的情况下压缩节点嵌入，并采用优化的执行流水线来消除碎片化的激活存储并减少数据移动。在采样的电路图上，GSR-GNN 实现了高达 87.2% 的峰值显存降低和超过 30 倍的训练加速，且基于相关性的质量指标下降微乎其微，使深层 GNN 在大规模 EDA 工作负载中具有实用性。

## Abstract
Graph Neural Networks (GNNs) show strong promise for circuit analysis, but scaling to modern large-scale circuit graphs is limited by GPU memory and training cost, especially for deep models. We revisit deep GNNs for circuit graphs and show that, when trainable, they significantly outperform shallow architectures, motivating an efficient, domain-specific training framework. We propose Grouped-Sparse-Reversible GNN (GSR-GNN), which enables training GNNs with up to hundreds of layers while reducing both compute and memory overhead. GSR-GNN integrates reversible residual modules with a group-wise sparse nonlinear operator that compresses node embeddings without sacrificing task-relevant information, and employs an optimized execution pipeline to eliminate fragmented activation storage and reduce data movement. On sampled circuit graphs, GSR-GNN achieves up to 87.2\% peak memory reduction and over 30$\times$ training speedup with negligible degradation in correlation-based quality metrics, making deep GNNs practical for large-scale EDA workloads.