---
title: "When Adaptive Rewards Hurt: Causal Probing and the Switching-Stability Dilemma in LLM-Guided LEO Satellite Scheduling"
title_zh: 当自适应奖励适得其反：LLM 引导的低轨卫星调度中的因果探测与切换-稳定性困境
authors: Yuanhang Li
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03562v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 大模型引导的低轨卫星调度，结合深度强化学习
tldr: 本研究探讨了低轨卫星调度中深度强化学习的自适应奖励设计，揭示了“切换-稳定性困境”：由于PPO算法需要准平稳奖励信号，频繁变动的动态权重反而会导致性能下降。通过因果探测方法，研究发现微调后的LLM因输出权重震荡而表现不佳，而简单的MLP方案更为稳健。研究为LLM与DRL在通信系统中的集成提供了实证指导，强调了输出一致性的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03562v1/fig-001.webp\", \"caption\": \"Fig. 3. Outage weight trajectories for MLP (top) and FT-LLM (bottom) architects during generalization experiments (seed 42). The MLP produces stable weights (range 0.94–1.57), while the LLM oscillates between extreme values (0.004–0.999). Dashed gray line marks the transition to novel regimes at step 250k.\", \"page\": 6, \"index\": 1, \"width\": 506, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.03562v1/fig-002.webp\", \"caption\": \"Fig. 2. Mean sum rate comparison across architect variants. The MLP (M3) achieves the highest throughput on both known and novel regimes. The FT-LLM (M4) collapses on novel regimes due to weight oscillation (see Section V-H).\", \"page\": 4, \"index\": 2, \"width\": 511, \"height\": 282}]"
motivation: 验证在多波束低轨卫星调度中，自适应奖励权重是否优于静态权重，并探究LLM引导奖励设计的有效性。
method: 提出单变量因果探测方法分析奖励项影响，并对比固定、规则、MLP及微调LLM四种权重生成架构。
result: 静态权重和MLP表现远超LLM，后者因权重频繁震荡导致PPO价值函数难以收敛，性能大幅缩水。
conclusion: LLM在通信系统中应侧重于意图理解而非直接生成动态奖励权重，奖励信号的稳定性是确保DRL性能的关键。
---

## 摘要
在多波束低轨（LEO）卫星调度的深度强化学习（DRL）中，自适应奖励设计的初衷是认为感知场景的奖励权重应优于静态权重。我们系统地测试了这一直觉，并揭示了一个“切换-稳定性困境”：近乎恒定的奖励权重（342.1 Mbps）优于精心调优的动态权重（103.3+/-96.8 Mbps），因为 PPO 算法需要准平稳的奖励信号来实现价值函数的收敛。无论权重质量如何，权重的自适应调整都会因反复重启收敛过程而降低性能。为了理解特定权重为何重要，我们引入了一种单变量因果探测方法，通过独立扰动每个奖励项 +/-20% 并测量 5 万步后的 PPO 响应。探测揭示了反直觉的杠杆作用：在极地切换场景中，增加 20% 的切换惩罚可带来 +157 Mbps 的增益，在冷热场景中则为 +130 Mbps；如果没有系统性的探测，人类专家或训练好的 MLP 无法获得这些发现。我们在已知和新颖的流量场景下评估了四种 MDP 架构变体（固定、基于规则、学习型 MLP、微调后的 LLM）。MLP 在已知场景下达到 357.9 Mbps，在新颖场景下达到 325.2 Mbps；而微调后的 LLM 则因权重振荡（而非缺乏领域知识）崩溃至 45.3+/-43.0 Mbps。输出的一致性而非知识储备，才是核心约束。我们的研究为通信系统中的 LLM-DRL 集成提供了基于实证的路线图，明确了 LLM 在何处具有不可替代的价值（自然语言意图理解），以及在何处简单方法便已足够。

## Abstract
Adaptive reward design for deep reinforcement learning (DRL) in multi-beam LEO satellite scheduling is motivated by the intuition that regime-aware reward weights should outperform static ones. We systematically test this intuition and uncover a switching-stability dilemma: near-constant reward weights (342.1 Mbps) outperform carefully-tuned dynamic weights (103.3+/-96.8 Mbps) because PPO requires a quasistationary reward signal for value function convergence. Weight adaptation-regardless of quality-degrades performance by repeatedly restarting convergence. To understand why specific weights matter, we introduce a single-variable causal probing method that independently perturbs each reward term by +/-20% and measures PPO response after 50k steps. Probing reveals counterintuitive leverage: a +20% increase in the switching penalty yields +157 Mbps for polar handover and +130 Mbps for hot-cold regimes-findings inaccessible to human experts or trained MLPs without systematic probing. We evaluate four MDP architect variants (fixed, rule-based, learned MLP, finetuned LLM) across known and novel traffic regimes. The MLP achieves 357.9 Mbps on known regimes and 325.2 Mbps on novel regimes, while the fine-tuned LLM collapses to 45.3+/-43.0 Mbps due to weight oscillation rather than lack of domain knowledge-output consistency, not knowledge, is the binding constraint. Our findings provide an empirically-grounded roadmap for LLM-DRL integration in communication systems, identifying where LLMs add irreplaceable value (natural language intent understanding) versus where simpler methods suffice.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）引导的低轨（LEO）卫星调度奖励函数设计进行了深入的实证研究，挑战了“动态奖励优于静态奖励”的直觉。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：LEO卫星需要根据不同的流量场景（如城市高需求、海洋稀疏需求、灾区突发需求）动态分配波束资源。深度强化学习（DRL）是主流方案，但通常使用固定的奖励函数。
*   **核心问题**：利用LLM的推理能力来动态调整奖励权重是否能提升性能？
*   **研究动机**：揭示在通信系统这种对数值精度和时间一致性要求极高的场景中，LLM与DRL集成时存在的根本性障碍。

### 2. 方法论
*   **三层架构设计**：
    *   **战略层（LLM）**：负责自然语言意图解析（如“优先保障应急通信”），将意图转化为目标配置文件（小时级）。
    *   **战术层（MLP/LLM）**：根据网络KPI（关键性能指标）生成具体的奖励权重向量 $w$（分钟级）。
    *   **操作层（PPO代理）**：根据当前奖励函数执行毫秒级的波束功率和带宽分配。
*   **单变量因果探测（Causal Probing）**：为了分析权重的影响，独立对每个奖励项进行 $\pm20\%$ 的扰动，观察5万步训练后的性能变化，从而构建奖励地形的因果图。
*   **场景检测**：使用 CUSUM（累积和）算法在线检测流量模式的变化。

### 3. 实验设计
*   **实验场景**：模拟19波束的Ka频段LEO卫星，考虑降雨衰减和四种典型流量模式（城市、海洋、灾难、混合）。
*   **对比方法（Architect Variants）**：
    1.  **M1 (Fixed)**：静态优化权重。
    2.  **M2 (Rule-based)**：基于专家规则的阈值分类器。
    3.  **M3 (Learned MLP)**：在模拟数据上训练的3层神经网络。
    4.  **M4 (Fine-tuned LLM)**：经过LoRA微调的Qwen3-4B模型。
*   **测试集**：包括已知场景和三种从未见过的“新颖场景”（IoT爆发、极地切换、冷热不均）。

### 4. 资源与算力
*   **硬件**：使用了 NVIDIA RTX 4060 GPU 进行推理。
*   **模型细节**：LLM 选用 Qwen3-4B，采用 4-bit NF4 量化和 LoRA 微调（33M 训练参数）。
*   **耗时/延迟**：LLM 推理延迟约 14秒/次，而 MLP 小于 1毫秒。PPO 代理训练步数为 50万步。

### 5. 实验数量与充分性
*   **实验规模**：每组实验使用 3 个随机种子（部分关键验证扩展至 8 个种子），报告均值和标准差。
*   **消融实验**：对比了四种架构在已知和未知场景下的表现，并进行了意图满意度分析。
*   **充分性**：实验设计较为客观，不仅展示了成功案例，还详细记录了 LLM 失败的案例（如权重震荡），提供了负面结果的深度分析。

### 6. 主要结论与发现
*   **切换-稳定性困境（Switching-Stability Dilemma）**：这是最重要的发现。PPO 算法需要准平稳的奖励信号来收敛价值函数。频繁切换权重（即使权重本身更优）会导致性能大幅下降（342.1 Mbps vs 103.3 Mbps）。
*   **LLM 的局限性**：微调后的 LLM 在新场景下表现崩溃（仅 45.3 Mbps），原因是其生成的数值权重存在严重的**震荡（Oscillation）**，变异系数比 MLP 高 4.6 倍。
*   **因果探测的价值**：发现“切换惩罚”权重对特定场景有极高的杠杆作用（+157 Mbps），这是人类专家难以直观发现的。
*   **分工建议**：LLM 擅长意图理解（不可替代），而 MLP 擅长数值优化（更稳定、更快）。

### 7. 优点
*   **实事求是**：没有盲目吹捧 LLM，而是通过严谨实验指出了 LLM 在实时控制回路中的“副作用”。
*   **因果分析**：引入因果探测方法，为黑盒 DRL 的奖励设计提供了可解释的诊断工具。
*   **架构实用**：提出的三层架构合理规避了 LLM 的高延迟问题。

### 8. 不足与局限
*   **模型规模限制**：仅测试了 4B 规模的模型，更大规模的模型是否能减少数值震荡尚不可知。
*   **仿真简化**：卫星模拟器简化了波束间干扰和复杂的轨道动力学。
*   **样本量**：虽然使用了多种子，但对于高方差的强化学习任务，3-8 个种子的覆盖面仍有提升空间。
*   **应用限制**：目前仅限于单星调度，未扩展到多星协作的复杂星座场景。

（完）
