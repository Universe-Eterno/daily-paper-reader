---
title: When does learning pay off? A study on DRL-based dynamic algorithm configuration for carbon-aware scheduling
title_zh: 学习何时能带来回报？一项关于碳感知调度的基于深度强化学习的动态算法配置研究
authors: "Andrea Mencaroni, Robbert Reijnen, Yingqian Zhang, Dieter Claeys"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01886v1"
tags: ["query:nco"]
score: 9.0
evidence: 基于深度强化学习的调度问题动态算法配置
tldr: 本研究探讨了基于深度强化学习（DRL）的动态算法配置（DAC）在碳感知流水车间调度中的应用。针对DRL训练成本高昂的问题，研究者开发了一个仅在简单小规模实例上训练的框架，并测试其在复杂未知实例上的泛化能力。结果表明，虽然在简单任务中与静态调优相当，但随着问题复杂度增加，DRL策略展现出显著的优越性和跨实例的鲁棒性，证明了其在实际优化场景中的投资价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01886v1/fig-001.webp\", \"caption\": \"Table 2: Overview of the instance datasets and their key features\", \"page\": 11, \"index\": 1, \"width\": 642, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-002.webp\", \"caption\": \"Table 5: Overview of the results, unknown instance types\", \"page\": 15, \"index\": 2, \"width\": 962, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of the MA-DRL-CAS-PFSP algorithm\", \"page\": 10, \"index\": 3, \"width\": 836, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-004.webp\", \"caption\": \"Figure 4: Convergence plots of average objective value per dataset, known instance types\", \"page\": 14, \"index\": 4, \"width\": 1006, \"height\": 698}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-005.webp\", \"caption\": \"Table 4: Overview of the results, known instance types\", \"page\": 14, \"index\": 5, \"width\": 958, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-006.webp\", \"caption\": \"Table 3: Overview of algorithm parameters with their default and tuned values\", \"page\": 13, \"index\": 6, \"width\": 688, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-007.webp\", \"caption\": \"Figure 1: Gantt chart, power requirement profiles, and resulting GHG emissions for the same instance optimized under two different objectives\", \"page\": 4, \"index\": 7, \"width\": 1013, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-008.webp\", \"caption\": \"Figure 3: Mean reward per episode during training\", \"page\": 12, \"index\": 8, \"width\": 601, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2604.01886v1/fig-009.webp\", \"caption\": \"Figure 5: Convergence plots of average objective value per dataset, unknown instance types\", \"page\": 16, \"index\": 9, \"width\": 1006, \"height\": 1053}]"
motivation: 旨在验证深度强化学习在动态算法配置中的高额训练投入是否能通过其在复杂未知实例上的泛化表现获得回报。
method: 构建了一个基于DRL的DAC框架，在简单调度实例上进行训练，并将其控制策略迁移至更复杂的碳感知调度问题中。
result: 实验发现DRL策略在处理与训练分布不同的复杂实例时，性能持续优于经过精细静态调优的基准算法。
conclusion: DRL能够习得具备强泛化能力的控制策略，使其在应对动态变化和高复杂度问题时比静态调优更具优势和应用价值。
---

## 摘要
深度强化学习（DRL）近期已成为动态算法配置（DAC）的一种极具前景的工具，使进化算法能够在线调整其参数，而非依赖于静态调优的配置。虽然 DRL 可以学习有效的控制策略，但其训练过程在计算上非常昂贵。如果学习到的策略具有泛化性，允许训练成果在不同实例类型和问题规模间迁移，那么这种成本可能是合理的。然而，对于现实世界的优化问题，这种承诺在实践中是否成立，以及在何种条件下学习投入能带来回报，目前尚不明确。在这项工作中，我们在碳感知置换流水车间调度问题的背景下研究了这一问题。我们开发了一个基于 DRL 的 DAC 框架，并仅在小型、简单的实例上对其进行训练。随后，我们将学习到的策略部署在相似以及更复杂的未见实例上，并将其性能与静态调优的基准进行比较，从而提供公平的对比。我们的研究结果表明，所提出的方法提供了一种强大的动态算法控制策略，可以有效地迁移到不同的未见问题实例中。值得注意的是，在与训练和调优过程中观察到的实例相似的简单且计算成本较低的实例上，DRL 的表现与静态调优基准相当。然而，随着实例特征的差异化和计算复杂度的增加，DRL 学习到的策略持续优于静态调优。这些结果证实了 DRL 可以获得鲁棒且具有泛化能力的控制策略，这些策略在训练实例分布之外依然有效。这种跨实例类型的泛化能力使得初始的计算投入物有所值，特别是在静态调优难以适应不断变化的问题场景的情况下。

## Abstract
Deep reinforcement learning (DRL) has recently emerged as a promising tool for Dynamic Algorithm Configuration (DAC), enabling evolutionary algorithms to adapt their parameters online rather than relying on static tuned configurations. While DRL can learn effective control policies, training is computationally expensive. This cost may be justified if learned policies generalize, allowing the training effort to transfer across instance types and problem scales. Yet, for real-world optimization problems, it remains unclear whether this promise holds in practice and under which conditions the investment in learning pays off. In this work, we investigate this question in the context of the carbon-aware permutation flow-shop scheduling problem. We develop a DRL-based DAC framework and train it exclusively on small, simple instances. We then deploy the learned policy on both similar and more complex unseen instances and compare its performance against a static tuned baseline, which provides a fair point of comparison. Our findings show that the proposed method provides a strong dynamic algorithm control policy that can be effectively transferred to different unseen problem instances. Notably, on simple and cheap to compute instances, similar to those observed during training and tuning, DRL performs comparably with the statically tuned baseline. However, as instance characteristics diverge and computational complexities increase, the DRL-learned policy continuously outperforms static tuning. These results confirm that DRL can acquire robust and generalizable control policies which are effective beyond the training instance distributions. This ability to generalize across instance types makes the initial computational investment worthwhile, particularly in settings where static tuning struggles to adapt to changing problem scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《When does learning pay off? A study on DRL-based dynamic algorithm configuration for carbon-aware scheduling》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：进化算法（EA）的性能高度依赖于参数配置。传统的静态调优（Static Tuning）不仅耗时，且无法适应搜索过程中动态变化的需求。虽然基于深度强化学习（DRL）的动态算法配置（DAC）能在线调整参数，但其训练成本极高。
*   **核心问题**：在现实世界的组合优化问题中，DRL 带来的高额训练投入是否真的“物有所值”？具体而言，在小规模实例上训练的 DRL 策略能否泛化到大规模、更复杂的未见实例上，从而超越经过同等计算预算调优的静态配置？

### 2. 论文提出的方法论
*   **核心框架**：提出了 **MA-DRL-CAS-PFSP** 框架，将 DRL 集成到模因算法（Memetic Algorithm）中，用于解决碳感知置换流水车间调度问题（CAS-PFSP）。
*   **MDP 建模细节**：
    *   **状态空间 (State)**：5维归一化向量，包括当前最佳适应度、种群平均适应度、适应度变异系数（代表多样性）、剩余计算预算比例、停滞计数。
    *   **动作空间 (Action)**：7维连续向量，映射到算法的关键参数：交叉率 ($\xi$)、作业/停顿交叉概率 ($\chi_j, \chi_p$)、作业/停顿变异概率 ($\pi_j, \pi_p$)、作业/停顿变异步长 ($\sigma_j, \sigma_p$)。
    *   **奖励函数 (Reward)**：采用密集奖励机制，根据相邻两代间最佳适应度的改进量计算。公式设计上对搜索后期的改进赋予更高权重，以激励智能体在困难阶段寻找突破。
*   **算法流程**：使用 **PPO (Proximal Policy Optimization)** 算法训练智能体。在每一代进化开始前，智能体观察算法状态并输出参数配置，随后算法执行进化操作并反馈奖励。

### 3. 实验设计
*   **问题场景**：碳感知调度（CAS），目标是在满足生产期限的同时，通过调整作业顺序和闲置时间，最大限度利用可再生能源并避开高碳排放时段。
*   **数据集**：共 10 个数据集（每组 50 个实例）。
    *   **已知类型 (Known)**：4 组小规模实例，用于训练和静态调优。
    *   **未知类型 (Unknown)**：6 组大规模实例（机器数量和作业数量显著增加），仅用于测试泛化性。
*   **基准测试 (Benchmarks)**：
    1.  **MA Default**：使用文献常用的默认固定参数。
    2.  **MA Tuned**：使用 Optuna 框架在与 DRL 训练完全相同的预算（400万次迭代）和训练集上进行的静态参数调优。
*   **对比方式**：在所有 10 个数据集上运行 10 次独立实验，通过 Wilcoxon 秩和检验评估统计显著性。

### 4. 资源与算力
*   **硬件环境**：11th Gen Intel Core i5-1145G7 (2.60 GHz)。
*   **算力投入**：
    *   **训练/调优预算**：设定为 400 万个算法步（Steps），即 4 万个训练回合（Episodes）。
    *   **GPU 使用**：文中**未明确提及 GPU 型号**，考虑到 PPO 在此类状态空间较小的任务中通常可在 CPU 上高效运行，且实验环境描述仅提及了 CPU。
    *   **软件**：基于 Python 3.10，使用 Stable-Baselines3 实现 PPO，Optuna 实现静态调优。

### 5. 实验数量与充分性
*   **实验规模**：总计测试了 500 个不同的问题实例（10个数据集 $\times$ 50个实例），每个实例重复 10 次以消除随机性影响。
*   **充分性与公平性**：
    *   **公平性**：这是本研究的亮点。作者确保了 DRL 的训练预算与静态调优的搜索预算完全对等，避免了因计算资源不平等导致的结论偏差。
    *   **客观性**：通过区分“已知”和“未知”实例，专门验证了 DRL 的迁移学习能力，而非仅仅在训练集上过拟合。

### 6. 论文的主要结论与发现
*   **学习的回报点**：在简单或与训练集相似的实例上，DRL 的表现与静态调优基本持平，没有显著优势。
*   **复杂度的正相关性**：随着问题规模和组合复杂度增加（进入“未知”领域），DRL 的优势逐渐显现。在最大规模的实例上，DRL 相比静态调优实现了超过 **3%** 的性能提升，相比默认参数提升达 **10%**。
*   **泛化能力验证**：证明了在小型实例上训练的 DRL 策略可以成功迁移到大型实例，这使得昂贵的初始训练成本可以通过多次跨规模应用得到补偿。

### 7. 优点
*   **实验严谨性**：通过对等预算对比，解决了 DAC 领域长期存在的“DRL 提升是否仅源于更多计算量”的质疑。
*   **实际应用导向**：选择了复杂的碳感知调度问题，而非简单的合成基准函数，具有更强的工业参考价值。
*   **泛化性洞察**：明确指出了 DRL 并非在所有场景下都优于调优，而是更适合处理具有分布偏移和高复杂度的任务。

### 8. 不足与局限
*   **算法单一性**：实验仅基于一种模因算法框架，尚未验证该 DRL 策略在其他类型的 EA（如差分进化或粒子群算法）上的通用性。
*   **状态特征局限**：状态空间主要基于算法运行指标，未包含问题本身的结构特征（Instance Features），这可能限制了智能体对特定问题属性的深度适应。
*   **跨类泛化未知**：虽然验证了跨规模泛化，但尚未探讨在一个问题类别（如调度）训练的策略是否能直接用于另一个类别（如路径规划）。

（完）
