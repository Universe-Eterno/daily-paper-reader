---
title: Self-Supervised Graph Neural Networks for Full-Scale Tertiary Voltage Control
title_zh: 用于全规模三级电压控制的自监督图神经网络
authors: "Balthazar Donon, Geoffroy Jamgotchian, Hugo Kulesza, Louis Wehenkel"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03087v1"
tags: ["query:nco"]
score: 9.0
evidence: 用于电压控制中混合整数非线性规划的自监督图神经网络
tldr: 针对大规模电力系统三级电压控制（TVC）中传统优化方法计算效率低、难以实时应用的问题，本文提出一种基于自监督学习的图神经网络（GNN）方法。该方法将TVC建模为摊销优化问题，通过在法国高压/超高压电网一年的预测数据上进行训练，使模型能够快速调整设定值以减少电压越限。研究证明了GNN在处理大规模非线性混合整数规划问题中的潜力，为实时电力调度提供了高效的决策支持。
selection_source: fresh_fetch
motivation: 传统的三级电压控制优化方法在大规模电网中计算缓慢，难以满足实时决策支持的需求。
method: 将电压控制建模为摊销优化问题，利用自监督学习训练图神经网络（GNN）来最小化电压违规。
result: 在法国全规模高压/超高压电网的一年日前预测数据上，该模型显著减少了平均电压违规次数。
conclusion: 自监督GNN能够作为高效的电压控制代理工具，在保证计算速度的同时有效提升大规模电网的电压稳定性。
---

## 摘要
调度员日益增长的工作量主要集中在三级电压控制（TVC）上，即通过调整一系列设定值和连接状态来调节电压。TVC 可以被建模为混合整数非线性规划问题，但最先进的优化方法在大型系统上的扩展性较差，使其在实际规模和实时决策支持中变得不切实际。观察到 TVC 并不需要任何最优性保证，我们将其建模为一个摊销优化问题，通过自监督训练图神经网络（GNN）来最小化电压越限。作为第一步，我们考虑了法国输电网运营商（TSO）所使用的预测流程后处理这一特定用例，其中训练好的 GNN 将充当 TVC 代理。在对法国全规模高压-超高压（HV-EHV）电网一年的日前预测数据进行训练后，我们的模型成功显著减少了平均电压越限次数。

## Abstract
A growing portion of operators workload is dedicated to Tertiary Voltage Control (TVC), namely the regulation of voltages by means of adjusting a series of setpoints and connection status. TVC may be framed as a Mixed Integer Non Linear Program, but state-of-the-art optimization methods scale poorly to large systems, making them impractical for real-scale and real-time decision support. Observing that TVC does not require any optimality guarantee, we frame it as an Amortized Optimization problem, addressed by the self-supervised training of a Graph Neural Network (GNN) to minimize voltage violations. As a first step, we consider the specific use case of post-processing the forecasting pipeline used by the French TSO, where the trained GNN would serve as a TVC proxy. After being trained on one year of full-scale HV-EHV French power grid day-ahead forecasts, our model manages to significantly reduce the average number of voltage violations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种利用自监督图神经网络（GNN）解决大规模电力系统三级电压控制（TVC）问题的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：三级电压控制（TVC）旨在通过调整发电机电压设定值、电容器/电抗器（并联补偿）开关、线路开关及变压器分接头来维持电网电压稳定。
*   **研究动机**：
    *   **复杂性**：TVC 是一个大规模的混合整数非线性规划（MINLP）问题，传统优化方法（如 AC-OPF）在处理全规模电网（如法国电网）时计算极其缓慢，无法满足实时决策支持的需求。
    *   **环境变化**：可再生能源并网和电网结构变化导致高压事件频发，增加了调度员的压力。
    *   **目标转变**：TVC 并不一定追求绝对最优解，而更看重在极短时间内找到能显著减少电压违规的有效可行解。

### 2. 论文提出的方法论
*   **核心思想**：将 TVC 建模为**摊销优化（Amortized Optimization）**问题。不再针对每个具体工况独立求解，而是训练一个参数化的 GNN 模型，使其能够直接从电网状态映射到控制决策。
*   **关键技术细节**：
    *   **数据表示**：采用**超异构多图（H2MG）**结构，能够原生处理电网中复杂的拓扑变化（如母线分裂、设备增减），无需繁琐的预处理。
    *   **模型架构**：使用 **H2MGNODE**（基于神经常微分方程的图神经网络），通过连续的时间演化模拟节点间的信息传递，增强了对拓扑结构的感知能力。
    *   **自监督学习**：通过与工业级电力系统模拟器（OpenLoadFlow）交互进行训练。由于模拟器不可导，论文采用了基于 **Log-derivative（对数导数）技巧**的梯度估计器（类似于强化学习中的策略梯度），最小化包含电压违规、电流过载和网损的综合目标函数。
    *   **决策处理**：模型同时处理连续变量（SVR 设定值）、二值变量（线路和并联补偿开关）和分类变量（变压器分接头设定值）。

### 3. 实验设计
*   **数据集**：使用法国全规模高压/超高压（HV-EHV）电网的一年日前预测数据，包含约 170,000 个运行状态。电网规模约为 7,000 个母线和 8,000 条线路。
*   **实验划分**：分为训练集（9-11月）、验证集（12月前两周）和测试集（12月后两周）。
*   **Benchmark（基准方法）**：对比了 **Init 基准**（即初始预测状态，仅对电压设定值进行简单的全局统一偏移优化）。
*   **对比维度**：电压违规次数（过压/欠压）、支路过载、焦耳损耗以及控制杠杆的使用分布。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA A10 GPU** 进行 GNN 的前向和后向计算，配合 **AMD EPYC 9554 64核处理器** 进行样本采样和梯度估计。
*   **训练时长**：训练 10,000 次迭代（Batch Size 为 4）耗时约 **5 天**。
*   **推理速度**：在推理阶段，处理 4 个电网文件（包括 GNN 预测、修改文件及运行 AC 潮流模拟）总计耗时约 **12 秒**。

### 5. 实验数量与充分性
*   **实验规模**：实验基于真实世界的大规模电网数据，测试集包含 9,264 个工况，样本量充足。
*   **充分性与客观性**：
    *   实验涵盖了全年的多种运行场景，具有较强的代表性。
    *   **局限性**：由于训练耗时较长，作者承认未能进行全面的超参数搜索。此外，对比的基准方法较为简单，尚未与更复杂的专家系统或启发式算法进行深度对比。

### 6. 主要结论与发现
*   **有效性**：GNN 模型将平均电压违规次数从 **38.8 次降低至 26.6 次**，显著优于基准方法。
*   **平衡性**：基准方法往往导致严重的过压，而 GNN 能够更好地平衡过压和欠压风险。
*   **副作用控制**：在改善电压的同时，支路过载和网损仅有极微小（约 0.5%）的增加，基本可以忽略。
*   **决策逻辑**：模型学会了有选择性地开启电抗器（降低电压）和极少数情况下断开线路，展现了复杂的控制逻辑。

### 7. 优点
*   **全规模应用**：成功将 GNN 应用于包含 7,000+ 节点的真实国家级电网，证明了模型的可扩展性。
*   **拓扑鲁棒性**：H2MG 架构解决了电力系统频繁变动拓扑导致的神经网络输入不固定问题。
*   **自监督闭环**：无需人工标注的最优解标签，通过直接最小化物理违反目标进行学习，降低了数据获取成本。

### 8. 不足与局限
*   **训练效率**：5 天的训练时间对于快速迭代仍显过长，限制了超参数优化。
*   **模拟器收敛问题**：在极端工况下，AC 潮流模拟器存在不收敛的情况（测试集中约 3.5% 的工况），这会干扰梯度估计。
*   **安全性保证**：虽然模型能减少违规，但作为深度学习方法，其输出仍需经过物理模拟器校验后才能应用于实际操作，缺乏严格的物理约束硬保证。

（完）
