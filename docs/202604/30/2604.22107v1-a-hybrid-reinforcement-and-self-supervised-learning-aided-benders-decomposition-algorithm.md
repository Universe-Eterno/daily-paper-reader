---
title: A Hybrid Reinforcement and Self-Supervised Learning Aided Benders Decomposition Algorithm
title_zh: 一种混合强化学习与自监督学习辅助的 Benders 分解算法
authors: "Bernard T. Agyeman, Zhe Li, Ilias Mitrai, Prodromos Daoutidis"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22107v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 用于Benders分解的混合强化学习和基于图的表示
tldr: "本研究提出一种结合强化学习与自监督学习的混合框架，旨在加速广义Benders分解（GBD）算法。该框架利用图强化学习代理解决主问题的整数变量分配，并结合KKT信息神经网络通过自监督学习预测子问题的原对偶解以直接构建割平面。在混合整数非线性规划（MINLP）案例中，该方法在保持最优性的同时，将求解时间缩短了57.5%，显著提升了求解效率。"
source: arxiv
selection_source: fresh_fetch
motivation: 针对广义Benders分解算法在处理复杂混合整数非线性规划问题时计算效率较低的挑战。
method: 结合图强化学习处理主问题整数分配，并利用KKT信息神经网络自监督预测子问题解以生成Benders割。
result: "在MINLP案例研究中，该框架比传统GBD算法减少了57.5%的求解时间，且所有测试实例均能获得最优解。"
conclusion: 混合强化学习与自监督学习能有效加速Benders分解过程，是解决复杂优化问题的有效途径。
---

## 摘要
我们提出了一种混合强化学习与自监督学习框架，用于加速广义 Benders 分解（GBD）。在该框架中，一个基于图的强化学习智能体在主问题的二分图表示上运行，并结合验证机制，确定解决主问题的整数变量赋值。这些赋值随后作为 KKT 信息神经网络的输入，该网络通过自监督训练来预测近似满足子问题 Karush-Kuhn-Tucker (KKT) 条件的原对偶解。预测的解被直接用于构建 Benders 割平面。该框架在一个混合整数非线性规划案例研究中进行了评估，结果表明，与经典的 GBD 相比，其求解时间缩短了 57.5%，同时在所有测试实例中均能稳定地恢复最优解。

## Abstract
We propose a hybrid reinforcement and self-supervised learning framework for accelerating generalized Benders decomposition (GBD). In this framework, a graph based reinforcement learning agent operates on a bipartite representation of the master problem and, together with a verification mechanism, determines the integer variable assignments that solve the master problem. These assignments are then used as inputs to a KKT informed neural network, trained via self supervision to predict primal dual solutions that approximately satisfy the Karush Kuhn Tucker conditions of the subproblem. The predicted solutions are used to construct Benders cuts directly. The framework is evaluated on a mixed integer nonlinear programming case study, where it achieves a 57.5% reduction in solution time relative to classical GBD while consistently recovering optimal solutions across all test instances.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合强化学习（RL）与自监督学习（Self-Supervised Learning）的混合框架，旨在加速求解混合整数非线性规划（MINLP）问题的广义 Benders 分解（GBD）算法。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：广义 Benders 分解（GBD）在处理 MINLP 问题时，主问题（Master Problem, MP）的整数搜索空间随迭代增加而变得极其复杂，且子问题（Subproblem, SP）的非线性计算开销巨大，导致整体求解效率低下。
*   **研究动机**：现有的机器学习加速方法通常只针对主问题或子问题中的某一个进行优化。本文试图建立一个统一的框架，同时利用学习型代理（Surrogates）替代主问题求解器和子问题求解器，以实现更大幅度的计算加速。

### 2. 方法论
该框架由两个核心学习组件和一个验证机制组成：
*   **主问题代理（基于图的强化学习）**：
    *   **核心思想**：将主问题建模为二分图（变量节点与约束节点），利用图神经网络（GNN）捕捉问题的结构特征。
    *   **技术细节**：采用 Actor-Critic 架构，通过近端策略优化（PPO）算法训练。智能体根据当前主问题的状态输出二进制变量的赋值概率。
    *   **验证机制**：引入置信度阈值（$\delta_1, \delta_2$）。高置信度的变量直接固定，低置信度的变量交给 MIP 求解器。同时，通过检查候选解的下界有效性，确保算法的收敛性。
*   **子问题代理（KKT 信息神经网络, KINN）**：
    *   **核心思想**：利用神经网络直接预测子问题的原变量（Primal）和对偶变量（Dual）解，从而直接构造 Benders 割平面，无需调用非线性求解器。
    *   **技术细节**：采用自监督学习，损失函数由 KKT 条件的残差组成，包括平稳性（Stationarity）、原可行性（Primal Feasibility）和互补松弛性（Complementarity）。
    *   **架构**：采用分支架构，共享特征提取层，分别预测原变量和两组对偶变量。

### 3. 实验设计
*   **案例场景**：一个具有参数化目标系数的 MINLP 案例研究（源自 Floudas, 1995），包含 5 个二进制变量和 6 个连续变量。
*   **Benchmark（基准）**：
    *   **Classical GBD**：使用 BONMIN（主问题）和 IPOPT（子问题）的传统 GBD。
*   **对比方法**：
    *   **Agent-only**：仅使用强化学习代理优化主问题。
    *   **KINN-only**：仅使用 KINN 优化子问题。
    *   **Proposed（混合框架）**：同时使用上述两个代理。
*   **测试规模**：在 100 个随机生成的测试实例上进行评估。

### 4. 资源与算力
*   **硬件环境**：台式机，配置为 Intel i7-14700 CPU，32 GB RAM。
*   **算力说明**：文中**未明确提及 GPU 型号及数量**。
*   **训练时长**：RL 智能体训练了 10,000 个回合（Episodes）；KINN 训练了 40,000 个轮次（Epochs），随后对对偶分支进行了额外的 40,000 轮微调。

### 5. 实验数量与充分性
*   **实验数量**：进行了 100 组测试实例的对比实验，并提供了消融实验（Agent-only 和 KINN-only）来验证各组件的贡献。
*   **充分性评价**：实验设计较为客观，对比了单组件优化与全组件优化的差异。然而，**实验案例的规模较小**（仅 5 个二进制变量），对于大规模、高维度 MINLP 问题的泛化能力和鲁棒性尚需进一步验证。

### 6. 主要结论与发现
*   **显著加速**：提出的混合框架相比传统 GBD 减少了 **57.5%** 的总求解时间。
*   **最优性保持**：在所有 100 个测试实例中，该框架均成功恢复了参考的最优解，证明了验证机制的有效性。
*   **不精确割的影响**：实验表明，尽管 KINN 生成的割平面是不精确的（基于近似 KKT 解），但它们在迭代过程中并未破坏 GBD 的收敛行为。

### 7. 优点
*   **双重加速**：同时针对主问题和子问题进行学习优化，比单一组件优化效果更佳。
*   **物理信息驱动**：KINN 结合了 KKT 条件的数学结构，使得自监督学习无需大量标注的最优解数据。
*   **鲁棒性设计**：通过验证机制和置信度处理，弥补了神经网络预测缺乏严谨可行性保证的缺陷。

### 8. 不足与局限
*   **问题规模受限**：测试案例属于小规模问题，在变量数成千上万的实际工业场景中，GNN 的推理开销和 KINN 的训练难度可能会显著增加。
*   **超参数敏感性**：损失函数中各项残差的权重（$\alpha, \beta, \gamma$）以及 RL 的奖励函数参数需要精细调优。
*   **应用限制**：目前仅针对具有特定线性/非线性结构的 MINLP 子类进行了验证，对于更通用的非凸问题可能面临收敛挑战。

（完）
