---
title: "RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS): A Structured Methodology Using Large Language Models for Hardware Design"
title_zh: RAG增强的基于内核的启发式合成 (RKHS)：一种利用大语言模型进行硬件设计的结构化方法论
authors: "Shiva Ahir, Alex Doboli"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26153v1"
tags: ["query:nco-llm"]
score: 9.5
evidence: 大模型驱动的调度与布线优化启发式算法合成
tldr: 本研究针对电子设计自动化（EDA）中启发式算法设计高度依赖专家经验的难题，提出了RKHS框架。该框架结合检索增强生成（RAG）、紧凑内核启发式模板及大语言模型（LLM）驱动的迭代优化循环，实现了可重用硬件设计优化策略的系统化合成。在高级综合（HLS）调度任务中，该方法显著降低了延迟，展示了LLM在自动化EDA工具开发中的巨大潜力。
source: arxiv
selection_source: fresh_fetch
motivation: 传统的EDA启发式算法设计高度依赖专家知识且开发周期长，需要一种更系统、自动化的方法来合成高效的优化策略。
method: 提出了RKHS方法，通过整合RAG技术、内核启发式模板和基于LLM的自我反馈迭代循环来生成和优化硬件设计启发式代码。
result: "在HLS列表调度实验中，该方法在仅增加1.3倍运行开销的情况下，将平均调度长度缩短了高达11%。"
conclusion: RKHS证明了结合RAG与结构化合成循环的LLM框架能有效解决复杂的EDA优化问题，具有良好的通用性和实用价值。
---

## 摘要
启发式设计支撑着现代电子设计自动化 (EDA) 工具，但制定有效的布局、布线和调度策略需要大量的专业知识。我们研究了大语言模型 (LLMs) 如何超越单次代码生成，系统地合成可重用的优化启发式算法。我们提出了 RAG 增强的基于内核的启发式合成 (RKHS)，它集成了检索增强生成 (RAG)、紧凑的内核启发式模板以及受迭代自我反馈启发的 LLM 驱动细化循环。在应用于高层综合 (HLS) 中最小化延迟的列表调度时，原型系统在仅增加 1.3 倍运行开销的情况下，比基准调度器减少了高达 11% 的平均调度长度，且这种结构化的检索-合成循环可推广到其他 EDA 优化问题。

## Abstract
Heuristic design upholds modern electronic design automation (EDA) tools, yet crafting effective placement, routing, and scheduling strategies entails substantial expertise. We study how large language models (LLMs) can systematically synthesize reusable optimization heuristics beyond one-shot code generation. We propose RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS), which integrates retrieval-augmented generation (RAG), compact kernel heuristic templates, and an LLM-driven refinement loop inspired by iterative self-feedback. Applied to latency-minimizing list scheduling in high-level synthesis (HLS), a prototype reduces average schedule length by up to 11 percent over a baseline scheduler with only 1.3x runtime overhead, and the structured retrieval-synthesis loop generalizes to other EDA optimization problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAG-Enhanced Kernel-Based Heuristic Synthesis (RKHS): A Structured Methodology Using Large Language Models for Hardware Design》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代电子设计自动化（EDA）工具高度依赖人工设计的启发式算法来解决布局、布线和调度等 NP-hard 问题。这些算法设计耗时耗力、领域特定且难以跨基准测试泛化。
*   **研究动机**：虽然大语言模型（LLM）在代码生成方面表现出色，但现有的方法大多将其视为“单次（one-shot）”生成器，缺乏系统性的机制来提取结构化特征并合成可重用的优化策略。本研究旨在探索如何利用 LLM 自动合成可执行、可解释且可迁移的硬件设计启发式算法。

### 2. 方法论：RKHS 框架
RKHS（RAG增强的基于内核的启发式合成）将启发式设计视为一个检索引导的搜索过程，核心步骤如下：
*   **核心思想**：不从零开始生成代码，而是将调度问题视为一个固定的元算法（如列表调度），通过 LLM 专门合成用于对操作进行排序的“优先级函数（Priority Function）”。
*   **关键技术细节**：
    *   **内核库（Kernel Library）构建**：从训练图中提取结构化基元（内核），每个内核包含“结构签名”（如关键路径摘要、扇出分布）和“启发式模板”（关联的调度偏好）。
    *   **检索增强生成（RAG）**：给定新图 $G$，计算其结构嵌入，通过余弦相似度从库中检索最相似的 $m$ 个内核。
    *   **LLM 合成与细化**：将检索到的内核作为上下文输入 LLM（如 GPT-4），合成确定性的优先级函数代码。
    *   **迭代反馈循环**：在验证集上评估生成的代码，将失败案例和性能反馈（如延迟、运行时间）重新喂给 LLM 进行自我修正和细化。
*   **公式化表达**：目标是寻找参数化的策略 $H_\theta$，通过最小化 Makespan（总延迟）和运行时间开销来优化评分函数 $J(H; G)$。

### 3. 实验设计
*   **应用场景**：高层综合（HLS）中资源受限的列表调度（List Scheduling），目标是最小化延迟。
*   **数据集**：使用了 200 个训练 DAG（有向无环图）和 50 个验证 DAG。
*   **基准测试（Benchmark）**：
    *   **Baseline**：经典的基于层级（Level-based）的列表调度器。
*   **对比实验**：
    *   **消融实验**：分别移除检索机制（No Retrieval）、移除基元抽象（No Motif）以及使用随机内核（Random Kernel）。
    *   **迭代对比**：对比不同迭代轮次（如 Iteration 0 到 2）生成的启发式策略性能。

### 4. 资源与算力
*   **模型使用**：明确指出使用了 **GPT-4** 作为核心合成引擎。
*   **算力细节**：文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。由于是基于 LLM API 的合成框架，其主要开销在于 API 调用和在 CPU 上运行调度器进行验证的时间。文中提到合成的启发式算法在推理时的运行开销仅为基准算法的 1.3 倍。

### 5. 实验数量与充分性
*   **实验规模**：共使用了 250 个图实例，进行了 3 轮主要的迭代合成，并针对 4 个代表性验证图（G1-G4）进行了详细的消融分析。
*   **充分性评价**：实验设计较为系统，包含了消融实验来验证 RAG 和基元提取的有效性。然而，验证集规模（50个图）相对较小，且主要集中在调度问题上，对于更复杂的 EDA 任务（如布线）的覆盖尚处于理论推导阶段。

### 6. 主要结论与发现
*   **性能提升**：RKHS 合成的启发式算法（如“扇出感知”策略）比基准调度器减少了高达 **11%** 的平均调度长度。
*   **效率平衡**：在获得显著延迟优化的同时，运行时间开销控制在合理的 **1.3 倍** 以内。
*   **结构化优势**：实验证明，结合结构化检索和确定性基元提取对于稳定提升 LLM 生成代码的质量至关重要，随机检索或无检索会导致性能显著下降。

### 7. 优点
*   **可解释性**：生成的优先级函数是紧凑且可读的数学表达式（如关键路径、扇出和层级的加权组合），而非黑盒模型。
*   **系统化流程**：通过 RAG 将领域知识（内核库）与 LLM 的生成能力解耦，降低了 LLM 产生幻觉的风险。
*   **闭环优化**：引入了基于失败案例的反馈机制，使模型能够根据实际运行结果进行自我进化。

### 8. 不足与局限
*   **泛化范围有限**：目前仅在 HLS 调度问题上进行了验证，尚未在布局、布线等更复杂的 EDA 环节进行大规模测试。
*   **依赖闭源模型**：实验高度依赖 GPT-4，未探讨开源模型（如 Llama 系列）在相同框架下的表现。
*   **内核库构建成本**：虽然内核提取是自动化的，但初始内核库的质量和多样性仍可能影响最终合成的效果。
*   **规模化挑战**：对于超大规模的 DAG，频繁的结构嵌入计算和检索可能会带来额外的计算负担。

（完）
