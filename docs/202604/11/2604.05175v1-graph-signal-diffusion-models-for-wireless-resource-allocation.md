---
title: Graph Signal Diffusion Models for Wireless Resource Allocation
title_zh: 用于无线资源分配的图信号扩散模型
authors: "Yigit Berkay Uslu, Samar Hadou, Shirin Saeedi Bidokhti, Alejandro Ribeiro"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05175v1"
tags: ["query:nco"]
score: 8.0
evidence: 基于GNN的扩散模型用于资源分配优化
tldr: 本文针对具有图结构干扰的无线网络资源分配问题，提出了一种基于图信号扩散模型的生成式策略。该方法利用对偶专家算法生成训练样本，将资源分配建模为图信号，并采用基于图神经网络（GNN）的U-Net架构进行条件分布学习。在推理阶段，该模型通过直接采样实现高效分配，在功率控制任务中达到了接近最优的性能，并展现出卓越的泛化与迁移能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05175v1/fig-001.webp\", \"caption\": \"Fig. 1: Primal-dual expert policies. Top-left panel shows the evolution of the dual multipliers associated with the ergodic minimum-rate constraints of selected receivers for an example network H in the power-control problem of Section 3. The golden shaded region marks the convergence regime. Bottom-left panel plots the per-iteration fraction of violated constraints against the worst-case constraint slack, averaged over a 2000-iteration moving window. Although the iterates x†(λk) remain infeasible for at least 20% of the receivers, the randomized policy D̂x(· |H) is near-optimal and feasible in the average-sense [cf. (11), (12)]. The long transient preceding convergence is evident from both panels. The panels on the right show several 2D slices of the allocation vectors drawn from the expert policy in the convergence regime, with samples from the transient regime shown as faint markers. The top-row slices correspond to the receivers plotted in the top-left panel. The orange overlays show the corresponding slices for samples generated by the learned policy Dx(· |H;θ∗), which closely matches the experts.\", \"page\": 3, \"index\": 1, \"width\": 1041, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2604.05175v1/fig-002.webp\", \"caption\": \"Fig. 2: Performance of the time-shared U-GNN policy. The U-GNN policy closely tracks the expert policy in terms of tail rates (left, middle) while achieving comparable mean-rate utility (right). The deterministic baselines fail to satisfy the QoS levels for the tail users.\", \"page\": 4, \"index\": 2, \"width\": 1044, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.05175v1/fig-003.webp\", \"caption\": \"Fig. 4: Size-transferability of U-GNN policies. The model is trained on networks of size N = 400 and four density levels ν ∈ {6.6, 8.2, 10.1, 11.9} tx-rx pairs/km2, and evaluated on networks of varying sizes at similar densities.\", \"page\": 5, \"index\": 3, \"width\": 447, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.05175v1/fig-004.webp\", \"caption\": \"Fig. 3: Generalization of U-GNN policies across fmin (QoS) levels. The model is trained on fmin ∈ {0.4, 0.5, 0.6, 0.7, 0.8} and evaluated at intermediate fmin levels.\", \"page\": 5, \"index\": 4, \"width\": 437, \"height\": 289}]"
motivation: 旨在解决传统无线资源分配算法在处理复杂干扰结构和动态约束时计算开销大、难以实时应用的问题。
method: 提出一种以GNN为基础的U-Net扩散模型，通过模仿对偶专家算法生成的分配分布来学习最优资源分配策略。
result: 实验表明，该模型在功率控制案例中实现了近乎最优的累积和速率和可行性约束，且在不同网络状态下具有强鲁棒性。
conclusion: 扩散模型结合图神经网络能有效摊销复杂优化算法的计算成本，为无线网络资源优化提供了一种高性能的生成式建模新途径。
---

## 摘要
我们考虑具有图结构干扰的无线网络中的约束遍历资源优化问题。我们训练一个扩散模型策略，以匹配资源分配上的专家条件分布。通过利用原对偶（专家）算法，我们生成原变量迭代值，作为每个训练网络实例对应专家条件分布的采样。我们将分配视为支撑在已知信道状态图上的随机图信号。我们将扩散模型架构实现为图神经网络（GNN）块的 U-Net 层级结构，并以信道状态和额外的节点状态为条件。在推理阶段，学习到的生成模型通过直接从近优条件分布中采样分配向量，从而摊销了迭代专家策略的计算开销。在功率控制案例研究中，我们表明，对生成的功率分配进行分时处理可以实现近优的遍历和速率效用以及近乎可行的遍历最小速率，并在不同网络状态下表现出强大的泛化能力和迁移性。

## Abstract
We consider constrained ergodic resource optimization in wireless networks with graph-structured interference. We train a diffusion model policy to match expert conditional distributions over resource allocations. By leveraging a primal-dual (expert) algorithm, we generate primal iterates that serve as draws from the corresponding expert conditionals for each training network instance. We view the allocations as stochastic graph signals supported on known channel state graphs. We implement the diffusion model architecture as a U-Net hierarchy of graph neural network (GNN) blocks, conditioned on the channel states and additional node states. At inference, the learned generative model amortizes the iterative expert policy by directly sampling allocation vectors from the near-optimal conditional distributions. In a power-control case study, we show that time-sharing the generated power allocations achieves near-optimal ergodic sum-rate utility and near-feasible ergodic minimum-rates, with strong generalization and transferability across network states.

---

## 论文详细总结（自动生成）

这是一份关于论文《Graph Signal Diffusion Models for Wireless Resource Allocation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在具有复杂干扰结构的无线网络中，高效地解决受限的**遍历资源优化（Ergodic Resource Optimization）**问题。
*   **研究背景**：在快速衰落的无线信道中，为了满足服务质量（QoS）约束，随机化策略（通过分时复用实现）通常优于确定性策略。然而，传统的原对偶（Primal-Dual）迭代算法虽然能找到近优解，但其在线计算开销巨大，难以满足实时性要求。
*   **整体含义**：本文旨在利用**生成式扩散模型（Diffusion Models）**来“摊销（Amortize）”迭代优化的计算成本。通过学习专家算法生成的分配分布，模型可以在推理阶段直接采样出高质量的资源分配方案。

### 2. 方法论
*   **核心思想**：将无线资源分配建模为**条件图信号生成问题**。利用扩散模型学习专家策略在给定信道状态下的条件概率分布。
*   **关键技术细节**：
    *   **专家数据生成**：采用原对偶算法。通过拉格朗日松弛法处理约束，生成原变量迭代序列。该序列的遍历均值（时间平均）被证明是近优且满足约束的，这些迭代点作为扩散模型的训练样本。
    *   **图信号建模**：将用户视为节点，干扰关系视为边，发射功率视为节点上的信号。
    *   **U-GNN 架构**：扩散模型的去噪骨干网络采用 U-Net 结构的图神经网络（GNN）。它包含编码器-解码器层级结构和跳跃连接（Skip Connections），能够捕获多尺度的空间干扰特征。
    *   **条件化处理**：去噪过程以信道状态信息（H）和节点状态（如 QoS 需求 $f_{min}$、直接链路强度等）为条件。
*   **算法流程**：
    1.  **训练阶段**：运行原对偶算法获取专家样本 $\rightarrow$ 对样本加噪 $\rightarrow$ 训练 U-GNN 预测噪声。
    2.  **推理阶段**：从高斯噪声开始，利用学习到的 U-GNN 进行条件去噪采样（使用加速的 DDIM 采样器），直接获得功率分配向量。

### 3. 实验设计
*   **场景与数据集**：
    *   **场景**：Ad-hoc 无线网络功率控制。
    *   **设置**：400 个发射机-接收机对，随机几何图（RGG）布局，包含大尺度路径损耗和瑞利衰落。
    *   **数据集**：128 个不同的网络配置，涵盖 4 种用户密度和 5 种不同的最小速率约束（QoS 级别）。
*   **Benchmark 与对比方法**：
    *   **Expert (Primal-Dual)**：作为性能上限的专家算法。
    *   **AP (Average Power)**：固定功率为专家分布的均值（确定性基准）。
    *   **FP (Full Power)**：全功率发射（最大化吞吐量但忽略公平性）。
    *   **U-GNN (本文方法)**：基于扩散模型的生成式策略。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号或训练总时长。
*   **训练细节**：提到了使用 AdamW 优化器，学习率为 $10^{-4}$，训练 5000 个 epoch。扩散过程使用 500 个噪声步长，推理时使用 DDIM 加速至 100 步。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 128 个网络实例，每个实例生成 200 个专家样本，总样本量约为 2.5 万组。
    *   进行了**泛化性实验**：在未见过的 QoS 需求（$f_{min}$）上进行测试。
    *   进行了**可迁移性实验**：在 400 节点规模训练，在 200 到 1600 节点规模上测试。
*   **充分性评价**：实验设计较为充分，不仅验证了基础性能，还重点考察了 GNN 的核心优势——可扩展性和泛化能力。对比基准涵盖了传统的确定性方案，体现了随机化策略的必要性。

### 6. 主要结论与发现
*   **性能接近专家**：U-GNN 策略在尾部速率（第 1 和第 5 百分位数）上紧贴专家算法，远优于确定性基准（AP 和 FP）。
*   **满足约束**：通过分时复用生成的样本，模型能够使 95% 以上的用户满足最小速率约束。
*   **高效推理**：扩散模型通过单次采样过程替代了成千上万次的原对偶迭代，显著降低了在线计算延迟。
*   **强泛化性**：模型能够平滑地内插未见过的 QoS 需求，并能直接迁移到更大规模的网络中而性能不下降。

### 7. 优点
*   **架构创新**：将扩散模型与 U-GNN 结合，充分利用了无线网络的图拓扑结构。
*   **解决非凸约束**：通过模仿专家分布而非直接求解优化，绕过了非凸优化在实时应用中的难题。
*   **随机化优势**：成功捕获了最优策略的随机性（多模态），这在无线资源分配的分时复用中至关重要。

### 8. 不足与局限
*   **推理延迟**：尽管使用了 DDIM 加速，但扩散模型的多步迭代采样相比于简单的 MLP 或单次前向 GNN 仍然较慢，对于极高频的信道变化可能仍有压力。
*   **依赖专家质量**：模型的性能上限受限于原对偶算法生成的专家数据质量。
*   **尾部性能差距**：在极端的第 1 百分位（p1）速率上，随着时间推移，模型与专家算法之间仍存在微小差距。

（完）
