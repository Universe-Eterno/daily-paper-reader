---
title: "Vehicle-as-Prompt: A Unified Deep Reinforcement Learning Framework for Heterogeneous Fleet Vehicle Routing Problem"
title_zh: Vehicle-as-Prompt：一种用于异构车队车辆路径问题的统一深度强化学习框架
authors: "Shihong Huang, Shengjie Wang, Lei Gao, Hong Ma, Zhanluo Zhang, Feng Zhang, Weihua Zhou"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05195v1"
tags: ["query:nco-llm"]
score: 10.0
evidence: 针对异构车队路径规划问题的深度强化学习框架
tldr: 针对异构车队路径规划问题（HFVRP）中车辆选择敏感及复杂约束的挑战，本文提出了一种统一的深度强化学习框架 VaP-CSMV。该框架引入“车辆即提示”（Vehicle-as-Prompt）机制，将问题建模为单阶段自回归决策过程，通过交叉语义编码器和多视图解码器捕捉车辆异构性与客户需求间的复杂关系。实验表明，该方法在求解质量上优于现有神经求解器，且推理速度极快，具备强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的深度强化学习方法多局限于同构车辆场景，难以处理异构车队在成本、容量及复杂约束下的优化难题。
method: 提出 Vehicle-as-Prompt 机制，并构建包含交叉语义编码器和多视图解码器的 VaP-CSMV 框架，实现单阶段自回归决策。
result: 实验证明该框架在多种变体上显著优于现有 DRL 求解器，且在保持秒级推理速度的同时，求解质量可媲美传统启发式算法。
conclusion: 该研究为解决复杂约束下的异构车队路径规划提供了一个高效、通用且泛化能力强的深度强化学习方案。
---

## 摘要
与传统的同构路径问题不同，异构车队车辆路径问题 (HFVRP) 涉及异构的固定成本、可变行驶成本和容量限制，使得解的质量对车辆选择高度敏感。此外，现实世界的物流应用通常会施加额外的复杂约束，显著增加了计算复杂度。然而，大多数现有的基于深度强化学习 (DRL) 的方法仅限于同构场景，在应用于 HFVRP 及其复杂变体时表现不佳。为了弥补这一差距，我们研究了复杂约束下的 HFVRP，并开发了一个能够解决各种变体设置下该问题的统一 DRL 框架。我们引入了“车辆即提示” (Vehicle-as-Prompt, VaP) 机制，将该问题建模为单阶段自回归决策过程。在此基础上，我们提出了 VaP-CSMV，该框架具有跨语义编码器和多视图解码器，能够有效处理各种问题变体，并捕捉车辆异构性与客户节点属性之间的复杂映射关系。大量的实验结果表明，VaP-CSMV 显著优于现有的最先进的基于 DRL 的神经求解器，并且与传统的启发式求解器相比，在将推理时间缩短至仅几秒的同时，实现了具有竞争力的解质量。此外，该框架在大规模和先前未见过的问题变体上表现出强大的零样本泛化能力，而消融研究验证了各组件的关键贡献。

## Abstract
Unlike traditional homogeneous routing problems, the Heterogeneous Fleet Vehicle Routing Problem (HFVRP) involves heterogeneous fixed costs, variable travel costs, and capacity constraints, rendering solution quality highly sensitive to vehicle selection. Furthermore, real-world logistics applications often impose additional complex constraints, markedly increasing computational complexity. However, most existing Deep Reinforcement Learning (DRL)-based methods are restricted to homogeneous scenarios, leading to suboptimal performance when applied to HFVRP and its complex variants. To bridge this gap, we investigate HFVRP under complex constraints and develop a unified DRL framework capable of solving the problem across various variant settings. We introduce the Vehicle-as-Prompt (VaP) mechanism, which formulates the problem as a single-stage autoregressive decision process. Building on this, we propose VaP-CSMV, a framework featuring a cross-semantic encoder and a multi-view decoder that effectively addresses various problem variants and captures the complex mapping relationships between vehicle heterogeneity and customer node attributes. Extensive experimental results demonstrate that VaP-CSMV significantly outperforms existing state-of-the-art DRL-based neural solvers and achieves competitive solution quality compared to traditional heuristic solvers, while reducing inference time to mere seconds. Furthermore, the framework exhibits strong zero-shot generalization capabilities on large-scale and previously unseen problem variants, while ablation studies validate the vital contribution of each component.

---

## 论文详细总结（自动生成）

以下是对论文《Vehicle-as-Prompt: A Unified Deep Reinforcement Learning Framework for Heterogeneous Fleet Vehicle Routing Problem》的结构化总结：

### 1. 核心问题与整体含义
论文关注的是**异构车队车辆路径问题 (HFVRP)**。在现实物流中，车队通常由不同容量、固定成本和单位行驶成本的车辆组成，且伴随复杂的约束（如时间窗、回程运输等）。
*   **研究动机**：现有的深度强化学习 (DRL) 神经求解器大多假设车队是同构的（车辆完全一致），难以处理异构车辆选择与路径规划之间的强耦合关系。
*   **核心挑战**：异构性导致搜索空间呈指数级增长；不同约束变体（如开环、限距）通常需要独立训练模型，缺乏通用性；现有方法在处理大规模异构车队时存在内存爆炸或信息融合不足的问题。

### 2. 方法论
论文提出了 **VaP-CSMV** 框架，核心包含以下三个关键技术：
*   **Vehicle-as-Prompt (VaP) 机制**：借鉴大语言模型的 Tokenization 思想，将“选择车型”抽象为一种“提示词（Prompt）”。它将仓库、$V$ 种车型和 $N$ 个客户节点映射到一个统一的动作空间。选择车型不产生物理位移，但会更新后续路径的容量和成本约束，从而将复杂的两阶段决策简化为单阶段自回归过程。
*   **跨语义编码器 (Cross-Semantic Encoder)**：采用双流交互架构，并行提取四个维度的特征：节点语义、车辆语义、车-项交互语义和全局上下文语义。利用 SwiGLU 激活函数和双重注意力机制，深度融合约束提示与环境状态。
*   **多视图解码器 (Multi-View Decoder)**：通过聚合上述四个独立语义视图的上下文向量，生成最终的动作概率分布，避免了单一视图导致的信息丢失。
*   **训练算法**：基于 REINFORCE 算法，引入了**样本共享基准线**、**熵系数正则化**以及**协方差引导的自适应熵控制**，以增强模型在复杂约束下的探索能力，防止陷入局部最优。

### 3. 实验设计
*   **数据集与场景**：涵盖了 5 种核心变体：容量限制 (C)、开环路径 (O)、回程运输 (B)、距离限制 (L) 和时间窗 (TW)。
*   **Benchmark (基准方法)**：
    *   **传统启发式**：PyVRP（目前最强的 HGS 算法实现）、Google OR-Tools、vrp-cli (ALNS)。
    *   **DRL 神经求解器**：RouteFinder 框架下的 RF-POMO、RF-MVMoE、RF-TE，以及专门针对异构问题的 HF-DRL。
*   **对比维度**：解的质量（Obj. Gap）、推理时间（Time）、零样本泛化能力（跨规模、跨未见变体）。

### 4. 资源与算力
*   **硬件环境**：双 AMD EPYC 9554 64核处理器，768 GB RAM。
*   **GPU 算力**：单张 **NVIDIA L40S (48 GB)**。
*   **训练时长**：每个模型达到收敛约需 **9 至 15 小时**。
*   **超参数**：隐藏层维度 128，Transformer 层数 6，注意力头数 8。

### 5. 实验数量与充分性
*   **实验规模**：在 $N=50$ 和 $N=100$ 规模上进行了完整训练与对比；在 $N=80$ 和 $N=120$ 上进行了规模泛化实验；针对 10 种以上的复合约束组合进行了零样本泛化测试。
*   **消融实验**：详细测试了跨语义编码器、多视图解码器、问题特征提示、熵正则化及协方差机制对性能的影响。
*   **评价**：实验设计非常充分，涵盖了从基础性能到极端泛化场景的全面评估，对比基准包含了目前工业界和学术界最顶尖的工具，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：VaP-CSMV 在所有 5 种基础变体上的表现均优于现有的 DRL 方法，在 $N=50$ 时差距控制在 3% 以内。
*   **效率优势**：在保持与传统启发式算法（如 PyVRP）相近的解质量时，推理时间从数百秒缩短至**秒级**。
*   **强泛化性**：模型在训练时未见过的复合约束（如 HFOVRPBLTW）和更大规模（$N=120$）下，依然能保持稳定的求解质量，证明了 VaP 机制的通用性。
*   **异构建模的必要性**：实验证明，仅考虑容量异构是不够的，固定成本的引入会显著改变最优路径结构，而 VaP 机制能有效捕捉这种多维异构属性。

### 7. 优点
*   **机制创新**：将车辆选择转化为 Prompt，巧妙地统一了异构 VRP 的决策空间，简化了模型架构。
*   **架构完备**：跨语义编码器和多视图解码器的设计针对性强，能够处理高维度的特征映射。
*   **工业潜力**：推理速度极快且支持多约束统一求解，非常适合实时调度场景。

### 8. 不足与局限
*   **训练成本**：虽然推理快，但针对不同规模的初始训练仍需较长时间（9-15小时）。
*   **约束硬性**：目前主要处理硬约束，对于现实中常见的软约束（如允许延迟但需罚款）探讨较少。
*   **动态性缺失**：实验主要基于静态场景，未涉及订单实时流入或交通状况动态变化的随机路径问题。

（完）
