---
title: Transfer Learning from Foundational Optimization Embeddings to Unsupervised SAT Representations
title_zh: 从基础优化嵌入到无监督 SAT 表示的迁移学习
authors: "Koyena Pal, Serdar Kadioglu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15448v1"
tags: ["query:nco"]
score: 8.0
evidence: 使用图表示的MIP和SAT基础优化嵌入
tldr: 本研究探讨了基础优化嵌入在布尔可满足性（SAT）问题上的迁移能力。通过将SAT的CNF公式映射为与混合整数规划（MIP）相同的二分约束-变量图，研究者在无需架构调整或监督微调的情况下，直接复用了预训练模型。实验证明，该嵌入能有效捕捉SAT实例的结构特征，支持聚类和分布识别等无监督任务，为构建优化与决策问题的统一表示框架迈出了重要一步。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15448v1/fig-001.webp\", \"caption\": \"Figure 1 Overview of Forge architecture [17]: Starting with the bipartite representation of\", \"page\": 3, \"index\": 1, \"width\": 823, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.15448v1/fig-002.webp\", \"caption\": \"Figure 2 Our transfer learning setup for Forge applied to SAT to enable unsupervised SAT representations. (I) convert SAT instances into MIP form. (II) construct either MIP or SAT node feature. (III) bipartite representation, with MIP or SAT node features, are fed into Forge architecture trained on MIP or SAT instances. Overall, we obtain SAT representations (embeddings) at the instance-, clause-, and variable level.\", \"page\": 4, \"index\": 2, \"width\": 819, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.15448v1/fig-003.webp\", \"caption\": \"Figure 3 Clustering SAT embeddings across static and different transfer learning featurizations.\", \"page\": 6, \"index\": 3, \"width\": 829, \"height\": 653}]"
motivation: 验证预训练的基础优化嵌入是否能跨越领域，从混合整数规划迁移到布尔可满足性等决策问题。
method: 将SAT的CNF公式转换为二分约束-变量图，从而直接应用预训练的MIP嵌入模型而无需额外训练。
result: 预训练嵌入成功捕捉了SAT实例的结构规律，并在实例聚类和分布识别等无监督任务中表现出色。
conclusion: 首次证明了基础优化嵌入可迁移至约束满足领域，展示了构建统一优化与决策表示框架的潜力。
---

## 摘要
基础优化嵌入最近已成为混合整数规划（MIP）问题的强大预训练表示。这些嵌入已被证明能够实现跨域迁移，并减少对求解器生成标签的依赖。在这项工作中，我们研究了此类表示是否能从优化问题泛化到决策问题，重点关注布尔可满足性（SAT）。我们通过将合取范式（CNF）公式映射到与 MIP 相同的二分约束-变量图表示，将基础优化架构适配到 SAT。这使得在无需更改架构或进行有监督微调的情况下，可以直接重用预训练的嵌入模型。我们的结果表明，这些嵌入捕捉到了 SAT 实例中的结构规律，并支持诸如实例聚类和分布识别等无监督任务。我们首次证明了基础优化嵌入可以迁移到约束满足领域。我们的发现是迈向优化和决策问题统一表示框架的一步。

## Abstract
Foundational optimization embeddings have recently emerged as powerful pre-trained representations for mixed-integer programming (MIP) problems. These embeddings were shown to enable cross-domain transfer and reduce reliance on solver-generated labels. In this work, we investigate whether such representations generalize beyond optimization to decision problems, focusing on Boolean satisfiability (SAT). We adapt the foundational optimization architecture to SAT by mapping CNF formulas into the same bipartite constraint-variable graph representation used for MIPs. This allows direct reuse of the pre-trained embedding model without architectural changes or supervised fine-tuning. Our results show that these embeddings capture structural regularities in SAT instances and support unsupervised tasks such as instance clustering and distribution identification. We demonstrate, for the first time, that foundational optimization embeddings can transfer to constraint satisfaction domains. Our findings is a step toward a unified representational framework for both optimization and decision problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Transfer Learning from Foundational Optimization Embeddings to Unsupervised SAT Representations》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：混合整数规划（MIP）和布尔可满足性（SAT）是组合推理的核心。虽然图神经网络（GNN）在特定领域表现优异，但通常依赖求解器生成的标签，且跨领域泛化能力差。
*   **核心问题**：原本为 MIP 结构设计并训练的“基础优化嵌入”（如 Forge 模型）能否在不进行有监督微调的情况下，为 SAT 等决策问题提供有用的表示？
*   **整体含义**：本研究探索了从优化领域到决策领域的跨域迁移学习，旨在构建一个能够统一处理优化和满足性问题的通用表示框架。

### 2. 论文提出的方法论
*   **核心思想**：利用 SAT 与 MIP 在图表示上的相似性，将 SAT 实例编码为 MIP 形式的二分图（约束-变量图），从而复用预训练的 Forge 架构。
*   **关键技术细节**：
    *   **SAT-to-MIP 编码**：将 CNF 公式中的每个子句转换为线性不等式，布尔变量转换为 0-1 变量。
    *   **Forge 架构**：采用基于 GraphSAGE 的编码器，通过矢量量化（VQ）码本对节点嵌入进行离散化，并使用解码器重建节点特征和边。
*   **三种迁移变体**：
    1.  **Forge-Mip**：直接将预训练的 MIP 模型和 MIP 特征应用于 SAT 实例（零修改迁移）。
    2.  **Forge-Mip-Sat**：保留 MIP 预训练权重，但将节点特征替换为 SAT 特定的统计特征（如子句宽度、变量正负度数等）。
    3.  **Forge-Sat**：保留架构但丢弃权重，在 SAT 数据集上从头开始进行无监督预训练。

### 3. 实验设计
*   **数据集**：使用 **G4SATBench**，包含随机（Random）、伪工业（Pseudo-industrial）和组合（Combinatorial）三大类共 7 种分布。
*   **实验场景**：聚焦于“困难（Hard）”级别的测试实例，共 1400 个公式（每种类型 200 个，SAT/UNSAT 各半）。
*   **Benchmark 与对比方法**：
    *   **Static-Sat**：基于人工设计的 SAT 特征的静态启发式基准。
    *   **对比维度**：通过 PaCMAP 降维可视化和聚类指标（NMI、Purity）评估嵌入质量。
*   **评估任务**：无监督聚类，看模型能否在没有标签的情况下区分问题类型及可满足性（SAT vs UNSAT）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **模型规模**：提到 Forge 架构使用两层 GraphSAGE，预训练在包含 28,781 个实例的 G4SATBench 训练集上进行。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 7 种不同的 SAT 问题分布，并针对 SAT/UNSAT 类别进行了细分，总计 14 个基准组。
*   **充分性评价**：
    *   **客观性**：使用了标准化的 NMI 和 Purity 指标，对比了静态特征和不同迁移深度的变体，逻辑清晰。
    *   **局限性**：实验目前仅限于无监督聚类任务，尚未在下游任务（如实际求解加速）中验证嵌入的有效性。

### 6. 论文的主要结论与发现
*   **跨域迁移可行性**：预训练的 MIP 嵌入（Forge-Mip）即使不经修改也能捕捉到 SAT 的结构规律，表现优于静态特征。
*   **特征适配的重要性**：引入 SAT 特定特征（Forge-Mip-Sat）能显著提升聚类性能（NMI 从 0.76 升至 0.77）。
*   **架构通用性**：Forge 架构在 SAT 数据上从头训练（Forge-Sat）达到了最高性能（NMI 0.79），证明该无监督训练范式具有跨领域的通用性。
*   **可满足性区分**：模型在无监督条件下能够一定程度上分离同一问题类型中的 SAT 和 UNSAT 实例。

### 7. 优点
*   **创新性**：首次展示了基础优化模型向约束满足领域的权重级和架构级迁移。
*   **无监督特性**：不依赖昂贵的求解器标签，降低了模型训练的门槛。
*   **开箱即用**：提供了开源的预训练模型和流水线，可直接为任意 SAT 实例生成有意义的嵌入。

### 8. 不足与局限
*   **下游任务缺失**：尚未证明这些嵌入如何直接提升 SAT 求解器的性能（如分支启发式或冲突学习）。
*   **特征工程简单**：目前使用的 SAT 节点特征较为基础（简单的统计描述），可能存在更优的特征组合。
*   **泛化范围有限**：预训练语料库仅限于特定的 SAT 基准，对于更广泛的工业级实例或 CSP 问题的泛化能力仍待验证。

（完）
