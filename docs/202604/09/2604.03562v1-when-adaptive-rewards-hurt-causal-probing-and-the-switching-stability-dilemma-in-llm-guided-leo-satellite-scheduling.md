---
title: "When Adaptive Rewards Hurt: Causal Probing and the Switching-Stability Dilemma in LLM-Guided LEO Satellite Scheduling"
title_zh: 当自适应奖励适得其反：大语言模型引导的低轨卫星调度中的因果探测与切换-稳定性困境
authors: Yuanhang Li
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03562v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 使用深度强化学习的LLM引导低轨卫星调度
tldr: 本研究探讨了低轨卫星调度中深度强化学习的自适应奖励设计，揭示了“切换-稳定性困境”。研究发现，由于PPO算法需要准平稳的奖励信号，静态奖励权重在性能上显著优于动态权重。通过引入单变量因果探测方法，研究揭示了奖励项对性能的非直观影响。实验对比了固定、规则、MLP及微调LLM等方案，发现LLM因权重震荡导致性能崩溃，而MLP表现最优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03562v1/fig-001.webp\", \"caption\": \"Fig. 3. Outage weight trajectories for MLP (top) and FT-LLM (bottom) architects during generalization experiments (seed 42). The MLP produces stable weights (range 0.94–1.57), while the LLM oscillates between extreme values (0.004–0.999). Dashed gray line marks the transition to novel regimes at step 250k.\", \"page\": 6, \"index\": 1, \"width\": 506, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.03562v1/fig-002.webp\", \"caption\": \"Fig. 2. Mean sum rate comparison across architect variants. The MLP (M3) achieves the highest throughput on both known and novel regimes. The FT-LLM (M4) collapses on novel regimes due to weight oscillation (see Section V-H).\", \"page\": 4, \"index\": 2, \"width\": 511, \"height\": 282}]"
motivation: 验证在多波束低轨卫星调度中，自适应奖励权重是否优于静态权重，并探究其对强化学习收敛性的影响。
method: 提出单变量因果探测方法，通过扰动奖励项测量PPO响应，并对比了固定、规则、MLP和LLM四种架构。
result: 静态权重和MLP表现优于动态权重，而微调后的LLM因权重震荡导致性能严重下滑。
conclusion: LLM在通信系统中更适合处理自然语言意图，而在需要高稳定性的奖励权重分配任务中，简单方法更为有效。
---

## 摘要
多波束低轨（LEO）卫星调度中深度强化学习（DRL）的自适应奖励设计源于一种直觉，即感知状态（regime-aware）的奖励权重应优于静态权重。我们系统地测试了这一直觉，并发现了一个“切换-稳定性困境”：近乎恒定的奖励权重（342.1 Mbps）优于精心调优的动态权重（103.3+/-96.8 Mbps），因为近端策略优化（PPO）算法需要准平稳的奖励信号来实现价值函数的收敛。无论质量如何，权重自适应都会因反复重启收敛过程而降低性能。为了理解特定权重为何重要，我们引入了一种单变量因果探测方法，该方法独立地对每个奖励项进行 +/-20% 的扰动，并测量 50k 步后的 PPO 响应。探测揭示了反直觉的杠杆作用：切换惩罚增加 20% 可使极地切换场景增加 157 Mbps，冷热状态场景增加 130 Mbps——如果没有系统探测，人类专家或训练好的多层感知器（MLP）无法获得这些发现。我们在已知和新颖的流量状态下评估了四种马尔可夫决策过程（MDP）架构变体（固定、基于规则、学习型 MLP、微调后的 LLM）。MLP 在已知状态下达到 357.9 Mbps，在新颖状态下达到 325.2 Mbps，而微调后的 LLM 由于权重振荡而非缺乏领域知识，性能崩溃至 45.3+/-43.0 Mbps——输出一致性而非知识才是关键约束。我们的研究结果为通信系统中的 LLM-DRL 集成提供了基于经验的路线图，确定了 LLM 在何处具有不可替代的价值（自然语言意图理解），以及在何处使用更简单的方法即可。

## Abstract
Adaptive reward design for deep reinforcement learning (DRL) in multi-beam LEO satellite scheduling is motivated by the intuition that regime-aware reward weights should outperform static ones. We systematically test this intuition and uncover a switching-stability dilemma: near-constant reward weights (342.1 Mbps) outperform carefully-tuned dynamic weights (103.3+/-96.8 Mbps) because PPO requires a quasistationary reward signal for value function convergence. Weight adaptation-regardless of quality-degrades performance by repeatedly restarting convergence. To understand why specific weights matter, we introduce a single-variable causal probing method that independently perturbs each reward term by +/-20% and measures PPO response after 50k steps. Probing reveals counterintuitive leverage: a +20% increase in the switching penalty yields +157 Mbps for polar handover and +130 Mbps for hot-cold regimes-findings inaccessible to human experts or trained MLPs without systematic probing. We evaluate four MDP architect variants (fixed, rule-based, learned MLP, finetuned LLM) across known and novel traffic regimes. The MLP achieves 357.9 Mbps on known regimes and 325.2 Mbps on novel regimes, while the fine-tuned LLM collapses to 45.3+/-43.0 Mbps due to weight oscillation rather than lack of domain knowledge-output consistency, not knowledge, is the binding constraint. Our findings provide an empirically-grounded roadmap for LLM-DRL integration in communication systems, identifying where LLMs add irreplaceable value (natural language intent understanding) versus where simpler methods suffice.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）在低轨（LEO）卫星调度任务中引导深度强化学习（DRL）的表现进行了深入的实证研究，挑战了“自适应奖励权重必然优于静态权重”的直觉。

### 1. 论文的核心问题与整体含义
*   **研究动机**：低轨卫星面临高度动态的流量需求（如城市密集区、海洋稀疏区、灾区突发流量）。传统的DRL调度器通常使用固定的奖励函数，难以适应所有场景。
*   **核心问题**：利用LLM或自动化方法根据当前环境动态调整奖励权重是否真的能提升性能？
*   **整体含义**：研究揭示了**“切换-稳定性困境”（Switching-Stability Dilemma）**，即频繁或不稳定的奖励权重调整会破坏PPO算法价值函数的收敛，导致性能反而不如静态权重。

### 2. 论文提出的方法论
论文提出了一个**三层时间尺度架构**，并引入了**单变量因果探测**技术：
*   **三层架构**：
    1.  **战略层（LLM，小时级）**：解析操作员的自然语言意图（如“优先保障应急通信”），生成目标配置文件。
    2.  **战术层（MLP/LLM，分钟级）**：监控网络KPI，通过CUSUM（累积和）算法检测流量状态变化，并输出具体的奖励权重向量 $w$。
    3.  **执行层（PPO Agent，毫秒级）**：根据当前奖励函数 $R_w$ 进行实时的波束功率和带宽分配。
*   **奖励函数公式**：$R_w = w_r \cdot \bar{R} - w_o \cdot O - w_s \cdot S - w_q \cdot Q + w_f \cdot F$（包含速率、中断、切换惩罚、队列溢出和公平性）。
*   **单变量因果探测（Causal Probing）**：为了诊断权重的影响，独立地对每个权重进行 $\pm20\%$ 的扰动，观察50k步后PPO性能的变化，从而构建奖励景观的因果图。

### 3. 实验设计
*   **场景设置**：模拟一个拥有19个波束的Ka频段低轨卫星系统。
*   **流量状态（Regimes）**：
    *   **已知状态**：城市（Urban）、海洋（Maritime）、灾难（Disaster）、混合（Mixed）。
    *   **新颖状态（测试泛化性）**：IoT爆发、极地切换、冷热不均。
*   **对比方法（Benchmarks）**：
    *   **M1 (Fixed)**：静态优化权重。
    *   **M2 (Rule-based)**：基于专家规则的阈值分类器。
    *   **M3 (Learned MLP)**：在模拟数据上训练的3层神经网络。
    *   **M4 (Fine-tuned LLM)**：经过LoRA微调的Qwen3-4B模型。

### 4. 资源与算力
*   **硬件**：使用了 NVIDIA RTX 4060 GPU。
*   **模型细节**：LLM选用 **Qwen3-4B**，采用 4-bit NF4 量化，通过 LoRA（Rank 16）进行微调，包含33M可训练参数。
*   **训练时长**：每个实验运行 500k 个时间步。LLM推理延迟约为 14秒/次，而MLP推理延迟小于 1毫秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   核心对比实验涵盖了4种架构在已知和新颖状态下的表现。
    *   使用了 3 个随机种子（42, 123, 456）进行重复实验，并报告了均值和标准差。
    *   针对关键结论（如切换-稳定性困境），进行了 8 个种子的扩展验证。
    *   包含消融实验和针对LLM权重震荡的深入分析。
*   **充分性**：实验设计较为全面，不仅对比了最终性能，还通过因果探测解释了“为什么”某些权重有效，客观地展示了LLM在数值一致性上的短板。

### 6. 主要结论与发现
*   **切换-稳定性困境**：PPO需要准平稳的奖励信号。即使权重是“最优”的，频繁切换也会导致价值函数反复重启收敛，使得动态权重的表现（103.3 Mbps）远低于近乎恒定的权重（342.1 Mbps）。
*   **LLM的局限性**：微调后的LLM在数值输出上存在**权重震荡（Weight Oscillation）**。其输出权重的变异系数（CV）比MLP高出4.6倍，这种不一致性是导致性能崩溃的主因，而非缺乏领域知识。
*   **MLP的优越性**：在已知和新颖场景下，轻量级的MLP架构表现最稳健，吞吐量最高。
*   **因果发现**：通过探测发现，切换惩罚项对极地切换等场景有极高的杠杆作用（+157 Mbps），这一发现超出了人类专家的直觉。

### 7. 优点
*   **视角独特**：没有盲目吹捧LLM，而是冷静地指出了LLM在处理连续数值奖励权重时的不稳定性问题。
*   **诊断工具创新**：提出的单变量因果探测方法为DRL奖励函数调优提供了一个高效的工具（比完整训练快10倍）。
*   **架构务实**：提出了三层混合架构，建议将LLM限制在其擅长的“意图理解”领域，而将数值优化交给MLP。

### 8. 不足与局限
*   **模型规模限制**：实验使用的Qwen3-4B模型较小，更大规模的模型（如70B+）是否能通过更强的推理能力减少数值震荡尚不可知。
*   **环境简化**：卫星模拟器简化了波束间的干扰和复杂的轨道动力学。
*   **泛化场景有限**：虽然测试了3种新颖场景，但实际卫星环境的复杂程度可能远超实验设置。
*   **应用限制**：研究结论主要针对需要高频收敛的DRL算法（如PPO），对于其他类型的优化算法可能结论不同。

（完）
