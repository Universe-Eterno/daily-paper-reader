---
title: "A First Guess is Rarely the Final Answer: Learning to Search in the Travelling Salesperson Problem"
title_zh: 初次尝试很少是最终答案：在旅行商问题中学习搜索
authors: Andoni Irazusta Garmendia
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06940v1"
tags: ["query:nco"]
score: 10.0
evidence: 针对旅行商问题（TSP）的神经求解器和学习改进策略
tldr: 本研究针对神经TSP求解器在搜索阶段的不足，提出了NICO-TSP框架。该方法采用与2-opt算子对齐的边标记表示法，并结合模仿学习与分组强化学习进行训练。NICO-TSP在搜索效率和跨规模泛化性上显著优于现有基准，既可作为高效的局部搜索工具，也能作为构建式求解器的强力后处理模块。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06940v1/fig-001.webp\", \"caption\": \"Table 4: Sensitivity of the RL stage to its main design choices. We report final best costs on 100 uniform instances with n = 100. We also report the relative training cost under a fixed number of parameter-update steps. The default configuration used in the final model is highlighted in bold.\", \"page\": 15, \"index\": 1, \"width\": 548, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2604.06940v1/fig-002.webp\", \"caption\": \"Table 3: Additional disentangled ablations of the final method. Results are reported on the main validation setting using the same evaluation protocol as in the main text on 100 uniform instances with n = 100. The default configuration used in the final model is highlighted in bold.\", \"page\": 15, \"index\": 2, \"width\": 590, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.06940v1/fig-003.webp\", \"caption\": \"Table 1: Best tour cost and inference time under a fixed budget of 10n improvement steps for search methods, and a single forward pass for constructive methods. Bold indicates the best cost and fastest time among learned improvement methods.\", \"page\": 7, \"index\": 3, \"width\": 806, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.06940v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the edge-based representation and policy architecture. Tours are represented as sequences of edge tokens, each encoding endpoint coordinates and local geometric features. These edge tokens are processed by an edge-attention encoder with local cycle mixing and global context fusion, producing contextualized edge representations. A pairwise decoder then scores all feasible 2-opt moves.\", \"page\": 4, \"index\": 4, \"width\": 748, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.06940v1/fig-005.webp\", \"caption\": \"Figure 5: Ablation study. Each row adds one component. Bars show optimality (%).\", \"page\": 9, \"index\": 5, \"width\": 809, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2604.06940v1/fig-006.webp\", \"caption\": \"Figure 2: Overview of the proposed two-stage learning framework. An imitation learning stage uses optimal short-horizon trajectories, followed by a group-based reinforcement learning stage that refines the policy through instance-wise competition in longer horizons.\", \"page\": 5, \"index\": 6, \"width\": 768, \"height\": 392}]"
motivation: 现有神经改进方法因沿用单次求解器的架构设计，导致在TSP局部搜索中效率低下且难以扩展到大规模问题。
method: 提出NICO-TSP框架，利用边标记表示路径并直接评估2-opt变换，采用模仿学习与无评论员分组强化学习的两阶段训练策略。
result: 在同等计算资源下，NICO-TSP在搜索效率和泛化能力上均显著优于现有启发式及学习型基准。
conclusion: 研究证明针对局部搜索机制定制架构是提升性能的关键，NICO-TSP为组合优化提供了高效且可扩展的神经搜索方案。
---

## 摘要
大多数针对旅行商问题（TSP）的神经求解器被训练为输出单一解，尽管从业者在实际应用中很少止步于此：在测试阶段，他们通常会投入额外的计算资源进行采样或事后搜索。这提出了一个自然的问题：搜索过程本身是否可以被学习？神经改进方法采取了这一视角，通过学习一种对候选解应用局部修改的策略，在改进轨迹上累积收益。然而，TSP 的学习改进方法仍相对不成熟，现有方法在鲁棒性和可扩展性能方面仍显不足。我们认为一个关键原因是设计不匹配：许多方法重用了从单一解方法继承的状态表示、架构选择和训练方案，而不是围绕局部搜索的机制构建。这种不匹配促使了 NICO-TSP（组合优化神经改进）的诞生：一个针对 TSP 的 2-opt 改进框架。NICO-TSP 使用与邻域算子对齐的恰好 n 个边标记（edge tokens）来表示当前路径，在没有路径位置编码的情况下直接对 2-opt 移动进行评分，并通过两阶段程序进行训练：首先是对短时界最优轨迹的模仿学习，随后是在更长展开（rollouts）上的无评论员（critic-free）分组强化学习。在衡量改进作为搜索步数和实际运行时间函数的计算匹配评估下，NICO-TSP 相比之前的学习和启发式搜索基准，提供了持续更强且步进效率显著更高的改进，能更可靠地泛化到更大的分布外实例，并既可以作为经典局部搜索的竞争性替代方案，也可以作为构造性求解器的强大测试时细化模块。

## Abstract
Most neural solvers for the Traveling Salesperson Problem (TSP) are trained to output a single solution, even though practitioners rarely stop there: at test time, they routinely spend extra compute on sampling or post-hoc search. This raises a natural question: can the search procedure itself be learned? Neural improvement methods take this perspective by learning a policy that applies local modifications to a candidate solution, accumulating gains over an improvement trajectory. Yet learned improvement for TSP remains comparatively immature, with existing methods still falling short of robust, scalable performance. We argue that a key reason is design mismatch: many approaches reuse state representations, architectural choices, and training recipes inherited from single-solution methods, rather than being built around the mechanics of local search. This mismatch motivates NICO-TSP (Neural Improvement for Combinatorial Optimization): a 2-opt improvement framework for TSP. NICO-TSP represents the current tour with exactly $n$ edge tokens aligned with the neighborhood operator, scores 2-opt moves directly without tour positional encodings, and trains via a two-stage procedure: imitation learning to short-horizon optimal trajectories, followed by critic-free group-based reinforcement learning over longer rollouts. Under compute-matched evaluations that measure improvement as a function of both search steps and wall-clock time, NICO-TSP delivers consistently stronger and markedly more step-efficient improvement than prior learned and heuristic search baselines, generalizes far more reliably to larger out-of-distribution instances, and serves both as a competitive replacement for classical local search and as a powerful test-time refinement module for constructive solvers.

---

## 论文详细总结（自动生成）

以下是对论文《A First Guess is Rarely the Final Answer: Learning to Search in the Travelling Salesperson Problem》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的神经组合优化（NCO）主流范式侧重于“一次性生成”解的构造性策略。然而，实际应用中，从业者通常会投入额外算力进行迭代搜索或后处理。
*   **核心问题**：现有的“神经改进”（Neural Improvement）方法表现不佳，主要归因于**设计不匹配**：
    1.  **表示不匹配**：使用针对构造任务设计的“节点中心”表示，而局部搜索（如 2-opt）本质上是“边操作”。
    2.  **编码脆弱**：依赖路径位置编码，导致在不同规模（$n$）或分布下泛化性差。
    3.  **训练困难**：在巨大的组合空间中进行长时界强化学习，信号稀疏且信用分配不稳定。
*   **整体含义**：论文旨在将“搜索”本身作为学习的核心对象，提出 NICO-TSP 框架，通过架构与算子的对齐来提升局部搜索的效率和泛化力。

### 2. 方法论：核心思想与技术细节
NICO-TSP 的核心在于将模型架构与 2-opt 局部搜索机制深度对齐：
*   **边中心表示（Edge-centric Representation）**：
    *   不使用节点标记，而是将当前路径的 $n$ 条边作为输入标记（Tokens）。
    *   **特征工程**：每个边标记包含位移向量、长度、归一化方向，以及局部几何特征（如相邻边的转角 $\cos/\sin$、相对长度比例、路径长度的 Z-score 归一化）。
    *   **无位置编码**：完全放弃路径位置编码，仅靠局部几何和循环混合模块（Cycle-mixing）捕捉拓扑结构，增强了跨规模泛化能力。
*   **策略架构**：
    *   **Encoder**：使用 $L$ 层自注意力机制，配合局部循环混合模块，生成上下文相关的边表示。
    *   **Decoder**：通过边表示对的缩放点积直接计算所有可行 2-opt 移动的概率分布，并应用对称化处理。
*   **两阶段训练流程**：
    1.  **模仿学习（IL）**：使用精确的 $K$ 步（实验中 $K=2$）前瞻预测器作为专家，引导模型学习高质量的短时界改进动作。
    2.  **分组强化学习（Group-based RL）**：在更长的展开（Rollout）中，通过同一实例的多个随机采样轨迹进行“内部分组竞争”，计算相对优势（Advantage），无需额外的评论员（Critic）网络。

### 3. 实验设计
*   **数据集/场景**：
    *   **均匀分布（Uniform）**：$n=50, 100, 500$ 的欧几里得 TSP 实例。
    *   **分布外（OOD）**：使用 TSPLIB-gen 评估模型在真实世界风格实例上的表现。
*   **Benchmark 与对比方法**：
    *   **经典启发式**：Greedy 2-opt/3-opt、Tabu Search（禁忌搜索）。
    *   **神经改进类**：2opt-DRL、GAT-Improv、DACT、NeuOpt。
    *   **构造类及搜索增强**：LEHD（强力构造基准）、POMO（采样）、EAS（高效主动搜索）。
*   **评估指标**：在固定计算预算（步数或墙钟时间）下的最优路径成本（Cost）和差距（Gap）。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA A100 GPU** 上完成。
*   **训练时长**：
    *   第一阶段（IL）：100 个 Epoch，针对 $n \in [20, 50]$ 的实例。
    *   第二阶段（RL）：200 个 Epoch，针对 $n \in [20, 100]$ 的实例。
*   **推理**：标准化了 Batch Size 和改进预算，以确保时间对比的公平性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 $n=50$ 到 $n=500$ 的跨度，对于神经改进方法而言，$n=500$ 是一个极具挑战性的泛化测试。
*   **消融实验**：非常详尽。论文通过“累加式”消融验证了稳定化组件、历史信息、分组 RL、边归纳偏置和 IL 预训练的各自贡献。
*   **鲁棒性测试**：提供了 5 个不同训练随机种子和 20 个推理随机种子的标准差分析。
*   **公平性**：通过“Anytime Performance”（随时间变化的性能曲线）对比，客观展示了模型在不同计算预算下的表现，避免了单一终点对比的偏差。

### 6. 主要结论与发现
*   **性能领先**：NICO-TSP 在所有测试规模上均优于现有的神经改进方法，且在 $n=500$ 时表现出极强的鲁棒性，而其他方法往往在此时崩溃。
*   **步进效率高**：相比 Greedy 2-opt，NICO 学习到了非贪婪的改进轨迹，能以更少的步数达到更优的解。
*   **多功能性**：它既可以作为独立的搜索策略，也可以作为 LEHD 等构造性方法的后处理模块，进一步显著降低误差。
*   **表示的重要性**：实验证明，将表示与算子（边与 2-opt）对齐是提升 NCO 性能的关键，远比单纯增加模型深度有效。

### 7. 优点（亮点）
*   **架构设计的直觉性**：放弃节点中心论，改用边中心表示，这非常符合 TSP 局部搜索的数学本质。
*   **泛化方案优雅**：通过去除位置编码并使用局部几何特征，解决了 NCO 领域长期存在的“规模缩放”难题。
*   **训练策略稳健**：结合了 IL 的快速起步和 RL 的长时界优化，分组 RL 避开了 Critic 网络训练不稳定的坑。

### 8. 不足与局限
*   **动作空间复杂度**：2-opt 的动作空间是 $O(n^2)$，虽然 NICO 在 $n=500$ 表现良好，但对于 $n > 1000$ 的超大规模问题，每一步评分的计算开销依然巨大。
*   **专家监督受限**：IL 阶段使用的专家仅有 2 步前瞻能力，这虽然保证了计算可行性，但可能无法教给模型更深层的搜索策略。
*   **算子单一**：目前仅针对 2-opt 进行了优化，尚未扩展到 3-opt 或更复杂的 LKH 式算子。

（完）
