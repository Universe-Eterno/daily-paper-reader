---
title: A Hybrid Reinforcement and Self-Supervised Learning Aided Benders Decomposition Algorithm
title_zh: 一种混合强化学习与自监督学习辅助的 Benders 分解算法
authors: "Bernard T. Agyeman, Zhe Li, Ilias Mitrai, Prodromos Daoutidis"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22107v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 用于MINLP中Benders分解的混合强化学习
tldr: "本研究提出一种结合强化学习与自监督学习的混合框架，旨在加速广义Benders分解（GBD）算法。该框架利用图强化学习代理解决主问题的整数变量分配，并结合KKT信息神经网络通过自监督学习预测子问题的原对偶解以直接构建割平面。在混合整数非线性规划（MINLP）案例中，该方法在保持最优性的同时，将求解时间缩短了57.5%，显著提升了求解效率。"
source: arxiv
selection_source: fresh_fetch
motivation: 针对广义Benders分解算法在处理复杂混合整数非线性规划问题时计算效率较低的挑战。
method: 结合图强化学习处理主问题整数分配，并利用KKT信息神经网络自监督预测子问题解以生成Benders割。
result: "在MINLP案例研究中，该框架比传统GBD算法减少了57.5%的求解时间，且所有测试实例均能获得最优解。"
conclusion: 混合强化学习与自监督学习能有效加速Benders分解过程，是解决复杂优化问题的有效途径。
---

## 摘要
我们提出了一种混合强化学习与自监督学习框架，用于加速广义 Benders 分解 (GBD)。在该框架中，一个基于图的强化学习智能体在主问题的二分图表示上运行，并结合验证机制，确定求解主问题的整数变量赋值。这些赋值随后被用作 KKT 感知神经网络的输入，该网络通过自监督训练来预测近似满足子问题 Karush-Kuhn-Tucker (KKT) 条件的原对偶解。预测的解被直接用于构建 Benders 割平面。该框架在混合整数非线性规划案例研究中进行了评估，结果显示，与经典 GBD 相比，其求解时间缩短了 57.5%，同时在所有测试实例中均能一致地恢复最优解。

## Abstract
We propose a hybrid reinforcement and self-supervised learning framework for accelerating generalized Benders decomposition (GBD). In this framework, a graph based reinforcement learning agent operates on a bipartite representation of the master problem and, together with a verification mechanism, determines the integer variable assignments that solve the master problem. These assignments are then used as inputs to a KKT informed neural network, trained via self supervision to predict primal dual solutions that approximately satisfy the Karush Kuhn Tucker conditions of the subproblem. The predicted solutions are used to construct Benders cuts directly. The framework is evaluated on a mixed integer nonlinear programming case study, where it achieves a 57.5% reduction in solution time relative to classical GBD while consistently recovering optimal solutions across all test instances.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Hybrid Reinforcement and Self-Supervised Learning Aided Benders Decomposition Algorithm》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：广义 Benders 分解（GBD）在求解混合整数非线性规划（MINLP）时，面临主问题（Master Problem, MP）组合爆炸和子问题（Subproblem, SP）非线性计算开销大的双重挑战。
*   **研究动机**：传统的加速策略通常只针对 MP 或 SP 之一进行优化。本文旨在提出一个**统一的机器学习框架**，同时利用强化学习（RL）加速主问题的整数变量分配，并利用自监督学习（KINN）加速子问题的原对偶解预测，从而全面提升 GBD 的收敛效率。

### 2. 方法论
该框架由两个核心学习组件和一个验证机制组成：
*   **主问题加速（图强化学习智能体）**：
    *   将主问题建模为**二分图**（变量节点与约束节点），利用图卷积网络（GNN）提取特征。
    *   采用 **Actor-Critic 框架**（PPO 算法）训练智能体，输出二值变量的概率分布。
    *   **验证机制**：引入置信度阈值。若智能体预测结果不满足约束或无法提供有效的下界，则回退至传统的 MIP 求解器，确保算法的收敛性。
*   **子问题加速（KKT 感知神经网络, KINN）**：
    *   **核心思想**：利用 KKT 条件作为损失函数进行自监督学习，无需标注数据。
    *   **技术细节**：设计了一个分支架构，分别预测原变量（Primal）和对偶变量（Dual）。损失函数包含平稳性残差、原可行性残差和互补松弛性残差（使用平滑的 Fischer-Burmeister 函数）。
    *   **应用**：直接通过 KINN 预测的对偶解构造 Benders 割平面，避免了调用非线性求解器（如 IPOPT）的开销。

### 3. 实验设计
*   **案例研究**：采用经典的混合整数非线性规划（MINLP）基准问题（源自 Floudas, 1995），通过随机采样目标函数系数生成多样化的实例。
*   **Benchmark（基准方法）**：
    1.  **Classical GBD**：传统 GBD（使用 BONMIN 求解 MP，IPOPT 求解 SP）。
    2.  **Agent-only**：仅使用 RL 加速主问题。
    3.  **KINN-only**：仅使用神经网络加速子问题。
*   **对比指标**：总求解时间（$t_{tot}$）、主问题时间（$t_{MP}$）、子问题时间（$t_{SP}$）、迭代次数（$N_{iter}$）以及解的准确性。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Intel i7-14700 CPU** 和 **32 GB RAM** 的台式机上运行。
*   **训练细节**：
    *   RL 智能体：训练 10,000 个回合（Episodes）。
    *   KINN：训练 40,000 个 Epochs，随后对对偶分支进行额外的 40,000 个 Epochs 微调。
*   **算力说明**：文中未明确提及是否使用了 GPU 进行加速训练，但考虑到使用了 GNN 和深度神经网络，通常会涉及 GPU 算力。

### 5. 实验数量与充分性
*   **实验规模**：在 **100 个随机生成的测试实例**上进行了评估。
*   **充分性评价**：
    *   **客观性**：通过消融实验（Agent-only vs KINN-only）展示了两个组件各自的贡献，证明了混合框架的优越性。
    *   **公平性**：所有方法均在相同的硬件和测试集下运行，并与成熟的商业/开源求解器（Gurobi, BONMIN, IPOPT）进行了对比。
    *   **局限性**：测试案例的规模较小（5 个二值变量，6 个连续变量），对于大规模工业级问题的泛化能力尚待进一步验证。

### 6. 主要结论与发现
*   **显著加速**：提出的混合框架相比经典 GBD 减少了 **57.5%** 的总求解时间。
*   **协同效应**：RL 智能体将主问题求解时间从 0.56s 降至 0.26s；KINN 将子问题求解时间从 71.2ms 降至 0.4ms（近乎瞬时）。
*   **最优性保持**：尽管 KINN 产生的割平面是近似的（不精确割），但在所有 100 个测试实例中，该框架均成功找回了全局最优解。

### 7. 优点
*   **端到端加速**：同时针对分解算法的两个核心瓶颈（MP 和 SP）进行优化。
*   **自监督学习**：KINN 不需要昂贵的标注数据（即不需要预先求解大量的子问题来获取标签），降低了数据准备成本。
*   **鲁棒性设计**：通过验证机制和置信度处理，克服了神经网络预测缺乏严谨约束保证的缺陷，确保了算法的收敛。

### 8. 不足与局限
*   **问题规模受限**：实验仅针对小型 MINLP 问题，未展示在具有成千上万变量的大规模问题上的表现。
*   **不精确割的风险**：虽然在本文案例中收敛良好，但在更复杂的非凸问题中，KINN 产生的近似割可能导致算法收敛到次优解或不收敛。
*   **超参数敏感性**：损失函数中各项残差的权重（$\alpha, \beta, \gamma$）以及 RL 的奖励函数参数需要精细调优，这可能限制了其在不同问题间的迁移能力。

（完）
