---
title: "SigGate-GT: Taming Over-Smoothing in Graph Transformers via Sigmoid-Gated Attention"
title_zh: SigGate-GT：通过 Sigmoid 门控注意力抑制图 Transformer 中的过度平滑
authors: "Dongxin Guo, Jikun Wu, Siu Ming Yiu"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17324v1"
tags: ["query:nco"]
score: 7.0
evidence: 采用Sigmoid门控注意力的图Transformer架构以解决过度平滑问题
tldr: 图Transformer在处理分子和长程推理任务时常面临过度平滑和注意力熵退化问题，其根源在于Softmax的归一化约束强制节点关注无用信息。本文提出SigGate-GT，通过在GraphGPS框架中引入可学习的逐头Sigmoid门控机制，允许模型选择性抑制非信息性连接。实验表明，该方法在多个基准数据集上达到SOTA，显著缓解了过度平滑并提升了训练稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17324v1/fig-001.webp\", \"caption\": \"Table 5. Ablation on gate placement and sharing. G1 (output) with per-head gating is consistently best.\", \"page\": 7, \"index\": 1, \"width\": 516, \"height\": 263}]"
motivation: 针对图Transformer中Softmax归一化约束导致的过度平滑和注意力熵退化问题，探索一种能抑制无用信息的注意力机制。
method: 在GraphGPS框架内引入逐头Sigmoid门控机制，通过学习到的门控值对注意力输出进行加权，从而实现对非信息性连接的静默。
result: "在ogbg-molhiv上取得82.47%的SOTA成绩，在ZINC上达到0.059 MAE，且相比GraphGPS在过度平滑指标上改善了30%。"
conclusion: Sigmoid门控机制能有效解决图Transformer的过度平滑问题，在保持极低参数开销的同时显著提升模型性能和训练稳定性。
---

## 摘要
图 Transformer 在分子和长程推理任务中取得了显著成果，但仍受到过度平滑（节点表示随深度增加而逐渐坍缩）和注意力熵退化的困扰。我们观察到，这些病态现象与大语言模型中的注意力汇聚（attention sinks）有着共同的根源：Softmax 注意力的归一化约束强制每个节点必须关注某些位置，即使不存在信息信号。受近期关于逐元素 Sigmoid 门控可消除大语言模型中注意力汇聚的发现启发，我们提出了 SigGate-GT，这是一种在 GraphGPS 框架内对注意力输出应用可学习的、逐头 Sigmoid 门控的图 Transformer。每个门控可以将激活抑制至趋于零，使注意力头能够选择性地屏蔽无信息的连接。在五个标准基准测试中，SigGate-GT 在 ZINC 数据集上达到了之前的最佳水平（0.059 MAE），并在 ogbg-molhiv 数据集上创下了新的 SOTA（82.47% ROC-AUC），在所有五个数据集上均比 GraphGPS 取得了具有统计学显著性的提升（p < 0.05）。消融实验表明，门控机制将过度平滑减少了 30%（4-16 层间的平均相对 MAD 增益），增加了注意力熵，并在 10 倍学习率范围内稳定了训练，而在 OGB 数据集上的参数开销仅约为 1%。

## Abstract
Graph transformers achieve strong results on molecular and long-range reasoning tasks, yet remain hampered by over-smoothing (the progressive collapse of node representations with depth) and attention entropy degeneration. We observe that these pathologies share a root cause with attention sinks in large language models: softmax attention's sum-to-one constraint forces every node to attend somewhere, even when no informative signal exists. Motivated by recent findings that element-wise sigmoid gating eliminates attention sinks in large language models, we propose SigGate-GT, a graph transformer that applies learned, per-head sigmoid gates to the attention output within the GraphGPS framework. Each gate can suppress activations toward zero, enabling heads to selectively silence uninformative connections. On five standard benchmarks, SigGate-GT matches the prior best on ZINC (0.059 MAE) and sets new state-of-the-art on ogbg-molhiv (82.47% ROC-AUC), with statistically significant gains over GraphGPS across all five datasets ($p < 0.05$). Ablations show that gating reduces over-smoothing by 30% (mean relative MAD gain across 4-16 layers), increases attention entropy, and stabilizes training across a $10\times$ learning rate range, with about 1% parameter overhead on OGB.