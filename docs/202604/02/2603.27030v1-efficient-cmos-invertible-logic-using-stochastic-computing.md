---
title: Efficient CMOS Invertible Logic Using Stochastic Computing
title_zh: 基于随机计算的高效 CMOS 可逆逻辑
authors: "Sean C. Smithson, Naoya Onizawa, Brett H. Meyer, Warren J. Gross, Takahiro Hanyu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27030v1"
tags: ["query:nco"]
score: 8.0
evidence: 用于组合优化的脉冲神经网络
tldr: 本文提出了一种基于随机计算（Stochastic Computing）的高效CMOS可逆逻辑实现方法。可逆逻辑支持正向计算和反向推理（如因式分解），传统上依赖复杂的玻尔兹曼机。研究者通过简单的脉冲神经网络设计了可逆随机门，并扩展至加法器和乘法器。实验通过ASIC芯片验证了该电路在乘法和因式分解任务中的正确性，为解决组合优化等难题提供了低功耗、小面积的硬件方案。
selection_source: fresh_fetch
motivation: 旨在利用随机计算简化传统可逆逻辑复杂的硬件实现，以更高效地解决因式分解和组合优化等挑战性问题。
method: 提出一种基于简单脉冲神经网络和随机计算的设计方法，构建出可综合的CMOS可逆随机逻辑门及算术电路。
result: 成功实现了具备可逆能力的加法器和乘法器，并通过ASIC测量验证了其在正向乘法和反向因式分解任务中的正确性。
conclusion: 该研究证明了随机计算是实现高效可逆逻辑的有效途径，为硬件加速复杂计算任务提供了低成本的CMOS解决方案。
---

## 摘要
可逆逻辑可以在两种模式下运行：1) 正向模式，即给定输入并产生单一、正确的输出；2) 反向模式，即固定输出，输入则取与输出一致的值。利用各种玻尔兹曼机（Boltzmann machine）配置可以构建可逆逻辑。此类系统已被证明能够快速解决某些具有挑战性的问题，例如因数分解和组合优化。在本文中，我们展示了可以利用基于随机计算的简单脉冲神经网络来实现可逆逻辑。我们提出了一种可逆随机门的设计方法，该方法可以使用少量的 CMOS 硬件实现。我们证明了我们的设计不仅能正确实现具有可逆能力的基础逻辑门，还可以扩展到构建可逆随机加法器和乘法器电路。实验结果展示了可综合的可逆电路在执行乘法和因数分解时的正确运行，并提供了可逆乘法器电路的 ASIC 制造测量结果。

## Abstract
Invertible logic can operate in one of two modes: 1) a forward mode, in which inputs are presented and a single, correct output is produced, and 2) a reverse mode, in which the output is fixed and the inputs take on values consistent with the output. It is possible to create invertible logic using various Boltzmann machine configurations. Such systems have been shown to solve certain challenging problems quickly, such as factorization and combinatorial optimization. In this paper, we show that invertible logic can be implemented using simple spiking neural networks based on stochastic computing. We present a design methodology for invertible stochastic gates, which can be implemented using a small amount of CMOS hardware. We demonstrate that our design can not only correctly implement basic gates with invertible capability, but can also be extended to construct invertible stochastic adder and multiplier circuits. Experimental results are presented which demonstrate correct operation of synthesizable invertible circuitry performing both multiplication and factorization, along with fabricated ASIC measurement results for an invertible multiplier circuit.

---

## 论文详细总结（自动生成）

这篇论文提出了一种利用随机计算（Stochastic Computing, SC）在标准 CMOS 工艺下实现高效可逆逻辑（Invertible Logic）的方法。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的数字逻辑是单向的（给定输入得到唯一输出），而可逆逻辑允许电路在正向（计算）和反向（推理，如从乘积推导因数）模式下运行。
*   **研究动机**：
    *   **解决复杂问题**：可逆逻辑在因式分解、组合优化和机器学习（如逆转损失函数）中具有巨大潜力。
    *   **硬件局限性**：现有的可逆逻辑实现通常依赖于“异质器件”（如超导、自旋电子器件），难以与主流 CMOS 工艺集成。
    *   **现有方案冗余**：现有的基于 CMOS 的可逆逻辑方案（如基于概率比特 p-bits）往往需要庞大的哈密顿量（Hamiltonian）和复杂的硬件结构。

### 2. 核心方法论
*   **核心思想**：将可逆逻辑表示为**玻尔兹曼机（Boltzmann Machine）**结构，并利用**随机计算（SC）**和**脉冲神经网络（SNN）**的原理在硬件上高效实现。
*   **关键技术细节**：
    *   **节点模型**：使用简化的数字脉冲神经元作为基本处理单元，通过饱和累加器实现双极性随机比特流（-1 到 +1）的非线性激活函数（tanh）。
    *   **哈密顿量设计**：通过精心设计的权重矩阵（$J$）和偏置向量（$h$），定义逻辑门（如 AND, XOR, 全加器）的能量景观，使逻辑正确状态处于能量最低点。
    *   **随机退火机制**：在电路中引入可调幅度的随机噪声源（$w_{rnd}$）。通过逐渐减小噪声，使系统从高随机状态收敛到能量最低的有效逻辑状态（模拟退火）。
    *   **紧凑型设计**：相比前人工作，本文通过融合逻辑节点（如将 AND 门与半加器融合），显著减少了实现复杂算术运算所需的节点数量。

### 3. 实验设计
*   **实验场景**：
    *   **逻辑验证**：基础逻辑门（AND, XOR）和算术单元（半加器、全加器）。
    *   **系统级应用**：2x2、3x3、4x4 及 5x5 位可逆乘法器。
    *   **功能测试**：正向乘法运算和反向因式分解（例如：固定输出为 55，观察输入是否收敛为 5 和 11）。
*   **Benchmark（对比基准）**：
    *   与传统的不可逆 CMOS 逻辑对比面积。
    *   与现有的可逆逻辑 FPGA 实现（如 Pervaiz 等人的工作）对比资源消耗（LUT 和寄存器）。
    *   与基于微控制器的软件模拟方案对比延迟。

### 4. 资源与算力
*   **硬件实现**：
    *   **ASIC**：采用 TSMC 65nm GP 工艺制造了 5x5 位可逆乘法器芯片。
    *   **FPGA**：使用 Xilinx Kintex UltraScale (XCKU040) 进行综合对比。
*   **算力说明**：论文侧重于硬件电路设计与实测，不涉及深度学习的大规模 GPU 训练。仿真环境为 SystemC 和 SystemVerilog。ASIC 在 200MHz 频率下运行，功耗约为 13.4mW。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了从基础门到 32 位纹波进位加法器（RCA）的 FPGA 综合对比。
    *   ASIC 测试涵盖了 340 组通用因式分解任务和 66 组质因数分解任务。
*   **充分性评价**：实验设计较为充分，不仅有 RTL 级仿真，还有实际的芯片流片测量结果。通过直方图和收敛曲线展示了随机过程的统计特性，结果具有客观性。

### 6. 主要结论与发现
*   **高效性**：提出的可逆全加器节点数比现有方法减少了约 64%。在 32 位 RCA 实现中，LUT 消耗仅为传统可逆方案的 27%。
*   **性能优势**：ASIC 实测显示，反向因式分解的平均延迟在微秒级（约 2.15μs），比基于微控制器的软件模拟方案快了几个数量级。
*   **可扩展性**：证明了该方法在 CMOS 工艺下的可综合性，且硬件面积随位宽呈平方级增长（与传统乘法器一致）。

### 7. 优点与亮点
*   **CMOS 兼容性**：这是首个在标准 CMOS 工艺下实现并流片验证的可逆逻辑硬件，具有极强的工程实用性。
*   **设计精简**：通过优化哈密顿量，极大地压缩了玻尔兹曼机的规模，降低了硬件开销。
*   **工具链友好**：方案完全可综合，可直接使用现有的 EDA 工具（如 Synopsys, Cadence）进行设计流片。

### 8. 不足与局限
*   **面积开销**：虽然比其他可逆方案精简，但与传统不可逆逻辑相比，面积仍大出约 13 倍（优化后），这限制了其在超大规模集成电路中的通用性。
*   **手动调优**：随机噪声（退火过程）的参数设置目前仍需一定程度的手动实验获取，缺乏完全自动化的控制算法。
*   **位宽限制**：目前展示的最高位宽仅为 5x5 乘法器，对于大数分解（如 RSA 加密破解所需的位宽）仍有巨大挑战。
*   **精度与时间权衡**：作为随机计算系统，获得高置信度结果需要较长的时钟周期进行统计采样。

（完）
