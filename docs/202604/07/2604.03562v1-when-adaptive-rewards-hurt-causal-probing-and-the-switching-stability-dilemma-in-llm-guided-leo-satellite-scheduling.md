---
title: "When Adaptive Rewards Hurt: Causal Probing and the Switching-Stability Dilemma in LLM-Guided LEO Satellite Scheduling"
title_zh: 当自适应奖励适得其反：大语言模型引导的低轨卫星调度中的因果探测与切换-稳定性困境
authors: Yuanhang Li
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03562v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 大模型引导的低轨卫星调度深度强化学习
tldr: 本研究探讨了低轨卫星调度中深度强化学习（DRL）的自适应奖励设计，揭示了“切换-稳定性困境”。研究发现，由于PPO算法需要准平稳的奖励信号，频繁变动的奖励权重反而会导致性能下降。通过引入单变量因果探测方法，研究揭示了奖励项对性能的非直观影响。实验对比了固定权重、MLP及微调LLM等方案，发现LLM因输出不一致导致权重振荡而表现不佳，为LLM与DRL在通信系统中的集成提供了实证指导。
selection_source: fresh_fetch
motivation: 验证在多波束低轨卫星调度中，自适应奖励权重是否优于静态权重，并探究其背后的性能影响机制。
method: 提出单变量因果探测方法，通过独立扰动奖励项并测量PPO响应，同时对比固定、规则、MLP和LLM四种权重生成方案。
result: 静态权重和MLP表现优异，而微调后的LLM因权重振荡导致性能大幅下降，且发现增加切换惩罚能反直觉地提升吞吐量。
conclusion: 奖励权重的稳定性对DRL收敛至关重要，LLM在通信系统中应侧重于意图理解而非频繁的奖励权重调整。
---

## 摘要
多波束低轨（LEO）卫星调度中深度强化学习（DRL）的自适应奖励设计，源于一种直觉：感知场景的奖励权重应优于静态权重。我们系统地测试了这一直觉，并揭示了一个“切换-稳定性困境”：近乎恒定的奖励权重（342.1 Mbps）优于精心调优的动态权重（103.3+/-96.8 Mbps），因为近端策略优化（PPO）算法需要准平稳的奖励信号来实现价值函数的收敛。权重的自适应调整——无论其质量如何——都会因反复重启收敛过程而降低性能。为了理解特定权重为何重要，我们引入了一种单变量因果探测方法，通过独立地对每个奖励项进行 +/-20% 的扰动，并测量 5 万步后的 PPO 响应。探测结果揭示了反直觉的杠杆效应：将切换惩罚增加 20%，在极地切换场景中可提升 157 Mbps，在冷热场景中可提升 130 Mbps——这些发现是人类专家或未经系统探测的已训练多层感知机（MLP）无法获得的。我们在已知和新颖的流量场景下评估了四种马尔可夫决策过程（MDP）架构变体（固定权重、基于规则、学习型 MLP、微调后的 LLM）。MLP 在已知场景中达到 357.9 Mbps，在新颖场景中达到 325.2 Mbps，而微调后的 LLM 则因权重振荡（而非缺乏领域知识）崩溃至 45.3+/-43.0 Mbps——输出的一致性而非知识储备，才是核心约束。我们的研究为通信系统中的 LLM-DRL 集成提供了基于实证的路线图，明确了 LLM 在何处具有不可替代的价值（自然语言意图理解），以及在何处使用更简单的方法即可满足需求。

## Abstract
Adaptive reward design for deep reinforcement learning (DRL) in multi-beam LEO satellite scheduling is motivated by the intuition that regime-aware reward weights should outperform static ones. We systematically test this intuition and uncover a switching-stability dilemma: near-constant reward weights (342.1 Mbps) outperform carefully-tuned dynamic weights (103.3+/-96.8 Mbps) because PPO requires a quasistationary reward signal for value function convergence. Weight adaptation-regardless of quality-degrades performance by repeatedly restarting convergence. To understand why specific weights matter, we introduce a single-variable causal probing method that independently perturbs each reward term by +/-20% and measures PPO response after 50k steps. Probing reveals counterintuitive leverage: a +20% increase in the switching penalty yields +157 Mbps for polar handover and +130 Mbps for hot-cold regimes-findings inaccessible to human experts or trained MLPs without systematic probing. We evaluate four MDP architect variants (fixed, rule-based, learned MLP, finetuned LLM) across known and novel traffic regimes. The MLP achieves 357.9 Mbps on known regimes and 325.2 Mbps on novel regimes, while the fine-tuned LLM collapses to 45.3+/-43.0 Mbps due to weight oscillation rather than lack of domain knowledge-output consistency, not knowledge, is the binding constraint. Our findings provide an empirically-grounded roadmap for LLM-DRL integration in communication systems, identifying where LLMs add irreplaceable value (natural language intent understanding) versus where simpler methods suffice.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）引导下的低轨（LEO）卫星调度奖励函数设计进行了深入探讨，挑战了“动态奖励优于静态奖励”的传统直觉。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多波束低轨卫星调度中，深度强化学习（DRL）通常依赖预设的奖励函数。由于卫星覆盖区域（如城市、海洋、灾区）的流量模式（Regimes）差异巨大，研究者直觉上认为奖励权重应随场景动态调整。
*   **研究动机**：验证 LLM 是否能利用其推理能力为不同场景生成最优奖励权重，并探究动态奖励调整对 DRL 算法（如 PPO）收敛性的实际影响。

### 2. 方法论
论文提出了一个**三层时间尺度架构**：
*   **战略层（LLM 意图解析器）**：小时级频率。利用 LLM 将运营商的自然语言指令（如“优先保障应急通信”）解析为结构化的目标配置文件。
*   **战术层（MDP 架构师）**：分钟级频率。监控网络关键绩效指标（KPI），利用 CUSUM 算法检测场景切换，并生成具体的奖励权重向量 $w$。
    *   对比了四种实现：固定权重（M1）、基于规则（M2）、学习型 MLP（M3）、微调后的 LLM（M4）。
*   **执行层（PPO 智能体）**：毫秒级频率。根据当前奖励函数执行实时的波束功率和带宽分配。
*   **关键技术：单变量因果探测（Causal Probing）**：
    *   为了理解权重影响，独立对每个奖励项进行 $\pm 20\%$ 的扰动，观察 5 万步后的性能变化，从而构建奖励景观的因果图。

### 3. 实验设计
*   **实验场景**：使用 19 波束 LEO 卫星仿真器，涵盖 4 种已知场景（城市、海洋、灾难、混合）和 3 种新颖场景（IoT 爆发、极地切换、冷热不均）。
*   **基准方法（Benchmark）**：
    *   固定权重（Static Baseline）。
    *   基于专家经验的规则分类器。
    *   离线训练的 MLP 映射网络。
*   **评估指标**：总吞吐量（Mbps）、中断率（Outage）、公平性指数、切换频率。

### 4. 资源与算力
*   **GPU 型号**：提及在 **NVIDIA RTX 4060** 上进行 LLM 推理。
*   **模型细节**：使用 **Qwen3-4B** 模型，通过 LoRA（秩 16）进行微调，采用 4-bit NF4 量化。
*   **训练时长**：PPO 智能体每个实验训练 50 万步；LLM 推理延迟约为 14 秒/次。

### 5. 实验数量与充分性
*   **实验规模**：所有核心实验均采用 3 个随机种子（42, 123, 456）取平均值，针对特定结论（如切换稳定性）进行了 8 个种子的扩展验证。
*   **消融实验**：对比了不同架构变体（M1-M4）在已知和未知场景下的表现。
*   **充分性评价**：实验设计较为全面，不仅覆盖了性能对比，还深入分析了权重振荡、推理延迟和因果敏感性，论证过程客观且具有统计学意义。

### 6. 主要结论与发现
*   **切换-稳定性困境（Switching-Stability Dilemma）**：这是最核心的发现。近乎恒定的权重（342.1 Mbps）远优于频繁调优的动态权重（103.3 Mbps）。原因是 PPO 需要准平稳的奖励信号来估计价值函数，权重的任何变动（无论质量好坏）都会迫使收敛过程重启。
*   **LLM 的局限性**：微调后的 LLM 在数值生成上表现极差（45.3 Mbps），原因并非缺乏知识，而是**输出不一致导致的权重振荡**（变异系数比 MLP 高 4.6 倍）。
*   **反直觉的因果发现**：通过探测发现，增加“切换惩罚”权重对极地切换场景有巨大的正面影响（+157 Mbps），这一规律是人类专家难以预见的。
*   **MLP 的优越性**：在数值映射任务中，轻量级 MLP 在速度（快 1800 倍）和稳定性上完胜 LLM。

### 7. 优点
*   **揭示了深层机制**：不仅指出 LLM 做不好数值生成，还通过实验解释了是因为 DRL 对非平稳奖励的敏感性。
*   **高效的探测方法**：提出的因果探测仅需完整训练 1/10 的步数即可获得有效的权重指导信号。
*   **实用的架构建议**：提出了“LLM 负责意图理解 + MLP 负责数值优化”的混合架构，兼顾了灵活性与稳定性。

### 8. 不足与局限
*   **仿真简化**：卫星环境虽然考虑了 Ka 频段和降雨衰减，但对波束间干扰和复杂的轨道动力学做了简化处理。
*   **模型规模限制**：仅测试了 4B 规模的 LLM，更大规模的模型是否能通过更强的上下文学习减少振荡尚不明确。
*   **应用限制**：目前结论主要基于 PPO 算法，对于其他对非平稳环境更鲁棒的强化学习算法（如元学习类 RL）的适用性有待验证。

（完）
