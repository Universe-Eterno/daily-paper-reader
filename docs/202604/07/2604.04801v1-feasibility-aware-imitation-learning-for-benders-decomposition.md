---
title: Feasibility-Aware Imitation Learning for Benders Decomposition
title_zh: 面向 Benders 分解的可行性感知模仿学习
authors: "Bernard T. Agyeman, Zhe Li, Ilias Mitrai, Prodromos Daoutidis"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04801v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 混合整数优化中Benders分解的模仿学习
tldr: 针对混合整数优化中Benders分解主问题求解慢的瓶颈，本文提出一种可行性感知模仿学习框架。该框架通过行为克隆和逻辑调整预测主问题整数变量，并结合显式可行性检查与限时求解器确保收敛性。在保持求解精度的同时，显著提升了求解速度，为加速复杂优化问题提供了新思路。
selection_source: fresh_fetch
motivation: Benders分解中主问题的重复求解是计算瓶颈，且现有模仿学习方法难以保证预测结果满足不断增加的可行性割平面约束。
method: 提出一种结合行为克隆与可行性逻辑调整的二阶段训练方法，并在代理辅助的Benders框架中集成显式可行性检查与下界验证。
result: 在典型案例研究中，该方法在保持解的准确性的前提下，比现有加速Benders分解的模仿学习方法具有更短的求解时间。
conclusion: 该框架通过将机器学习预测与传统优化约束相结合，实现了在保证有限收敛性的同时大幅提升Benders分解的计算效率。
---

## 摘要
混合整数优化问题广泛存在于各种控制应用中。Benders 分解是一种广泛用于解决此类问题的算法，它将问题分解为一个混合整数主问题和一个连续子问题。一个关键的计算瓶颈是在迭代过程中重复求解日益复杂的主问题。在本文中，我们提出了一种可行性感知模仿学习框架，该框架在每次迭代时预测主问题整数变量的值，同时考虑关于允许整数赋值的约束以及累积的 Benders 可行性割集的可行性。该智能体采用两阶段程序进行训练，结合了行为克隆与基于可行性的 Logit 调整，以使预测偏向于满足不断演化的割集的赋值。该智能体部署在一个基于智能体的 Benders 分解框架中，该框架结合了显式可行性检查与受时限约束的求解器有效下界计算。由于下界在每次迭代中都经过验证，所提出的方法保留了有限收敛特性。对原型案例研究的应用表明，与现有的加速 Benders 分解的模仿学习方法相比，所提出的方法在保持求解准确性的同时，缩短了求解时间。

## Abstract
Mixed-integer optimization problems arise in a wide range of control applications. Benders decomposition is a widely used algorithm for solving such problems by decomposing them into a mixed-integer master problem and a continuous subproblem. A key computational bottleneck is the repeated solution of increasingly complex master problems across iterations. In this paper, we propose a feasibility-aware imitation learning framework that predicts the values of the integer variables of the master problem at each iteration while accounting for feasibility with respect to constraints governing admissible integer assignments and the accumulated Benders feasibility cuts. The agent is trained using a two-stage procedure that combines behavioral cloning with a feasibility-based logit adjustment to bias predictions toward assignments that satisfy the evolving cut set. The agent is deployed within an agent-based Benders decomposition framework that combines explicit feasibility checks with a time-limited solver computation of a valid lower bound. The proposed approach retains finite convergence properties, as the lower bound is certified at each iteration. Application to a prototypical case study shows that the proposed method improves solution time relative to existing imitation learning approaches for accelerating Benders decomposition, while preserving solution accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Feasibility-Aware Imitation Learning for Benders Decomposition》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：Benders 分解（BD）是求解混合整数优化问题的经典方法，但其计算瓶颈在于需要反复求解日益复杂的**主问题（Master Problem, MP）**。
*   **研究动机**：
    *   现有的机器学习（ML）加速方法（如模仿学习或强化学习）直接预测主问题的解，但往往无法保证预测结果满足主问题的约束，尤其是动态增加的**可行性割平面（Feasibility Cuts）**。
    *   缺乏可行性保证会导致算法收敛失败或产生无效的下界（Lower Bound）。
    *   本文旨在开发一种既能利用 ML 的预测速度，又能确保可行性和算法收敛性的框架。

### 2. 方法论
核心思想是结合**图神经网络（GNN）**和**两阶段模仿学习**，并将其嵌入到一个带有显式验证机制的 Benders 框架中。
*   **动作空间设计**：利用原始问题中的纯二变量约束预先筛选出合法的赋值集合 $Y_{pb}$，将预测任务转化为在该集合上的分类问题。
*   **智能体架构**：采用二部图（Bipartite Graph）表示主问题，变量和约束作为节点。使用**边条件卷积（ECC）**层提取特征，通过全局池化和全连接层输出 Logits。
*   **两阶段训练程序**：
    1.  **行为克隆（Stage 1）**：学习模仿专家（MIP 求解器）的最优决策。
    2.  **可行性感知逻辑调整（Stage 2）**：冻结图层，通过一个惩罚项调整输出的 Logits。如果某个预测赋值违反了当前的 Benders 可行性割平面，则降低其得分。
*   **代理辅助 Benders 算法**：
    *   **显式检查**：预测结果需通过可行性割平面验证，若失败则回退至传统求解器。
    *   **限时求解验证**：即使预测通过，也会调用限时（Time-limited）求解器进行热启动求解，以获取受认证的有效下界，确保算法的有限收敛性。

### 3. 实验设计
*   **场景/数据集**：采用一个典型的混合整数非线性规划（MINLP）案例研究（源自 Floudas, 1995）。通过随机采样参数（如成本系数、约束界限等）生成了 300 个不同的问题实例。
*   **数据集规模**：总计 24,638 个数据点，其中约 2.3 万用于训练，1,232 个用于评估。
*   **Benchmark 与对比方法**：
    *   **Classical GBD**：传统的广义 Benders 分解。
    *   **Independent IL**：现有的模仿学习方法（Agyeman et al., 2024），独立预测每个二变量。
    *   **Baseline**：仅完成第一阶段训练（无可行性感知）的模型。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-14700 处理器（20 核），32 GB RAM，运行 Ubuntu 24.04。
*   **软件工具**：Gurobi 12.0.1（主问题求解器）、Ipopt 3.12.6（子问题求解器）、BONMIN 1.8.9（获取真实解）。
*   **算力说明**：文中未提及使用 GPU 进行训练，考虑到所处理的图规模较小，可能在 CPU 上完成或未详细说明 GPU 型号。

### 5. 实验数量与充分性
*   **实验规模**：在 30 个全新的测试实例上进行了完整算法流程的验证。
*   **充分性评价**：
    *   **消融实验**：对比了 Baseline 和 Proposed，证明了两阶段训练中“可行性感知”步骤的有效性（可行性满足率从 89% 提升至 93.7%）。
    *   **对比实验**：与 Independent IL 对比，展示了在主问题求解时间上的显著优势。
    *   **客观性**：实验涵盖了迭代次数、求解时间、解的准确率等多个维度，且确保了所有方法最终都能收敛到相同的最优解，对比基础公平。

### 6. 主要结论与发现
*   **效率提升**：相比传统 GBD，主问题求解时间减少了 **65.3%**；相比现有的 Independent IL 方法，时间减少了约 **42%**。
*   **可行性增强**：所提方法生成的预测在 99% 的迭代中是可行的，而 Independent IL 仅为 25%。
*   **准确性**：在所有测试实例中，该方法均能 100% 找回最优解，且平均迭代次数与传统方法一致。
*   **收敛性**：通过限时求解器提供下界认证，成功在保持 ML 加速效果的同时保留了 BD 的有限收敛特性。

### 7. 优点（亮点）
*   **结构化先验**：通过限制动作空间和 Logit 调整，将优化问题的硬约束（Hard Constraints）巧妙地融入了深度学习模型。
*   **理论完备**：不同于纯黑盒预测，该框架通过“预测+验证+限时求解”的闭环，解决了 ML 在严谨优化算法中缺乏收敛保证的痛点。
*   **通用性**：该方法不仅适用于广义 Benders 分解，也可推广至经典的线性 Benders 分解。

### 8. 不足与局限
*   **规模限制**：案例研究中的二变量数量较少（5 个），动作空间 $Y_{pb}$ 的大小（12 个组合）在变量规模大幅增加时可能会面临维度灾难，导致分类任务难以处理。
*   **参数敏感性**：Logit 调整中的惩罚参数 $\omega$ 和限时求解的时间预算 $T_k$ 需要手动调优，可能影响不同问题上的泛化表现。
*   **实验覆盖面**：仅在一个 MINLP 案例上进行了验证，缺乏在大规模工业级问题（如电力系统调度或大规模物流）上的压力测试。

（完）
