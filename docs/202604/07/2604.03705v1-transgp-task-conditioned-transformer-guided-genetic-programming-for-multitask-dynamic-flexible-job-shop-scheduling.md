---
title: "TransGP: Task-Conditioned Transformer-Guided Genetic Programming for Multitask Dynamic Flexible Job Shop Scheduling"
title_zh: TransGP：用于多任务动态柔性作业车间调度的任务条件 Transformer 引导遗传规划
authors: "Meng Xu, Jiao Liu, Hua Yu, Yew Soon Ong"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03705v1"
tags: ["query:nco"]
score: 8.0
evidence: 用于作业车间调度的Transformer引导遗传规划
tldr: 针对动态柔性作业车间调度（DFJSS）问题，本文提出TransGP框架，将任务条件Transformer与遗传编程（GP）结合。该方法利用Transformer学习精英启发式算法的分布，并根据特定任务信息生成定制化启发式规则，克服了传统GP收敛慢和多任务GP缺乏任务针对性的缺陷，显著提升了多任务调度场景下的搜索效率和解的质量。
selection_source: fresh_fetch
motivation: 传统遗传编程在处理动态调度问题时收敛缓慢，且现有方法难以根据具体任务特征生成定制化的启发式规则。
method: 提出TransGP框架，通过Transformer模型学习精英启发式分布并进行任务条件生成，引导GP在更有潜力的空间内进行进化搜索。
result: 实验表明TransGP在多种DFJSS场景下均优于传统多任务GP、手工规则及纯Transformer模型，具有更快的收敛速度和更强的鲁棒性。
conclusion: 将生成式Transformer与进化计算相结合，为解决复杂多任务动态调度问题提供了一种高效且具备知识迁移能力的通用框架。
---

## 摘要
超启发式算法已成为解决动态柔性作业车间调度（DFJSS）问题的流行方法。它们利用遗传规划（GP）等无梯度优化技术来演化不可微的启发式规则。然而，传统的 GP 方法往往收敛缓慢，因为它们仅依赖进化搜索来寻找优秀的启发式规则。现有的多任务 GP 方法可以同时解决多个任务，并通过在相似任务之间转移知识来加速搜索。但它们大多只是交换启发式规则的构建块，而没有真正根据任务信息生成启发式规则。在本文中，我们旨在通过引入任务条件 Transformer 模型来加速收敛并实现特定任务的启发式规则生成。该 Transformer 以两种方式发挥作用：首先，它学习精英启发式规则的分布，引导搜索向启发式空间中更具潜力的区域靠拢；其次，通过条件生成，它产生为特定任务量身定制的启发式规则，使模型能够同时处理多个调度任务并提高整体优化效率。基于这些想法，我们提出了 TransGP，一种任务条件 Transformer 引导的 GP 框架。这种进化范式将生成式建模与 GP 相结合，实现了高效的多任务启发式学习和知识转移。我们在多种 DFJSS 场景下评估了 TransGP。实验结果表明，TransGP 始终优于多任务 GP 基准、广泛使用的手工启发式规则以及纯 Transformer 模型，实现了更快的收敛速度、更优的解质量和更强的鲁棒性。

## Abstract
Hyper-heuristics have become a popular approach for solving dynamic flexible job shop scheduling (DFJSS) problems. They use gradient-free optimization techniques like Genetic Programming (GP) to evolve non-differentiable heuristics. However, conventional GP methods tend to converge slowly because they rely solely on evolutionary search to find good heuristics. Existing multitask GP methods can solve multiple tasks simultaneously and speed up the search by transferring knowledge across similar tasks. But they mostly exchange heuristic building blocks without truly generating heuristics conditioned on task information. In this paper, we aim to accelerate convergence and enable task-specific heuristic generation by incorporating a task-conditioned Transformer model. The Transformer works in two ways. First, it learns the distribution of elite heuristics, biasing the search toward promising regions of the heuristic space. Second, through conditional generation, it produces heuristics tailored to specific tasks, allowing the model to handle multiple scheduling tasks at once and improving overall optimization efficiency. Based on these ideas, we propose TransGP, a Task-Conditioned Transformer-Guided GP framework. This evolutionary paradigm integrates generative modeling with GP, enabling efficient multitask heuristic learning and knowledge transfer. We evaluate TransGP on a range of DFJSS scenarios. Experimental results show that TransGP consistently outperforms multitask GP baselines, widely used handcrafted heuristics, and the pure Transformer model, achieving faster convergence, superior solution quality, and enhanced robustness.

---

## 论文详细总结（自动生成）

以下是对论文《TransGP: Task-Conditioned Transformer-Guided Genetic Programming for Multitask Dynamic Flexible Job Shop Scheduling》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：针对动态柔性作业车间调度（DFJSS）问题，传统的遗传规划（GP）算法存在收敛速度慢、进化算子（交叉/变异）随机性强导致搜索效率低的问题。此外，现有的多任务 GP 方法虽能实现知识迁移，但缺乏根据具体任务环境（如负载、机器数）生成定制化启发式规则的能力。
*   **研究背景**：DFJSS 是典型的 NP-hard 问题，工业界对决策规则的可解释性有很高要求。GP 能生成显式的符号规则，但其“盲目”的随机搜索难以捕捉复杂的结构模式。

### 2. 方法论
TransGP 框架将生成式 Transformer 与进化搜索相结合，核心步骤如下：
*   **符号规则向量化**：利用前序遍历（Prefix Traversal）将 GP 的树状启发式规则线性化为 Token 序列，并加入 START/END 标记，使其适配 Transformer 的输入格式。
*   **任务条件 Transformer 训练**：
    *   **模型结构**：采用 Transformer Decoder 架构。
    *   **任务嵌入**：将任务特征（如系统利用率、机器数量）编码为任务嵌入（Task Embeddings），作为条件输入引导模型生成。
    *   **离线学习**：从多个调度任务的精英个体中收集数据，训练模型学习高水平规则的分布。
*   **Transformer 引导的进化搜索**：
    *   **语义感知变异**：在 GP 进化过程中，Transformer 充当变异算子。它根据父代规则的上下文和目标任务信息，自回归地生成符合语法规范且具有语义意义的子树。
    *   **语法约束**：在解码过程中使用基于栈（Stack-based）的机制，确保生成的 Token 序列能还原为合法的二叉树结构。
    *   **多任务并行进化**：种群中的个体被分配到不同任务中评估，通过 Transformer 实现跨任务的结构知识迁移。

### 3. 实验设计
*   **实验场景**：设计了 3 个大类场景（分别以最大流程时间 Fmax、平均流程时间 Fmean、平均误期时间 Tmean 为目标），每个场景包含 3 个不同难度（利用率 0.75-0.95，机器数 6-10）的任务。
*   **基准方法（Benchmarks）**：
    *   **GP 类**：标准 GP、TGP（带任务标签的多任务 GP）。
    *   **手工规则**：SPT、FIFO、LPT、EDD 等 8 种经典调度规则组合。
    *   **生成模型类**：PureTrans（仅使用 Transformer 生成规则，不进行进化优化）。
*   **评估指标**：测试集性能均值/标准差、收敛曲线、规则大小（Size）、统计显著性（Wilcoxon 秩和检验）。

### 4. 资源与算力
*   **算力说明**：论文明确提到 Transformer 模型是在 **CPU** 上进行训练的。
*   **计算开销分析**：作者指出 Transformer 模型非常轻量化（256 隐藏维度，4 层，4 头），离线训练成本是一次性的。在线进化过程中，Transformer 推理开销远小于 DFJSS 模拟器的适应度评估开销，因此不会显著增加总运行时间。

### 5. 实验数量与充分性
*   **实验规模**：共涉及 9 个具体的调度任务。每个算法在每个任务上独立运行 **30 次**以保证统计意义。
*   **分析维度**：除了性能对比，还进行了温度系数（Temperature）敏感性分析、规则大小分析、规则结构相似度分析（Jaccard 相似度）以及变异过程的透明度分析。
*   **充分性评价**：实验设计较为全面，涵盖了不同目标和环境复杂度，对比了从传统手工到前沿生成式模型的多种路径，实验结果经过了严格的统计检验，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能优越性**：TransGP 在所有任务中均显著优于传统 GP 和手工规则，且比纯 Transformer 生成的规则更稳定、质量更高。
*   **收敛加速**：引入 Transformer 引导后，GP 能够更快地发现高质量区域，显著提升了收敛效率。
*   **抗膨胀能力**：TransGP 生成的规则比传统 GP 更简洁（Size 更小），有效抑制了 GP 中常见的“代码膨胀（Bloat）”现象，增强了规则的可解释性。
*   **知识迁移**：任务条件机制使得模型能学习到跨任务的通用结构模式（如特定的 MWT、WIQ 组合），并能根据任务差异进行微调。

### 7. 优点与亮点
*   **神经符号结合**：成功将 Transformer 的强大分布学习能力与 GP 的符号搜索能力结合，解决了 GP 变异盲目性的痛点。
*   **任务感知生成**：通过任务嵌入实现了“因地制宜”的规则生成，而非简单的跨任务子树交换。
*   **可解释性强**：不仅生成的规则本身是符号化的，作者还通过可视化手段展示了 Transformer 如何学习并重组有效的计算原语（Primitives）。

### 8. 不足与局限
*   **离线数据依赖**：Transformer 的训练依赖于预先收集的精英启发式规则，如果初始数据质量较差，可能会限制模型的引导效果。
*   **函数集限制**：目前仅支持二元函数（Binary Functions），对于多元函数或更复杂的语法结构，其编码和解码机制可能需要进一步扩展。
*   **任务特征维度**：实验中使用的任务特征（利用率、机器数）较为简单，对于更复杂的现实干扰（如机器故障、订单取消）的表征能力尚待验证。

（完）
