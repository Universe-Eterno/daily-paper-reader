---
title: Differentiable Initialization-Accelerated CPU-GPU Hybrid Combinatorial Scheduling
authors: "Mingju Liu, Jiaqi Yin, Alvaro Velasquez, Cunxi Yu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28943v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 用于组合调度和整数线性规划的CPU-GPU混合框架
tldr: "针对大规模组合调度问题（ILP）的NP-hard挑战，本文提出一种CPU-GPU混合框架。该框架通过可微预求解技术在GPU上快速生成高质量初始解，为CPLEX、Gurobi等传统求解器提供热启动，显著提升了剪枝效率。实验表明，该方法在工业级基准测试中实现了高达10倍的性能提升，且最优性差距小于0.1%，成功将机器学习基础设施与经典精确优化方法相结合。"
selection_source: fresh_fetch
motivation: 传统的整数线性规划求解器在处理大规模组合调度问题时，由于其NP-hard特性，难以在有限时间内获得最优解。
method: 提出一种结合可微优化与经典ILP求解的混合框架，利用GPU加速的可微预求解生成高质量部分解作为求解器的热启动。
result: "在工业规模基准测试中，该方法相比基准求解器实现了高达10倍的加速，并将最优性差距缩小至0.1%以内。"
conclusion: 本研究首次证明了利用可微优化初始化精确ILP求解器的有效性，为集成机器学习与经典优化方法开辟了新方向。
---

## Abstract
This paper presents a hybrid CPU-GPU framework for solving combinatorial scheduling problems formulated as Integer Linear Programming (ILP). While scheduling underpins many optimization tasks in computing systems, solving these problems optimally at scale remains a long-standing challenge due to their NP-hard nature. We introduce a novel approach that combines differentiable optimization with classical ILP solving. Specifically, we utilize differentiable presolving to rapidly generate high-quality partial solutions, which serve as warm-starts for commercial ILP solvers (CPLEX, Gurobi) and rising open-source solver HiGHS. This method enables significantly improved early pruning compared to state-of-the-art standalone solvers. Empirical results across industry-scale benchmarks demonstrate up to a $10\times$ performance gain over baselines, narrowing the optimality gap to $<0.1\%$. This work represents the first demonstration of utilizing differentiable optimization to initialize exact ILP solvers for combinatorial scheduling, opening new opportunities to integrate machine learning infrastructure with classical exact optimization methods across broader domains.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Differentiable Initialization-Accelerated CPU-GPU Hybrid Combinatorial Scheduling** 的混合框架，旨在通过可微优化技术加速精确的整数线性规划（ILP）求解器，处理大规模组合调度问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：组合调度（如硬件综合中的任务分配）是典型的 NP-hard 问题。传统的 ILP 求解器（如 Gurobi, CPLEX）虽然能保证最优性，但在处理大规模问题时由于搜索空间巨大，计算时间呈指数级增长；而启发式方法或纯机器学习方法虽快，但往往陷入局部最优，缺乏最优性保证。
*   **研究动机**：利用 GPU 的并行计算能力和可微优化的快速收敛特性，为 CPU 上的精确 ILP 求解器提供高质量的“热启动”（Warm-start）初始解，从而在保证最优性的前提下大幅提升求解效率。

### 2. 方法论
该框架采用**两阶段混合优化流程**：
*   **第一阶段：可微热启动生成（GPU/CPU）**
    *   **核心思想**：将离散的调度决策转化为连续的概率分布。
    *   **关键技术**：利用 **Gumbel-Softmax** 技巧进行随机松弛，并结合 **受限 Gumbel 技巧（Constrained Gumbel Trick）**。通过对差分约束系统（SDC）进行编码，确保在梯度下降过程中始终尊重任务间的依赖关系。
    *   **置信度引导提取**：不直接使用不成熟的全量解，而是根据概率向量的分布计算置信度 $C_i$，提取置信度高于阈值 $\tau_C$ 的变量作为“部分解”（Partial Solution）。
*   **第二阶段：并行化求解器执行（CPU）**
    *   将第一阶段生成的多个高置信度部分解作为“提示”（Hints）输入到商业（CPLEX, Gurobi）或开源（HiGHS）ILP 求解器中。
    *   求解器利用这些提示进行早期剪枝，缩小搜索空间，最终输出精确的最优解。

### 3. 实验设计
*   **数据集/场景**：
    *   **EPFL Benchmark Suite**：逻辑综合领域的标准基准测试。
    *   **Post-mapped Designs**：基于 7nm ASAP 库的实际电路设计。
    *   **Random Workloads (RW)**：12 个不同规模和密度的合成随机工作负载。
    *   共计 24 个基准测试用例，约束数量从几万到数百万不等。
*   **对比方法（Benchmark）**：
    *   **DiffSched**：纯可微优化方法。
    *   **Cold-start Solvers**：无初始解直接运行的 CPLEX、Gurobi 和 HiGHS。
    *   **Full-solution Warm-start**：使用全量解（而非部分解）进行热启动的对比。

### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**：Intel® Xeon® Gold 6418H。
    *   **GPU**：NVIDIA RTX™ A6000。
*   **时长与设置**：
    *   设置了 3600 秒（1 小时）的超时限制。
    *   可微优化阶段运行 30 次迭代以收集初始解。
    *   ILP 求解器采用单线程模式以确保公平对比，但通过 30 个并行种子/初始解来模拟并行搜索。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 24 个不同规模的基准测试，每个测试均进行了 30 次迭代/种子的并行实验。
*   **充分性**：实验设计较为全面，不仅对比了不同求解器（商业 vs 开源），还进行了消融研究（如全量解 vs 部分解的对比，见图 2），并分析了置信度阈值对结果的影响。实验涵盖了从小型到工业级超大规模的约束系统，验证了方案的扩展性。

### 6. 主要结论与发现
*   **性能提升**：在大型基准测试中，该方法相比于纯冷启动求解器实现了高达 **10 倍** 的性能提升。
*   **最优性**：成功将最优性差距（Optimality Gap）缩小至 **< 0.1%**。
*   **开源增强**：该方法显著提升了开源求解器 HiGHS 的表现，使其在某些复杂任务上能够接近商业求解器 Gurobi 的水平。
*   **部分解的优势**：实验证明，提供高置信度的“部分解”比提供可能陷入局部最优的“全量解”更能有效引导求解器。

### 7. 优点
*   **协同效应**：首次将基于梯度的可微优化与精确 ILP 求解器结合用于调度问题，兼顾了 GPU 的速度和 CPU 的精确度。
*   **无监督/无数据依赖**：该方法不需要预先标注的训练数据（Dataless），直接在目标问题上进行优化。
*   **通用性**：基于 SDC 约束构建，理论上可以扩展到其他符合 ILP 形式的组合优化问题。

### 8. 不足与局限
*   **阈值敏感性**：置信度阈值 $\tau_C$ 需要根据不同类型的基准测试进行微调（如 RW 设为 0.2，EPFL 设为 0.127），缺乏完全自动化的自适应机制。
*   **第一阶段开销**：对于超大规模设计（如 Voter(M)），生成初始解的开销可能达到 30 分钟，虽然在总时长内可控，但仍是不可忽视的成本。
*   **应用限制**：目前主要针对 SDC（差分约束系统）类问题，对于更复杂的非线性约束或非 SDC 类的 ILP 问题的泛化能力尚待验证。

（完）
