---
title: "TestDecision: Sequential Test Suite Generation via Greedy Optimization and Reinforcement Learning"
title_zh: TestDecision：通过贪心优化与强化学习实现序列化测试套件生成
authors: "Guoqing Wang, Chengran Yang, Xiaoxuan Zhou, Zeyu Sun, Bo Wang, David Lo, Dan Hao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01799v1"
tags: ["query:nco-llm"]
score: 8.0
evidence: 基于大模型、贪心优化和强化学习的序列测试集生成
tldr: 针对开源大模型在自动化测试生成中缺乏全局视野、难以最大化边际收益的问题，本文提出TestDecision。该方法将测试套件生成建模为马尔可夫决策过程（MDP），利用其单调次模性，通过强化学习将LLM训练为“神经贪婪专家”。TestDecision包含逐步贪婪推理框架和最大化边际收益的RL训练流水线。实验显示，该方法显著提升了分支覆盖率和执行通过率，使7B模型达到了与顶级闭源模型相当的水平，并展现出极强的找Bug能力和泛化性。
selection_source: fresh_fetch
motivation: 开源大模型在生成测试套件时存在“结构性近视”，无法根据当前覆盖情况生成具有高边际收益的新测试用例。
method: 将测试生成形式化为MDP，并结合贪婪优化理论，通过强化学习训练模型以实现逐步最大化覆盖率的决策能力。
result: "在ULT基准上，分支覆盖率提升38.15%-52.37%，通过率提升298.22%-558.88%，且发现Bug的能力大幅增强。"
conclusion: 通过将LLM转化为序列决策专家，TestDecision有效解决了测试套件生成的全局优化难题，显著提升了开源模型的测试质量。
---

## 摘要
随着大语言模型（LLMs）的飞速发展，自动化软件测试正经历着范式转变。虽然像 GPT-4o 这样的闭源模型展示了令人印象深刻的能力，但其高昂的部署成本和数据隐私问题使得开源 LLMs 成为许多学术和工业场景下的必然选择。在自动化测试生成领域，基于 LLMs 构建测试套件已演变为迭代式工作流。在利用开源 LLMs 时，我们通过实证观察发现它们缺乏套件层面的视角，存在结构性近视（structural myopia）——即无法根据当前的覆盖状态生成具有较大边际收益的新测试。本文从序列的角度出发，将测试套件生成形式化为马尔可夫决策过程（MDP），并证明其目标具有单调次模性（monotone submodularity），从而将这一 NP 难的全局优化问题有效地松弛为可处理的分步贪心过程。在此启发下，我们提出了 TestDecision，将 LLMs 转化为神经贪心专家。TestDecision 由两个协同组件组成：（1）一个遵循分步贪心策略实现测试套件构建的推理框架；（2）一个强化学习训练流水线，使基础 LLM 具备序列化测试生成能力，以最大化边际收益。在 ULT 基准测试上的全面评估表明，TestDecision 显著优于现有的先进方法。与所有基础模型相比，它在分支覆盖率上提升了 38.15-52.37%，在执行通过率上提升了 298.22-558.88%，在 7B 骨干模型上实现了与规模大得多的闭源模型 GPT-5.2 相当的性能。此外，TestDecision 比原始基础 LLMs 能多发现 58.43-95.45% 的漏洞，并在 LiveCodeBench 上表现出卓越的泛化能力，证明了其构建高质量测试套件的能力。

## Abstract
With the rapid evolution of LLMs, automated software testing is witnessing a paradigm shift. While proprietary models like GPT-4o demonstrate impressive capabilities, their high deployment costs and data privacy concerns make open-source LLMs the practical imperative for many academic and industrial scenarios. In the field of automated test generation, it has evolved to iterative workflows to construct test suites based on LLMs. When utilizing open-source LLMs, we empirically observe they lack a suite-level perspective, suffering from structural myopia-failing to generate new tests with large marginal gain based on the current covered status. In this paper, from the perspective of sequences, we formalize test suite generation as a MDP and demonstrate that its objective exhibits monotone submodularity, which enables an effective relaxation of this NP-hard global optimization into a tractable step-wise greedy procedure. Guided by this insight, we propose TestDecision, which transforms LLMs into neural greedy experts. TestDecision consists of two synergistic components: (1) an inference framework which implements test suite construction following a step-wise greedy strategy; and (2) a training pipeline of reinforcement learning which equips the base LLM with sequential test generation ability to maximize marginal gain. Comprehensive evaluations on the ULT benchmark demonstrate that TestDecision significantly outperforms existing advanced methods. It brings an improvement between 38.15-52.37% in branch coverage and 298.22-558.88% in execution pass rate over all base models, achieving a comparable performance on 7B backbone with a much larger proprietary LLM GPT-5.2. Furthermore, TestDecision can find 58.43-95.45% more bugs than vanilla base LLMs and exhibit superior generalization on LiveCodeBench, proving its capability to construct high-quality test suites.

---

## 论文详细总结（自动生成）

### 论文总结：TestDecision —— 基于贪心优化与强化学习的序列化测试套件生成

#### 1. 核心问题与研究动机
*   **研究背景**：自动化测试生成已从单次合成演变为迭代式工作流（根据执行反馈逐步构建测试套件）。
*   **核心问题**：开源大模型（LLMs）在生成测试套件时存在“结构性近视”（Structural Myopia）。它们缺乏套件层面的全局视角，往往生成与已有测试功能重复的代码，导致边际覆盖收益迅速递减，难以触达复杂的逻辑分支。
*   **研究动机**：旨在为序列化测试生成提供理论保证，并开发一种能让中小型开源模型通过学习具备“贪心决策”能力的框架，从而在不依赖昂贵闭源模型的情况下生成高质量测试套件。

#### 2. 方法论：核心思想与技术细节
*   **理论形式化**：将测试套件生成建模为**马尔可夫决策过程（MDP）**。作者证明了该优化目标具有**单调次模性（Monotone Submodularity）**。根据数学理论，在这种特性下，每一步选择边际收益最大的动作（贪心策略）可以保证达到全局最优解的 $(1-1/e)$ 近似。
*   **TestDecision 框架**：
    *   **推理框架（感知与决策）**：采用“代码状态摘要”技术。通过动态插桩获取覆盖率，利用 AST（抽象语法树）在未覆盖的代码行后注入 `#uncovered` 标记，将“测试前沿”显式暴露给模型。
    *   **训练流水线（强化学习）**：使用 **GRPO（组相对策略优化）** 算法。
    *   **奖励函数设计**：奖励信号 $r$ 由“执行有效性”和“边际覆盖增量”组成。如果测试用例无法执行，奖励为 0；若执行成功，则根据其对剩余未覆盖代码的贡献比例给予奖励。
    *   **数据构造**：利用高质量的轨迹回放（State Replay）构造训练数据，确保模型在各种中间状态下都能学习如何寻找“增量”。

#### 3. 实验设计
*   **数据集**：
    *   **ULT Benchmark**：包含 3,909 个真实世界 Python 函数的单元测试任务，经过严格过滤以防数据泄露。
    *   **LiveCodeBench (LCB)**：用于评估模型对 2024 年 6 月以后发布的全新编程任务的泛化能力（OOD 评估）。
*   **对比方法**：
    *   **基础模型**：Qwen2.5-Coder-7B, Llama-3.1-8B, CodeLlama-7B。
    *   **先进提示工程**：ChatTester（迭代修复）、SymPrompt（路径感知提示）。
    *   **学习类方法**：TestCTRL（基于 PPO 和 CoT 的强化学习方法）。
    *   **其他**：GPT-4o, GPT-5.2, 以及传统的搜索式测试工具 CODAMOSA。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **软件框架**：基于 **Verl** 强化学习框架进行训练。
*   **训练细节**：学习率 1e-6，采用 AdamW 优化器，设置了 3 秒的执行超时沙箱以防恶意代码或死循环。文中未明确给出总训练时长，但提到了训练数据量为 8,396 条状态序列。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 14B 不同参数规模的模型，并在两个大型基准上进行了测试。
*   **充分性**：
    *   所有实验重复 3 次并取平均值，通过 **Wilcoxon 符号秩检验** 验证了统计显著性（$p < 0.01$）。
    *   包含了详尽的**消融实验**，分别验证了 RL 训练、贪心推理框架以及基础模型能力的贡献。
    *   进行了**泛化性测试**（LCB）和**找 Bug 能力测试**（变异测试及真实 Bug 检测），实验设计客观且全面。

#### 6. 主要结论与发现
*   **性能飞跃**：TestDecision 使基础模型的分支覆盖率提升了 38.15-52.37%，执行通过率提升了 298.22-558.88%。
*   **以小博大**：经过训练的 **7B 开源模型**在测试生成任务上达到了与 **GPT-5.2** 相当的水平，显著超越了 GPT-4o。
*   **序列优化**：TestDecision 生成的第一个测试用例通常就能覆盖核心逻辑，且在后续步骤中保持较高的边际收益，有效克服了“结构性近视”。
*   **找 Bug 能力**：在真实 Bug 检测中，比原始模型多发现 58.43-95.45% 的漏洞。

#### 7. 优点
*   **理论支撑强**：并非盲目尝试 RL，而是先证明了目标的次模性，为贪心策略提供了数学上的近似保证。
*   **协同效应**：将推理时的状态增强（AST 标记）与训练时的策略对齐（GRPO）结合，解决了模型“看不见目标”和“写不出正确代码”的双重问题。
*   **高可靠性**：显著提升了生成代码的执行通过率，解决了 LLM 在复杂逻辑下容易产生语法错误或运行时异常的痛点。

#### 8. 不足与局限
*   **语言局限性**：目前仅在 **Python** 语言上进行了验证，虽然理论上框架通用，但迁移到静态编译语言（如 Java/C++）需要重新开发插桩工具。
*   **系统级挑战**：实验主要集中在函数级单元测试，对于涉及复杂类依赖、外部数据库或网络环境的系统级集成测试，RL 环境的模拟难度会大幅增加。
*   **变异测试开销**：虽然使用了执行正确性作为奖励代理，但真正的故障检测能力评估（变异测试）在训练中依然因开销过大而无法直接作为奖励函数。

（完）
