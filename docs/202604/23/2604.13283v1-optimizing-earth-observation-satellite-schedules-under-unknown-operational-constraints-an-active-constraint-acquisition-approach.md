---
title: "Optimizing Earth Observation Satellite Schedules under Unknown Operational Constraints: An Active Constraint Acquisition Approach"
title_zh: 未知运行约束下的地球观测卫星调度优化：一种主动约束获取方法
authors: Mohamed-Bachir Belaid
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13283v1"
tags: ["query:nco"]
score: 8.0
evidence: 具有主动约束获取的卫星调度组合优化
tldr: 本文针对地球观测卫星调度中约束条件隐含在复杂仿真器中而无法预先获知的难题，提出了一种保守约束获取（CCA）程序。该程序集成在“学习与优化”框架中，通过与二元黑盒交互学习可行性模型。实验证明，该方法在显著减少查询次数和计算时间的同时，大幅提升了调度方案的质量，优于贪婪算法和两阶段获取基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13283v1/fig-001.webp\", \"caption\": \"Table 2: Results averaged over 20 seeds (±std). FAO: 100 acquisition queries + final CP-SAT solve (total time includes the final solve). L&O: cutoff Q=100, terminating when the current CP-SAT proposal under L is accepted by the oracle. Gap: relative to the CP-SAT reference († = best feasible in 120 s). q∗: main query at which the best solution was first found during interleaving. Speedup: FAO time / L&O time.\", \"page\": 10, \"index\": 1, \"width\": 633, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.13283v1/fig-002.webp\", \"caption\": \"Figure 1: Convergence of L&O on a representative n=50 instance. Red markers indicate oracle rejections, each followed by a CCA update. The blue curve is the value returned by the timelimited CP-SAT solve under the current learned model L. The black curve is the best oracleconfirmed solution found so far. The plot illustrates the typical anytime behavior of the current implementation: the constraint-model value changes as CCA refines L, while the best accepted solution improves in discrete jumps and the run terminates once the current proposal under L is accepted by the oracle.\", \"page\": 11, \"index\": 2, \"width\": 846, \"height\": 485}]"
motivation: 卫星调度的物理约束常隐含在工程仿真器中而非显式数学模型，导致传统优化方法难以直接应用。
method: "提出保守约束获取（CCA）程序并结合 Learn&Optimize 框架，通过交替执行模型优化与针对性黑盒查询来学习约束。"
result: "实验显示 L&O 框架在 50 个任务规模下比两阶段基准节省了 5 倍执行时间，且查询次数从 100 次降至约 21 次。"
conclusion: 主动约束获取方法能高效处理黑盒约束下的组合优化问题，在保证调度质量的同时显著降低了交互成本。
---

## 摘要
地球观测（EO）卫星调度（即决定执行哪些成像任务以及何时执行）是一个经过深入研究的组合优化问题。现有方法通常假设运行约束模型是预先完全指定的。然而在实践中，管理观测间隔、功率预算和热限制的约束通常嵌入在工程制品或高保真模拟器中，而非显式的数学模型中。我们研究了“未知约束”下的 EO 调度：目标函数已知，但可行性必须通过二元查询接口（binary oracle）交互式地学习。在仅限于成对间隔和全局容量约束的简化模型下，我们引入了保守约束获取（CCA），这是一种特定领域的程序，旨在实践中高效识别合理的约束，同时限制对学习模型的过度收紧。CCA 嵌入在 Learn&Optimize (L&O) 框架中，支持一种交互式搜索过程，该过程在基于已学习约束模型的优化与有针对性的查询接口询问之间交替进行。在具有多达 50 个任务和密集约束网络的合成实例上，L&O 优于无先验知识的贪婪基准，并且比两阶段“先获取后求解”基准（FAO）使用的主要查询次数少得多。对于 n ≤ 30，使用 L&O 后，平均差距从 65-68%（优先级贪婪算法）下降到 17.7-35.8%。在 n=50 时（以 CP-SAT 在 120 秒内找到的最佳可行解为参考），L&O 的平均表现优于 FAO（17.9% 对比 20.3%），同时仅使用 21.3 次主要查询（而非 100 次），且执行时间缩短了约 5 倍。

## Abstract
Earth Observation (EO) satellite scheduling (deciding which imaging tasks to perform and when) is a well-studied combinatorial optimization problem. Existing methods typically assume that the operational constraint model is fully specified in advance. In practice, however, constraints governing separation between observations, power budgets, and thermal limits are often embedded in engineering artefacts or high-fidelity simulators rather than in explicit mathematical models. We study EO scheduling under \emph{unknown constraints}: the objective is known, but feasibility must be learned interactively from a binary oracle. Working with a simplified model restricted to pairwise separation and global capacity constraints, we introduce Conservative Constraint Acquisition~(CCA), a domain-specific procedure designed to identify justified constraints efficiently in practice while limiting unnecessary tightening of the learned model. Embedded in the \textsc{Learn\&Optimize} framework, CCA supports an interactive search process that alternates optimization under a learned constraint model with targeted oracle queries. On synthetic instances with up to 50~tasks and dense constraint networks, L\&O improves over a no-knowledge greedy baseline and uses far fewer main oracle queries than a two-phase acquire-then-solve baseline (FAO). For $n\leq 30$, the average gap drops from 65--68\% (Priority Greedy) to 17.7--35.8\% using L\&O. At $n{=}50$, where the CP-SAT reference is the best feasible solution found in 120~s, L\&O improves on FAO on average (17.9\% vs.\ 20.3\%) while using 21.3 main queries instead of 100 and about $5\times$ less execution time.