---
title: "DiffHLS: Differential Learning for High-Level Synthesis QoR Prediction with GNNs and LLM Code Embeddings"
title_zh: DiffHLS：基于图神经网络与大语言模型代码嵌入的高层次综合 QoR 预测差分学习框架
authors: "Zedong Peng, Zeju Li, Qiang Xu, Jieru Zhao"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09240v1"
tags: ["query:nco-llm"]
score: 9.0
evidence: 结合图神经网络与大模型代码嵌入进行硬件优化预测
tldr: 针对高层次综合（HLS）中优化空间探索耗时的问题，本文提出DiffHLS框架。该框架采用差分学习方法，通过GNN提取内核与设计变体的图特征，并结合预训练代码大模型（LLM）的嵌入向量来捕捉代码层面的细微差异。DiffHLS通过联合预测基准性能与性能增量（delta）来估算最终QoR，在PolyBench和ForgeHLS数据集上显著提升了预测精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09240v1/fig-001.webp\", \"caption\": \"TABLE II QOR PREDICTION ACCURACY UNDER DIFFERENT GNN BACKBONES.\", \"page\": 4, \"index\": 1, \"width\": 520, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2604.09240v1/fig-002.webp\", \"caption\": \"TABLE V ABLATION STUDY: CODE LLM BACKBONE.\", \"page\": 6, \"index\": 2, \"width\": 1076, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.09240v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of DIFFHLS. We encode kernel and design IR graphs, inject LLM code embeddings into the delta pathway, and predict the kernel baseline and delta; finally the design prediction is obtained by composition.\", \"page\": 3, \"index\": 3, \"width\": 806, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2604.09240v1/fig-004.webp\", \"caption\": \"Fig. 2. Example kernel code (top) and design code (bottom) illustrating HLS pragma insertion.\", \"page\": 3, \"index\": 4, \"width\": 500, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2604.09240v1/fig-005.webp\", \"caption\": \"TABLE III ABLATION STUDY: DIFFERENTIAL VS. DIRECT PREDICTION AND EFFECT OF CODE EMBEDDINGS.\", \"page\": 5, \"index\": 5, \"width\": 1076, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2604.09240v1/fig-006.webp\", \"caption\": \"TABLE IV ABLATION STUDY: PREDICTION HEAD ACCURACY.\", \"page\": 5, \"index\": 6, \"width\": 1076, \"height\": 435}]"
motivation: 传统的HLS设计空间探索因频繁调用耗时的综合工具而效率低下，且现有模型难以准确捕捉Pragma优化带来的性能波动。
method: 提出一种差分学习框架，利用双分支GNN处理IR图并引入LLM代码嵌入，通过联合预测基准值与性能增量来获取最终QoR。
result: 在PolyBench和ForgeHLS数据集上，DiffHLS在多种GNN骨干网络下均取得了比基准模型更低的平均绝对百分比误差。
conclusion: 结合差分学习、GNN和LLM嵌入能有效提高HLS性能预测的准确性，为高效的设计空间探索提供了有力支持。
---

## 摘要
高层次综合（HLS）将 C/C++ 编译为 RTL，但由于每个设计点都需要耗时的综合过程，探索由 pragma 驱动的优化选择仍然代价高昂。我们提出了 DiffHLS，这是一个用于 HLS 结果质量（QoR）预测的差分学习框架，它从“内核-设计”对（即内核基准和插入 pragma 的设计变体）中进行学习。DiffHLS 使用专门的图神经网络（GNN）分支对内核和设计的中间表示（IR）图进行编码，并利用预训练代码大语言模型（LLM）的代码嵌入来增强增量路径。我们并非直接回归绝对目标，而是联合预测内核基准和设计引起的增量（delta），并将它们组合以获得最终的设计预测。在 PolyBench 数据集上，DiffHLS 在四种 GNN 骨干网络下的平均绝对百分比误差（MAPE）均低于 GNN 基准模型，且 LLM 代码嵌入相比于仅使用 GNN 的消融实验表现出持续的改进。我们进一步在 ForgeHLS 数据集上验证了其可扩展性。

## Abstract
High-Level Synthesis (HLS) compiles C/C++ into RTL, but exploring pragma-driven optimization choices remains expensive because each design point requires time-consuming synthesis. We propose \textbf{\DiffHLS}, a differential learning framework for HLS Quality-of-Result (QoR) prediction that learns from kernel--design pairs: a kernel baseline and a pragma-inserted design variant. \DiffHLS~encodes kernel and design intermediate-representation graphs with dedicated graph neural network (GNN) branches, and augments the delta pathway with code embeddings from a pretrained code large language model (LLM). Instead of regressing absolute targets directly, we jointly predict the kernel baseline and the design-induced delta, and compose them to obtain the design prediction. On PolyBench, \DiffHLS~attains lower average MAPE than GNN baselines under four GNN backbones, and LLM code embeddings consistently improve over a GNN-only ablation. We further validate scalability on the ForgeHLS dataset.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DiffHLS** 的框架，旨在通过结合图神经网络（GNN）和大型语言模型（LLM）的代码嵌入，提高高层次综合（HLS）中结果质量（QoR）预测的准确性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 FPGA 开发中，高层次综合（HLS）允许使用 C/C++ 进行硬件设计，但通过 Pragma（编译指令，如流水线、展开等）进行优化时，每尝试一种配置都需要运行耗时的综合工具。现有的基于机器学习的 QoR 预测模型在处理不同内核（Kernel）间巨大的性能差异，以及捕捉 Pragma 带来的细微语义变化方面存在不足。
*   **研究动机**：
    1.  **预测增量比预测绝对值更容易**：不同内核的基准性能差异巨大，而同一内核下不同 Pragma 导致的性能波动相对较小。
    2.  **多模态信息互补**：编译器中间表示（IR）图能捕捉结构依赖，而源代码（Source Code）则保留了显式的 Pragma 指令和局部语义模式。

### 2. 方法论
DiffHLS 的核心是一个**差分学习（Differential Learning）**框架，其主要组成部分包括：
*   **差分公式**：将设计目标的预测分解为内核基准值（Kernel Baseline, $y_k$）和设计引起的增量（Delta, $\Delta$）之和，即 $y_d = y_k + \Delta$。
*   **双分支图编码**：使用两个参数独立的 GNN 分支分别处理原始内核和插入 Pragma 后的设计变体的控制数据流图（CDFG）。
*   **LLM 代码嵌入**：引入冻结的预训练代码大模型（如 Qwen2.5-Coder 或 Llama-3.2），提取设计源代码的隐藏层状态作为语义特征，通过一个轻量级适配器（Adapter）注入到增量预测路径中。
*   **联合训练目标**：采用三项损失函数（SmoothL1 Loss）联合优化：
    1.  内核基准预测损失。
    2.  增量预测损失。
    3.  最终组合预测（基准+增量）与真实值之间的 consistency（一致性）损失。

### 3. 实验设计
*   **数据集**：
    *   **PolyBench/C**：包含 27 个内核，通过 Pragma 扩展出 10,108 个设计点。
    *   **ForgeHLS**：用于验证大规模场景下的可扩展性。
*   **预测目标**：DSP、FF、LUT（资源占用）和 CP（关键路径延迟）。
*   **基准方法（Baselines）**：对比了传统的 GNN 预测模型 [1] 和分层 GNN 模型 HGNN [2]。
*   **骨干网络**：在 PNA、GraphSAGE、GCN 和 GAT 四种不同的 GNN 架构上进行了验证。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **24 GB 显存的 NVIDIA RTX 4090 GPU** 上运行。
*   **训练细节**：使用 Adam 优化器，学习率为 $5 \times 10^{-4}$，Batch Size 为 16，隐藏层维度为 128。使用了学习率衰减策略（ReduceLROnPlateau）。
*   **时长**：文中未明确给出具体的训练总时长，但提到 LLM 是冻结的（Frozen），这显著降低了计算开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种 QoR 指标分别训练了模型。
    *   横跨 4 种 GNN 骨干网络进行了对比实验。
    *   进行了详细的消融实验（验证差分学习的作用、验证代码嵌入的作用、验证不同 LLM 后端的影响）。
    *   在两个不同规模的数据集上进行了测试。
*   **充分性评价**：实验设计较为全面，通过多指标（MAPE, MAE, $R^2$）和多骨干网络的交叉验证，证明了方法的稳健性。消融实验清晰地展示了各组件的贡献，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **差分学习至关重要**：相比直接预测绝对值，预测“基准+增量”显著降低了 MAPE（例如在 LUT 预测上，MAPE 从 13.39% 降至 6.11%）。
*   **LLM 嵌入提供增益**：LLM 能够捕捉到 IR 图中可能丢失的 Pragma 语义，在所有 GNN 骨干网络上均提升了预测精度。
*   **GraphSAGE 表现最优**：在四种 GNN 骨干中，GraphSAGE 配合 DiffHLS 框架达到了最佳的预测效果。
*   **可扩展性强**：在更大规模的 ForgeHLS 数据集上，DiffHLS 依然保持了较低的预测误差。

### 7. 优点
*   **思路新颖**：将差分学习引入 HLS QoR 预测，有效解决了跨内核方差大的问题。
*   **多模态融合**：巧妙地结合了结构化的图特征和非结构化的 LLM 语义特征，且无需微调昂贵的 LLM。
*   **诊断性强**：通过分解预测，可以清晰地识别误差是来源于基准估计错误还是增量建模错误。

### 8. 不足与局限
*   **LLM 依赖性**：虽然 LLM 是冻结的，但在推理阶段仍需调用 LLM 提取嵌入，这比纯 GNN 模型增加了额外的计算开销和内存占用。
*   **泛化范围**：实验主要集中在 PolyBench 等计算密集型内核上，对于更复杂的 SoC 级设计或具有复杂控制逻辑的非规则算法，其表现尚待验证。
*   **Pragma 覆盖面**：虽然验证了常见的优化指令，但对于一些罕见或特定厂商的 Pragma，LLM 的预训练语料库可能覆盖不足。

（完）
